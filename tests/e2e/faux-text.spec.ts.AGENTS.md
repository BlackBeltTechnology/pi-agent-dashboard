# faux-text.spec.ts — index

Playwright spec. Sends `[[faux:plain-text]]` via `sendPrompt`. Asserts `PLAIN_TEXT_MARKER` text renders in message DOM. Needs `PI_E2E_SEED=1`. See change: add-e2e-faux-model-roundtrip.

Row summary (formerly inline in `tests/e2e/AGENTS.md`): Playwright spec. Faux `[[faux:plain-text]]` roundtrip. Needs `PI_E2E_SEED=1`.
