# folder-collapse-seek.spec.ts — index

L3 for `persist-folder-collapse-server-side` (test-plan #F5, #F6, #F8). Seek into a collapsed folder; held echo proves the add-only expand; #F8 documents an unreachable race. → see `folder-collapse-seek.spec.ts.AGENTS.md`

Row summary (formerly inline in `tests/e2e/AGENTS.md`): L3 `persist-folder-collapse-server-side` #F5/#F6/#F8: Seek into a collapsed folder, held echo.
