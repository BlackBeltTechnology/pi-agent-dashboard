# redesign-providers-list.spec.ts — index

L3 for redesign-providers-settings-page: the connected list (test-plan #F7–#F11, #X7–#X9). Routes the section's three reads (`/status`, `/api/providers`, `catalogue-ready`); write routes mutate their own payload like the real server. → see `redesign-providers-list.spec.ts.AGENTS.md`
