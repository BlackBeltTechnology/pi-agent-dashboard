# README.md — index

Docs for browser E2E. Prerequisites: Docker, `npx playwright install chromium`. Run via `npm run test:e2e`. `PW_E2E_USE_RUNNING=1` fast path. Relationship to `qa/` + `site/`. Documents faux specs + `[[faux:<id>]]` sentinel convention + catalog source `qa/fixtures/faux-scenarios.ts`. Documents NO committed wall-clock budget (config sets no `globalTimeout`, #450; pass `--global-timeout` for a local cap; CI shard job owns `timeout-minutes`) + the CI-only `blob` reporter. Documents the co-resident harness guard (`(n+1) × MEM_LIMIT >= MemTotal` refuses; `PI_HARNESS_ALLOW_OVERSUBSCRIBE=1` overrides) + `## When a run dies mid-way` (`docker/harness-audit.sh` events capture, #451). See change: add-playwright-e2e. See change: add-e2e-faux-model-roundtrip. See change: stabilize-browser-e2e.
