# folder-collapse-persistence.spec.ts — index

L3 for `persist-folder-collapse-server-side` (test-plan #F1, #F2, #F3, #F9). Collapse survives reload / a fresh context / a zero-session pin, with zero expanded-then-corrected frames. → see `folder-collapse-persistence.spec.ts.AGENTS.md`
