# sessions-page-stub-group.spec.ts — index

L3 for F3 (fix-connect-snapshot-frame-loss): clicking the out-of-window stub group's ended expander pages <=50 more rows and hides "more" once held == label.

Row summary (formerly inline in `tests/e2e/AGENTS.md`): L3 for the `sessions_page` stub-group paging path.
