# access-grant-dialog.spec.ts — index

L3 for the access-grant prompt dialog + YOLO UI (change: add-access-grant-dialog, tasks 10.60-10.75; test-plan #E32, #E42-#E46, #E50, #E54-#E56, #F1-#F9). VERIFIED against the docker harness: 13 passed. `beforeAll` flips `hostGate.mode=enforce` + `accessGrants.promptEnabled` in-container (read live from config.json), `afterAll` restores config.json, access-grants.json, access-refusals.json byte-for-byte and ends YOLO. Held requests via `fetch` inside the page (wrapper adds `X-Pi-Grant-Channel`); `page.request` never carries it (recorded, unheld). One probe dir per test (D9 120 s backoff); a file denial names its DIRECTORY. Network prompt: `trustedNetworks` = 32-CIDR complement of the container eth0 IP, container fetches its own eth0 -> genuine network denial. CORS: markup-bearing Origin is not a URL origin -> no dialog; `https://evil.example.com` renders as text. Reconnect (#F9): `routeWebSocket` drop flag hides `grant_dismiss`, closing the route forces reconnect + reconcile. #F3 waits the real 120 s TTL (own 200 s timeout). Skips under `PI_DASHBOARD_DISABLE_GRANT_PROMPT=1` (task 9.2 run).
