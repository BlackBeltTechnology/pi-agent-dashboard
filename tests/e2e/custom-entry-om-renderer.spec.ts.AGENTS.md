# custom-entry-om-renderer.spec.ts — index

L3 (change: add-custom-entry-renderer-slot, F15). `[[faux:om-entry]]` drives real `pi.appendEntry("om.observations.recorded", ...)`; the `om.*` `memory` group ships HIDDEN so the spec enables it via the View popover, then asserts the blackhole `custom-entry-renderer` owns the row: collapsed line = labelled summary (not JSON), expand = discrete observation records from the on-demand payload endpoint. → see `custom-entry-om-renderer.spec.ts.AGENTS.md`
