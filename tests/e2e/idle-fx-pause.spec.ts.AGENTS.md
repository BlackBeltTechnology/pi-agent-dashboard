# idle-fx-pause.spec.ts — index

L3 for §7 idle FX pause (test-plan #F16–#F20): idle delay pauses decorative FX; input resumes; `animate-spin`/`fx-progress` exempt; `app-hidden` + `.fx-offscreen` re-pause. → see `idle-fx-pause.spec.ts.AGENTS.md`

Row summary (formerly inline in `tests/e2e/AGENTS.md`): L3 for idle FX pause (test-plan #F16-#F20): idle delay pauses decorative FX; input resumes;
