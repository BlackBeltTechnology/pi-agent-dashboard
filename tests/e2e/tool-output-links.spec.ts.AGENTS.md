# tool-output-links.spec.ts — index

Playwright E2E for tool-output file-link behaviour (change: selectable-tool-output-links). `[[faux:text-difflinks]]` streams unified-diff header `diff --git a/src/ghost.ts b/src/ghost.ts`; MarkdownContent linkifies `a/`/`b/` paths. Asserts the tokenizer strips the `a/` diff prefix from the RESOLVED path: the server-side mention resolver returns null for the absent ghost.ts, so the link flips to the not-found affordance (`data-not-found="true"`) whose tooltip names the STRIPPED `src/ghost.ts`. Superseded the old overlay/`/api/file`-404 assertion (drift, stabilize-browser-e2e). See change: server-side-file-mention-resolution.
