# lazy-feature-bootstrap.spec.ts — index

L3 gate for `add-lazy-terminal-diff-bootstrap`: cold landing fetches zero `xterm-*`/`diff-*` chunks; first terminal activation fetches xterm exactly once; root JS transfer ≥15% below baseline; diff chunk lazy on `diff:` open; mobile stays jsdiff-only; aborted chunk fetch contained by `ErrorBoundary`. → see `lazy-feature-bootstrap.spec.ts.AGENTS.md`
