# faux-tool.spec.ts — index

Playwright spec. Sends `[[faux:tool-read]]` via `sendPrompt`. Asserts read tool renderer mounts (path `src/example.ts` visible). Needs `PI_E2E_SEED=1`. See change: add-e2e-faux-model-roundtrip.

Row summary (formerly inline in `tests/e2e/AGENTS.md`): Faux-provider tool-call round trip.
