# mcp-token-settings.spec.ts — index

L3 for the Settings → Paired Devices MCP-client token flow (change: mcp-legacy-clients-and-token-issuance, F1/F2/F3): mint → one-shot token + `claude mcp add` snippet, dismiss-unrecoverable + `manual` badge, revoke → bearer dies on `/mcp`. Arms a session cookie (narrowed harness config, restored in afterAll) because the operator guard admits the docker-sourced browser only via `authVia: "session"`. → see `mcp-token-settings.spec.ts.AGENTS.md`
