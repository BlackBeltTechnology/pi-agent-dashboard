# host-pressure-badge.spec.ts — index

L3 for `fix-false-unresponsive-badge` (#F3, #F4). Synthetic bridge goes quiet at the pi gateway; card acquires then loses the pressure pill with no reload,… → see `host-pressure-badge.spec.ts.AGENTS.md`
