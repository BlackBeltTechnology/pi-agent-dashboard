# DOX — tests/e2e/extension-slash-inprocess.spec.ts

Per-file detail for one row of `tests/e2e/AGENTS.md`. Pull-only sidecar (its name is not `AGENTS.md`, so pi never auto-injects it).

| File | Purpose |
|------|---------|
| `extension-slash-inprocess.spec.ts` | L3 for change `retire-slash-dispatch-via-expand-prompt-templates`. Test-plan F1 + F2. Target command `/dashboard-where` — the bridge's OWN `pi.registerCommand` extension command, whose handler writes to stderr and never starts an agent turn, so "no model turn" is directly observable. **#F1** (headless shape): `setSpawnStrategy(page,"headless")` → `spawnFreshGitSession` → `sendPrompt(page, "/dashboard-where")` → the `CommandFeedbackCard` pill for `/dashboard-where` converges to label `completed` within 10 s; exactly ONE pill (no `error`); transcript occurrence count of `/dashboard-where` settles to exactly 1 (the pill's own `<code>` — a user-row echo would make it 2); session `tokensIn+tokensOut` unchanged (a fall-through to the LLM, the exact regression this change closes, would consume tokens). **#F2** (reattach): after the terminal pill, `page.reload()` → reopen the same card by `data-session-id` → the pill renders `completed` again, rebuilt from the REPLAYED persisted event. A broadcast-only terminal (no `store.insertEvent`) would render the bridge's persisted `started` as "in progress" and fail here. **Pill selector**: `CommandFeedbackCard` renders `<code>{command}</code><span>{label}</span>` with NO testid, so `pillsFor(page, cmd)` walks `document.querySelectorAll("code")` — same technique as `headless-reload-dispatch.spec.ts`. `setSpawnStrategy` is restored in a `finally` because specs share one container and the harness default is `tmux`. `dismissOverlays` escapes a possible OpenSpec propose dialog whose backdrop eats composer clicks. F4 (terminal-hosted pi in tmux, judged by hand on stderr `[dashboard] where:`) stays manual-only — the container has no terminal-hosted pi. |

Row summary (formerly inline in `tests/e2e/AGENTS.md`): L3 in-process extension slash dispatch (test-plan F1, F2).
