# TESTING.md — index

Runbook for disposable isolated test harness. Quick start. Isolation-guarantee table: home-lock, mDNS, ports, `~/.pi` state. Path-parity explanation (Mermaid overlay diagram: host cwd ro lowerdir, tmpfs upper/work, `mount -t overlay`). Overlay-vs-copy-mode tradeoff. Co-resident harness guard (#451 part 2): `list_running_harness_projects` + `(n+1) × MEM_LIMIT >= MemTotal` refuses before build/state-file write, `PI_HARNESS_ALLOW_OVERSUBSCRIBE=1` overrides, fits-but-peer-up warns, unreadable memory warns + proceeds. Harness audit (#451 part 1): `docker/harness-audit.sh` docker-events capture, no fix claimed. Fixtures-vs-mount usage. UI-only-vs-e2e key seeding. agent-browser manual-QA entry. Files table includes `harness-audit.sh`. See change: docker-test-harness. See change: stabilize-browser-e2e.
