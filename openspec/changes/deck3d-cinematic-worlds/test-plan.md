# Test Plan — deck3d-cinematic-worlds

Stage: design   Generated: 2026-09-19

Clarifications C1–C5 were answered at the hard gate and folded into the specs (values ≥ 0 with zero-stub; autoplay 1–600 int, 0 off; allowed schemes file/data/blob/about; local name `^[a-z][a-z0-9-]{0,63}$`; second fixture ≤ 60 s / ≤ 6 MB).

Level note: `packages/deck3d` is a CLI + static-HTML package with no server/UI surface in the dashboard harness, so every automated row is **L1** (vitest under `packages/deck3d/src/**/__tests__/`); rows that need a browser are chromium-gated via `src/__tests__/helpers/chromium.ts` like the existing `f*`/`e4x` suites. Exemplar = nearest existing test to copy harness glue from.

---

## Scenarios

### Edge-case

| id | requirement | technique | level | disposition | input | trigger | expected observable |
|----|-------------|-----------|-------|-------------|-------|---------|---------------------|
| E1 | local-effects / Local effect files (name grammar) | BVA | L1 | automated | ids `local:a`, `local:` + 64 chars `a…`, `local:` + 65 chars, `local:9x`, `local:A1`, `local:a_b` | `validate deck.json` | first two exit 0; last four exit non-zero naming the id and `^[a-z][a-z0-9-]{0,63}$` — exemplar `ir/__tests__/e11-schema-validation.test.ts` |
| E2 | local-effects / Local effect files (size cap) | BVA | L1 | automated | `fx/big.js` of exactly 65 536 bytes; then 65 537 bytes (valid module both) | `validate` | 65 536 → exit 0; 65 537 → exit non-zero naming `fx/big.js` and `64 KiB` — exemplar `props/__tests__/e31-oversized.test.ts` |
| E3 | local-effects / Local effect files (hash) | EP | L1 | automated | valid module; override `sha256` = correct / one hex digit off / uppercase of correct | `validate`, then `render` | correct → 0; off-by-one → non-zero naming `overrides.slides["geo"].effects[0].sha256` + file, no HTML written; uppercase → treated as mismatch (hex compare is case-sensitive lowercase) — exemplar `props/__tests__/e30-hash-mismatch.test.ts` |
| E4 | local-effects / Local effect files (card kind) | decision-table | L1 | automated | card `kind` ∈ {background, motion, post, material, light, edge, transition} | `validate` | background, motion → 0; other five → non-zero naming card + `background, motion` — exemplar `fx/__tests__/e24-card-params.test.ts` |
| E5 | local-effects / Local effect files (params bounded) | BVA | L1 | automated | local card `density` schema `0..1`; params `0`, `1`, `-0.01`, `1.01`, `"1"` | `validate` | `0`,`1` → 0; other three → non-zero naming `…effects[i].params.density` + range — exemplar `fx/__tests__/e24-card-params.test.ts` |
| E6 | local-effects / Module shape and static lint | decision-table | L1 | automated | modules: (a) comment + `export default function`; (b) `const x=1;` then default export; (c) default export + `export const h=1`; (d) body containing `import(`; (e) body containing `new Function(`; (f) body containing `function inner(){}`; (g) string `"require"` inside a literal | `validate` | a, f, g → 0; b → "must start with export default"; c → "single default export"; d → names `import`; e → names `Function` — exemplar `ir/__tests__/e11-schema-validation.test.ts` |
| E7 | local-effects / Embedding deterministic | determinism | L1 (chromium) | automated | deck with 2 local effects + 1 corpus effect | `render` twice | byte-identical HTML; both module sources present as JSON strings; zero `<script src=` — exemplar `render/__tests__/p3-render-determinism.test.ts` |
| E8 | local-effects / Embedding (script terminator) | fault-injection (input) | L1 (chromium) | automated | module containing literal `"</script><script>window.pwned=1</script>"` | `render`, open headless | `window.pwned` undefined; `__deck3d` defined; the effect's `object` present on its slide — exemplar `render/__tests__/e35-json-safe.test.ts` |
| E9 | local-effects / Runtime isolation (shadowed globals) | decision-table | L1 (chromium) | automated | module that reads each of the 29 shadowed identifiers and returns their `typeof` in `object.userData` | open deck, `gotoSlide` | every entry `"undefined"`; `typeof Math.random` `"function"` — exemplar `render/__tests__/runtime.test.ts` |
| E10 | local-effects / Runtime isolation (deterministic random) | determinism | L1 (chromium) | automated | module placing 200 points via `Math.random()` | open deck twice, read positions via `measure()` rect of a labelled child | identical arrays across loads — exemplar `render/__tests__/f1-measure-stable.test.ts` |
| E11 | local-effects / Local effects enter composition | BVA | L1 | automated | `quality: low` (budget 6): corpus cost 5 + local card cost 1; then local cost 2 | `render` | 6 → no warning; 7 → warning `7 > 6` and `check` `warn budget` — exemplar `fx/__tests__/e21-budget.test.ts` |
| E12 | local-effects / composition (conflict) | decision-table | L1 | automated | local card `conflicts: ["aurora"]`; slide effects = [aurora, local:x] / [local:x] / [starfield, local:x] | `render` | first → exit non-zero naming `local:x`, `aurora`, slide; other two → 0 — exemplar `fx/__tests__/e22-conflict.test.ts` |
| E13 | local-effects / CLI (`fx scaffold`) | happy+idempotence | L1 | automated | empty deck dir; `fx scaffold neural-mesh --for ai` ; run twice | CLI | first: both files exist, printed entry's sha256 equals `sha256(fx/neural-mesh.js)`, pasting it → `validate` 0; second run: exit non-zero "exists", files unchanged — exemplar `fx/__tests__/fx-cli.test.ts` |
| E14 | local-effects / CLI (`fx hash`) | happy | L1 | automated | scaffolded module, then append a comment | `fx hash neural-mesh` before/after | two different 64-hex strings, second equals recomputed sha256 — exemplar `fx/__tests__/fx-cli.test.ts` |
| E15 | local-effects / CLI (`fx promote`) | decision-table | L1 | automated | flags: none / `--source` only / `--licence` only / both with `--licence Prosperity-3.0.0` / both valid MIT | `fx promote neural-mesh …` | first four → non-zero naming missing flag or non-permissive licence, `fx/` untouched, `src/fx` untouched; last → `src/fx/neural-mesh.ts`+`.meta.json` exist with given source+licence, `fx/neural-mesh.*` gone, catalogue regenerated (run against a temp copy of `src/fx`) — exemplar `fx/__tests__/e47-catalogue-sync.test.ts` |
| E16 | ir / IR carries every knob (`diagram.kind` enum) | decision-table | L1 | automated | `overrides.slides[x].diagram.kind` ∈ {none,brain,loop,swarm,bars,funnel,timeline-rail,globe,orbit-cluster,stack,flowchart,sequence,"pie"} | `validate` | first ten → 0; `flowchart`,`sequence`,`pie` → non-zero naming path + enum — exemplar `ir/__tests__/e11-schema-validation.test.ts` |
| E17 | ir / IR carries every knob (`data.values` ≥ 0) | BVA | L1 | automated | values `[0]`, `[0.0001]`, `[-0.0001]`, `[3,-1]`, `["3"]` | `validate` | first two → 0; others → non-zero naming `…data.values[i]` + `minimum 0` / type — exemplar `ir/__tests__/e11-schema-validation.test.ts` |
| E18 | ir / palette enum | decision-table | L1 | automated | `overrides.deck.palette` ∈ 10 values + `"sunset"` | `validate` | ten → 0; `sunset` → non-zero + enum — exemplar `ir/__tests__/schema.test.ts` |
| E19 | ir / Overrides survive re-parse (`data` atomic) | EP | L1 | automated | derived `data {labels:[a,b], values:[1,2]}`; override `data {labels:[x,y]}` | `parse` merged view | `diagram.data` deep-equals `{labels:[x,y]}` (no `values` key) — exemplar `ir/__tests__/merge.test.ts` |
| E20 | ir / Overrides survive re-parse (mermaid wins) | state-transition | L1 (chromium) | automated | slide with `diagram.kind: brain` override; markdown edited to add a `flowchart` block | `parse` | derived kind `flowchart`; override retained in `overrides`; warning line names `overrides.slides["flow"].diagram.kind` + "ignored"; exit 0 — exemplar `ir/__tests__/e5-overrides-survive-reparse.test.ts` |
| E21 | ir / Overrides survive re-parse (unsupported fence) | state-transition | L1 (chromium) | automated | slide with a ```pie fence and `diagram.kind: bars` override | `parse` | derived kind `bars` (override honoured), unsupported-type warning present, no "ignored" warning — exemplar `ir/__tests__/e5-overrides-survive-reparse.test.ts` |
| E22 | ir / `overrides apply` | decision-table | L1 | automated | existing `slides.geo {mode:light}`; file `{slides:{geo:{camera:{distance:11}}}}`; then file `{slides:{geo:{effects:[{id:"aurora"}]}}}` over existing `effects:[a,b]`; then file `{slides:{geo:{diagram:{data:{labels:[x]}}}}}` over `data{labels,values}`; then file with `depthRelief:"x"` | `deck3d overrides apply deck.json f.json` | (1) `{mode:light,camera:{distance:11}}`; (2) effects `[aurora]`; (3) data `{labels:[x]}`; (4) exit non-zero + JSON path, deck.json unchanged — exemplar `ir/__tests__/e9-merge-grammar.test.ts` |
| E23 | ir / IR is schema-validated (local referents) | decision-table | L1 | automated | `local:globe` referenced; `fx/globe.js` missing / present w/o card / present with card | `validate deck.json` in deck dir; and `validate` on a copy of deck.json in a dir without `fx/` | in-dir: missing → non-zero naming path + `fx/globe.js`; no card → non-zero naming card; ok → 0. Copy-without-fx → non-zero (file missing) — exemplar `ir/__tests__/e11-schema-validation.test.ts` |
| E24 | render / Diagram builders (bars from data) | BVA | L1 (chromium) | automated | `bars` with values `[1,2,4]`; then `[0,5]`; then labels only (3) no values | open, `measure()` | 3 `node` kinds; projected heights ratio 1:2:4 ±5 %; `[0,5]` → first rect height > 0 (stub) and label present; no values → three heights equal ±2 % — exemplar `render/__tests__/f1-measure-stable.test.ts` |
| E25 | render / Diagram builders (every built kind) | decision-table | L1 (chromium) | automated | 9 slides, one per built kind, 4 labels each | open, `measure()` per slide | each slide returns ≥ 4 labelled `node` entries; none throws; `timeline-rail` labels alternate above/below rail centre y — exemplar `render/__tests__/f7-message-order.test.ts` |
| E26 | render / Navigation (keys) | state-transition | L1 (chromium) | automated | 3-slide deck | `ArrowRight`×2, `ArrowLeft`, `Space`, `End`, `Home`, `PageDown`, `ArrowUp` | current = 2,3,2,3,3,1,2,1; hash tracks each — exemplar `src/__tests__/navigation.test.ts` (already on tree) |
| E27 | render / Navigation (clamp + modifiers) | BVA | L1 (chromium) | automated | on slide 1: `ArrowLeft`; on last: `ArrowRight`, click; `Meta+ArrowRight` on slide 1 | keys/click | stays 1; stays 3; stays 3; stays 1 — exemplar `src/__tests__/navigation.test.ts` |
| E28 | render / Navigation (live hash) | state-transition | L1 (chromium) | automated | on slide 1 set `location.hash="#3"`, then `"#0"`, then `"#99"` | hashchange | current 3, then 1, then 1 — exemplar `src/__tests__/navigation.test.ts` |
| E29 | effects / topic vocabulary | decision-table | L1 | automated | corpus card fixture with `tags.topic: ["finance"]`; with `["geo"]`; corpus card with `source: "local"` | corpus test | first → fails naming card + vocabulary; second → passes; third → fails — exemplar `fx/__tests__/corpus.test.ts` |
| E30 | effects / Every topic has a background | coverage | L1 | automated | shipped cards | corpus test | for each of 12 topics ≥ 1 `background` card lists it — exemplar `fx/__tests__/e27-corpus-licence.test.ts` |
| E31 | effects / extended ctx | regression | L1 | automated | every corpus effect | construct with `{THREE,palette,mode,quality,rng,slide}` + dispose | none throws — exemplar `fx/__tests__/corpus.test.ts` |
| E32 | effects / Deterministic defaults (order) | decision-table | L1 | automated | slides: (a) title "Security data flows"+flowchart; (b) title-ish "Data Platform 2030"; (c) content "Regional trade shifts" 3 bullets; (d) same as c with `autoStyle:false`; (e) content "Weather" 3 bullets | `defaultEffectsFor` | a→glyph-rain (v1 keyword outranks diagram, unchanged); b→data-columns (v1 keyword outranks titleish); c→globe-arcs; d→particles; e→particles — exemplar `fx/__tests__/defaults.test.ts` |
| E33 | effects / Deterministic defaults (v1 prefix invariant) | property | L1 | automated | every slide of `fixtures/strategy-lab.md` | `defaultEffectsFor` old vs new | identical for every slide that did not resolve to `particles` under v1 — exemplar `fx/__tests__/e25-defaults.test.ts` |
| E34 | effects / catalogue (`--topic`) | EP | L1 | automated | `fx list --topic geo`; `--topic finance` | CLI | first lists `globe-arcs`, not `neural-mesh`; second exits non-zero naming vocabulary — exemplar `fx/__tests__/fx-cli.test.ts` |
| E35 | effects / catalogue (`fx preview local:` + `--palette`) | happy | L1 (chromium) | automated | deck dir with scaffolded `fx/neural-mesh.*` | `fx preview local:neural-mesh --palette ember -o p.png` | PNG exists, not all-black (>1 % non-bg pixels) — exemplar `fx/__tests__/fx-cli.test.ts` |
| E36 | effects / Topic background deterministic | determinism | L1 (chromium) | automated | `fx preview globe-arcs` ×2 | CLI | PNG bytes identical — exemplar `render/__tests__/p3-render-determinism.test.ts` |
| E37 | parse / D2 built-kind table | decision-table | L1 | automated | content slides: "- 2026: MVP / - 2027: GA" (years); "- 88% adoption / - 12% none"; "Deal pipeline review"; "Our agents"; "Feedback loop"; "The LLM"; "Global trade"; "Partner ecosystem"; "Compute layers"; "Lunch menu"; a `section` slide "Timeline" | `parse` | timeline-rail (years never bars), bars, funnel, swarm, loop, brain, globe, orbit-cluster, stack, none, none — exemplar `parse/__tests__/derive.test.ts` |
| E38 | parse / D2 data harvest | BVA | L1 | automated | bullets "- 88% adoption", "- 2026: MVP", "- 0 churn", "- plain" | `parse` `diagram.data` | labels `[adoption, 2026: MVP, churn, plain]`; values `[88, ∅, 0, ∅]` → since not all numeric, `values` omitted (equal heights) — exemplar `parse/__tests__/derive.test.ts` |
| E39 | props / ambient filter | BVA | L1 | automated | mocked catalogue with tris 1999, 2000, 2001 | `props search x --role ambient` | 1999 and 2000 printed with template `role:"ambient",count:12,restyle:"palette",size:0.6,anim:<enum value>`; 2001 absent; template passes `validate` after `sha256` filled — exemplar `props/__tests__/search.test.ts` |
| E40 | configurator / autoplay bounds | BVA | L1 (chromium) | automated | inputs `0`, `1`, `600`, `601`, `0.5`, `-1` | type into autoplay field | 0→off; 1,600→accepted, state reads value; 601, 0.5, -1 → rejected, state unchanged — exemplar `render/__tests__/runtime.test.ts` |
| E41 | configurator / export shape | decision-table | L1 (chromium) | automated | slide `geo`: set mode light + distance 11; slide `ai`: uncheck 1 of 3 effects; deck: nothing | Export | file = `{slides:{geo:{mode:"light",camera:{distance:11}}, ai:{effects:[<2 remaining in order>]}}}`, no `deck` key — exemplar `render/__tests__/runtime.test.ts` |
| E42 | render / Build budget (second fixture) | threshold | L1 (chromium) | automated | `fixtures/business-2031/deck.md` | `build` | post-launch ≤ 60 s; html ≤ 6 291 456 bytes — exemplar `render/__tests__/p1-build-budget.test.ts`, `p2-build-size.test.ts` |
| E43 | skill / help lists commands | EP | L1 | automated | — | `deck3d --help` | output contains `fx scaffold`, `fx hash`, `fx promote`, `overrides apply`, `check --style`, `--role`, `--prompt` — exemplar `src/__tests__/cli.test.ts` |
| E44 | skill / SKILL.md Style pass | content | L1 | automated | `.pi/skills/deck3d/SKILL.md` | read | contains a numbered tune loop with a step titled `Style`, mentions `check --style`, `fx scaffold`, `props search --role ambient`, `overrides apply`, "markdown inline overrides win", `Math.random` — exemplar `src/__tests__/skill.test.ts` |
| E45 | palettes / contrast | threshold | L1 | automated | all 9 named palettes × {dark, light} | computed WCAG ratio text/bg | every pair ≥ 4.5:1 — exemplar `fx/__tests__/corpus.test.ts` |

### Performance

| id | requirement | technique | level | disposition | workload | metric + threshold | window |
|----|-------------|-----------|-------|-------------|----------|--------------------|--------|
| P1 | render / Build budget (second fixture) | threshold | L1 (chromium) | automated | `build fixtures/business-2031/deck.md` on CI | wall ≤ 60 s post-launch; size ≤ 6 MB | single run (same as E42; kept here for the perf ledger) |
| P2 | effects / topic backgrounds honour quality | threshold | L1 (chromium) | automated | each topic background at `quality: low` vs `high` | instance count at `low` ≤ 50 % of `high` (read from `object.userData.count`) | single frame |
| P3 | local-effects / composition budget | threshold | L1 | automated | business fixture, all slides | no slide exceeds its `quality` budget in `check` report (`warn budget` count = 0) | one `check` run |

### Frontend-quirk

| id | requirement | technique | level | disposition | input | trigger | expected observable (invariant) |
|----|-------------|-----------|-------|-------------|-------|---------|---------------------------------|
| F1 | configurator / Panel visibility | state-transition | L1 (chromium) | automated | fresh deck | `C`, `Escape`, gear click, `C` again | panel `hidden` attr toggles false/true/false/true; counter text `1 / 3` — exemplar `render/__tests__/runtime.test.ts` |
| F2 | configurator / hidden = no visual difference | determinism | L1 (chromium) | automated | `fixtures/strategy-lab.md` deck, panel never opened | open, `measure()` on slide 1; and `getComputedStyle(panel).display` | panel `display` is `none`; `measure()` JSON deep-equals the committed pre-change fixture `render/__tests__/fixtures/strategy-lab-slide1.measure.json` (recorded before this change lands) — exemplar `render/__tests__/f1-measure-stable.test.ts` |
| F3 | configurator / Live application (palette) | state-convergence | L1 (chromium) | automated | select palette `ember` (deck scope) | change event | scene background, fog, rim light AND the baked title material all equal the ember colours; `window.__DECK.defaults.palette` unchanged. Asserts colours via `__deck3d.debug.look()`, NOT `Buffer.compare` of screenshots — the camera's idle drift makes any two frames differ, so the compare spelling cannot fail — exemplar `render/__tests__/e20-quality-effects.test.ts` |
| F9 | configurator / Live application (geometry) | state-convergence | L1 (chromium) | automated | slide scope on a `bars` deck | set `camera.distance` 16, then `labels.size` 0.4 | camera z moves back and the projected title shrinks; diagram label widens; `__DECK.slides[0].camera` still the embedded default — exemplar `render/__tests__/e20-quality-effects.test.ts` |
| F4 | configurator / focus suppresses nav | state-transition | L1 (chromium) | automated | focus `durationSec` input on slide 1 | type `2`, press `Space`, press `C`, press `ArrowRight` | current still 1; panel still open — exemplar `src/__tests__/navigation.test.ts` |
| F5 | configurator / click inside panel | state-transition | L1 (chromium) | automated | panel open, slide 1 | click panel body, click gear | current still 1 — exemplar `src/__tests__/navigation.test.ts` |
| F6 | configurator / reload restores | state-transition | L1 (chromium) | automated | set quality low; reload same html; then re-parse deck (new derivedHash) and open | reload | first reload: panel shows `low`, `__deck3d.effects().active` excludes bloom; re-parsed deck: panel default `high` — exemplar `render/__tests__/runtime.test.ts` |
| F7 | configurator / override marker | decision-table | L1 (chromium) | automated | `overrides.slides.geo.camera.distance` set; deck `overrides.deck.mode` set | open panel on `geo`, both scopes | slide scope: only `camera.distance` has `●`; deck scope: only `mode` has `●` — exemplar `render/__tests__/runtime.test.ts` |
| F8 | render / Navigation click outside panel | state-transition | L1 (chromium) | automated | panel closed | click at (400,300) | current 2 — exemplar `src/__tests__/navigation.test.ts` |
| F9 | render / built diagrams look right | visual/subjective | — | manual-only | business fixture slides with `bars`, `funnel`, `timeline-rail`, `globe`, `orbit-cluster`, `stack` | human views snapshots | [judgment: proportions/readability "look right"] |
| F10 | effects / topic backgrounds are recognisably topical | visual/subjective | — | manual-only | `fx preview` of the 8 new backgrounds | human views | [judgment: reads as globe/city/mesh/vault/racks/tape/agents/paper] |
| F11 | palettes / aesthetics | visual/subjective | — | manual-only | 6 new palettes on the fixture | human views | [judgment: pleasant, brand-neutral] |
| F12 | configurator / panel usability | visual/subjective | — | manual-only | live deck | human drives | [judgment: discoverable, not in the way] |

### Error-handling

| id | requirement | technique | level | disposition | fault | trigger | expected observable |
|----|-------------|-----------|-------|-------------|-------|---------|---------------------|
| X1 | local-effects / error containment (create) | fault-injection | L1 (chromium) | automated | module throws in factory | open deck, goto slide | slide title+bullets measurable; `effects().errors` = `[{slide, effectId:"local:x", phase:"create"}]`; other effects active — exemplar `render/__tests__/runtime.test.ts` |
| X2 | local-effects / error containment (tick, dispose) | fault-injection | L1 (chromium) | automated | throws on 3rd tick; another throws in dispose | `setTime` ×3; navigate away | `phase:"tick"` recorded once, effect object removed, loop continues (`current()` responds); `phase:"dispose"` recorded, navigation completes — exemplar `render/__tests__/runtime.test.ts` |
| X3 | local-effects / timers unavailable | fault-injection | L1 (chromium) | automated | module calls `setTimeout` in factory | open | `phase:"create"` error; no timer fires (a global counter stays 0 after 200 ms) — exemplar `render/__tests__/runtime.test.ts` |
| X4 | render / check `local-fx-error` finding | fault-injection | L1 (chromium) | automated | deck with X1 module on slide `geo` | `check` | report has `{rule:"local-fx-error", slide:"geo", effectId:"local:x", phase:"create", severity:"error", suggestion:'overrides.slides["geo"].effects'}`; no `message` key; exit non-zero — exemplar `check/__tests__/e40-suggestions.test.ts` |
| X5 | render / check `local-fx-network` | fault-injection | L1 (chromium) | automated | module uses `ctx.THREE.TextureLoader().load("https://example.com/t.png")`; another uses a `data:` texture | `check` | one finding `{rule:"local-fx-network", slide, host:"example.com", severity:"error"}`; `data:` load produces none; exit non-zero — exemplar `render/__tests__/f8-offline-open.test.ts` |
| X6 | render / check byte-equality classes | determinism | L1 (chromium) | automated | deck with X4 module + a legibility warn | `check` ×2, strip `contrast` and `local-fx-network` findings | remaining report JSON byte-identical (includes `local-fx-error`) — exemplar `check/__tests__/check.test.ts` |
| X7 | render / check `--style` | decision-table | L1 (chromium) | automated | 4 slides: bare; effects override; prop only; built-kind override only | `check --style` vs `check` | with flag: exactly one `style-defaults` warn (bare slide), exit 0; without flag: none — exemplar `check/__tests__/check-ignore.test.ts` |
| X8 | render / build style line | EP | L1 (chromium) | automated | 5-slide deck, 2 with effects overrides | `build` | stdout ends with `style: 2/5 slides styled` — exemplar `src/__tests__/e44-one-shot-build.test.ts` |
| X9 | configurator / check ignores panel | fault-injection | L1 (chromium) | automated | pre-seed `localStorage["deck3d:<hash>"]` with `{quality:"low"}` | `check` | report equals fresh-profile report; key cleared afterwards — exemplar `check/__tests__/e42-clean-deck.test.ts` |
| X10 | props / generate `--prompt` unreachable | fault-injection (abort) | L1 | automated | `DECK3D_T2I_URL` → mock returning 500 | `props generate --prompt "ship" --name ship` | exit non-zero naming the endpoint; no `ship.glb`, no `ship.png` — exemplar `props/__tests__/x8-generate-unavailable.test.ts` + `helpers/mock-http.ts` |
| X11 | props / generate `--prompt` happy (mocked) | integration (mock) | L1 | automated | mock T2I returns PNG; mock image-to-3D path returns GLB | same | `ship.png` + `ship.glb` in cache; printed entry has `licence: generated`, `restyle: palette`, 64-hex `sha256`; `validate` accepts — exemplar `props/__tests__/e33-fetch-entry.test.ts` |
| X12 | props / generate `--prompt` without python client | fault-injection | L1 | automated | `PATH` without `python3` | same | exit non-zero with install hint; no files — exemplar `props/__tests__/x8-generate-unavailable.test.ts` |
| X13 | local-effects / render refuses like validate | fault-injection | L1 | automated | valid deck.json; delete `fx/x.js` after validate | `render` | exit non-zero naming file; no HTML — exemplar `render/__tests__/x13-props-cache-miss.test.ts` |
| X14 | effects / `fx promote` corpus test gate | fault-injection | L1 | automated | local module that throws on construct | `fx promote x --source … --licence MIT` in temp corpus copy | promote exits non-zero from the corpus test; files rolled back — exemplar `fx/__tests__/e47-catalogue-sync.test.ts` |
| X15 | ir / `overrides apply` invalid file | fault-injection | L1 | automated | file is not JSON; file has unknown key `slides.geo.foo` | CLI | both exit non-zero (parse error / JSON path `overrides.slides.geo.foo`); deck.json byte-unchanged — exemplar `ir/__tests__/x11-bad-json.test.ts` |

---

## Coverage summary

- Requirements covered: 22/22 delta requirements (local-effects 6, configurator 4, ir 3, render 5, effects 5, props 2, skill 2 — every requirement has ≥ 1 row)
- Scenarios by class: edge 45 · perf 3 · frontend 12 · error 15
- Scenarios by level: L1 71 (of which chromium-gated 38) · L2 0 · L3 0 · manual-only 4
- Scenarios by disposition: automated 71 · manual-only 4

## New infra needed

- `fixtures/business-2031/` (second fixture: the restyled `presentations/business-next-5-years` deck + its `fx/`) — created by the change itself, not a harness.
- Test helper to run a mutable copy of `src/fx/` for `fx promote` tests (E15, X14) so the real corpus is never written by tests.
- Test helper to construct a `check`-time Playwright context with the allowed-scheme route filter (X5) — extends `check/index.ts`, no new harness.
