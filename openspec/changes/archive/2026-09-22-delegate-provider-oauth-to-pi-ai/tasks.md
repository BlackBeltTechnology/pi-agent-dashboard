## 1. Dependency floor (design D3)

- [x] 1.1 Bump all six governed pi pins to `0.86.1`: `packages/server/package.json` dependency (`^0.86.1`) + `piCompatibility.minimum` + `piCompatibility.recommended`, `pnpm-workspace.yaml` override, `docker/Dockerfile` install pin, `scripts/verify-release-deps.mjs` `minVersion`; run `pnpm install`; verify `node scripts/verify-release-deps.mjs` passes and `node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-ai/dist/auth/oauth/meta.js` exists
- [x] 1.2 Test (ci, test-plan E28): extend the existing `verify-release-deps` step assertion in `.github/workflows/ci.yml:35` so the six-pin coherence check is what gates the bump — repo at HEAD · run `node scripts/verify-release-deps.mjs` · exits 0 and all six pins read `0.86.1` (harness exemplar: `scripts/__tests__/lint-ledger.test.mjs` for a script-level vitest if a unit assertion is preferred over the workflow step)

## 2. Shared types (design D2, D4)

- [x] 2.1 In `packages/shared/src/rest-api.ts` add `OAuthFlowStatus` (`flowId, provider, status, authUrl?, message?, pending?, error?`) with the `pending` union (`device_code | manual_code | text | select`), widen `ProviderAuthStatus.expires?: number | null`, and keep `ProviderAuthInfo` shape; in `packages/shared/src/route-tiers.ts` drop the `device-status`/`authorize`/`device-code` entries (`:142,:230,:231`) and add `POST start`, `GET flow/:flowId`, `POST flow/:flowId/input`, `DELETE flow/:flowId`; verify `npm run lint` (tsc) passes and `mcp-manifest-completeness.test.ts` passes
- [x] 2.2 Add `packages/server/src/auth/pi-oauth-types.ts` with the local structural types (`OAuthLoginFlow`, `LoginInteraction`, prompt + event unions, `OAuthRegistryEntry`) — no import from `@earendil-works/pi-ai`; verify tsc passes with `packages/server` having zero `pi-ai` imports (`rg "@earendil-works/pi-ai" packages/server/src` returns nothing)

## 3. Test infrastructure

- [x] 3.1 Create `packages/server/src/__tests__/helpers/fake-oauth-flow.ts`: a scripted `OAuthLoginFlow` fake driven by a step list (`notify`, `prompt`, `resolve`, `reject`, `hang`), honouring the interaction's outer `signal` **the way pi-ai does** (outer abort only cancels the wait; `listenerClosed` flips only after the pending `manualPromise` settles), exposing `signal.aborted`, `listenerClosed`, `promptsSeen[]`, `openListeners` counters, and cooperating with `vi.useFakeTimers()`; ship provider presets for anthropic, openrouter, openai-codex, github-copilot, xai (harness exemplar for fake timers: `packages/server/src/__tests__/auto-shutdown.test.ts`); verify a smoke test drives the anthropic preset through auth_url → manual_code → resolve

## 4. Server: registry and adapter (design D1, D3) — TDD

- [x] 4.1 Write failing tests in `packages/server/src/__tests__/provider-auth-registry.test.ts` (real runtime): test-plan E1 — resolved pi-coding-agent 0.86.1 via `ModelRuntime.create({ modelsPath:null, credentials: emptyStore })` · build registry · id set exactly the 7, `radius` absent, every entry has `name` + `login` fn; E2 — the 8 listed ids · map through `FLOW_TYPE_HINT` · first three `auth_code`, rest incl. unknown `device_code`; E3 — `models.json` custom `oauth:"radius"` id + an extension-registered id · build · neither present; verify they fail
- [x] 4.2 Implement `packages/server/src/auth/provider-auth-registry.ts`: lazy `await import("@earendil-works/pi-coding-agent")`, `ModelRuntime.create({ modelsPath: null, credentials: EMPTY_READONLY_STORE })`, filter `auth?.oauth && id !== "radius"`, `FLOW_TYPE_HINT`, exported `oauthRegistryReady` promise + sync `registrySnapshot`, try/catch → `registryError` string containing `VERSION` (fallback: `package.json` via `import.meta.resolve` dir, else `unknown`); verify 4.1 passes
- [x] 4.3 Test (L1, test-plan P1): in the same file — cold `import()` + `create()` 5 runs · measure wall time · p95 < 1500 ms and `GET /handlers` answers within that bound after `listen` (harness exemplar: `provider-auth-routes.test.ts` `app.inject` bootstrap at `:62`); verify passes on CI runner
- [x] 4.4 Write failing adapter tests in `packages/server/src/__tests__/provider-auth-adapter.test.ts` using the 3.1 fake: test-plan E9 — anthropic preset · start · record holds BOTH `authUrl` and `pending.kind manual_code`; E14 — resolve after paste · terminal · `pending` absent, further input 409; E7 — fake issuing `text` twice with `preAnswers:["x.ghe.com"]` · observe · first answered silently, second surfaces as `pending.kind text`; X9 — fake issues `secret` prompt · start · `status error`, `error "unsupported prompt: secret"`, fake promise rejected; X13 — prompt carries its own `signal` that aborts while login continues · observe · `pending` cleared, later input 409; X6 — `login()` resolves 10 ms after abort · DELETE then wait · `error "Cancelled"`, `writeCredential` not called; X5 — xai preset mid-poll rejects `"Login cancelled"` on abort · DELETE · stored `error` is `"Cancelled"`; E21 — `device_code expiresInSeconds:60`, reject `"Device flow timed out"` at t=61 s · GET · `status expired`; E22 — same fake rejects at t=30 s · GET · `status error` with message; E20 — `expiresInSeconds` `undefined` and `"600"` · inspect · `expiresAt` unchanged, no `NaN`; verify they fail
- [x] 4.5 Implement `packages/server/src/auth/provider-auth-adapter.ts` — `createFlowInteraction(flow)` per the D1 table (sticky `authUrl`, `pending` per prompt kind, `preAnswers` consumed only while `promptCount === 0`, per-prompt `signal` handling, reject-all-pending on flow abort, resolve → cancelled-check → `writeCredential` → `notifyBridges` → `complete`, write-throw → `error`, reject → `cancelled`/`deadline`/`error` derivation, terminal clears `pending` + resolvers) and the flow store (`createFlow` with `crypto.randomUUID`, `expiresAt`/`deviceCodeDeadline` per D7, `pruneFlows` that cancels live flows, 60 s timer, `abortAllFlows`); verify 4.4 passes
- [x] 4.6 Rewrite `packages/server/src/auth/provider-auth-handlers.ts` to export only the registry surface consumers need (`getOAuthRegistry()`, `OAuthRegistryEntry`); delete `AuthCodeHandler`/`DeviceCodeHandler`/`ProviderHandler`, `generatePKCE`, `generateState`, `postJson`, the three handler objects; delete `packages/server/src/auth/oauth-callback-server.ts` and `__tests__/oauth-callback-server.test.ts` + `__tests__/provider-auth-handlers.test.ts`; verify `rg "oauth-callback-server|AuthCodeHandler|DeviceCodeHandler" packages/` returns nothing
- [x] 4.7 Re-point `provider-auth-storage.ts`: `_buildAuthStatus(catalogue, authData, oauthEntries: OAuthRegistryEntry[])` stays pure and keys on `entry.id` / `entry.name`; treat any `auth.json` `{ type:"oauth" }` entry as an OAuth row even when not in `oauthEntries`; emit `expires: null` when `!refresh`; `getAuthStatus()` passes `registrySnapshot`; `resolveAuthJsonKey` and the DELETE route's `isOAuthRow` read the same union (registry ∪ stored-oauth ids); update `__tests__/build-auth-status.test.ts:8` and `provider-auth-storage.test.ts:80` fixtures to the new entry shape; verify both pass
- [x] 4.8 Tests (L1) in `packages/server/src/__tests__/build-auth-status.test.ts` (harness exemplar: same file, existing `makeOAuthHandler` → new `makeOAuthEntry`): test-plan E23 — `openrouter` cred `refresh:""` + anthropic `refresh:"r"` · `GET /status` · `openrouter.expires === null`, `anthropic.expires === 1234`; E24 — `refresh` key absent · status · `expires null`; E25 — `openrouter:{type:"api_key"}` + catalogue `openrouter` · status · `openrouter` oauth row `authenticated:false` AND `openrouter-api` row `authenticated:true` (repeat fixture for kimi-coding, meta, xai); E26 — `some-future-provider:{type:"oauth"}` not in registry · status then DELETE · row present + connected, DELETE removes key; E27 — catalogue `custom-llm hasOAuth custom` · status + start · no oauth row, 400 `Unknown OAuth provider: custom-llm`; verify passes

## 5. Server: routes (design D2, D6, D7) — TDD

- [x] 5.1 Write failing route tests in `packages/server/src/__tests__/provider-auth-routes.test.ts` (harness exemplar: existing `app.inject` bootstrap `:62`, fake from 3.1 injected as the registry): test-plan E4 — presets anthropic/openrouter/codex/copilot/kimi/meta/xai · `POST /start` · 200 each, `pending.kind` per table, `authUrl` only for anthropic+openrouter, openrouter's `progress` did not resolve the handshake; E5 — copilot + `enterpriseDomain:""` · start · `pending device_code`, text prompt answered `""`, never reported pending; E6 — `enterpriseDomain: null` and `42` · start · `pending text`, no TypeError; E8 — ids `radius`, `google-gemini-cli`, `google-antigravity`, `custom-llm`, `mistral`, `""` · start · 400 `Unknown OAuth provider: <id>`, no record created; E10 — codex · input browser id then GET · 202, `authUrl` + `manual_code`; E11 — codex · input device id · `device_code`, no `authUrl`; E12 — any pending flow · GET · body keys ⊆ allowed set, no server-only fields; E13 — 50 starts · collect ids · UUID v4, distinct; E15 — xai `device_code` pending · input · 409 `No input pending for this flow`; E16 — logger spied · input URL containing `SECRET123` · 202, string appears in no log/body/error; E17 — copilot no domain · input `"company.ghe.com"` · `device_code` with `verificationUri` host `company.ghe.com`; verify they fail
- [x] 5.2 Implement in `packages/server/src/routes/provider-auth-routes.ts`: `POST /start` (D6 handshake: same-provider supersede, `typeof enterpriseDomain === "string"` guard, `Promise.race` first-event / login-settled / 15 s with timer cleared, resolve-before-event runs the normal write path, `openInBrowser` on first `authUrl`), `GET /flow/:flowId`, `POST /flow/:flowId/input` (202 / 409 / 404, value never logged), `DELETE /flow/:flowId` (204 / 404); remove `authorize`, `device-code`, `device-status`; every route `await oauthRegistryReady`; replace `server.ts:3380` shutdown hook with `abortAllFlows()`; verify 5.1 passes
- [x] 5.3 Tests (L1, error-handling) in `packages/server/src/__tests__/provider-auth-flow-lifecycle.test.ts` (harness exemplar: `auto-shutdown.test.ts` for `vi.useFakeTimers`; fake from 3.1): test-plan X1 — fake rejects `EADDRINUSE …:53692` before any event · start · 500 `{ error }` containing `53692`, `GET /flow/:id` 404; X2 — fake never emits · start, advance 15 s · 504 `Provider did not respond`, `signal.aborted`, record deleted; and a fake emitting at 14.9 s → 200; X3 — pending anthropic A · start anthropic again · A aborted + `Cancelled`, B 200, only B in map; X4 — anthropic preset modelling pi-ai's `finally` semantics · DELETE · 204, `Cancelled` within 1 s, `listenerClosed === true`; X7 — `openrouter:{type:"api_key"}` stored, fake resolves OAuth cred · complete · `status error` with `CredentialTypeConflictError` message, `auth.json` byte-identical; X8 — fake throws `"OAuth state mismatch"` on bad state · input · 202 then `status error "OAuth state mismatch"`; X12 — open listener, advance past `expiresAt`, prune · `signal.aborted`, `listenerClosed`, record gone, new start 200; E18 — `expiresInSeconds:900` · advance 10 min + 1 s · still 200; E19 — no device code · 9:59 → 200, 10:01 → 404; E31 — three pending fakes · server `close()` · all aborted, all `Cancelled`, map empty; P2 — 200 starts over simulated 30 min · after prune · map size 0, all aborted, `openListeners === 0`; verify passes
- [x] 5.4 Tests (L1) in `packages/server/src/__tests__/health-endpoint.test.ts` (harness exemplar: same file): test-plan X10 — `import()` stubbed to throw, and separately `create()` returning `{ getProviders: () => [] }` · boot · `/handlers` `{ ids: [] }`, `/api/health` `providerAuth.error` non-empty containing a version or `unknown`, `/status` 200; X11 — registry failed + stored `anthropic` OAuth · status / DELETE / start · row connected, DELETE removes, start 400; verify passes
- [x] 5.5 Update `__tests__/provider-auth-credential-conflict.test.ts:29`, `provider-auth-device-code-conflict.test.ts`, `provider-auth-device-code-await.test.ts` to the new routes or fold their assertions into 5.1/5.3 and delete them; verify `set -o pipefail; npm test -- packages/server 2>&1 | tee /tmp/pi-test.log` green

## 6. Client: prompt-driven pane (design D5; specs provider-auth-ui, provider-add-flow) — TDD

- [x] 6.1 Write failing component tests (harness exemplar: `packages/client/src/__tests__/ProviderAddDialog.test.tsx` `stubFetch(script)` at `:70`): test-plan E29 — mocked `/start` → `manual_code` / `select` / `text` / `device_code` · render · paste field / option buttons / text field / device pane, auth link rendered whenever `authUrl`; E30 (in `ProviderAuthSection.test.tsx`) — row `openrouter authenticated:true expires:null` · render · no expiry text, no "expired", `relativeExpiry` not called with `null`; verify they fail
- [x] 6.2 Rework `packages/client/src/components/settings/ProviderAuthSection.tsx:380-503` (visual spec: `mockups/sign-in-pane.html` states 1–8 + `mockups/ui-plan.md` decisions; auth link and paste field render together from the first poll; helper text uses `--text-tertiary`, not `--text-muted`): replace the `authorize`/`device-code` starts + `/status`/`device-status` polls with one `POST /start { provider, enterpriseDomain? }` and a `GET /flow/:flowId` poll for every flow (timer in `flowsTimersRef`); `waiting` payload becomes `OAuthFlowStatus`; render `pending` per kind (`manual_code`/`text` → labelled field + Submit posting to `/input`, cleared after; `select` → one button per option; `device_code` → existing device pane); Cancel → `DELETE /flow/:flowId` + stop timer + picker; 500/504 on start → error + Try Again, no poll; `expires === null` → no expiry; GitHub Enterprise prompt sends `enterpriseDomain`; update `ProviderAuthSection.refetch.test.tsx:34` and `ProviderAuthSection.poll-budget.test.tsx:76` mocks; verify 6.1 and the existing client suite pass
- [x] 6.3 i18n: add keys for the paste/text field fallback label, Submit, Cancel, select prompt, and the start-failure state; verify `npm run i18n:parity` and `npm run i18n:lint` pass

## 7. E2E (L3, docker harness) — test-plan frontend rows

- [x] 7.1 Author `tests/e2e/delegate-provider-oauth-flow.spec.ts` (harness exemplar: `tests/e2e/redesign-provider-add-flow.spec.ts` `page.route` mocks at `:44`; read the harness port from `.pi-test-harness.json`): test-plan F1 — `/start` → `{ authUrl, pending manual_code }`, `/flow/:id` pending until `/input` seen then `complete`, `/status` lists anthropic · Add → Anthropic → paste → Submit · field cleared, dialog closed, connected, exactly one `POST /input`; F2 — `/start` → `select [browser, device]`, `/input` device → `device_code` · choose device · device pane for the SAME `flowId`, no second `/start`, no tab opened; F3 — picker with `xai`, `kimi-coding`, `meta` · select each · identical device-pane test ids as Copilot; F4 — select GitHub Copilot · enter `company.ghe.com` · `POST /start` body has `enterpriseDomain`, no text field rendered after; F5 — pending flow · Cancel · one `DELETE /flow/:id`, no `/flow/:id` GET within 5 s, picker shown, not connected; F6 — `/flow/:id` → `expired` · poll · "Code expired" + Try Again → new `POST /start`; F7 — `/start` → 500 `Port 53692 in use` and 504 `Provider did not respond` · start · error verbatim + Try Again, zero `/flow/*` GETs; F8 — pending flow · dismiss, wait, reopen · ≥ 1 `/flow/:id` GET during the gap, list refreshes on `complete`; F9 — `window.open` stubbed → `null` · start anthropic · copyable link AND paste field visible; verify `npm run test:e2e -- delegate-provider-oauth-flow` green via `docker/test-up.sh` / `test-down.sh`
- [x] 7.2 Update `tests/e2e/redesign-provider-add-flow.spec.ts:44,238` mocks from `/authorize` to `/start`; verify that spec still passes

## 8. Verification, manual scenarios, docs

- [x] 8.1 Run `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` and `npm run quality:changed`; verify green
- [x] 8.2 Manual (test-plan X14, manual-only): real Anthropic Claude Pro/Max account · sign in via callback on the same host, sign out, sign in via paste from a remote browser · diff `~/.pi/agent/auth.json` entries against a pre-change login (superset of fields; a pi session can use it) — record outcome in `test-plan.md`
- [x] 8.3 Manual (test-plan X15, manual-only): real GitHub Copilot + one of xai/kimi-coding/meta · sign in from the dashboard · `pi` → `/login` status shows each credential
- [x] 8.4 Manual (test-plan X16, manual-only): docker harness up, browser outside the container · start anthropic, complete via paste · `status: "complete"` against the real provider
- [x] 8.5 Manual (test-plan F10, manual-only): paste + select + text + device panes in all four themes · human review of layout
- [x] 8.6 Delegate `docs/` prose to DocScribe: `docs/architecture.md` provider-auth section (ModelRuntime-sourced registry, `/start` + `/flow/:id` + `/input` + cancel, D3 resolution rule, six pins), `packages/server/src/auth/AGENTS.md` rows (delete `oauth-callback-server.ts`; add `pi-oauth-types.ts`, `provider-auth-registry.ts`, `provider-auth-adapter.ts`; rewrite `provider-auth-handlers.ts`), `packages/server/src/routes/AGENTS.md`, `packages/server/src/__tests__/helpers/AGENTS.md` (new `fake-oauth-flow.ts`), and a note in `openspec/changes/add-mid-turn-tool-oauth/design.md` pointing its PATTERN B reference at git history; verify `kb_search "provider-auth-adapter"` returns the new rows
