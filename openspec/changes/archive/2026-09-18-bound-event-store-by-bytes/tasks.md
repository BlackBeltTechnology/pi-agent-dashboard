## 1. Config surface (`packages/shared`, `packages/server/src/config-api.ts`)

- [x] 1.1 Test loader partitions for `maxBytesPerSession` — see `packages/shared/src/__tests__/config.test.ts` for harness glue. Triple: absent / `0` / `-1` / `"x"` / `1000` (input) · config load (trigger) · absent+`-1`+`"x"` → `33554432`, `0` preserved as `0`, `1000` loaded as-is for the store to clamp (observable). Verify red first. (test-plan #E11)
- [x] 1.2 Test loader partitions for `maxTotalEventBytes` — see `packages/shared/src/__tests__/config.test.ts`. Triple: absent / `0` / garbage (input) · config load (trigger) · absent+garbage → `805306368`, `0` preserved (observable). Verify red first. (test-plan #E12)
- [x] 1.3 Test loader partitions for `maxCachedSessions` — see `packages/shared/src/__tests__/config.test.ts`. Triple: absent / `0` / negative / garbage / `1` (input) · config load (trigger) · absent+`0`+negative+garbage → `32`, `1` honoured (observable). Verify red first. (test-plan #E13)
- [x] 1.4 Test store default and config default agree — see `packages/shared/src/__tests__/config.test.ts`. Triple: the two constants living in different packages (input) · direct comparison (trigger) · `DEFAULT_MAX_CACHED_SESSIONS` === the configured default, both `32` (observable). Verify red first. (test-plan #E14)
- [x] 1.5 Implement `MemoryLimitsConfig.maxBytesPerSession` (32 MiB), `maxTotalEventBytes` (768 MiB) and `maxCachedSessions` (32) in `memory-limits.ts`, plus the `config.ts` loader branches (D6/D7/D8). The loader does type/negative validation ONLY — the floor clamp belongs to the store (D5). Move `DEFAULT_MAX_CACHED_SESSIONS` 100 → 32. Verify 1.1–1.4 green and the `config-api.test.ts` `writeConfigPartial` round-trip still green with the new keys present.

## 2. Store accounting — per-session budget (`packages/server/src/persistence/memory-event-store.ts`)

- [x] 2.1 Test near-ceiling flood stays under budget — see `packages/server/src/__tests__/memory-event-store.test.ts`. Triple: budget 1 MiB, per-event ceiling 256 KiB, count cap 100 000 (input) · insert 100 × ~200 KiB non-essential events (trigger) · resident bytes ≤ 1 MiB + `BYTE_TRIM_SLACK` after every insert, surviving seqs are the newest (observable). Verify red first. (test-plan #E1)
- [x] 2.2 Test reclaim targets the budget, not the trigger — see `memory-event-store.test.ts`. Triple: budget 1 MiB, total driven just past `budget + slack` (input) · the insert that crosses the threshold (trigger) · resident bytes ≤ budget after the pass, NOT merely ≤ budget + slack (observable). Verify red first. (test-plan #E2)
- [x] 2.3 Test the slack window suppresses the next pass — see `memory-event-store.test.ts` (reuse the trim-pass probe). Triple: buffer just reclaimed to budget (input) · insert events totalling less than `BYTE_TRIM_SLACK` (trigger) · zero further reclaim passes (observable). Verify red first. (test-plan #E3)
- [x] 2.4 Test the exact-threshold boundary does not trim — see `memory-event-store.test.ts`. Triple: resident bytes driven to exactly `budget + BYTE_TRIM_SLACK` (input) · next read (trigger) · no reclaim has run, all events resident (observable). Verify red first. (test-plan #E4)
- [x] 2.5 Test budget `0` disables the byte bound — see `memory-event-store.test.ts`. Triple: `maxBytesPerSession` `0`, count cap 100 (input) · insert 100 × 200 KiB (trigger) · all 100 resident, zero byte-triggered passes INCLUDING on the first event ever stored (observable). Verify red first. (test-plan #E5)
- [x] 2.6 Test a single event larger than the budget is admitted — see `memory-event-store.test.ts`. Triple: budget 1 MiB, one 2 MiB event, empty buffer (input) · insert it (trigger) · event retained, buffer not emptied, one-event exception holds (observable). Verify red first. (test-plan #E6)
- [x] 2.7 Test the chat head survives an ordinary byte trim — see `memory-event-store.test.ts`. Triple: seq 1 `message_start`, seq 2 `message_end`, then large `tool_execution_update`s past budget (input) · reclaim runs (trigger) · seq 1 and seq 2 present, only oldest non-essentials dropped (observable). Verify red first. (test-plan #E7)
- [x] 2.8 Test essentials drop only when essentials alone exceed the budget — see `memory-event-store.test.ts`. Triple: a session whose `message_*` events alone exceed the budget (input) · reclaim runs (trigger) · every non-essential dropped first, then oldest essentials, total within budget (observable). Verify red first. (test-plan #E8)
- [x] 2.9 Test the floor clamp does not touch `0` — see `memory-event-store.test.ts`. Triple: `maxBytesPerSession` `0`, per-event ceiling 256 KiB (input) · store constructed (trigger) · effective budget stays `0`/unlimited, no byte bound enforced (observable). Verify red first. (test-plan #E9)
- [x] 2.10 Test the floor is meaningful when the per-event ceiling is disabled — see `memory-event-store.test.ts`. Triple: per-event ceiling `0`, `maxBytesPerSession` 1 MiB (input) · store constructed (trigger) · effective budget raised above the finite measurement cap, not left at 1 MiB (observable). Verify red first. (test-plan #E10)
- [x] 2.11 Test the bound-enabled matrix — see `memory-event-store.test.ts`. Triple: all four combinations of `maxEvents > 0` × `maxBytes > 0` (input) · insert past each bound (trigger) · only enabled bounds trigger a pass, both-disabled retains everything (observable). Verify red first. (test-plan #E16)
- [x] 2.12 Test accounting is exact after every removal path — see `memory-event-store.test.ts`. Triple: one session driven through count trim, `collapseSuperseded`, `collapseOnEnd`, `deleteEventsForSession` (input) · after each step (trigger) · `getBufferBytes(sid)` === Σ `e.bytes` over `getEvents(sid, 0)` (observable). Verify red first. (test-plan #E21)
- [x] 2.13 Test byte reclaim is amortized — see the existing trim-linearity probe in `memory-event-store.test.ts`. Triple: 10 000 events under a trimming per-session budget (input) · bulk insert loop (trigger) · trim passes ≈ inserts / slack-window, NOT ≈ inserts (observable). Verify red first. (test-plan #P1)
- [x] 2.14 Implement D1–D3 + D5 + D9: `StoredEvent.bytes`, `SessionBuffer.bytes`, measurement in `insertEvent` via `measureBytes` with a FINITE `MEASURE_CEILING_FALLBACK` when the per-event ceiling is `0`, the in-store floor clamp (exempting `0`, derived from the EFFECTIVE ceiling), `trimBufferToLimit(buf, {maxEvents, maxBytes})` returning `bytesDropped`, the hysteretic `maxBytes > 0`-guarded trigger reclaiming DOWN TO the budget, the single-event floor, and decrements on `dropIfSuperseded` / `deleteEventsForSession`. Verify 2.1–2.13 green and the full existing `memory-event-store*` suite green.

## 3. Store accounting — global budget

- [x] 3.1 Test global budget evicts LRU-first — see `memory-event-store.test.ts`. Triple: `maxTotalEventBytes` 4 MiB, per-session budget `0`, count cap high, 4 unpinned sessions × ~1.5 MiB (input) · inserts push the total past `budget + GLOBAL_TRIM_SLACK` (trigger) · global total ≤ budget after the pass, least-recently-accessed buffers gone, newest intact (observable). Verify red first. (test-plan #E17)
- [x] 3.2 Test pinned sessions survive global reclaim — see `memory-event-store.test.ts`. Triple: same setup with the LRU session pinned via `isSessionPinned` (input) · global reclaim runs (trigger) · pinned buffer resident, an unpinned more-recently-accessed buffer evicted instead (observable). Verify red first. (test-plan #E18)
- [x] 3.3 Test global budget `0` disables global reclaim — see `memory-event-store.test.ts`. Triple: `maxTotalEventBytes` `0`, total far above any budget (input) · inserts (trigger) · no buffer evicted on account of bytes, including on the first event (observable). Verify red first. (test-plan #E19)
- [x] 3.4 Test the per-session budget never byte-trims under the global bound — see `memory-event-store.test.ts`. Triple: `maxBytesPerSession` 32 MiB, 32 unpinned sessions each holding 32 MiB, global 768 MiB (input) · global reclaim runs (trigger) · whole buffers evicted until ≤ 768 MiB, no individual session byte-trimmed (observable). Verify red first. (test-plan #E20)
- [x] 3.5 Test configured resident count is honoured — see `memory-event-store.test.ts`. Triple: `maxCachedSessions` 8 with 8 unpinned buffers resident (input) · a 9th unpinned buffer is created (trigger) · least-recently-accessed unpinned buffer evicted, 8 resident (observable). Verify red first. (test-plan #E15)
- [x] 3.6 Test global total exactness — see `memory-event-store.test.ts`. Triple: multiple sessions driven through every removal path including eviction (input) · after each step (trigger) · global total === Σ per-session totals === Σ resident `e.bytes` (observable). Verify red first. (test-plan #E22)
- [x] 3.7 Test global reclaim is amortized, not per-insert — see the trim-linearity probe in `memory-event-store.test.ts`. Triple: sustained inserts across N sessions resting at the global budget (input) · continued inserts (trigger) · global reclaim passes ≪ inserts, no per-insert buffer-map sort (observable). Verify red first. (test-plan #P2)
- [x] 3.8 Test the all-pinned fallback spares essentials — see `memory-event-store.test.ts`. Triple: every resident session pinned and the global total over threshold (input) · fallback reclaim runs (trigger) · only non-essentials dropped, that session's `message_start` / `message_end` survive (observable). Verify red first. (test-plan #X1)
- [x] 3.9 Test the fallback reclaims below the budget — see `memory-event-store.test.ts`. Triple: all pinned, over threshold (input) · fallback runs (trigger) · global total reclaimed to `budget - GLOBAL_TRIM_SLACK`, next insert does not re-trigger it (observable). Verify red first. (test-plan #X2)
- [x] 3.10 Test the exhausted fallback accepts overshoot and latches — see `memory-event-store.test.ts`. Triple: all pinned with no non-essentials left to drop (input) · fallback runs (trigger) · essentials retained, total stays above budget, `globalBudgetExceeded` latch set (observable). Verify red first. (test-plan #X3)
- [x] 3.11 Test the latch clears on a state change — see `memory-event-store.test.ts`. Triple: latch set (input) · a buffer is deleted / a session unpins / the total falls below budget (trigger) · latch clears and reclaim resumes on the next qualifying insert (observable). Verify red first. (test-plan #X4)
- [x] 3.12 Test the latch stops the rescan — see the trim-linearity probe in `memory-event-store.test.ts`. Triple: all sessions pinned, non-essentials exhausted, inserts continue (input) · continued inserts (trigger) · fallback scan runs once then zero further scans until a latch-clearing event (observable). Verify red first. (test-plan #P3)
- [x] 3.13 Test measurement stays bounded with the ceiling disabled — see `memory-event-store.test.ts`. Triple: per-event ceiling `0` and a deeply nested / very large payload (input) · insert (trigger) · measurement terminates at the finite fallback cap, accounted at cap + 1, no unbounded walk (observable). Verify red first. (test-plan #X5)
- [x] 3.14 Test cold-load hydration respects the budget — see `memory-event-store.test.ts`. Triple: a transcript larger than `maxBytesPerSession` replayed through `insertEvent` (input) · session cold-loads (trigger) · insert loop completes, resident bytes within budget, restored depth bounded by bytes rather than the count cap (observable). Verify red first. (test-plan #X7)
- [x] 3.15 Implement D7 + D8: incremental global total maintained from the per-session totals (no walk), decremented on every removal path including LRU eviction; `GLOBAL_TRIM_SLACK` hysteresis reclaiming down to the budget; `maxTotalEventBytes > 0` guard; LRU-first whole-buffer eviction skipping pinned; the non-essential-only all-pinned fallback with its `globalBudgetExceeded` latch and clear conditions. Verify 3.1–3.14 green and the full existing `memory-event-store*` suite green.

## 4. Telemetry (`memory-event-store.ts`, `packages/server/src/routes/system-routes.ts`)

- [x] 4.1 Test byte trim moves both counters — see `memory-event-store.test.ts`. Triple: a byte-triggered pass dropping N events / B bytes (input) · pass runs (trigger) · `trimmedBytes` += B and `trimmedEvents.total` += N (observable). Verify red first. (test-plan #T1)
- [x] 4.2 Test count-only trim does not move the byte counter — see `memory-event-store.test.ts`. Triple: count trim while the byte total is under budget (input) · pass runs (trigger) · `trimmedBytes` unchanged (observable). Verify red first. (test-plan #T2)
- [x] 4.3 Test eviction is counted in bytes separately — see `memory-event-store.test.ts`. Triple: whole-buffer global eviction (input) · reclaim runs (trigger) · `evictedBytes` += released bytes, `evictedSessions` += 1, `trimmedBytes` unchanged (observable). Verify red first. (test-plan #T3)
- [x] 4.4 Test fallback reclaim counts as a byte trim — see `memory-event-store.test.ts`. Triple: all-pinned fallback drops non-essentials (input) · reclaim runs (trigger) · `trimmedBytes` += released bytes (observable). Verify red first. (test-plan #T4)
- [x] 4.5 Test health carries the new counter additively — see `packages/server/src/__tests__/health-shape.test.ts`. Triple: `/api/health` requested (input) · route handler (trigger) · `storeTrim.trimmedBytes` present and every prior `storeTrim` field present with its original name and type (observable). Verify red first. (test-plan #T5)
- [x] 4.6 Test retention signals sit outside the counter struct — see `health-shape.test.ts`. Triple: `/api/health` requested (input) · route handler (trigger) · `storeRetention` carries `residentBytes`, effective budgets and `globalBudgetExceeded`; `storeTrim` carries none of them; `heapSizeLimit` sits beside the process gauges (observable). Verify red first. (test-plan #T6)
- [x] 4.7 Test the effective budget reflects the store's clamp — see `health-shape.test.ts`. Triple: a configured per-session budget below the floor (input) · `/api/health` requested (trigger) · reported effective budget is the clamped value, not the configured one (observable). Verify red first. (test-plan #T7)
- [x] 4.8 Implement D4: `TrimStats.trimmedBytes` + `evictedBytes` with the attribution table, `EMPTY_TRIM_STATS` updated, the new `storeRetention` health block (`residentBytes`, effective budgets, `globalBudgetExceeded`), `server.heapSizeLimit` via `v8.getHeapStatistics().heap_size_limit`, and a TEST-ONLY `getBufferBytes(sessionId)` probe in the style of `getRangeProbe`. Verify 4.1–4.7 green.
- [x] 4.9 Test health fields are non-null on a running server — see `qa/tests/02-server-start.sh` for harness glue (CLI/process smoke, NO rendered-UI asserts). Triple: a booted dashboard server (input) · `curl /api/health` (trigger) · `storeRetention.residentBytes`, `storeRetention.effective.*` and `server.heapSizeLimit` present and non-null (observable). (test-plan #T8)

## 5. Wiring + Settings (`server.ts`, `packages/client/src/components/settings/`)

- [x] 5.1 Implement: thread `config.memoryLimits.maxBytesPerSession`, `maxTotalEventBytes` and `maxCachedSessions` into `createMemoryEventStore`, REPLACING the hardcoded `undefined, // maxCachedSessions (use default)` at `server.ts:925`. Verify the server boots (`curl /api/health`) and `server.test.ts` is green.
- [x] 5.2 Test controls render configured values — see `packages/client/src/components/settings/__tests__/settings-field-contract.test.tsx`. Triple: config `maxBytesPerSession` `33554432` and `maxTotalEventBytes` `805306368` (input) · settings panel loads (trigger) · controls show `32` and `768`, MiB converted at the edge (observable). Verify red first. (test-plan #F1)
- [x] 5.3 Test controls render defaults when absent — see `settings-field-contract.test.tsx`. Triple: a config carrying none of the three keys (input) · settings panel loads (trigger) · controls show `32`, `768`, `32` (observable). Verify red first. (test-plan #F2)
- [x] 5.4 Test the edited value is written back in bytes — see `settings-field-contract.test.tsx`. Triple: user sets the per-session control to `32` (input) · save (trigger) · write includes `memoryLimits.maxBytesPerSession` = `33554432` (observable). Verify red first. (test-plan #F3)
- [x] 5.5 Test editing one control does not pin the others — see `settings-field-contract.test.tsx`. Triple: user changes only `maxCachedSessions` (input) · save (trigger) · write includes `maxCachedSessions` only, not `maxTotalEventBytes`, not `maxBytesPerSession` (observable). Verify red first. (test-plan #F4)
- [x] 5.6 Test an unrelated Memory Limits field pins none of the new keys — see `settings-field-contract.test.tsx`. Triple: user changes only `maxEventsPerSession` (input) · save (trigger) · write includes none of the three new keys (observable). Verify red first. (test-plan #F5)
- [x] 5.7 Test restart-required is indicated — see `settings-field-contract.test.tsx`. Triple: user changes any of the three controls (input) · change committed (trigger) · restart-required badge shown, consistent with the sibling controls (observable). Verify red first. (test-plan #F6)
- [x] 5.8 Test a sub-floor value is accepted as typed — see `packages/client/src/components/settings/__tests__/settings-bespoke-validation.test.tsx`. Triple: user types a per-session budget below the floor (input) · save (trigger) · value written as typed with no client-side clamp or block; the hint states a floor applies (observable). Verify red first. (test-plan #F7)
- [x] 5.9 Implement the three controls in `SettingsPanel.tsx` Memory Limits with MiB ↔ bytes conversion at the edge; hints per the settings-panel spec (global bound evicts whole idle sessions; evicted sessions re-read from transcript; a floor applies and the effective value is visible in health); i18n keys with an English fallback in every locale file. Verify 5.2–5.8 green and `settings-bespoke-validation.test.tsx` green.

## 6. Browser-visible behaviour (`tests/e2e/`)

- [x] 6.1 Test a byte-trimmed range still heals on resubscribe — see `tests/e2e/history-backfill-gap.spec.ts` for harness glue (docker harness port from `.pi-test-harness.json` `dashboardPort`, never hardcode `:18000`). Triple: a browser subscribed with a `lastSeq` inside a byte-trimmed range (input) · resubscribe (trigger) · the server serves the events above `lastSeq` that remain and the view converges with a gap, no error (observable). Verify red first. (test-plan #F8)
- [x] 6.2 Test history backfill degrades gracefully after a byte trim — see `tests/e2e/history-backfill-gap.spec.ts`. Triple: a session whose oldest events were released by a byte reclaim (input) · the client requests history older than the oldest retained event (trigger) · exactly one `history_backfill_result` returning what remains, signalling no-further-history, no error and no hang (observable). Verify red first. (test-plan #X6)

## 7. Docs + closeout

- [x] 7.1 Delegate to `DocScribe`: `docs/architecture.md` memory-limits rows for `maxBytesPerSession`, `maxTotalEventBytes`, `maxCachedSessions` (defaults, shed order, `0`, the in-store floor clamp); one line in the event-store section noting the envelope is the PRODUCT of the per-session budget and the resident count; one line that the budget counts serialized bytes, not RSS. Verify grep finds all three keys in `docs/architecture.md`.
- [x] 7.2 Update `packages/server/src/persistence/memory-event-store.ts.AGENTS.md` (bytes accounting, generalized trim, global budget + latch, `trimmedBytes`/`evictedBytes`, probe), `packages/shared/src/AGENTS.md` (`memory-limits.ts` row), `packages/server/src/routes/AGENTS.md` (`system-routes.ts` `EMPTY_TRIM_STATS` + `storeRetention`), and the client settings `AGENTS.md`. Verify `kb dox lint` clean.
- [x] 7.3 Full suite `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` with zero failures; `npm run quality:changed` clean.
- [x] 7.4 Comment on #425 with the change name.
- [x] 7.5 File the follow-up change for heap-pressure shedding (cut from this change during review: no defined trigger site, no defined target at budget `0`, and unreachable at the default budgets).
- [x] 7.6 Manual: after the live server has run ≥24 h on the new defaults, confirm `storeRetention.residentBytes` holds under `maxTotalEventBytes`, `evictedSessions` is now non-zero, and `heapUsed` no longer trends toward the ceiling. Compare against the pre-change baseline (686 MB strings / 798 MB live set / `evictedSessions = 0`). (test-plan: manual-only)
