## 1. Real-process project (`packages/server/`, root `vitest.config.ts`, `package.json`)

- [x] 1.1 Census — run the D2 spawn predicate over `packages/server/src/**/__tests__/*.test.ts`, read each hit, and write `packages/server/vitest.real-process-files.ts` (`REAL_PROCESS_TESTS: string[]`) with a one-line reason per entry (keeper/mock-pi/wrapper/full-server/signal). Verify: every listed file exists; the 23 grep hits are each either listed or annotated `// real-process-exempt: <reason>` at the call.
  - 10 members. The design's literal token list (`spawn(process.execPath`, `fork(`, …) under-collected — it found only 4 files and missed `cli-signal-forwarding` (spawns a wrapper path), `headless-shutdown-fallback` and `shutdown-terminates-any-strategy` (`spawn("sleep")`). Implemented predicate: a value-import of `spawn`/`spawnSync`/`fork` from `node:child_process`, or `spawnKeeper(`/`bootRealServer(`. `execSync`/`execFileSync` deliberately excluded — the ~12 git/render tests run a short synchronous CLI and observe nothing through the process table.
  - Of the 23 broad-grep hits, 13 are non-members: `tunnel.test.ts` `vi.mock`s child_process; the rest only mention `SIGTERM` in a comment or a mocked `killPidWithGroup` assertion. The proposal's "first members" guesses (`headless-pid-registry-kill-*`, `recovery-exit-intent`, `shutdown-orphan-signal`, `keeper-electron-run-as-node`) spawn nothing; `reclamation-soak` does not exist. Verified per file, not by name.
- [x] 1.2 Test the disjointness guard — new `packages/shared/src/__tests__/real-process-project-guard.test.ts` (tree-scan pattern of the fixed-tick guard): (a) listed files exist; (b) main include minus exclude is disjoint from the list; (c) a fixture file under a temp dir that calls `spawnKeeper(` and is not listed fails naming it; (d) no vitest config in the tree other than the real-process one contains `retry:`. Verify red first (c, d).
  - Red verified for all four: (a)/(b)/(c) red before the implementation; (c) re-verified by adding a temp `zz-tmp-fixture.test.ts` that imports `spawn` (failed naming the file, passed once annotated `// real-process-exempt:`); (d) re-verified by injecting `retry: 1` into `packages/shared/vitest.config.ts` (failed, then restored).
- [x] 1.3 Implement D1: `packages/server/vitest.real-process.config.ts` (`name: "server-real-process"`, include from the list, `maxWorkers: 2`, 60 s budgets, `retry: process.env.CI ? 1 : 0`, same globalSetup/setupFiles/alias as the server config); `packages/server/vitest.config.ts` `exclude: REAL_PROCESS_TESTS`; root `package.json` `test` = main run `&&` real-process run (same `HOME`/`NODE_OPTIONS` env), plus `test:real-process` alone. Verify 1.2 green; `npm test` runs both phases; `npx vitest run --config packages/server/vitest.real-process.config.ts` runs only the listed files.
  - Defect found + fixed during verification: invoked from the REPO root (which is how `npm run test:real-process` runs it) vitest resolved `include: src/**` against the repo root and collected ZERO files — a silently-empty phase. Config now pins `root: __dirname`.
  - `vitest list --config packages/server/vitest.real-process.config.ts` shows exactly the 10 listed files; the phase runs green in 52 s (9 passed, 1 skipped — `faux-session.integration` self-skips without `pi` on PATH).
- [x] 1.4 Verify no starvation: run `npm test` three times on a loaded machine (a parallel `npm run build` in another shell) — keeper E5 passes every time with no retry (`grep -c "retry x" /tmp/pi-test.log` = 0). Record the three timings in `openspec/changes/isolate-real-process-tests/measurements.md`.

## 2. CI report artifact (`.github/workflows/ci.yml`)

- [x] 2.1 Test the workflow contract — extend the existing ci-workflow repo-lint (or add a case beside `nightly-workflow-contract.test.ts`): the `pnpm test` step is followed by an `actions/upload-artifact` step with `if: always()` whose `path` includes `test-results/vitest*.json`. Verify red first.
  - New `packages/shared/src/__tests__/ci-vitest-report-artifact.test.ts`. Red first (3 failing). `if: always()` re-verified fail-closed by deleting the line and re-running.
- [x] 2.2 Implement D3: add `--reporter=default --reporter=json --outputFile=test-results/vitest.json` to both phases in the `test` script (or via `vitest.config` `reporters` under `CI`); the upload step with `retention-days: 14`. Verify 2.1 green; a dispatch of `ci.yml` on the branch shows the artifact on the run page.
  - Took the `vitest.config` route (design's stated alternative) so local runs write nothing: root config and the real-process config each add `reporters: ["default","json"]` + `outputFile` only under `CI`. Separate files (`test-results/vitest.json`, `test-results/vitest-real-process.json`) so the second phase cannot clobber the first; the upload glob `test-results/vitest*.json` covers both. `test-results/` was already gitignored.
  - **D3's stated attribution field does not exist.** Verified against vitest 4.1.11 with a fail-once fixture: the JSON reporter emits `{ancestorTitles, fullName, status, title, duration, failureMessages, meta, tags}` — no `retryCount`. A retried PASS is instead `status: "passed"` with the failed attempt's error retained in `failureMessages`; the default reporter's `(retry x1)` marker only shows for tests it lists, so a retried pass is invisible in the console. The spec scenario, `ci.yml`, both vitest configs, `docs/code-quality.md` and the `ci-troubleshoot` skill now carry that signature and its jq recipe. Caught by the step-4.5 review, not by a test.

## 3. jsdom races (`packages/client/src/**/__tests__/`)

- [x] 3.1 Root-cause each member — for `SettingsPanel` "Initialize on worktree", `EditorSearchPanel` "Esc dismisses the hoisted overlay", `EditorSearchPanel` "keyboard: ArrowDown + Enter …", `FilePreviewContext` "E24 …": read the test and the component, name the effect/flush the one-shot read or premature `fireEvent` races, and write it as a one-line `// race: …` comment above the fix. Verify: reproduce at least one by running the file under `stress` (`vitest run <file> --repeat 20` with a parallel `npm run build`) before the fix.
  - Test-name drift corrected against the tree: "Esc dismisses the hoisted overlay" lives in `FilePreviewContext.test.tsx`, not `EditorSearchPanel.test.tsx`. No test named `E24: link handover …` exists; the link-handover test is `FilePreviewContext` "opening file A then file B renders exactly one overlay, showing B", which ALREADY polls with `waitFor` and is compliant as written — left untouched.
  - Races named: `SettingsPanel` — the label renders from the defaults before the fetched config baseline commits, so a click inside that window is diffed against an unset baseline and no Save Bar appears. `EditorSearchPanel` — `onKeyDown` closes over `activeIndex`; `Enter` dispatched before the `ArrowDown` state re-renders runs the stale closure and opens item 0. `FilePreviewContext` — the Escape listener is registered in an effect and unmounts the overlay on a later commit, so a one-shot read can see it still mounted.
  - **Not reproduced locally.** 22 stress iterations of the three files (12 against a `npm run build` loop, 10 against a concurrent full `npm run test:parallel`) all passed: `FAILS=0/12` and `FAILS=0/10`. Vitest 4 dropped `--repeat`/`--repeats`, so the stress was a shell loop. These are CI-load flakes this machine does not starve hard enough to surface; each fix is instead justified statically from the code paths above, and each new poll was verified fail-closed (the `EditorSearchPanel` selection poll reds when pointed at row 0 instead of row 1).
- [x] 3.2 Fix each per D4 (poll the dependent state before the event; `findBy*` before the click). Verify: the four files pass `--repeat 20` under load, and the fixed-tick guard stays green.
  - `SettingsPanel`: poll `aria-checked === "false"` on the switch before the toggle; `await screen.findByTestId("save-btn")` before the Save click. `EditorSearchPanel`: poll the rendered selection (active row carries a bare `bg-[var(--bg-hover)]`) before `Enter`, and `waitFor` both callback assertions. `FilePreviewContext`: `waitFor` the overlay removal after the Escape dispatch. No assertion was deleted, skipped, or weakened.

## 4. Docs + closeout

- [x] 4.1 `docs/code-quality.md` "Running tests" gains the two-phase shape and `npm run test:real-process`; `.pi/skills/ci-troubleshoot/SKILL.md` gains "check the vitest JSON artifact first" and the retry semantics. Delegate prose to `DocScribe`. Verify grep `test:real-process` in both.
- [x] 4.2 `AGENTS.md` rows: `packages/server/AGENTS.md` (two new config files), `packages/shared/src/__tests__/AGENTS.md` (guard), `.github/workflows/AGENTS.md`. Verify `kb dox lint` clean.
  - The shared per-file rows live in `packages/shared/src/AGENTS.md` (there is no `src/__tests__/AGENTS.md`); both new tests added there.
- [x] 4.3 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` zero failures; `npm run quality:changed` clean; comment on #652 and #604 with the change name.
  - `npm test` green both phases (1709 + 10 files, 20 151 tests); all step-4.4 enforcers clean. Biome: `--changed` reports "0 files" with everything staged (the documented quirk), so the changed set was passed explicitly — 4 `organizeImports` fixes applied to this change's own files, and the guard's clause-(d) walk was extracted to a `vitestConfigs` helper to clear `noExcessiveCognitiveComplexity`; the pre-existing `EditorSearchPanel` import-sort and `SettingsPanel:1185` complexity warnings were left alone (not this change's lines).
  - Commented on [#652](https://github.com/BlackBeltTechnology/pi-agent-dashboard/issues/652#issuecomment-5717543776) and [#604](https://github.com/BlackBeltTechnology/pi-agent-dashboard/issues/604#issuecomment-5717544155).

## Follow-ups (not this change)

- `packages/client/src/components/DirectorySettings/__tests__/FilePicker.test.tsx` "collapsing a directory hides its descendants and persists only that path" flaked in 1 of 3 loaded runs (see `measurements.md`). Same one-shot-after-`fireEvent` class as the members fixed here, but outside the spec's named set — needs its own change.
