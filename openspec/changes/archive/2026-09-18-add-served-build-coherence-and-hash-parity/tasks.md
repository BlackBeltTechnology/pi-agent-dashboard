# Tasks — served-client build coherence + plugin-registry hash parity

Order follows design.md — *Migration Plan*. TDD: write the failing test named in
each task first, then the implementation.

## 1. Shared client-registry plugin set (the parity fix)

- [x] 1.1 Add `packages/dashboard-plugin-runtime/src/server/client-registry-set.ts` exporting `selectClientRegistryPlugins(discovered, { isProd })` — requires `clientEntryPath`, applies the fixture policy, and drops plugins discovered only from a runtime-only root (design D0); verify unit tests cover: a client-less plugin excluded, a fixture plugin excluded under `isProd: true` and kept under `isProd: false`, and a plugin from a runtime-only root excluded under both.
- [x] 1.2 Rewire `loadPluginEntries` in `packages/dashboard-plugin-runtime/src/vite-plugin/index.ts` (build path **and** the `configureServer` dev-regeneration path) to select through the shared helper; verify the generated `plugin-registry.tsx` is byte-identical to the committed one on this checkout (no plugin-set change intended for the in-repo set).
- [x] 1.3 Rewire `bundleHash` in `packages/server/src/routes/system-routes.ts:900` to select through the shared helper; verify a parity test computes both sides over a fixture discovery set containing a client-less plugin and asserts the hashes are equal.
- [x] 1.4 Rewire `scripts/generate-plugin-registry.mjs` to select through the shared helper; verify running it produces a registry whose `PLUGIN_REGISTRY_HASH` equals the vite-plugin's for the same checkout.
- [x] 1.5 Add a `PluginStalenessBanner` test proving the banner is **hidden** when the discovered set includes a client-less plugin and the bundle was built from that checkout (the regression this change fixes); verify the existing `PluginStalenessBanner.test.tsx` cases stay green.
- [x] 1.6 Verify end-to-end on this repo: a script/test asserts the runtime-selected hash equals the embedded `PLUGIN_REGISTRY_HASH` in `packages/client/src/generated/plugin-registry.tsx` (today they are `30cffc01…` vs `4e1f10fb…`).

## 2. Build declaration in dashboard-plugin-runtime

- [x] 2.1 Add `packages/dashboard-plugin-runtime/src/server/build-metadata.ts` defining the declaration shape (`schemaVersion`, `pluginRegistryHash`, `fixturePolicy: "excluded" | "included"`), the filename constant `pi-dashboard-build.json`, and read/validate/write helpers; verify unit tests cover a valid round-trip plus missing file, malformed JSON, wrong `schemaVersion`, missing hash, and unknown `fixturePolicy` — each a distinct non-throwing outcome.
- [x] 2.2 Add `packages/dashboard-plugin-runtime/src/server/build-declaration-sdk.ts` computing a declaration from an explicitly supplied plugin list by calling `selectClientRegistryPlugins` then the existing `pluginRegistryHash` (no second serializer, per design D2); verify a test asserts the declaration hash equals `pluginRegistryHash` over the same selection, and that the same set supplied in a different order from a different root yields identical declaration bytes.
- [x] 2.3 Re-export both modules plus the selector from `packages/dashboard-plugin-runtime/src/server/index.ts`; verify `npx tsc --noEmit` passes for that package and the symbols are importable from the package's server entry.
- [x] 2.4 Emit the declaration from the vite-plugin's **production** output lifecycle into the client build output; verify a test asserts (a) production emit writes a declaration whose hash equals the embedded `PLUGIN_REGISTRY_HASH` and whose `fixturePolicy` is `excluded`, and (b) dev/HMR regeneration writes no declaration.

## 3. Server static-root resolution + health surface

- [x] 3.1 Add `packages/server/src/lib/client-dist.ts` exporting the resolver (`string | null`; installed `@blackbelt-technology/pi-dashboard-web/package.json` → `<pkg>/dist`, workspace `../../client/dist` **only when `require.resolve` throws**, `index.html` existence check) and a declaration reader producing the four-state snapshot; verify temp-directory tests cover package-wins-over-workspace, workspace-only, neither-present, and **package-resolvable-but-no-`index.html` ⇒ no static client (no workspace fallback)**.
- [x] 3.2 Move static-root resolution in `packages/server/src/server.ts` from ~L2013 to above the `registerSystemRoutes` call at L1668 and feed the single snapshot to `registerManifestRoute`, `fastifyStatic`, and all three `setNotFoundHandler` branches; verify existing server/packaging tests stay green and a boot smoke check still serves `index.html` and still enters API-only mode with no build.
- [x] 3.3 Add the additive `/api/health.clientBuild` `{ pluginRegistryHash, status }` computed from the startup snapshot against the runtime set selected under the **artifact's declared fixture policy** (design D4); verify health tests cover all four states (`matched`, `mismatched`, `metadata-missing`, `not-served`), assert `bundleHash` and every pre-existing field keep their shape, and assert a dev-mode server compared against a production artifact containing `demo-plugin` reports `matched`, not `mismatched`.
- [x] 3.4 Emit a startup diagnostic naming the coherence condition with no filesystem path, including on the declaration-read error path (catch, report by status, never echo the `fs` error message); verify a test asserts the emitted line matches no path-like substring for both a mismatched declaration and an unreadable one.

## 4. Verified local rebuild path

- [x] 4.1 Add `scripts/sync-served-client.mjs` resolving the destination through the same resolver (jiti precedent: `scripts/generate-plugin-registry.mjs`) with the design-D5 outcome table: no-op when destination == source; **mirror** (superseded assets removed) + post-verify when they differ and the destination is structurally a client build, **including a legacy destination with no declaration**; explicit "API-only host" success when nothing resolves; non-zero refusal when the source declaration is missing/invalid, when the destination is not a client build, or when declarations still differ after the copy; verify temp-directory tests cover every row, including that a refusal never writes to the destination and that stale hashed assets are gone after a mirror.
- [x] 4.2 Report an `EACCES` destination as a refusal with a remediation hint rather than an unhandled throw; verify a test simulates a non-writable destination and asserts a non-zero exit with no partial write.
- [x] 4.3 Wire the verify step between build and restart in **both** `scripts/rebuild-restart.sh` and `scripts/rebuild-and-restart.sh`; verify by running each script against a forced-refusal destination and asserting it exits non-zero **before** the `pi-dashboard restart` line — never let a success path run (it would restart the live server and reload every bridge).

## 5. Folded test scenarios (from test-plan.md)

Each row below folds one `automated` scenario from `test-plan.md`. Copy harness glue from the named exemplar.

### 5a. Plugin-set selector + parity — L1 vitest

- [x] 5.1 E1 (test-plan #E1): selector drops client-less plugins — input: discovery set of 2 client-bearing plugins + 1 without a client entry · trigger: `selectClientRegistryPlugins(set,{isProd:true})` · observable: exactly the 2 client-bearing plugins, and `pluginRegistryHash` of the result equals the hash of those 2 alone. New `packages/dashboard-plugin-runtime/src/server/__tests__/client-registry-set.test.ts`; see `packages/dashboard-plugin-runtime/src/server/__tests__/plugin-enabled.test.ts` for fixture-manifest glue.
- [x] 5.2 E2 (test-plan #E2): fixture policy — input: set including a `fixture:true` plugin **with** a client entry · trigger: select with `isProd:true` then `isProd:false` · observable: excluded then included, no other row differs. Same file as 5.1; see `plugin-enabled.test.ts`.
- [x] 5.3 E3 (test-plan #E3): runtime-only root — input: set with a client-bearing plugin whose packageDir is under a runtime-only root · trigger: select under both policies · observable: absent from both results. Same file as 5.1; see `plugin-enabled.test.ts`.
- [x] 5.4 E4 (test-plan #E4): repo-level parity — input: the real repo discovery set (17 plugins, 1 client-less) · trigger: compute build-side and server-side hash through the shared selector · observable: both equal the committed `PLUGIN_REGISTRY_HASH` in `packages/client/src/generated/plugin-registry.tsx`. New `packages/dashboard-plugin-runtime/src/server/__tests__/registry-hash-parity.test.ts`; see `plugin-enabled.test.ts` for discovery invocation.
- [x] 5.5 E5 (test-plan #E5): declaration order-independence — input: the same 3 plugins reversed and rooted elsewhere · trigger: build a declaration from each · observable: byte-identical declarations. New `packages/dashboard-plugin-runtime/src/server/__tests__/build-declaration-sdk.test.ts`; see `config-redact.test.ts` for pure-function test shape.
- [x] 5.6 E6 (test-plan #E6): declaration set-delta — input: a 3-plugin set vs the same set plus one client-bearing plugin · trigger: build a declaration from each · observable: `pluginRegistryHash` values differ. Same file as 5.5.

### 5b. Declaration emit + validation — L1 vitest

- [x] 5.7 E7 (test-plan #E7): production emit — input: a production vite-plugin run over a fixture plugin set · trigger: build completes · observable: `pi-dashboard-build.json` present with `schemaVersion`, `fixturePolicy:"excluded"`, hash == emitted `PLUGIN_REGISTRY_HASH`. New `packages/dashboard-plugin-runtime/src/vite-plugin/__tests__/build-declaration-emit.test.ts`; see `packages/dashboard-plugin-runtime/src/server/__tests__/plugin-enabled.test.ts` for temp-repo fixture glue.
- [x] 5.8 E8 (test-plan #E8): dev emits nothing — input: a dev/HMR regeneration · trigger: regeneration completes · observable: no `pi-dashboard-build.json` written. Same file as 5.7.
- [x] 5.9 E9 (test-plan #E9): declaration validation table — input: six files (valid · absent · malformed JSON · wrong `schemaVersion` · missing hash · unknown `fixturePolicy`) · trigger: `readBuildDeclaration(dir)` · observable: six distinct non-throwing outcomes, only the first usable. New `packages/dashboard-plugin-runtime/src/server/__tests__/build-metadata.test.ts`; see `config-redact.test.ts`.

### 5c. Static-root resolution + health — L1 vitest

- [x] 5.10 E10 (test-plan #E10): resolution precedence — input: four temp layouts (both · package-only · workspace-only · neither) · trigger: resolve the static root · observable: package, package, workspace, `null`. New `packages/server/src/lib/__tests__/client-dist.test.ts`; see `packages/server/src/__tests__/home-lock.test.ts` for temp-dir + resolver glue.
- [x] 5.11 E11 (test-plan #E11): no silent fallback — input: web package resolvable but its `dist/` lacks `index.html`, valid workspace dist present · trigger: resolve · observable: `null` (API-only), NOT the workspace dir. Same file as 5.10.
- [x] 5.12 E12 (test-plan #E12): four health states — input: four server fixtures (hash equal · differing · dir without declaration · no static dir) · trigger: `GET /api/health` · observable: `matched` / `mismatched` / `metadata-missing` / `not-served` with hash or `null` accordingly. New `packages/server/src/__tests__/health-client-build.test.ts`; see `packages/server/src/__tests__/contention-resume-guard-api.test.ts` for in-process fastify + route-registration glue.
- [x] 5.13 E13 (test-plan #E13): additive-only health — input: any of the four fixtures · trigger: `GET /api/health` · observable: `bundleHash` is hex and every pre-existing key keeps its type; `clientBuild` is the only new key. Same file as 5.12.
- [x] 5.14 E14 (test-plan #E14): declared fixture policy honoured — input: dev-mode server + artifact declaring `fixturePolicy:"excluded"`, discovery set containing `demo-plugin` · trigger: `GET /api/health` · observable: `clientBuild.status` is `matched`. Same file as 5.12.
- [x] 5.15 P1 (test-plan #P1): health stays snapshot-cheap — workload: 200 sequential `GET /api/health` with a resolved static dir · metric: mean added wall-clock vs a baseline without `clientBuild` < 1 ms (no per-request filesystem I/O) · window: the 200-request run. Same file as 5.12; see `packages/client/src/__tests__/monaco-chunk-size.test.ts` for threshold-assertion style.
- [x] 5.16 X1 (test-plan #X1): unreadable declaration leaks no path — fault: `pi-dashboard-build.json` unreadable (EACCES or a directory in its place) · trigger: startup declaration read · observable: status `metadata-missing`; log line names the condition and matches no path-like token. Same file as 5.12.
- [x] 5.17 X2 (test-plan #X2): mismatch diagnostic leaks no path — fault: served dir declaring a different hash · trigger: startup · observable: diagnostic names `mismatched`, contains no filesystem path. Same file as 5.12.
- [x] 5.18 X6 (test-plan #X6): dev-mode scoping — input: dev-mode server with a production fallback dir · trigger: `GET /api/health` · observable: `clientBuild` describes the fallback dir's declaration and the response is well-formed whether or not Vite is reachable. Same file as 5.12.

### 5d. Sync helper — L1 vitest + L2 qa smoke

- [x] 5.19 E15 (test-plan #E15): sync outcome table — input: five temp-dir pairs (dest == source · valid dest with declaration · valid dest **without** declaration (legacy) · no dest · dest without `index.html`) · trigger: run the sync helper · observable: no-op success · mirror+verify · mirror+verify (adopted) · "API-only host" success · non-zero refusal with the destination unmodified. New `scripts/__tests__/sync-served-client.test.mjs`; see `scripts/__tests__/check-conventions.test.mjs` for script-under-test + temp-dir glue.
- [x] 5.20 E16 (test-plan #E16): bad source refused — input: source declaration absent or invalid · trigger: run the sync helper · observable: non-zero exit, destination byte-identical. Same file as 5.19.
- [x] 5.21 X3 (test-plan #X3): non-writable destination — fault: destination chmod'd non-writable · trigger: run the sync helper · observable: non-zero exit with a remediation hint, contents unchanged, no partial write. Same file as 5.19.
- [x] 5.22 X4 (test-plan #X4): post-copy verification failure — fault: destination declaration still differs after the copy · trigger: run the sync helper · observable: non-zero exit reported as a mismatch, never as success. Same file as 5.19.
- [x] 5.23 X5 (test-plan #X5): both rebuild scripts abort before restart — fault: forced-refusal destination with `pi-dashboard` and `reload-all.sh` replaced by `PATH`-shadowed recording stubs · trigger: run `scripts/rebuild-restart.sh`, then `scripts/rebuild-and-restart.sh` · observable: both exit non-zero and neither stub is invoked. New `qa/tests/`-style shell test; see `qa/tests/02-server-start.sh` for stub/PATH and assertion glue. **Never let a success path run** — it would restart the live server and reload every bridge.

### 5e. Banner convergence — L1 vitest + L3 Playwright

- [x] 5.24 F1 (test-plan #F1): banner hidden on a parity host — input: mocked `/api/health` whose `bundleHash` equals the imported `PLUGIN_REGISTRY_HASH`, server set containing a client-less plugin · trigger: `PluginStalenessBanner` mounts · observable: no `plugin-staleness-banner` testid rendered. Extend `packages/client/src/components/__tests__/PluginStalenessBanner.test.tsx`.
- [x] 5.25 F2 (test-plan #F2): banner still fires on a genuine delta — input: mocked `/api/health` with a differing `bundleHash` · trigger: mount · observable: banner renders with Refresh, and dismiss suppresses it for the session. Same file as 5.24.
- [x] 5.26 F3 (test-plan #F3): end-to-end convergence — input: docker harness serving a client built from the same checkout (port from `.pi-test-harness.json` `dashboardPort`, never hardcoded) · trigger: load the dashboard root and let the health probe resolve · observable: the staleness banner never appears and `/api/health` reports `clientBuild.status:"matched"`. New `tests/e2e/plugin-hash-parity.spec.ts`; see `tests/e2e/bind-reachability-advisory.spec.ts` for harness attach + health-read glue.

## 6. Review, docs, suite

- [x] 6.1 (`observability-instrumentation`) Confirm the new health surface is diagnosable: `clientBuild.status` distinguishes all four conditions, the startup diagnostic names the condition, and neither leaks a path; verify by reading `/api/health` in each of the four states in tests.
- [x] 6.2 (`review-code`) Review the filesystem-mutating `sync-served-client.mjs` and the `bundleHash` basis change (every browser tab compares against it); verify all findings at major severity or above are resolved or dismissed with a stated reason.
- [x] 6.3 Update the nearest-directory `AGENTS.md` rows for every new/changed file (`dashboard-plugin-runtime/src/server`, `src/vite-plugin`, `server/src/lib`, `server/src/routes`, `scripts`); verify the repo's DOX lint reports no missing/stale rows for the touched files.
- [x] 6.4 Delegate to `DocScribe`: `README.md` distinguishing a workspace-only build from the verified local deployment path, and `docs/architecture.md` — its *Plugin Staleness Detection* section is now inaccurate about which set each hash covers, plus the new build → served artifact → health flow; verify both files state the corrected basis and the parent applies the returned tree rows.
- [x] 6.5 Run the full suite once to a log and grep it (`set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log`); verify no `FAIL` and the summary reports all tests passing.
