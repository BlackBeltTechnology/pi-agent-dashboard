## 0. Prerequisite

- [ ] 0.1 Confirm `expose-plugin-credential-and-oauth-seams` is merged (`ctx.credentials`, `ctx.oauth.startFlow`, `createLoopbackCallback`, `registerPiRequestHandler`, `ui:oauth-flow`). Verify: those symbols resolve from `@blackbelt-technology/dashboard-plugin-runtime`.

## 1. Package scaffold

- [ ] 1.1 Create `packages/gmail-plugin/` with the manifest (`id: gmail`; `server`/`bridge`/`client` entries; `settings-section` claim), `configSchema.json`, i18n catalog, `oauth4webapi` dependency, and `AGENTS.md` (use the `dashboard-plugin-scaffold` skill). Verify: the manifest validator passes and the plugin loads in the dashboard.

## 2. Server

- [ ] 2.1 `accounts.ts`: records in the `gmail` namespace (`client`, `acct:<sub>`), resolve from one `snapshot()` (alias then email), upserts via `update()`, unique aliases, same-email ambiguity error, email update on re-auth. Verify: unit tests.
- [ ] 2.2 `google-oauth.ts`: `OAuthLoginFlow` (PKCE, nonce, helper-owned state, loopback path `/` with `interaction.signal`, manual_code race with a catch on the losing promise, pasted-URL parse compared to `callback.state`, code exchange, id_token claim validation incl. nonce, `email_verified`, no `include_granted_scopes`, flow keys `add-<uuid>` / `reauth-<sub>`). Verify: tests against a fake token endpoint for success, state mismatch, nonce mismatch, unverified email, a missing refresh token on a new account, two parallel adds both completing, cancel closing the loopback listener, no unhandled rejection when the paste wins, and flow errors and logs never containing the pasted URL or code.
- [ ] 2.3 Persist and tier routes: start flow per tier (with `login_hint` on re-auth), lower tier without re-consent, alias, revoke (best-effort remote + local delete). All mounted behind `ctx.networkGuard`. Verify: route tests.
- [ ] 2.4 `lease.ts`: `gmail/lease` request handler with tier + granted-scope checks using the implication table (`modify ⇒ readonly, compose`), 60 s pre-expiry refresh, single-flight per `sub` writing via `update()`, `invalid_grant` → `reauth_required`, secret-free logging. Verify: tests for tier_denied, single-flight (one fake refresh call for two concurrent leases), reauth marking, and a log assertion.

## 3. Bridge tools

- [ ] 3.1 `lease-client.ts`: request lane, one lease per tool call (no bridge cache), and a check of the returned `tier` against the op. Verify: unit tests, including a lowered level refusing the very next call.
- [ ] 3.2 Read tools: `gmail_accounts` (no account, not untrusted); `gmail_search`, `gmail_get`, `gmail_labels`, `gmail_attachments` (required `account`; all four `details.untrusted = true`; HTML `contentType`). Attachment save via `@blackbelt-technology/pi-dashboard-shared` path containment + realpath + `wx` + `O_NOFOLLOW`. Guard registry declarations at load (reads untrusted, writes self-confirming; order-independent). Verify: tests with a mocked Gmail REST for traversal refused, symlink escape refused, no-overwrite, and declarations recorded.
- [ ] 3.3 Write tools `gmail_draft`, `gmail_send`, `gmail_reply` (threading headers), `gmail_modify`, `gmail_trash`, with `ctx.ui.confirm` (dismiss/timeout = deny) and a headless block. `mime.ts` builder. Verify: tests for confirm-deny, confirm-timeout, headless block, reply threading headers, and a MIME round-trip.

## 4. Client

- [ ] 4.1 Setup wizard (steps 1–6 of design D7 with deep links using the project id, the no-gcloud path, fallback descriptions, JSON upload validation, test sign-in error mapping) using the `ui:oauth-flow` primitive. Verify: component tests for the web-client rejection and the error→step mapping.
- [ ] 4.2 Accounts panel (list, add, re-auth, level change, alias, revoke, status badges incl. testing-mode hint, level help text stating the honest scope limit). Verify: component tests.

## 4b. Test-only endpoint override and fake Google

- [ ] 4b.1 Implement the `PI_E2E_GOOGLE_BASE_URL` override (design D8): loopback-only, warning on anything else, covering authorize/token/revoke/Gmail base URLs. Verify: covered by test-plan #E28.
- [ ] 4b.2 Build `tests/e2e/helpers/fake-google.ts` (authorize redirect, token incl. `invalid_grant` toggle, revoke with a down toggle, id_token claims, minimal Gmail REST search/get/labels/send with recorded calls). Pass `PI_E2E_GOOGLE_BASE_URL` through `docker/test-up.sh`. Verify: the helper starts in a spec's `beforeAll`, and the dashboard container reaches it.

## 5. Docs and closeout

- [ ] 5.1 README with Google Cloud setup (screenshots), the level/scope table and the honest scope limit. `AGENTS.md` rows for every file. `docs/` prose via DocScribe.
- [ ] 5.2 Run `npm test` + `npm run quality:changed` + `review-code`. Verify: green.

## 6. Tests (folded from test-plan.md)

- [ ] 6.1 L1 vitest test E1 — setup: client validation (decision-table). Input: JSON with `installed` + id `x.apps.googleusercontent.com` + secret; `web` block; `installed` without secret; id without that suffix; non-JSON. Trigger: upload. Observable: first accepted and stored under `client`; the others rejected with step-4/5 guidance. Harness exemplar: see `packages/blackhole-plugin/src/client/__tests__/ChainEditor.test.tsx` (test-plan #E1)
- [ ] 6.2 L1 vitest test E2 — setup: error→step map (decision-table). Input: callback errors `access_denied`, `org_internal`, `redirect_uri_mismatch`, `invalid_client`. Trigger: test sign-in fails. Observable: mapped to steps 3, 3, 4, 5 respectively. Harness exemplar: see `packages/blackhole-plugin/src/client/__tests__/ChainEditor.test.tsx` (test-plan #E2)
- [ ] 6.3 L1 vitest test E3 — setup: deep links (EP). Input: project id `my-proj-1`. Trigger: render wizard. Observable: links contain `?project=my-proj-1` for branding/audience/clients; the gcloud commands contain `my-proj-1`. Harness exemplar: see `packages/blackhole-plugin/src/client/__tests__/ChainEditor.test.tsx` (test-plan #E3)
- [ ] 6.4 L1 vitest test E4 — sign-in: auth URL (EP). Input: tier `draft`, re-auth of `a@x.com`. Trigger: build URL. Observable: `scope` = `openid email …gmail.readonly …gmail.compose`; `access_type=offline`; `prompt=consent select_account`; `login_hint=a@x.com`; `nonce` present; no `include_granted_scopes`; redirect `http://127.0.0.1:<port>/`. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E4)
- [ ] 6.5 L1 vitest test E5 — sign-in: id_token claims (decision-table). Input: id_token variants: valid; wrong `aud`; wrong `iss`; expired; nonce mismatch; `email_verified:false`. Trigger: code exchange. Observable: only valid → account stored; others fail with no store write. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E5)
- [ ] 6.6 L1 vitest test E6 — sign-in: missing refresh (new) (EP). Input: token response without `refresh_token`, new `sub`. Trigger: persist. Observable: fails with the "revoke app access and retry" message; no record. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E6)
- [ ] 6.7 L1 vitest test E7 — sign-in: re-auth in place (state-transition). Input: existing `acct:s1` alias `work`; re-auth returns `sub s1`, new email. Trigger: persist. Observable: one record `acct:s1`, alias `work`, email updated. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E7)
- [ ] 6.8 L1 vitest test E8 — sign-in: parallel adds (state-transition). Input: two add flows (`add-<uuid1>`, `add-<uuid2>`). Trigger: complete both. Observable: two records; neither flow superseded. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E8)
- [ ] 6.9 L1 vitest test E9 — paste path (decision-table). Input: pasted `http://127.0.0.1:1/?code=c&state=<callback.state>`; wrong state; garbage. Trigger: manual_code input. Observable: first completes; second `state_mismatch`; third `invalid_redirect`; errors never contain the input. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E9)
- [ ] 6.10 L1 vitest test E10 — account resolution (decision-table). Input: accounts: s1 `a@x.com` alias `work`; s2 `B@y.com`; s3 & s4 both `dup@z.com`. Trigger: resolve `work`, `b@Y.com`, `dup@z.com`, `nobody`. Observable: s1; s2; ambiguity error asking for an alias; not-found listing the aliases/emails. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E10)
- [ ] 6.11 L1 vitest test E11 — alias uniqueness (EP). Input: alias `work` on s1. Trigger: set alias `work` on s2. Observable: rejects. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E11)
- [ ] 6.12 L1 vitest test E12 — tier × op matrix (decision-table). Input: tiers readonly/draft/send × ops read/draft/send/modify/trash. Trigger: lease. Observable: allowed iff tierRank ≥ opRank; else `tier_denied`. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E12)
- [ ] 6.13 L1 vitest test E13 — scope implication (decision-table). Input: send tier with grantedScopes `[gmail.modify]`. Trigger: lease op `read` and `draft`. Observable: allowed (implication table); readonly tier with `[gmail.readonly]` op draft → `tier_denied`. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E13)
- [ ] 6.14 L1 vitest test E14 — scope missing (EP). Input: draft tier but grantedScopes `[gmail.readonly]`. Trigger: lease op draft. Observable: `scope_missing`. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E14)
- [ ] 6.15 L1 vitest test E15 — downgrade immediate (state-transition). Input: account send tier. Trigger: set tier readonly, then `gmail_send`. Observable: the very next call is refused `tier_denied`; no Gmail call recorded. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E15)
- [ ] 6.16 L1 vitest test E16 — lease reply shape (EP). Input: valid lease. Trigger: inspect reply. Observable: keys exactly `accessToken, expiresAt, email, tier`; no `refresh`. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E16)
- [ ] 6.17 L1 vitest test E17 — refresh window (BVA). Input: access expires in 61 s / 60 s / 59 s. Trigger: lease. Observable: no refresh / refresh / refresh. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E17)
- [ ] 6.18 L1 vitest test E18 — tools: accounts (EP). Input: two accounts. Trigger: `gmail_accounts` (no args). Observable: lists both with level + status; `details.untrusted` absent. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E18)
- [ ] 6.19 L1 vitest test E19 — tools: account required (EP). Input: `gmail_search` without `account`. Trigger: call. Observable: error listing the connected accounts; no lease request. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E19)
- [ ] 6.20 L1 vitest test E20 — tools: untrusted marking (EP). Input: search/get/labels/attachments results. Trigger: call each. Observable: `details.untrusted === true` on all four; HTML get has `contentType: text/html`. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E20)
- [ ] 6.21 L1 vitest test E21 — guard declarations (EP). Input: bridge load, guard absent. Trigger: load. Observable: registry `declarations` has reads→untrusted and writes→selfConfirming. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E21)
- [ ] 6.22 L1 vitest test E22 — search limits (BVA). Input: `maxResults` 0 / 1 / 50 / 51. Trigger: `gmail_search`. Observable: 0 and 51 rejected by the tool schema before any lease request; 1 and 50 accepted. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E22)
- [ ] 6.23 L1 vitest test E23 — confirm content (EP). Input: `gmail_send` to 2 recipients, subject S, 5 KB body. Trigger: confirm prompt. Observable: shows account, both recipients, S, ≤ 500-char body preview. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E23)
- [ ] 6.24 L1 vitest test E24 — confirm outcomes (decision-table). Input: write tool. Trigger: confirm true / false / dismissed / timeout; `hasUI=false`. Observable: executes / not executed ×4; recorded Gmail calls = 1/0/0/0/0. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E24)
- [ ] 6.25 L1 vitest test E25 — reply threading (EP). Input: original message id `<m1@x>`, thread `t1`. Trigger: `gmail_reply` confirmed. Observable: sent raw has `In-Reply-To: <m1@x>`, `References` ending `<m1@x>`, request body `threadId: t1`. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E25)
- [ ] 6.26 L1 vitest test E26 — MIME (EP). Input: UTF-8 subject `Árvíztűrő`, body, 1 attachment. Trigger: build + parse. Observable: round-trips subject, body and attachment bytes. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E26)
- [ ] 6.27 L1 vitest test E27 — attachment confinement (decision-table). Input: targets `a.pdf`, `../../.ssh/x`, `sub/link` (symlink → /tmp), an existing file, `\\\\?\\C:\\x`. Trigger: save. Observable: only `a.pdf` written; others refused; the existing file unchanged. Harness exemplar: see `packages/flows-anthropic-bridge-plugin/src/__tests__/server-route-registration.test.ts` (test-plan #E27)
- [ ] 6.28 L1 vitest test E28 — endpoint override gate (decision-table). Input: `PI_E2E_GOOGLE_BASE_URL` = `http://127.0.0.1:9999`, `http://localhost:9999`, `https://evil.com`, unset. Trigger: resolve endpoints. Observable: first two used; third ignored + warning; unset → Google defaults. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E28)
- [ ] 6.29 L1 vitest test E29 — secrets never logged (invariant). Input: full sign-in + lease + refresh + revoke with a captured logger. Trigger: run. Observable: logs contain no access/refresh token, client secret, code or pasted URL. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #E29)
- [ ] 6.30 L1 vitest test P1 — single-flight refresh (load). Input: 10 concurrent leases on an expiring account. Trigger: fake token endpoint receives exactly 1 refresh; all 10 get the same token. Observable: single run. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #P1)
- [ ] 6.31 L3 Playwright e2e test F1 — wizard + add account (state-convergence). Input: harness with `PI_E2E_GOOGLE_BASE_URL` → fake-google; valid client JSON. Trigger: upload JSON → Add account → flow view → paste the redirect from fake-google. Observable: accounts panel converges to 1 row `a@fake.test`, level readonly, status ok. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F1)
- [ ] 6.32 L3 Playwright e2e test F2 — two accounts (state-convergence). Input: F1 state. Trigger: add a second account `b@fake.test` at level send. Observable: 2 rows with their own levels. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F2)
- [ ] 6.33 L3 Playwright e2e test F3 — level change (state-transition). Input: account at readonly. Trigger: raise to send (re-consent via fake-google) then lower to draft. Observable: the row shows send after consent, then draft immediately with no flow shown. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F3)
- [ ] 6.34 L3 Playwright e2e test F4 — revoke (state-transition). Input: account present; fake-google revoke endpoint returns 200, then a second run with it down. Trigger: click Revoke. Observable: row removed; UI says remote revoke ok / must revoke manually. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F4)
- [ ] 6.35 L3 Playwright e2e test F5 — tool round-trip (state-convergence). Input: account `a@fake.test` level send; harness session. Trigger: prompt: send a mail via `gmail_send` from `a@fake.test`. Observable: confirm card appears; after approve, fake-google records 1 send with the expected recipients. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F5)
- [ ] 6.36 L3 Playwright e2e test F6 — reauth badge (state-transition). Input: fake-google returns `invalid_grant` on refresh. Trigger: trigger a tool call. Observable: panel shows "re-auth needed"; the tool result reports reauth_required. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F6)
- [ ] 6.37 Manual check F7 — real Google end-to-end: real GCP project via the wizard, 2 real accounts · add both, read, draft, send-to-self, revoke · [judgment: works against live Google incl. testing-mode notice] (test-plan: manual-only)
- [ ] 6.38 Manual check F8 — wizard clarity: wizard steps 1–6 · a non-expert follows it · [judgment: can complete setup without external docs] (test-plan: manual-only)
- [ ] 6.39 L1 vitest test X1 — invalid_grant (fault-injection (abort)). Input: token endpoint returns `invalid_grant`. Trigger: lease. Observable: record status `reauth_required`; reply `reauth_required`; later leases refused without network. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X1)
- [ ] 6.40 L1 vitest test X2 — token endpoint down (fault-injection (abort)). Input: refresh returns HTTP 503. Trigger: lease. Observable: reply error `refresh_failed`; status stays `ok`; no token leaked in the error. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X2)
- [ ] 6.41 L1 vitest test X3 — token endpoint slow (fault-injection (delay)). Input: refresh delayed 20 s. Trigger: lease. Observable: lane `timeout` at 15 s for the caller; refresh completes and is stored; the next lease succeeds without a new refresh. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X3)
- [ ] 6.42 L1 vitest test X4 — concurrent refresh + re-auth (fault-injection (race)). Input: refresh in flight while re-auth persists new tokens for the same sub. Trigger: both finish. Observable: the final record holds the re-auth refresh token (update-based merge; nothing clobbered). Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X4)
- [ ] 6.43 L1 vitest test X5 — revoke offline (fault-injection (abort)). Input: revoke endpoint unreachable. Trigger: revoke. Observable: local record deleted; result flag `remoteRevoked:false`. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X5)
- [ ] 6.44 L1 vitest test X6 — Gmail API 429/5xx (fault-injection (abort)). Input: Gmail returns 429 with Retry-After 2. Trigger: `gmail_search`. Observable: tool error states rate-limited + retry-after; no retry storm (≤ 1 call). Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X6)
- [ ] 6.45 L1 vitest test X7 — cancel closes listener (fault-injection (abort)). Input: add flow pending. Trigger: cancel flow. Observable: loopback port closed within 100 ms; no unhandled rejection. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X7)
- [ ] 6.46 L1 vitest test X8 — paste wins race (fault-injection). Input: paste completes before the callback. Trigger: flow ends. Observable: callback closed; `unhandledRejection` listener not fired. Harness exemplar: see `packages/blackhole-plugin/src/server/__tests__/routes.test.ts` (test-plan #X8)
