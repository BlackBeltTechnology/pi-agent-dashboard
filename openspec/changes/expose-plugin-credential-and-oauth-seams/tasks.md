## 1. Lock module extraction (auth.json unchanged)

- [ ] 1.1 Extract `withLockedJsonFile`, `readJsonChecked`, `writeJsonAtomic` and quarantine from `packages/server/src/auth/provider-auth-storage.ts` into `packages/server/src/auth/locked-json-file.ts`. Keep `LOCK_OPTIONS`, the `ELOCKED`-only retry, backoff and async release verbatim. Change the quarantine dedup to `Map<path, Set<sha256>>`, and keep `_resetQuarantineDedupForTests` exported from `provider-auth-storage.ts`, delegating to the new module. Verify: the existing `provider-auth-storage` tests pass unmodified, and a cross-file identical-corruption test shows two backups.
- [ ] 1.2 Rewire `provider-auth-storage.ts` onto the module with `AUTH_PATH`, keeping public exports identical. Verify: `tsc` is clean and the full server suite is green.

## 2. Plugin credential store

- [ ] 2.1 Implement `packages/server/src/auth/plugin-credential-store.ts` over `~/.pi/agent/plugin-credentials.json`: null-prototype parse, reserved-key reject, 64 KiB record / 256 keys / 2 MiB namespace caps, `list()` keys only, `snapshot()` of the own namespace, `update(key, fn)` inside the lock, `structuredClone` on reads, unlocked reads that never create the file. Verify: unit tests, including concurrent `update` of the same key keeping both field changes.
- [ ] 2.2 Add optional `credentials` to `ServerPluginContext` / `ServerContextDeps` in `packages/dashboard-plugin-runtime/src/server/server-context.ts`, bound to the manifest id in `createServerPluginContext`. Wire it in `server.ts`. Verify: a runtime test shows two contexts cannot see each other's keys.

## 3. Plugin OAuth flows

- [ ] 3.1 Extract `beginFlow()` (queueStart + supersede, `awaitStartOutcome` race with `FLOW_START_TIMEOUT_MS`, forgetFlow on early failure, openInBrowser with its `VITEST` suppression preserved) from `packages/server/src/routes/provider-auth-routes.ts` into the adapter layer, and make `POST /api/provider-auth/start` use it. Verify: the existing route and adapter tests pass unmodified, and a test asserts `openInBrowser` is not invoked under vitest.
- [ ] 3.2 Add `oauth.startFlow({ key, loginFlow, persist })` to the plugin context via `beginFlow` (`provider = plugin:<id>:<key>`), rejecting with the runtime-exported `PluginFlowStartError { code: start_timeout | login_failed }`. Add the runtime client helper `oauthFlowClient` (`status` / `input` / `cancel`). Verify: tests with a fake `OAuthLoginFlow` for credential→persist with `auth.json` untouched, a throw-before-first-step rejecting with no lingering record, a timeout, and same-key supersede.
- [ ] 3.2a Skip `await registryReady` in the flow status / input / cancel routes for `plugin:` flows, and pass the login result to `persist` untouched. Verify: a test with the registry forced to fail still serves a plugin flow, and a test shows extra credential fields reach `persist`.
- [ ] 3.3 Mirror the OAuth structural types (`PluginOAuthLoginFlow`, `PluginLoginInteraction`, `PluginLoginPrompt`, `PluginLoginEvent`, `PluginOAuthCredential`) in the runtime. Verify: a type-level test asserts mutual assignability with `server/src/auth/pi-oauth-types.ts`.
- [ ] 3.4 Assert plugin flow input is never logged. Verify: a test submits a `code=` URL and the captured logger output lacks it.
- [ ] 3.5 Implement `createLoopbackCallback` in `packages/dashboard-plugin-runtime/src/server/` (helper-owned state, path filter, state check before `error=`, 400 and keep waiting on a mismatch, length-safe compare, 5 min timeout, idempotent close). Export it from `./server`. Verify: unit tests for the match, favicon + wrong/short state followed by a match, forged `error=` without state ignored, `error=` with state rejecting, the timeout, abort, and loopback-only binding.
- [ ] 3.6 Extract `OAuthFlowView` from `SignInPane` in `packages/client/src/components/settings/ProviderAddDialog.tsx`, with `SignInPane` importing it directly. Add `ui:oauth-flow` to `packages/shared/src/dashboard-plugin/ui-primitives.ts` and register it at client startup. Verify: the existing ProviderAddDialog tests pass, and a new test renders the view via `useUiPrimitive("ui:oauth-flow")` inside a provider.

## 4. Bridge request/reply lane

- [ ] 4.1 Add `plugin_request` (extension→server: `requestId`, `pluginId`, `messageType`, `payload`) and `plugin_reply` (server→extension: `requestId`, `ok`, `result|error`) to `packages/shared/src/protocol.ts`. Verify: `tsc` is clean.
- [ ] 4.2 Server: a separate request registry keyed `(pluginId, type)` with duplicate registration throwing, and `ctx.registerPiRequestHandler`. The host sends `plugin_reply` directly (not via the gated `sendExtensionMessage`), with serialization and 256 KiB reply checks. Verify: tests for success, throw, no_handler, cross-plugin routing, a priority-1000 plugin being answered, circular → reply_not_serializable, and oversize → reply_too_large.
- [ ] 4.3 Extension `packages/extension/src/bridge.ts`: install `globalThis[Symbol.for("pi-dashboard.pluginRequest")]` while connected; pending map with a 15 s timeout; `disconnected` on socket close; drop late replies; 256 KiB request check. The plugin-side helper returns `unavailable` when the symbol is absent. Verify: tests for delivery, timeout, disconnect, late-drop, unavailable, and no `pi.events` emission of request or reply.
- [ ] 4.4 Add a coordination note to `openspec/changes/harden-trust-and-credential-boundaries/tasks.md`: the `plugin_request` / `plugin_reply` protocol messages are not `plugin_emit_event` names, and `plugin_reply` is sent host-internally regardless of plugin priority (design D7). Verify: the note is present.
- [ ] 4.5 Add a coordination note to `openspec/changes/add-connector-layer/tasks.md` task 1.2: consume `packages/server/src/auth/locked-json-file.ts` instead of a second extraction. Verify: the note is present.
- [ ] 4.6 Classify the new `ServerPluginContext` members (`credentials`, `oauth`, `registerPiRequestHandler`) as denied in the `mcp-server-plugin` context-member partition (`packages/mcp-server-plugin/src/server/tools.ts` `ALL_CONTEXT_MEMBERS`). Verify: `tools.test.ts` is updated and green.

## 4b. End-to-end fixture

- [ ] 4b.1 Extend `packages/demo-plugin` with `server` and `bridge` entries per design D8: `demo/echo` request handler, a fake login flow started from the demo settings section and rendered via `ui:oauth-flow`, and a `demo_echo` bridge tool. Update the manifest and `AGENTS.md`. Verify: the demo plugin loads in the docker harness (`docker/test-up.sh`) and its settings section shows the start button.

## 5. Docs and closeout

- [ ] 5.1 Update `AGENTS.md` rows for every touched or new file (server/auth, server/routes, runtime/server, extension, client/settings, shared). Delegate `docs/` prose (plugin-authoring seams + trust boundary) to DocScribe.
- [ ] 5.2 Run `npm test` + `npm run quality:changed`, and review with `review-code`. Verify: green.

## 6. Tests (folded from test-plan.md)

- [ ] 6.1 L1 vitest test E1 — credential-store: namespaced (EP). Input: plugin `gmail` stored key `a@x.com`; plugin `other` context. Trigger: `other.list()` + `other.get("a@x.com")`. Observable: `[]` and `undefined`. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E1)
- [ ] 6.2 L1 vitest test E2 — credential-store: namespace from manifest (EP). Input: key `"../other/x"` from plugin `gmail`. Trigger: `set` then read file. Observable: record under `gmail["../other/x"]`, no `other` entry. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E2)
- [ ] 6.3 L1 vitest test E3 — credential-store: key validation (BVA). Input: keys `""`, 1 char, 200 chars, 201 chars, `__proto__`, `constructor`. Trigger: `set` each. Observable: 1 and 200 accepted; the other four reject typed; `Object.prototype` unchanged. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E3)
- [ ] 6.4 L1 vitest test E4 — credential-store: record size (BVA). Input: records of 65 535 / 65 536 / 65 537 serialized bytes. Trigger: `set`. Observable: first two accepted; third rejects and the file bytes are unchanged. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E4)
- [ ] 6.5 L1 vitest test E5 — credential-store: namespace caps (BVA). Input: namespace with 255, 256 keys; namespace at 2 MiB − 1 KiB. Trigger: `set` a new key. Observable: 256th accepted; 257th rejects; write crossing 2 MiB rejects. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E5)
- [ ] 6.6 L1 vitest test E6 — credential-store: non-plain record (EP). Input: `[]`, `"str"`, `null`, `new Date()`. Trigger: `set`. Observable: each rejects typed, no write. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E6)
- [ ] 6.7 L1 vitest test E7 — credential-store: copies (state). Input: stored `{a:1}`. Trigger: mutate `get()` result and `snapshot()` result. Observable: a new `get()` returns `{a:1}`. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E7)
- [ ] 6.8 L1 vitest test E8 — credential-store: list keys only (EP). Input: two records with `refresh` fields. Trigger: `list()`. Observable: returns exactly the two key strings. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E8)
- [ ] 6.9 L1 vitest test E9 — credential-store: reads don't create (state). Input: no file on disk. Trigger: `get`, `list`, `snapshot`. Observable: empty results; `fs.existsSync(path) === false`. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E9)
- [ ] 6.10 L1 vitest test E10 — credential-store: 0600 create (EP). Input: no file. Trigger: first `set`. Observable: file mode `0o600` (POSIX). Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E10)
- [ ] 6.11 L1 vitest test E11 — credential-store: auth.json untouched (invariant). Input: auth.json with fixture bytes. Trigger: 10 × set/update/remove. Observable: auth.json sha256 unchanged. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E11)
- [ ] 6.12 L1 vitest test E12 — auth.json preserved (regression). Input: existing provider-auth-storage suites. Trigger: run unmodified. Observable: all pass. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #E12)
- [ ] 6.13 L1 vitest test E13 — plugin flow: credential pass-through (EP). Input: fake loginFlow resolving `{access,refresh,expires,sub:"s1",email:"e"}`. Trigger: complete flow. Observable: `persist` receives an object deep-equal to that value; auth.json unchanged. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #E13)
- [ ] 6.14 L1 vitest test E14 — plugin flow: same-key supersede (state-transition). Input: pending flow key `k`. Trigger: start second flow key `k`. Observable: first flow status `error`/cancelled; second pending. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #E14)
- [ ] 6.15 L1 vitest test E15 — plugin flow: distinct keys coexist (state-transition). Input: pending flow key `k1`. Trigger: start flow key `k2`. Observable: both `pending`. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #E15)
- [ ] 6.16 L1 vitest test E16 — provider flow behaviour preserved (regression). Input: existing provider-auth routes/adapter suites. Trigger: run unmodified. Observable: all pass. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #E16)
- [ ] 6.17 L1 vitest test E17 — flow status never echoes input (EP). Input: plugin flow pending manual_code. Trigger: POST input `http://127.0.0.1/?code=SECRET&state=x`. Observable: status JSON and captured logs contain no `SECRET`. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #E17)
- [ ] 6.18 L1 vitest test E18 — public types (type-level). Input: runtime mirror types + server pi-oauth-types. Trigger: `tsc` type test (mutual assignability). Observable: compiles. Harness exemplar: see `packages/dashboard-plugin-runtime/src/__tests__/server-context-capability.test.ts` (test-plan #E18)
- [ ] 6.19 L1 vitest test E19 — loopback: match (EP). Input: helper on `127.0.0.1:0`, path `/callback`. Trigger: GET `/callback?state=<helper.state>&code=abc`. Observable: resolves `{code:"abc"}`; response 200 HTML; port closed afterwards. Harness exemplar: see `packages/dashboard-plugin-runtime/src/__tests__/server-context-model-runtime.test.ts` (test-plan #E19)
- [ ] 6.20 L1 vitest test E20 — loopback: stray & mismatch keep waiting (state-transition). Input: helper waiting. Trigger: GET `/favicon.ico` → 404; GET `/callback?state=short` → 400; GET `/callback?state=<wrong same length>` → 400; then matching GET. Observable: resolves with code from the last request only. Harness exemplar: see `packages/dashboard-plugin-runtime/src/__tests__/server-context-model-runtime.test.ts` (test-plan #E20)
- [ ] 6.21 L1 vitest test E21 — loopback: forged error (decision-table). Input: helper waiting. Trigger: GET `/callback?error=access_denied` (no state), then `?error=access_denied&state=<helper.state>`. Observable: first → 400 and keeps waiting; second → rejects `access_denied`. Harness exemplar: see `packages/dashboard-plugin-runtime/src/__tests__/server-context-model-runtime.test.ts` (test-plan #E21)
- [ ] 6.22 L1 vitest test E22 — loopback: bind (EP). Input: helper listening. Trigger: inspect `server.address()`. Observable: address `127.0.0.1`. Harness exemplar: see `packages/dashboard-plugin-runtime/src/__tests__/server-context-model-runtime.test.ts` (test-plan #E22)
- [ ] 6.23 L1 vitest test E23 — lane: success / error / no_handler (decision-table). Input: handlers: returns `{t:1}`; throws `Error("tier_denied")`; none registered. Trigger: request each. Observable: `{ok:true,result:{t:1}}`; `{ok:false,error:"tier_denied"}` with no `stack`; `{ok:false,error:"no_handler"}`. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E23)
- [ ] 6.24 L1 vitest test E24 — lane: routing by plugin (decision-table). Input: plugins `a` and `b` both register `lease`. Trigger: request naming `b`. Observable: only `b` handler called (spy counts a=0, b=1). Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E24)
- [ ] 6.25 L1 vitest test E25 — lane: duplicate registration (EP). Input: plugin `a` registered `lease`. Trigger: register `lease` again. Observable: throws. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E25)
- [ ] 6.26 L1 vitest test E26 — lane: session attribution (EP). Input: request from socket of session S1 with payload `{sessionId:"S2"}`. Trigger: dispatch. Observable: handler ctx.sessionId === `S1`. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E26)
- [ ] 6.27 L1 vitest test E27 — lane: priority independence (EP). Input: plugin priority 1000 with handler. Trigger: request. Observable: reply delivered. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E27)
- [ ] 6.28 L1 vitest test E28 — lane: payload caps (BVA). Input: request 256 KiB, 256 KiB + 1; handler reply 256 KiB + 1. Trigger: send. Observable: first ok; second `request_too_large` (never sent to server); third `reply_too_large`. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E28)
- [ ] 6.29 L1 vitest test E29 — lane: non-serializable reply (EP). Input: handler returns a circular object. Trigger: request. Observable: `reply_not_serializable`. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E29)
- [ ] 6.30 L1 vitest test E30 — lane: privacy (invariant). Input: spy on `pi.events.emit` for all event names. Trigger: full request/reply. Observable: no emitted payload contains the request or reply. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #E30)
- [ ] 6.31 L1 vitest test E31 — ui:oauth-flow registered (EP). Input: client primitive registry after startup. Trigger: `useUiPrimitive("ui:oauth-flow")` inside the provider. Observable: returns the component; renders auth link + paste field for a `manual_code` status. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #E31)
- [ ] 6.32 L1 vitest test E32 — mcp-server context partition (invariant). Input: `ALL_CONTEXT_MEMBERS`. Trigger: `tools.test.ts`. Observable: new members classified denied; length matches the interface. Harness exemplar: see `packages/mcp-server-plugin/src/server/__tests__/tools.test.ts` (test-plan #E32)
- [ ] 6.33 L1 vitest test P1 — credential-store: concurrent writes (load). Input: 20 concurrent `update` calls on one key, each incrementing a counter. Trigger: final counter = 20; every call settles within the 2 s lock budget. Observable: single run. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #P1)
- [ ] 6.34 L3 Playwright e2e test F1 — plugin flow UI + completion (state-convergence). Input: demo-plugin settings section in docker harness. Trigger: click "Start demo sign-in" → flow view shows auth link + paste field → paste `ok`. Observable: status converges to "complete"; demo section shows "signed in"; `plugin-credentials.json` in the container has a `demo` key. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F1)
- [ ] 6.35 L3 Playwright e2e test F2 — plugin flow cancel (state-transition). Input: demo flow pending. Trigger: click Cancel. Observable: view converges to the cancelled state; a new start works. Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F2)
- [ ] 6.36 L3 Playwright e2e test F3 — provider dialog unchanged (regression). Input: Settings → Providers → add an OAuth provider. Trigger: open the sign-in pane. Observable: auth link, paste field and title render as before (existing provider-add spec passes). Harness exemplar: see `tests/e2e/delegate-provider-oauth-flow.spec.ts` (test-plan #F3)
- [ ] 6.37 Manual check F4 — flow view visual parity: demo-plugin flow view vs provider dialog pane · human compares · [judgment: identical look and spacing in both themes] (test-plan: manual-only)
- [ ] 6.38 L1 vitest test X1 — cross-file quarantine (fault-injection). Input: auth.json and plugin-credentials.json both contain bytes `{"a":`. Trigger: write to each. Observable: two distinct `*.corrupt-*` backups, byte-exact to the input. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #X1)
- [ ] 6.39 L1 vitest test X2 — backup impossible (fault-injection (abort)). Input: corrupt file, backup dir read-only. Trigger: `set`. Observable: rejects; the original corrupt file is unchanged. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #X2)
- [ ] 6.40 L1 vitest test X3 — lock permission error (fault-injection (abort)). Input: lock acquisition throws `EACCES`. Trigger: `set`. Observable: rejects within 100 ms with `EACCES` (no 2 s retry). Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #X3)
- [ ] 6.41 L1 vitest test X4 — lock contention (fault-injection (delay)). Input: lock held by another process for 500 ms. Trigger: `set`. Observable: succeeds after release; held for 3 s → rejects `ELOCKED` at about 2 s. Harness exemplar: see `packages/server/src/__tests__/provider-auth-storage-corrupt.test.ts` (test-plan #X4)
- [ ] 6.42 L1 vitest test X5 — early login failure (fault-injection (abort)). Input: loginFlow throws before emitting. Trigger: `startFlow`. Observable: rejects `PluginFlowStartError{code:"login_failed"}`; no record in the flow store. Harness exemplar: see `packages/server/src/__tests__/provider-auth-adapter.test.ts` (test-plan #X5)
- [ ] 6.43 L1 vitest test X6 — start timeout (fault-injection (delay)). Input: loginFlow never emits (fake timers). Trigger: `startFlow`. Observable: rejects `{code:"start_timeout"}` at 15 s. Harness exemplar: see `packages/server/src/__tests__/provider-auth-adapter.test.ts` (test-plan #X6)
- [ ] 6.44 L1 vitest test X7 — registry failure isolation (fault-injection (abort)). Input: provider registry build rejects. Trigger: start + GET/POST/DELETE a plugin flow. Observable: all served normally. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #X7)
- [ ] 6.45 L1 vitest test X8 — openInBrowser under vitest (invariant). Input: VITEST=true, local server. Trigger: start provider flow. Observable: `open` spawn spy not called. Harness exemplar: see `packages/server/src/__tests__/provider-auth-routes.test.ts` (test-plan #X8)
- [ ] 6.46 L1 vitest test X9 — lane timeout (fault-injection (delay)). Input: handler never resolves (fake timers). Trigger: request. Observable: `timeout` at 15 s; a late reply is dropped. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #X9)
- [ ] 6.47 L1 vitest test X10 — lane disconnect (fault-injection (abort)). Input: socket closes while pending. Trigger: close. Observable: pending call resolves `disconnected`. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #X10)
- [ ] 6.48 L1 vitest test X11 — lane unavailable (fault-injection (abort)). Input: symbol absent (no dashboard bridge). Trigger: plugin helper request. Observable: resolves `unavailable` immediately. Harness exemplar: see `packages/extension/src/__tests__/mcp-token-delivery.test.ts` (test-plan #X11)
- [ ] 6.49 L1 vitest test X12 — loopback timeout / abort (fault-injection (delay/abort)). Input: no callback; separately an aborted signal. Trigger: wait 5 min (fake timers) / abort. Observable: rejects timeout / abort; port closed; `close()` twice doesn't throw. Harness exemplar: see `packages/dashboard-plugin-runtime/src/__tests__/server-context-model-runtime.test.ts` (test-plan #X12)
- [ ] 6.50 L3 Playwright e2e test X13 — live lane round-trip (fault-injection (none)). Input: demo-plugin `demo_echo` tool in a harness session. Trigger: prompt the session to call `demo_echo` with `hi`. Observable: tool result shows `echo: hi` in the chat view. Harness exemplar: see `tests/e2e/bus-client-goal-plugin-action.spec.ts` (test-plan #X13)
