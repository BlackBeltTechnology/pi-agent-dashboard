## 1. macOS floor canary + lookup axis (#533)

Tests first (verify red before implementing). Exemplar for every L1 row below: `packages/electron/src/__tests__/macos-floor-check.test.ts` — it already reads `_electron-build.yml`, owns the `not-extractable`/`non-numeric` cases, and is collected by `packages/electron/vitest.build-contract.config.ts` (pure-only, runs on Linux where `otool` is absent).

- [ ] 1.1 L1 test — blind extractor: input `otool -l` fixture with neither `LC_BUILD_VERSION` nor `LC_VERSION_MIN_MACOSX` · trigger canary predicate invoked with that fixture · observable status `blind-extractor`, message contains the literal `extractMinosValues` and the prebuilt path, verdict maps to error/exit 1 and never to `::warning::`. See `macos-floor-check.test.ts` (extend its `describe` set). Verify red first. (test-plan #E3)
- [ ] 1.2 L1 test — non-numeric canary result: input canary fixture whose `minos` major is `n/a` (present but unusable) · trigger canary predicate invoked · observable the error verdict, NOT `ok` and NOT the `non-numeric`→warning mapping the produced-binary path uses. See `macos-floor-check.test.ts`. Verify red first. (test-plan #E5)
- [ ] 1.3 L1 test — canary cannot run its tool: fault `otool` not executable (absent from `PATH`, or non-zero exit), expressed to the helper as `execFailed` · trigger canary runs over the prebuilt · observable error verdict + exit 1 naming extractor and sample, NOT the `::warning::`+exit-0 path `verify-macos-floor.mjs:46` uses for the produced binary. See `macos-floor-check.test.ts`. Verify red first. (test-plan #X1)
- [ ] 1.4 L1 test — canary sample missing: fault `<electron-dir>/dist/.../Electron` absent, expressed as `sampleMissing` · trigger canary runs · observable error verdict + exit 1 naming the sample path, and no fall-through to the produced-binary check. See `macos-floor-check.test.ts`. Verify red first. (test-plan #X2)
- [ ] 1.5 L1 test — pass-through preserved: input canary fixture with one slice, numeric major `12` · trigger canary passes, then produced-binary text yields no `minos` · observable canary status `ok` and produced-binary status stays `not-extractable` → `::warning::` + exit 0 (pre-existing tolerance intact). See `macos-floor-check.test.ts`. Verify red first. (test-plan #E4)
- [ ] 1.6 ci test — install step precedes the floor check: input `_electron-build.yml` darwin leg step list · trigger workflow-contract test parses the file · observable a `node install.js` step exists on the darwin legs, its electron dir comes from `pi-dashboard-resolve-tool.cjs electron`, and its index is before the floor-check step's index. See the `_electron-build.yml` parsing already in `macos-floor-check.test.ts`. Verify red first. (test-plan #E1)
- [ ] 1.7 ci test — lookup miss fails: input the floor-check step's `else` branch in `_electron-build.yml` · trigger workflow-contract test parses the branch · observable branch contains `::error::` and `exit 1`, the string `skipping otool check` is absent, and `hdiutil detach` still runs before the exit. See `macos-floor-check.test.ts`. Verify red first. (test-plan #E6)
- [ ] 1.8 L1 test — hard-coded-path lint reaches the workflow: input a literal `node_modules/electron/dist` string planted in `_electron-build.yml` · trigger `no-hardcoded-node-modules-paths.test.ts` runs · observable the test FAILS naming `_electron-build.yml` (i.e. `SCAN_FILES` now reaches it), and passes with the resolver call instead. See `packages/shared/src/__tests__/no-hardcoded-node-modules-paths.test.ts` (`SCAN_FILES` at ~:80). Verify red first. (test-plan #E2)
- [ ] 1.9 Implement the pure canary helper in `packages/electron/scripts/macos-floor.mjs` per design D1: signature takes `{ otoolOutput, execFailed, sampleMissing }` and returns a status including the new `blind-extractor`; all five canary failure modes (blind, non-numeric, missing sample, exec failure, mismatch) map to the error verdict. Verify 1.1–1.5 green.
- [ ] 1.10 Implement the canary call in `verify-macos-floor.mjs`: resolve the prebuilt, own the `execFileSync("otool", …)`, route every outcome through the 1.9 helper, and run it BEFORE the produced-binary check. Update the script's header status table. Verify 1.1–1.5 still green; on a local macOS build the script exits 0 and prints the canary line.
- [ ] 1.11 Add the darwin `node install.js` step to `.github/workflows/_electron-build.yml` before packaging, resolving the electron dir via `node packages/shared/bin/pi-dashboard-resolve-tool.cjs electron` (mirror the existing linux/arm64 step at ~:156-169). Verify 1.6 green.
- [ ] 1.12 Harden the floor-check `else` branch in `_electron-build.yml` to `::error::` + `hdiutil detach` + `exit 1`, and update the step comment to describe the canary. Verify 1.7 green.
- [ ] 1.13 Add `.github/workflows/_electron-build.yml` to `SCAN_FILES` in `no-hardcoded-node-modules-paths.test.ts`. Verify 1.8 green.

## 2. Dead `release:` trigger (#580) — `.github/workflows/sync-release-version.yml`

Exemplar for every row below: `packages/shared/src/__tests__/site-deploy-workflow-contract.test.ts` (E10/E11 blocks). Author a NEW `describe` naming `sync-release-version.yml` — E10's title and messages say `deploy-site.yml`.

- [ ] 2.1 L1 test — block form refused: input `sync-release-version.yml` containing `on:`→`release:` in block form · trigger the contract test runs · observable test FAILS, failure message contains `sync-release-version.yml`, and the owning `describe` names that workflow. Verify red first. (test-plan #E7)
- [ ] 2.2 L1 test — inline-mapping form refused: input `on: {release: [published]}` · trigger same test runs · observable test FAILS — proving the block-form-only regex `/^\s*release:\s*$/m` is insufficient. Verify red first. (test-plan #E8)
- [ ] 2.3 L1 test — sequence form refused: input `on: [release]` · trigger same test runs · observable test FAILS. Verify red first. (test-plan #E9)
- [ ] 2.4 L1 test — dispatch input survives: input the shipped `sync-release-version.yml` after the trigger removal · trigger same test runs · observable `workflow_dispatch.inputs.correlation` present and the suite passes. Verify red first. (test-plan #E10)
- [ ] 2.5 Implement D2: delete the `release: [published, edited]` trigger, keep `workflow_dispatch` with `correlation`. Verify 2.1–2.4 green.
- [ ] 2.6 Rewrite the docstring per D2: `workflow_dispatch`-only triggers; a hand-published draft needs TWO manual dispatches (`sync-release-version`, then `deploy-site`); remove the false claim that `deploy-site.yml`'s `paths:` filter picks up this workflow's own `GITHUB_TOKEN` commit. Update the `.github/workflows/AGENTS.md` row.

## 3. Text + spec corrections (#577, #578)

- [ ] 3.1 L1 test — site design tokens: input `site/index.html` and `site/404.html` text · trigger token-contract test parses both · observable `:root` declares all 13 named tokens (`--bg-primary|secondary|tertiary`, `--text-primary|secondary|tertiary`, `--accent|-solid|-text`, `--status-idle|working|needs-you`, `--border`), a `[data-theme="light"]` block redeclares the same set, and occurrences of `--pi-`, `rgb(var(`, and `root.dark` are each 0. NEW test file — no existing unit test reads `site/`; author it in the text-contract style of `packages/shared/src/__tests__/pnpm-migration-contract.test.ts`. Verify red first (it should fail against the CURRENT spec wording, not the tree). (test-plan #E11)
- [ ] 3.2 L1 test — workflow install/order pinned: input `ci.yml` and `publish.yml` text · trigger `pnpm-migration-contract.test.ts` X5 runs · observable both install via `pnpm install --frozen-lockfile` and run lint→test→build in that order, with no `npm ci` in either install path. Extend X5 if it asserts only the install verb and not the order. See `packages/shared/src/__tests__/pnpm-migration-contract.test.ts` (`describe("X5 …")` at ~:109). Verify red first if extending. (test-plan #E12)
- [ ] 3.3 Implement D4 — `packages/shell/vite.config.ts` `spa404Fallback` docstring: Pages serves the repo-root `site/404.html` for any unmatched path including under `/app/`; the shell dist's copied `404.html` is reached only if the shell is deployed at a root; hash routing keeps deep links off the server. Verify the shell build is unchanged.
- [ ] 3.4 Spec deltas complete (D3, D5, D6): `openspec validate fix-ci-pipeline-followups --strict` green; every MODIFIED block preserves its scenario TITLES verbatim (a retitled scenario reads as a drop and `openspec archive` refuses the sync).

## 4. Manual verification (deferred post-merge per the manifest)

- [ ] 4.1 Manual — live dispatch path: after a hand-published draft release, dispatch `sync-release-version` from the Actions UI, confirm it rewrites the download block and commits to `develop`, then dispatch `deploy-site` and confirm the site shows the new version. Requires live GitHub Actions and a real release; the static contract tests explicitly disclaim live coverage. (test-plan: manual-only)
- [ ] 4.2 Manual — post-archive spec text: after `openspec archive`, confirm each re-stated requirement block carries the pnpm / static-site wording, and that the deliberately-surviving hits are untouched — `npm ci` in "Build tools referenced by workflows MUST be declared dependencies" and in marketing-site's forbidden-command SHALL NOT, `Astro` in marketing-site "Performance and accessibility budgets". Scoped to those requirement blocks, NOT a whole-file grep (which is unsatisfiable by design). (test-plan: manual-only)
- [ ] 4.3 Manual — docstring reads true: `packages/shell/vite.config.ts` comment names `site/404.html` and `/app/` and no longer claims Pages serves the shell for unknown paths. (test-plan: manual-only)

## 5. Closeout

- [ ] 5.1 `AGENTS.md` rows: `packages/electron/AGENTS.md` (the `scripts/macos-floor.mjs` + `scripts/verify-macos-floor.mjs` rows live HERE — there is no `packages/electron/scripts/AGENTS.md`), `.github/workflows/AGENTS.md` (`_electron-build.yml`, `sync-release-version.yml`, and the stale `npm ci` in the `ci.yml` row), `packages/shell/AGENTS.md` if `vite.config.ts` has a row. Verify `kb dox lint` clean.
- [ ] 5.2 `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` zero failures; `npm run quality:changed` clean; comment on #533, #577, #578, #580 with the change name.
