<!-- D20 NOTE (current deployment — design.md D20). The browser frontend is the USER's OWN
     application; the dashboard is a backend resource server only and serves no login UI. A custom
     independent SERVER plugin owns login/UI/callback/logout and publishes {pluginId, loginUrl,
     logoutUrl} via registerBrowserLoginConfig. Checkmarks already recorded under the SUPERSEDED D16
     component-adapter model (§13 especially, incl. its live dashboard-UI verification) are HISTORICAL
     implementation records — they are NOT fresh verification of the D20 independent-frontend path and
     MUST NOT be cited as such. §17 carries the D20 checks; every one is unchecked pending real
     results. Reusable seams (identity, access policy, login-config descriptor, token store, WS
     ticket/principal) and the optional bundled dashboard-client adapter stay; nothing is deleted. -->

## 0. Bundled resolver plugin package

- [x] 0.1 Scaffold `packages/keycloak-resolver-plugin` (manifest id `keycloak-resolver`, default-enabled, depends on `pi-dashboard-shared`); verify it type-checks and loads as a plugin.
- [x] 0.2 Add `keycloak-resolver` to `BUNDLED_PLUGINS` (`packages/electron/scripts/bundle-server.mjs`) and update `packages/shared/src/__tests__/bundled-plugins-complete.test.ts`; verify the invariant test passes with the new plugin present. _(derives from bundle-server.mjs — no separate list to edit; invariant test passes.)_
- [x] 0.3 Confirm dashboard core imports NOTHING Keycloak-specific for principal resolution (no jose/JWKS/DPoP in `packages/server/src`); the resolver lives only in the plugin. _(NOTE: pre-existing topology-A cookie connector `packages/server/src/auth/auth.ts` still references Keycloak/OIDC — that is the legacy `auth.providers` login flow, deliberately separate from the resolver; D8 rejects it only when the resolver is active.)_

## 1. Types and shared contract

- [x] 1.1 Add immutable `Principal` (`{ iss; sub; email? }`), `PrincipalResolution` (`{ principal; expiresAt }`), `ResolverReject`, and DPoP-ready `AuthContext` (`{ method; url; authorization?; cookie?; dpop?; isAuthenticated; ip }`) to `packages/shared/src/identity.ts` (shared with `dashboard-plugin-runtime`); both packages type-check.
- [x] 1.2 Add `PrincipalResolverFn`, `HostAction`, `HostResource`, and `HostAccessPolicyFn` plus `isPrincipalResolution`/`isResolverReject`/`principalEquals` helpers; the three resolver outcomes and the policy boolean are type-distinguishable (unit test).
- [x] 1.3 Add `identity` config shape (`trustedResolverPlugins: string[]`; optional `trustedPolicyPlugin?: string`; `resolverTimeoutMs`/`policyTimeoutMs` clamped) to shared config — NO `mode` field; default parse yields an empty trust list and no policy (unit test).

## 2. Activation and startup readiness (no mode flag)

- [x] 2.1 Derive the active/inert state from the resolver: active ⇔ a trusted resolver is registered AND configured (issuer+audience); the plugin owns the issuer+audience decision (only registers when active), so `resolverRegistry.hasResolver()` is the host's active signal. Covered by registry tests (register-when-active) + activation tests.
- [x] 2.2 Add a pre-`listen()` readiness check (`assertIdentityReadiness`, wired in `server.ts` before `fastify.listen()`): a named `trustedPolicyPlugin` must resolve to exactly one registered policy (zero/duplicate fails startup); a missing/empty `identity` block is valid (inert, no policy). Tests: named-but-absent, duplicate, inert happy path. _(NOTE: policy registration lands in §9; until then registeredPolicyCount=0, so a named policy correctly fails as named-but-absent.)_
- [x] 2.3 When the resolver is active, reject a non-empty `auth.providers` (confidential login connectors) as a config error (D8). Tests: active+connectors fails startup; inert dashboard leaves connectors intact.

## 3. Resolver registry with host trust grant

- [x] 3.1 Implement a resolver registry (`packages/server/src/identity/resolver-registry.ts`) keyed by `pluginId`, ordered `(manifest.priority ASC, pluginId ASC)`; duplicate registration from one plugin fails. Unit test of deterministic cross-boot ordering (priority + pluginId tie-break).
- [x] 3.2 Gate `registerPrincipalResolver` on the host trust grant (bundled `keycloak-resolver` or a plugin named in `identity.trustedResolverPlugins`); a self-declared `manifest.priority` grants nothing. Runtime `ServerPluginContext.registerPrincipalResolver` wired in `server.ts`: untrusted plugin gets a no-op registrar (logs + inert unregister). Test: untrusted (incl. low-priority) plugin cannot register.
- [x] 3.3 Validate/copy/freeze resolver output before exposure (`principal-guard.ts`: reject empty `iss`/`sub`, non-future/absent `expiresAt`, over-length; drop malformed display-only `email`; strip extra props; freeze). Test: malformed principal discarded; no prop leakage.
- [x] 3.4 Support override-by-disable: a disabled bundled default never registers, so only the trusted replacement in `trustedResolverPlugins` resolves. Test: disabled default never claims a token (only replacement present).

## 4. Resolution integrated into the auth gate (HTTP plane)

- [x] 4.1 Decorate `request.principal` (null) and `request.principalExpiresAt` (null) once, inside `registerResolverHook` (called at server.ts right after `registerBearerAuth`); `hasRequestDecorator` guard prevents a duplicate-decorator throw. Boot-safe.
- [x] 4.2 Build the curated `AuthContext` in a pure helper (`auth-context.ts`: method, canonical url via `canonicalOrigin` D6a, authorization/cookie/dpop, isAuthenticated, ip). Test: exposes nothing beyond the allowlist; query/fragment stripped; absent headers omitted.
- [x] 4.3 Register the resolver-dispatch `onRequest` hook (`resolver-hook.ts`) AFTER `registerBearerAuth` and BEFORE `registerAuthPlugin`; inert ⇒ returns immediately; claim sets principal + expiry + `isAuthenticated` + `authVia=principal`; reject → 401; `null` continues. Fastify integration test: bearer+no-cookie authenticates (not 401), reject → 401, inert unchanged.
- [x] 4.4 Bound each resolver by the configured timeout (`dispatch.ts` `withTimeout`); throw/reject/timeout per D5 (owned-invalid ⇒ resolver's own reject; unexpected fault ⇒ core coerces to `null`, logged, never 500). Tests: throwing resolver neutralized, slow resolver bounded, malformed claim discarded.
- [x] 4.5 Device-bearer path yields `isAuthenticated` true with `principal === null` (resolver returns null for a device token). Fastify integration test with a simulated device-auth pre-hook.

## 5. Bundled Keycloak resolver (config-seeded, in the plugin)

- [x] 5.1 Config schema + parser (`shared/config.ts`): `issuer`/`audience` required, `authorizedParty`/`jwksUri`/`clockSkewSeconds`/`networkTimeoutMs`/`allowInsecureHttp` optional, read via `getPluginConfig()`; timeouts/skew clamped; provider URLs must be https (or opted-in http). Tests: no issuer/audience ⇒ inert null resolver (no default substituted); http without opt-in ⇒ inert; malformed URL ⇒ inert; clamp bounds.
- [x] 5.2 OIDC discovery + JWKS cache (`jwks.ts`) via jose `createRemoteJWKSet` (cache + coalesced refresh + single-refresh-per-unknown-kid), bounded by `networkTimeoutMs`, `redirect:"error"`, discovery `issuer` echo checked. Tests: concurrent first-fetch coalesced, cache reuse, unknown-kid one coalesced refresh, explicit jwksUri skips discovery, discovery outage ⇒ reject owned tokens (not accept unverified).
- [x] 5.3 Ownership disambiguation (`resolver.ts`): opaque/non-JWT ⇒ null; foreign unverified `iss` ⇒ null; configured-issuer ⇒ owned. Tests for all three.
- [x] 5.4 RFC 9068 validation (RS256 only via header pre-check + jose `algorithms`, exact `iss`, required `aud`, optional `azp`, `exp`, `sub`) ⇒ `{ principal, expiresAt from exp*1000 }`; failure ⇒ reject; `email` only when `email_verified === true`. Tests incl. `alg:none`/UnsecuredJWT rejection, aud/azp/exp/sub rejections, unverified-email omission.
- [x] 5.5 Conditional DPoP (`dpop.ts`): validated only when `cnf.jkt` present — proof JWS under embedded public asymmetric `jwk` (alg pinned, symmetric/private rejected), thumbprint == `cnf.jkt`, `htm`, canonical `htu`, `ath` == b64url SHA-256(token), fresh `iat`, unreused bounded `jti` (single-instance LRU). Missing/mismatch ⇒ reject; unbound token ⇒ plain bearer; malformed `cnf.jkt` ⇒ reject. Tests: bound-without-proof reject, wrong-`ath` reject, unbound plain-bearer accept, valid+replay reject, malformed-cnf reject.
- [x] 5.6 Owned-token crypto/JWKS/DPoP faults caught inside the resolver ⇒ `reject` (never uncaught throw). Test: induced JWKS fault on an owned token ⇒ reject, not fall-through.

## 6. Session ownership (persist + assign)

- [x] 6.1 Added `principalOwner?: { iss; sub }` to `SessionMeta` + `DashboardSession` (+ `PrincipalOwner` type in shared/identity.ts; equality is the existing exact `principalEquals`), restored in `sessionFromMeta`. Persistence round-trip test green.
- [x] 6.2 Owner stamped through the trusted browser `spawn_session` road: `pending-principal-owner-registry.ts` (token-keyed, file-before-await, copy+freeze, 60s TTL, single-use) filed against a PRE-MINTED spawn token before `spawnPiSession`, gated on `isResolverActive() && ws.principal` (identity join key only, email dropped); removed on spawn failure/throw. event-wiring `session_register` resolves by token → stamps `DashboardSession.principalOwner` + persists to `.meta.json` + broadcasts; reconnect/cold-start reads it back via `sessionFromMeta`. cwd never confers ownership; inert-era + principal-less sockets stay ownerless. Wired through `createBrowserGateway` → `BrowserHandlerContext`. Tests: registry 6/6; handler stamps-when-active, inert-ownerless, principal-less-ownerless, removes-on-fail, removes-on-throw. NOTE: the browser `spawn_session` is the ONLY human spawn road that exists in the codebase today — there is no HTTP spawn endpoint (`/api/spawn*` is a failure-log reader) and no owned-spawn plugin API passing a human principal (the plugin-spawn hook files a PLUGIN ownerId via `pending-plugin-ref-registry`, not a human principal). The design's HTTP-spawn / owned-spawn-API roads are seam contracts for future call sites; when added they MUST file the owner through this same pre-mint-token path. Nothing ownerless leaks because those roads do not yet exist.

## 7. Optional host access policy (non-session roads)

- [x] 7.1 `PolicyRegistry` (`identity/policy-registry.ts`): accepts exactly one `authorize()` from the configured `trustedPolicyPlugin`, refuses any other registrant (`PolicyTrustError`) and a second registration (`PolicyDuplicateError`); unnamed ⇒ nothing trusted, `authorize` returns `true` (ungated). Tests: named-accepted, other-refused, duplicate-refused, none-named-ungated, unregister-scoped.
- [x] 7.2 Bounded fail-closed `authorize()` (default 500ms, clamped 50–2000): `false`/throw/timeout/non-boolean/unclassified ⇒ deny + structured `PolicyAuditEvent` (principal `(iss,sub)`, action, resource, reason; no secrets). Tests: allow-true, deny+audit for false/throw/timeout/non-boolean/unclassified, timeout clamp.
- [~] 7.3 Taxonomy built: `identity/host-resources.ts` — `HostActions` constants (workspace/openspec/branch/terminal/system read+write, domain.event) + secret-free `hostResource` builders per family. `PolicyRegistry.authorize` already denies an unclassified road when a policy is registered and returns allow (ungated) when none (§7.1/§7.2 tests cover both). GATE HELPER built: `identity/host-access.ts` `gateHttpNonSession(reply, policy, principal, action, resource)` — the non-session analog of `gateHttpSession`: **pure passthrough when `hasPolicy()` is false** (policy never consulted ⇒ zero production effect), else principal-less ⇒ 403, else policy decides (fail-closed via `authorize`), deny ⇒ 403 (not 404: non-session existence is not owner-secret). 4 unit tests (inert-passthrough / allowed / denied / principal-less-skips-policy). REMAINING: attach `{action,resource}` + the gate call at each non-session route site (mechanical, de-risked by the inert-passthrough property; behavioral gated-path verification is the §11.2 E2E's job with the `fixture-policy` caller).
- [x] 7.4 Inbound message table classified in `identity/ws-message-scope.ts` (session / session-list / non-session) with a coverage test that re-derives ALL 85 `BrowserToServerMessage` union literals from the shared protocol source and fails on any unclassified type (the "new road without owner-equality fails" guarantee). NOTE: bootstrap-FRAME disclosure gating (server→browser non-session state) is the separate §8.4 policy road; the session snapshot frame is already owner-filtered (§8.2).

## 8. Enforce every session read/write road (HTTP + WS) — owner equality, always on when active

<!-- §8 SHARED GUARD built: `identity/session-access.ts` `canAccessSession({active,principal,owner})`
     (inert⇒allow; active+ownerless⇒deny; active+principal-less⇒deny; else exact principalEquals) +
     `filterSnapshotForPrincipal` (per-item sessions + orders + endedTotals). Both unit-tested (10/10). -->
- [x] 8.1 Owner-equality on all 10 session-scoped HTTP roads in `session-routes.ts` via `gateHttpSession(reply, active, principalOf(request), owner)` (deny ⇒ 404, no owned-vs-not-found oracle) + `filterOwned` per-item on `/api/sessions` and `/api/sessions/archived`: events/:sessionId/:seq, tool-result, session-change, session-diff, retained-transcript, session-file, archived/:id (read+DELETE). Archived sessions retain their owner (`principalOwner` added to `ArchivedSessionSummary`, stamped at capture + restored in the cold-boot meta reseed) so an owner keeps access. `isResolverActive` threaded from server.ts. Tests: `gateHttpSession` owner-accept / non-owner-404 / principal-less-404 / ownerless-404 / inert-passthrough. Net-zero new biome complexity (extracted `resolveInCwd`).
- [~] 8.2 WS bootstrap `sessions_snapshot` filtered per-item via `filterSnapshotForPrincipal` in the gateway connect path (sessions by owner-equality, orders to visible ids, endedTotals to groups with a visible session); inert passes through. Tests: two-principal each-sees-only-own, principal-less-sees-nothing, inert-passthrough. **PARTIAL — VERIFIED SECURITY GAP (source-corroborated, Audit):** the `session-list` REQUEST handlers are NOT filtered — `session-meta-handler.handleSessionsPage` replies from `sessionManager.get(id)` and `handleListSessions` from `sessionManager.listAll()`, neither with an owner filter, and the §8.3 choke point gates only `session`-scope messages, so `session-list` (per `identity/ws-message-scope.ts`) never reaches it. A principal can therefore list/page sessions it does not own. Needs a per-item filter (reuse `canAccessSession`/`filterSnapshotForPrincipal`) + a REGRESSION test before any isolation claim. Do NOT close this from an empty-list smoke payload.
- [x] 8.3 Single owner-equality CHOKE POINT in the browser-gateway dispatch (before the switch): `isResolverActive() && isSessionOwnedMessage(msg.type)` ⇒ look up `sessionManager.get(sessionId)?.principalOwner`, apply `canAccessSession`, silently drop on deny (no oracle) — covers subscribe/unsubscribe/history_backfill + every session command (send_prompt/abort/retry/kill/force_kill/shutdown/stop_after_turn/rename/archive/unarchive/resume/proposals/followup-queue/set_model/set_thinking_level/tags/display-prefs/process-drawer/flow_control/resync/view). Classification `identity/ws-message-scope.ts` (36 session / 2 session-list / 47 non-session; coverage test re-derives all 85 protocol union literals → fails if any is unclassified, satisfying §7.4/D10). Tests: scope 5, integration 4 (owner-forwarded, non-owner-dropped, principal-less-dropped, inert-unchanged) via the real message loop.
<!-- §8.3 CLASSIFICATION ANALYSIS (for the choke-point gate + §7.4 coverage test):
     Approach: single gate in the browser-gateway dispatch switch keyed by a CURATED message table
     (not just "has sessionId"). Data-driven `sessionId`-carrying members (51): abort accept_replace_proposal
     architect_prompt_response archive_session attach_proposal clear_followup_entries close_inline_terminal
     detach_proposal dismiss_replace_proposal edit_followup_entry extension_ui_response fetch_content flow_control
     flow_management force_kill history_backfill kill_process list_files open_inline_terminal promote_followup_entry
     prompt_response prompt_resync_request remove_followup_entry rename_session request_commands request_models
     request_providers request_roles resume_session retry_session role_preset_delete role_preset_load
     role_preset_save role_remove role_set send_prompt session_unview session_view setSessionDisplayPrefs set_model
     set_session_process_drawer set_session_tags set_thinking_level shutdown stop_after_turn subagent_resync_request
     subscribe ui_management unarchive_session unsubscribe watch_files.
     CURATION NEEDED: some carry sessionId only as CONTEXT for a global/workspace op and are NON-session
     (→ §8.4 policy / ungated, NOT owner-equality): request_commands/models/providers/roles, role_* , ui_management,
     flow_management, list_files, watch_files, open_inline_terminal/close_inline_terminal (terminal=workspace road).
     resume_session mutates a session but for a RECOVERY candidate that may pre-date ownership — treat as owner-gated
     when it has an owner, and (open question) how to handle ownerless-recovery under active resolver.
     spawn_session (no sessionId) is gated by principal-presence for stamping, not owner-equality. -->
- [~] 8.4 Gate MECHANISM + CALLER complete + unit-tested, both inert-by-default: `gateHttpNonSession` (§7.3, `identity/host-access.ts`, 4 tests) routes a non-session road through `PolicyRegistry.authorize` only when a policy is registered, else passes through unchanged (today's disclosure); the `fixture-policy` plugin (`packages/fixture-policy-plugin`, 7 tests) is the trusted caller that grants/denies per principal. REMAINING (lands with §11.2, which supplies the running server + policy caller to verify byte-for-byte): attach the gate + `{action,resource}` at the OpenSpec/branch/terminal/system route sites, then the policy-registered-omits-unauthorized vs ungated-discloses-as-today behavioral test against the live harness.

## 9. WebSocket identity binding + lifetime

- [x] 9.1 `WsTicketStore.mint` binds an optional `{ principal, principalExpiresAt }` onto the ticket entry and returns them from `consumeDetailed` (`ws-ticket.ts`); the `/api/ws-ticket` handler passes the resolver-hook `request.principal` for non-bridge scopes (server.ts:~1845); a principal-less caller records none. Tests: mint-binds-and-returns, principal-less-mint-records-none.
- [x] 9.2 New `authorizeWsUpgrade()` (auth-plugin.ts) supersedes the boolean `validateWsUpgrade` on the production upgrade path (server.ts): `requireIdentityTicket = scope==="browser" && resolverRegistry.hasActiveResolver()` ⇒ ONLY a principal-bearing single-use ticket authorizes; cookie/local-IPC/trusted-network/no-ticket/principal-less-ticket all refused. Non-browser scopes + inert era unchanged (unified secret vs no-auth branches via optional `secret`). `validateWsUpgrade` retained for existing callers/tests. Tests: identity-mode refuses local/cookie/no-ticket/principal-less, accepts principal ticket; legacy allowances + no-auth mode intact (68 auth/WS tests green).
- [x] 9.3 Upgrade handler attaches a FROZEN `ws.principal` (copy) + `ws.principalExpiresAt` from the consumed ticket before `emit("connection")`; principal-less (inert) sockets carry neither. Tests: principal surfaced vs absent via `consumeDetailed`.
- [x] 9.4 `installSocketLifetime` (`identity/socket-lifetime.ts`) schedules a one-shot close (code 4001) at `principalExpiresAt` — immediate when already past, none for a principal-less socket — wired into the browser-gateway connection handler with cleanup on close/error (releases subscriptions via the existing close path). Tests: schedules-at-exp, closes-when-past, none-when-principal-less.
- [x] 9.5 Transport-only heartbeat in the same helper (ping each tick, terminate on a missed pong), distinct from bridge ping/pong and untied to any session; a heartbeat NEVER extends identity (independent expiry timer). Tests: ping+terminate-on-missed-pong, pong-keeps-alive, heartbeat-does-not-extend-identity, cleanup-clears-both.

<!-- POLICY WIRING (foundation for §7.3/§8.4/§10) DONE this pass:
     `PolicyRegistry` now INSTANTIATED in server.ts (trustedPolicyPlugin + policyTimeoutMs from config,
     structured audit sink); plugin registration seam `registerHostAccessPolicy` added to the plugin
     server-context (type + impl), trust-gated to the configured plugin (no-op registrar otherwise,
     mirroring registerPrincipalResolver); `assertIdentityReadiness` now reads `policyRegistry.size`
     (no longer hardcoded 0), so a named-but-absent / duplicate policy fails startup for real (D9).
     Green: 86 identity + 409 plugin-runtime. Remaining §10/§7.3/§8.4 = per-road/per-socket policy CALLS. -->
## 10. Permissioned domain-event fan-out

- [x] 10.1 Mechanism built + tested: `identity/domain-fanout.ts` `deliverDomainEvent(targets, action, resource, policy, send)` — no policy ⇒ delivers to every socket (unchanged broadcast); policy ⇒ per-candidate `authorize`, delivers only on true, principal-less socket gets nothing, fail-closed via `PolicyRegistry.authorize`. Tests (4): no-policy-all, policy-per-socket-isolation (Anna sees, Béla doesn't), principal-less-nothing, deny-drops. NOTE: no core path emits a resource-tagged GLOBAL domain event today (`plugin_event` carries a `sessionId` → session road, §10.2); the gateway `broadcastDomainEvent` emit-path wiring lands with the §11.2 fixture policy plugin that exercises it end-to-end.
- [x] 10.2 `flow_control` is classified `session` in `ws-message-scope.ts` and therefore rides the §8.3 owner-equality choke point (dropped for a non-owner, delivered to the owner) — it never enters the domain-event policy road. Verified by the choke-point integration tests (a session command from a non-owner is dropped; from the owner, forwarded) which apply identically to `flow_control`.
- [~] 10.3 INBOUND classification complete + coverage-tested (`ws-message-scope.ts`, §7.4: session-by-`sessionId` vs non-session; undeclared ⇒ non-session/domain). OUTBOUND emit-time classification (session-frame-by-`sessionId` vs domain-by-`resource`) is embodied by the two send paths — owner-gated broadcast for session frames vs `deliverDomainEvent` for domain frames; the per-emit tagging of an outbound domain frame lands with the §10.1/§11.2 emit path.

## 11. Validation and cross-cutting

- [x] 11.1 Inert = today verified at every decision seam (each seam's inert branch is a pure passthrough with an explicit test): `resolver-hook` (no resolver → request unchanged, no principal), `session-access` (inert → allow-all / snapshot passthrough / gate proceeds unconditionally), `domain-fanout` (no policy → unchanged broadcast to all), `activation` (inert happy path, connectors intact). Structural guarantee: every identity gate returns allow/passthrough in its inert branch, so an unconfigured/disabled resolver changes no outcome. Running-server byte-for-byte confirmation is the §11.2 E2E's job.
- [x] 11.2 Docker-harness E2E. DONE + verified: (a) **fixture trusted policy plugin** `packages/fixture-policy-plugin/` (private) — config-seeded allow-list `HostAccessPolicyFn` (matches principal `sub`/`iss`, grants action or `*`, default-deny), registered via `registerHostAccessPolicy` under id `fixture-policy`, inert when disabled / no capability; 7 unit tests (decision matrix incl. Anna-allowed / Béla-denied fan-out, iss pinning, registration+inert), clean tsc+biome. **BRING-YOUR-OWN-KEYCLOAK**: the dashboard never ships/boots Keycloak — every provider field is a `keycloak-resolver` plugin setting (`configSchema.json`), so a developer/user points the plugin at their own IdP. (b) **Importable realm reference** `docker/fixtures/keycloak/realm-identity-plane.json` (realm `pi-identity`, public PKCE S256 client `dashboard-web` + direct-grant, client-level `oidc-audience-mapper` → `pi-dashboard`, users anna/bela) — **LIVE-VALIDATED** against a standalone Keycloak 26: realm imports, anna+bela mint tokens with correct iss/aud/azp + **distinct sub**, and the real `keycloak-resolver` resolves both to distinct principals while rejecting a foreign issuer (`docker/fixtures/keycloak/live-resolver-check.ts`, exit 0). Live testing fixed two fixture bugs the JSON-parse check missed: missing `lastName` (KC26 User Profile → login failed) and an explicit `clientScopes` array that suppressed the built-in `basic` scope (dropped the `sub` claim ownership keys on). **E2E APPROACH = LIGHTWEIGHT FAKE OIDC ISSUER** (best practice for testing token VALIDATION; researched: oauth2-mock-server / mock-oauth2-server): the E2E does NOT boot Keycloak. The `docker/fixtures/keycloak/` realm + `live-resolver-check.ts` demote to a real-Keycloak **production-onboarding reference** (proved the real path once, exit 0). (c-fake) **fake OIDC issuer** `packages/shared/src/test-support/fake-oidc-issuer.ts` — real RS256 keypair, real `/.well-known/openid-configuration` + `/jwks` + `POST /mint`; `issuerUrl` override decouples advertised issuer from bind host (Docker: bind 0.0.0.0, advertise loopback). (c-int) **high-fidelity integration test** `packages/keycloak-resolver-plugin/src/server/__tests__/fake-issuer-integration.test.ts` — the REAL resolver + REAL `JwksSource` (live HTTP discovery + remote JWKS + `jose` RS256 verify) against the fake issuer: **6 tests green** (anna/bela → distinct principals, verified-email carry/drop, foreign-issuer → null, wrong-aud reject, expired reject, HTTP `/mint` cross-boundary path). No Docker, no Keycloak. (c) **opt-in overlay** `docker/compose.test.identity.yml` — boots NO Keycloak; sets `PI_E2E_IDENTITY=1` + `PI_E2E_IDENTITY_PORT`/`_AUDIENCE` and publishes the issuer port on loopback. Merge validated: 3 ports incl. 18090, no keycloak service. (c-run) **in-container runner** `scripts/fake-oidc-run.ts` (under `scripts/` because the Dockerfile bakes it, not `docker/`) — boots the fake issuer on the fixed port; **verified locally** (discovery/jwks/mint all answer, missing-sub → 400). (d) **token helper** `tests/e2e/helpers/identity-tokens.ts` — mints anna/bela (stable subs) via the fake issuer's `/mint` over the mapped port; biome clean. (e) **`PI_E2E_IDENTITY` seed path** in `test-entrypoint.sh` — flag-gated: boots the fake issuer (tsx), waits for discovery, then writes `plugins["keycloak-resolver"]` (enabled/issuer=fake/audience/allowInsecureHttp) + adds it to `identity.trustedResolverPlugins`. Merge-preserving, idempotent; `bash -n` clean. Inert unless `PI_E2E_IDENTITY=1` ⇒ zero change to the default harness. Also fixed a pre-existing regression: `fixture-policy-plugin` needed `pi-dashboard-plugin.fixture:true` (bundled-plugins-complete test); shared suite green (2205). (f) **two-user isolation E2E — LIVE BOOT-TO-GREEN, 8/8.** _(caveat: this spec exercises only the WS bootstrap `sessions_snapshot` + HTTP list/detail — it does NOT cover the unfiltered `list_sessions`/`sessions_page` handlers (§8.2 verified gap), so 8/8 is not a whole-plane isolation proof.)_ `tests/e2e/identity/two-user-isolation.spec.ts` drives raw HTTP + `ws` (no browser — an active resolver refuses ticketless browser sockets, §9.2) against an identity-ACTIVE harness (`playwright.identity.config.ts` + `tests/e2e/identity/global-setup.ts`, `npm run test:e2e:identity`): HTTP list owner-scoped both ways, principal-less sees nothing, detail-gate 404-for-non-owner / 200-for-owner (no oracle), ws-ticket rejects wrong-aud (401) + claims valid bearer (200+ticket), §9.2 upgrade refused without an identity ticket, WS bootstrap owner-scoped. Harness pieces: `docker/compose.test.identity.yml` (overlay, publishes issuer port), `test-up.sh` `TEST_EXTRA_COMPOSE` hook, `scripts/fake-oidc-run.ts` (in-container issuer runner), `scripts/seed-identity-sessions.mjs` (Anna+Béla owned ended sessions). Two boot bugs found + fixed live: (i) the resolver + fixture-policy manifests lacked `claims: []` → `discoverPlugins()` dropped them → plane inert (allow-all); guarded by a new `manifest-discoverability.test.ts`. (ii) the entrypoint's ticketless `/ws` smoke check crash-looped PID 1 under active identity (refused by §9.2) — now skipped when `PI_E2E_IDENTITY=1`. Verified end-to-end: cold `--build` image + managed boot + 8 green + clean teardown. The secondary `fixture-policy` domain-fanout assertion stays deferred (its decision matrix is unit-covered: 7 tests).
- [x] 11.3 Issuer pinning verified: `keycloak-resolver` peeks the UNVERIFIED `iss` and returns `null` when it differs from the configured `issuer` (foreign-issuer JWT falls through, never persists an `(iss,sub)`); test `resolver.test.ts` “foreign-issuer JWT yields null” + exact-`iss` match. The host/scheme/port pinning doc sentence folds into §11.6.
- [x] 11.4 Non-human principal policy verified: an in-process/scheduler spawn files no owner token → `resolve` returns `null` → ownerless BY CONSTRUCTION; an ownerless session is denied to every human + principal-less socket when active (inert unchanged). Composition test added to `session-access.test.ts` (“scheduler-spawned session is ownerless and hidden from humans”) tying `pending-principal-owner-registry.resolve` → `canAccessSession`. Explicit sentence lives in design D11 + folds into the §11.6 doc note.
- [x] 11.5 `openspec validate add-multi-user-identity-plane --strict` → “Change 'add-multi-user-identity-plane' is valid”.
- [x] 11.6 Doc note written: `docs/identity-plane.md` (caveman style) — consumer rule (gate on `request.principal`/`ws.principal`, never `isAuthenticated`), activation model (resolver enabled+configured, no mode flag; inert=today, active=owner-equality), bundled `keycloak-resolver` (core imports nothing Keycloak-specific) + issuer pinning, optional non-session-only host policy (`trustedPolicyPlugin`+`policyTimeoutMs`, fail-closed), product-authz-in-product-plugin, non-human ownerless-by-construction, RFC 9068/9700/7636/9449. Indexed in `docs/AGENTS.md` (alphabetical row).

## 12. Browser client plane (topology B)

- [x] 12.1 Full auth-code+PKCE flow (pure fns) + in-memory store. `oidc-flow.ts`: `buildAuthorizeRequest(config,pkce?)` → S256 authorize URL + verifier/state to retain; `exchangeCode(config,{code,verifier},extraHeaders?)` → form-encoded token POST, normalized `{accessToken,expiresIn,tokenType}`, throws non-2xx / missing token (`extraHeaders` carries the §12.5 DPoP proof). Redirect/callback UI glue + server-provided `OidcClientConfig` are the integration seam (§11.2). Test `oidc-flow.test.ts` (5). PLUS PKCE primitives + in-memory token store: `client/src/lib/identity/pkce.ts` (`createCodeVerifier` 43-char base64url, `deriveCodeChallenge` S256, `createPkcePair`, `createRandomState` — WebCrypto via `globalThis.crypto`, S256 only) + `token-store.ts` (module-scoped access token, NEVER localStorage; `setAccessToken(tok, expiresInSec)` / `getAccessToken(now?)` null-past-expiry / `getExpiresAt` / `hasLiveToken` / `clearAccessToken`). Test (9): base64url window, S256 determinism, RFC 7636 Appendix-B known-answer vector, entropy uniqueness, store live/expiry/clear/empty.
- [x] 12.2 `device-auth.ts` gains `getApiBearer()` = identity token (`getAccessToken`) ∥ device bearer (`getDeviceBearer`) fallback; `installDeviceAuthFetch` now attaches `getApiBearer()` to same-origin `/api`/`/v1`, still never overriding an explicit Authorization header. Test `device-auth-bearer.test.ts` (8): precedence (identity > device > null), attach-to-/api, explicit-header-preserved, cross-origin-skip, non-/api-skip, inert-no-attach.
- [x] 12.3 `mintWsTicket` authenticates with `getApiBearer()` (carries the identity bearer); `useWebSocket.connect` gate changed `getDeviceBearer()` → `getApiBearer()`, so an unpaired human with a live identity token mints a `browser`-scope ticket and presents only `?ticket=` (durable/bearer token never rides WS, F6). Test `useWebSocket-ticket.test.ts` (2): plane-active mints + `?ticket=` on socket + token absent from URL + `Bearer <identity>` on mint; no-credential opens ticketless (inert).
- [x] 12.4 Heartbeat = native WS ping/pong (§9.5 uses `ws.ping()`, browser auto-pongs — no app code). Identity expiry: `useWebSocket(url, onIdentityExpired?)` handles server close code `4001` (`IDENTITY_EXPIRED_CLOSE_CODE`, §9.4) by `clearAccessToken()` → awaiting the app re-acquire hook → immediate reconnect (no backoff/offline flicker), via `onIdentityExpiredRef` (no stale closure). Test `useWebSocket-ticket.test.ts` (2 more): 4001 clears+re-acquires+reconnects with fresh token; non-4001 (1006) does NOT re-acquire.
- [x] 12.5 Conditional DPoP (RFC 9449) primitives: `client/src/lib/identity/dpop.ts` — `isDpopBound(resp)` (true only on non-empty `cnf.jkt`; unbound ⇒ no proof path), `generateDpopKey()` (NON-extractable ES256 keypair, exports only public `x`/`y`, never `d`), `createDpopProof(key, htm, htu, nonce?)` (typ `dpop+jwt`/ES256 header + fresh `jti`/`iat` per call, conditional `nonce`). `exchangeCode` already threads a token-request proof via `extraHeaders`. Test `dpop.test.ts` (4): bound-vs-unbound detection, non-extractable key + public-only JWK, proof verifies against exported pubkey + fresh-jti-per-call, nonce omitted when absent. Per-call/per-mint proof ATTACHMENT (holding the bound key + adding the `DPoP:` header in the fetch wrapper + `mintWsTicket`) is the integration seam wired with the §11.2 full flow.
- [x] 12.6 Client inert against an inert dashboard: `getApiBearer()` null ⇒ fetch wrapper attaches no Authorization (test `device-auth-bearer.test.ts` “attaches nothing when no credential (inert)”), `useWebSocket.connect` opens the socket ticketless (test `useWebSocket-ticket.test.ts` “no credential opens ticketless (inert)”, asserts no `/api/ws-ticket` call + bare url), and PKCE/oidc-flow run only when the app explicitly invokes them. Today's cookie/loopback UX unchanged.

_(Scenarios from `scenario-design` fold into §8/§10/§11/§12 as they are drafted.)_

## 13. Browser login gate (D16 — folded from plan-proposal; test-plan LG-1..LG-20)

<!-- D16 MODEL SUPERSEDED BY D20 (see the top-of-file note + design.md D20). The §13 lives below
     describe the OPTIONAL bundled dashboard-client adapter — the dashboard's OWN React client as the
     frontend, with core mounting a trusted login-provider COMPONENT (component-only; no startLogin
     function rides the manifest). They are NOT the current deployment and are NOT a requirement of
     it. The §13 checkmarks predate D20/D18/D19 and are historical: do not treat them as fresh results
     for the D20 independent-frontend path (§17). The generic pieces they built and that remain live
     are: the login-config descriptor endpoint (13.1), the component-only slot (13.2), and the
     trust-bound selection (13.10/13.11). 13.3/13.4's resolver-owned dashboard component and 13.5's
     dashboard-UI Keycloak E2E belong to the superseded model. -->

Detachable browser login: core triggers + routes + owns return-to; the trusted resolver plugin owns all OIDC (discovery, PKCE, code exchange). Core imports nothing Keycloak/OIDC. Hardened by a 2-cycle cross-model doubt-review (findings B1–B5, H1–H7, F1–F7 folded into design.md D16 + specs).

### 13.a Implementation

- [x] 13.1 Host `GET /api/identity/login-config` (pre-auth) returning `{ active, issuer?, clientId? }`, relayed from a generic `ctx.registerBrowserLoginConfig({ issuer, clientId, pluginId })` seam the resolver SERVER plugin calls — core reads NO `plugins["keycloak-resolver"].*` key (I1/D7). Add the path to `auth/localhost-guard.ts` `PUBLIC_IN_NAMESPACE_PATHS` AND the auth bypass so an untrusted-network browser reaches it (B1). `active` requires a registered `browserClientId`; `issuer` is the browser-reachable `browserIssuer` (falls back to validation `issuer`).
- [x] 13.2 Add slot id `login-provider` to the taxonomy: `manifest-validator.ts` branch (requires non-empty `component`), `SlotId`/`SlotPropsMap` entry with props `{ phase: "start"|"callback", returnTo, onComplete }`, and the `dashboard-shell-slots` spec delta. Component-only — no function carried through the manifest (H7).
- [x] 13.3 `keycloak-resolver-plugin` gains a `client` manifest entry claiming `login-provider`: a component that in `start` phase fetches login-config → OIDC discovery → `buildAuthorizeRequest` (reuse `oidc-flow.ts`) → persist {verifier,state,returnTo} in `sessionStorage` → `location.assign`; in `callback` phase verifies state → `exchangeCode` → `setAccessToken` → `onComplete(returnTo)`. Plus server-side `browserClientId`/`browserIssuer` config-schema fields + the `registerBrowserLoginConfig` call.
- [x] 13.4 Core gate + pre-token `/callback` route in `main.tsx` (mirror `PairLanding`), enable/trust-filtered before the shell mounts (B5): select ONLY a `login-provider` from a plugin in `identity.trustedResolverPlugins` (never by priority, B3); match the mounted component to the login-config owning `pluginId` (F6); single-flight automatic redirect + always-present manual affordance (F4/H4); `clearAccessToken()` on host refusal of a fresh token (F2); same-origin `returnTo` validation resolving against origin, rejecting `//host`/`/callback`/`/auth/login` (F1/H6); reconcile the legacy `auth_required` `/auth/login` banner to invoke the gate (H5); handle IdP `?error=` and no-provider-on-callback fallbacks (F5). DPoP-bound browser login is the documented non-goal (B4). LIVE-KC-VERIFIED (worktree server + real Keycloak, untrusted browser via XFF proxy): gate→KC→`/callback`→token→authenticated shell. Three integration gaps surfaced + fixed: (a) `configSchema.json` was missing the `browserClientId`/`browserIssuer` fields 13.3 claims (operators could not declare browser login) — added; (b) the no-cookie-auth `/auth/status` fallback always said `{authenticated:true,authEnabled:false}`, so the client never escalated to `auth_required` and the gate could not fire — made identity-aware via `identity/browser-login-auth-status.ts` (unit-tested); (c) the 13.1 `/api/identity/login-config` route lacked a `ROUTE_TIERS` entry + MCP denylist classification (manifest total-partition guard) — both added.
- [ ] 13.5 Opt-in real-Keycloak E2E harness for LG-20: promote `docker/fixtures/keycloak/live-resolver-check.ts` + `realm-identity-plane.json` (anna) into a `docker/compose.test.keycloak.yml` overlay booting `quay.io/keycloak/keycloak:26.0 --import-realm`, driven by a Playwright spec through the gate. Non-gating (opt-in job), exemplar `docker/compose.test.identity.yml` + `tests/e2e/identity/global-setup.ts`.

### 13.b Folded scenarios (automated)

- [ ] 13.6 login-config active shape. `{active:true, issuer, clientId}` returned unauth when resolver active+trusted+`browserClientId` registered. see `packages/server/src/**/__tests__/` route tests · input: active+trusted+registered clientId · trigger: GET /api/identity/login-config · observable: `{active:true, issuer(browser-reachable), clientId}`. (test-plan #LG-1)
- [ ] 13.7 login-config inactive discloses nothing. see sibling route test · input: inert OR no browserClientId · trigger: GET /api/identity/login-config · observable: `{active:false}`, no issuer/clientId field. (test-plan #LG-2)
- [x] 13.8 login-config reachable from untrusted network. see `packages/server/src/auth/__tests__/network-guard-namespace-coverage.test.ts` · input: unauth browser, non-trusted network · trigger: GET /api/identity/login-config · observable: admitted (path in PUBLIC_IN_NAMESPACE_PATHS), not 403 network_not_allowed. (test-plan #LG-3)
- [ ] 13.9 Core stays OIDC-free (I1 grep-guard). see existing invariant test `packages/server/src/__tests__/` · input: core server/client identity modules · trigger: static scan · observable: no read of `plugins["keycloak-resolver"].*`, no OIDC import in core; config sourced only via `registerBrowserLoginConfig`. (test-plan #LG-4)
- [x] 13.10 Trust-bound selection, not priority. see `packages/client/src/lib/identity/__tests__/gate.test.ts` (selectLoginProvider) + `manifest-validator.test.ts` · input: untrusted plugin claims login-provider at higher priority than trusted resolver · trigger: provider selection · observable: only the trusted resolver's provider mounts. (test-plan #LG-5)
- [x] 13.11 Validator accepts/rejects login-provider claim. see `packages/dashboard-plugin-runtime/src/__tests__/manifest-validator.test.ts` · input: claim with non-empty component / with empty component · trigger: validateManifest · observable: accepted / throws naming plugin+slot. (test-plan #LG-6)
- [ ] 13.12 Disabled plugin → no gate pre-shell. see `packages/dashboard-plugin-runtime/src/__tests__/` slot-registry tests · input: sole login-provider plugin disabled · trigger: pre-App mount enable-filter · observable: /callback + start-gate render nothing. (test-plan #LG-7)
- [x] 13.13 Start-phase builds PKCE + persists + redirects. see `packages/keycloak-resolver-plugin/src/client/__tests__/login-flow.test.ts` (flow moved into the plugin with the OIDC mechanics; identity primitives now in client-utils) · input: active, no token, trusted provider · trigger: start phase · observable: PKCE-S256 authorize URL, {verifier,state,returnTo} in sessionStorage, location.assign. (test-plan #LG-8)
- [ ] 13.14 Callback exchanges + core restores return-to. see `tests/e2e/identity/two-user-isolation.spec.ts` (new browser spec) · input: /callback?code&state matching, stashed returnTo=/session/abc · trigger: callback phase · observable: exchangeCode→setAccessToken(memory)→navigate("/session/abc",{replace}). (test-plan #LG-9)
- [x] 13.15 State mismatch aborts. see `keycloak-resolver-plugin/src/client/__tests__/login-flow.test.ts` · input: /callback state ≠ stashed · trigger: callback · observable: no exchangeCode, no token stored. (test-plan #LG-10)
- [ ] 13.16 Refused fresh token cleared, no loop. see `packages/client/src/lib/identity/__tests__/useWebSocket-ticket.test.ts` · input: gate mints token, host refuses · trigger: post-callback connect · observable: clearAccessToken(), error+manual affordance, no auto re-redirect. (test-plan #LG-11)
- [x] 13.17 Open-redirect + recursion rejected. see `packages/client/src/lib/identity/__tests__/gate.test.ts` (safeReturnTo) · input: returnTo ∈ {https://evil, //evil.com, /\evil.com, /callback, /auth/login} · trigger: stash/navigate validation · observable: navigate to "/" each. (test-plan #LG-12)
- [x] 13.18 Missing stash lands safely. see `keycloak-resolver-plugin/src/client/__tests__/login-flow.test.ts` · input: /callback, no persisted verifier/state · trigger: callback · observable: no exchange, manual affordance on "/", no error/auto-redirect. (test-plan #LG-13)
- [x] 13.19 IdP error response handled. see `keycloak-resolver-plugin/src/client/__tests__/login-flow.test.ts` · input: /callback?error=access_denied · trigger: callback · observable: no exchange, message + link to "/". (test-plan #LG-14)
- [ ] 13.20 Callback with no trusted provider mid-flight. see new browser spec `tests/e2e/identity/` · input: /callback after plugin disabled/trust edited · trigger: callback mount · observable: safe fallback (message+link), no blank/crash. (test-plan #LG-15)
- [ ] 13.21 Single-flight automatic redirect. see gate hook unit · input: active, no token, one page load · trigger: gate · observable: ≤1 automatic redirect, manual affordance always present. (test-plan #LG-16)
- [x] 13.22 Owning-resolver pluginId match. see `packages/client/src/lib/identity/__tests__/gate.test.ts` (selectLoginProvider) · input: two trusted resolvers A,B active, login-config pluginId=A · trigger: mount · observable: A's component renders (matched by pluginId), never B's against A's config. (test-plan #LG-17)
- [ ] 13.23 Legacy banner reconciled. see new browser spec + `App.tsx` auth_required unit · input: active+trusted provider, auth_required · trigger: render · observable: gate invoked, legacy /auth/login link not rendered. (test-plan #LG-18)
- [ ] 13.24 DPoP-bound non-goal. see `packages/client/src/lib/identity/__tests__/dpop.test.ts` sibling · input: cnf.jkt-bound token via gate · trigger: callback store · observable: plain bearer stored, no DPoP key persisted across redirect. (test-plan #LG-19)
- [ ] 13.25 Real-Keycloak full round-trip. see `docker/fixtures/keycloak/live-resolver-check.ts` + `docker/compose.test.identity.yml` · input: real Keycloak (anna) + browser, deep link /session/x · trigger: gate→KC login→/callback · observable: lands authenticated on /session/x. (test-plan #LG-20)

## 14. Remote insecure-context resilience (D17; R1–R4)

- [x] 14.1 PKCE secure-context fallback (R1): vendor pure-JS SHA-256 in `packages/client-utils/src/identity/pkce.ts`, used only when `crypto.subtle` is absent; `deriveCodeChallenge` async path unchanged. Tests: FIPS 180-4 vectors (`abc`, empty, 448-bit chunk), parity with `crypto.subtle.digest` for random verifiers, fallback selected when `subtle` deleted. `crypto.getRandomValues` never polyfilled.
- [x] 14.2 Honest offline/auth classification (R2): `useWebSocket` — `/auth/status` probe rejection keeps `connecting` + re-probes on the existing backoff; `authenticated:false` → `auth_required` always wins; `offline` only after N consecutive probe rejections. Tests: probe-reject ≠ offline (first N-1), auth-false wins over prior offline, N rejections → offline.
- [x] 14.3 Typed gate failure reasons (R3): `login-flow.ts` returns reason codes (`insecure-context`|`discovery-failed`|`exchange-failed`|`state-mismatch`); `KeycloakLogin` renders reason + retry + return-home. Tests per reason arm.
- [ ] 14.4 Docs (R4, DocScribe): Tailscale/plain-HTTP deployment note — supported after R1; `tailscale serve` HTTPS as recommended hardening; KC redirect-URI/web-origins checklist.

## 15. Three-way split — no login UI in core or the resolver plugin (D18)

- [x] 15.1 Core: drop the legacy `/auth/login` link from `AuthRequired` (inactive → static "no sign-in method installed" text); tests first
- [x] 15.2 Shared: `login-provider` slot phase union gains `"logout"`; core `/logout` route mounts `LoginGate phase="logout"`
- [x] 15.3 Move reusable OIDC mechanics (`beginLogin`/`completeLogin`/`beginLogout`, typed reasons, `id_token` retention for `id_token_hint`) into `client-utils/identity/login-flow.ts`; relocate its unit tests
- [x] 15.4 keycloak-resolver-plugin → resolver-only: delete client bundle (`KeycloakLogin`, `login-flow`, `index`, `i18n`), remove `login-provider` claim + client manifest fields, stop publishing the browser-login descriptor, drop dead `browserClientId`/`browserIssuer` config
- [x] 15.5 Regenerate `plugin-registry.tsx` (no keycloak login-provider); seam-only — no in-tree login UI ships
- [ ] 15.6 Docs: identity-plane.md — document the three-way split + how an authz plugin claims `login-provider` and drives the client-utils mechanics (DocScribe)

## 16. Separate-view (URL) login provider (D19)

- [x] 16.1 Shared: `BrowserLoginConfig` gains optional `loginUrl`/`logoutUrl`; `issuer`/`clientId` become optional (component kind)
- [x] 16.2 Client: pure `providerRedirect` + `resolveGateRedirect` units (same-origin, loop-refusal, no logout→login fallback); tests first
- [x] 16.3 Client: `LoginGate` redirects on a URL provider; `AuthRequired` renders a plain link to `loginUrl`; tests first
- [x] 16.4 Server: `sanitizeBrowserLoginConfig` at the trust boundary + relay the new fields on `GET /api/identity/login-config`; tests first
- [x] 16.5 Smoke: drop-in server-only plugin (`spike/identity-smoke-plugin/`) serving `/identity-smoke/login` + `/logout` views and publishing the descriptor
- [ ] 16.6 Token handoff for a real separate-view provider (fragment/postMessage bootstrap) — explicitly out of scope for the smoke

## 17. Independent-frontend deployment (D20) — disposable smoke, PARTIALLY VERIFIED (one live LAN run)

<!-- D20 (design.md): the browser frontend is the USER's OWN application; the dashboard is a backend
     resource server only; a custom independent SERVER plugin owns login/UI/callback/logout and
     publishes {pluginId, loginUrl, logoutUrl} via registerBrowserLoginConfig. The dashboard React UI
     is NEVER navigated by the smoke. Non-goals: no BFF, no reverse proxy, no global cookies, no new
     production IdP, no cross-origin trust. The spike's informal "D20 handoff" label is NOT
     established spec and is not a dependency.
     STATUS (updated with the ONE live result): `spike/identity-login-plane/lan-e2e.mjs` ran GREEN
     against real Keycloak on the LAN (`192.168.0.157 8010`; spike node route tests 20/20 green). It
     covers the HTTP login chain, a bearer resource-server call, the WS ticket/upgrade round-trip, the
     ticketless refusal and the logout target — so 17.1/17.3/17.4/17.7 stay UNCHECKED as PARTIAL (`[~]`)
     and 17.2/17.5/17.6 are UNRUN. STILL NOT RUN: the browser-RENDERED clickthrough, two-user isolation
     (SM-5), and expiry (SM-6). An EMPTY owned list is NOT an ownership result — never claim SM-3/SM-5
     owner-scoping from a 0-session payload.
     VERIFIED SECURITY GAP (blocks any isolation claim): `session-meta-handler.handleSessionsPage` and
     `handleListSessions` return sessions with NO owner filter (`sessionManager.get(id)` / `listAll()`),
     and the §8.3 choke point gates `session` scope only, never `session-list` (§8.2). Needs a per-item
     filter + regression test before isolation is asserted. (An Audit no-bearer-200 finding was DISPROVED
     by the live LAN 403 — omit it.)
     The §13/§15/§16 checkmarks are historical records of the SUPERSEDED component-adapter model and
     MUST NOT be cited as evidence here. Do not check an item until it has been run and observed. -->

Smoke target: `spike/identity-login-plane/` (disposable; the same-origin, plugin-served frontend page.
Note §16.5 recorded it as `spike/identity-smoke-plugin/` — the artifact now lives at
`spike/identity-login-plane/`.) It signs in against REAL Keycloak, keeps the token in the smoke page's
OWN memory, and talks only HTTP/WS to the dashboard — it never mounts or navigates the dashboard React
UI. All checks below target the D20 model: password/PKCE login owned by the independent plugin, the
dashboard reached as a resource server via bearer + WS ticket.

- [~] 17.1 SM-1 — sign-in with NO dashboard UI. Input: real Keycloak (anna), a browser on the plugin-served same-origin page. Trigger: open the smoke page → sign in. Observable: authorize → code → token completes entirely in the plugin's own pages; the token stays in the smoke page's OWN memory; the dashboard React UI is NEVER mounted or navigated; no token is handed to the dashboard client (no `#access_token=` adoption path is required). (test-plan SM-1) — **PARTIAL, live LAN run:** `lan-e2e.mjs 192.168.0.157 8010` observed the whole SERVER-side chain — `/identity-login/start` → KC authorize (PKCE S256 + `pi_login_bind` binding cookie) → credential POST → `/identity-login/callback` → `/identity-login/app#access_token=…`; the handoff stayed on the dashboard origin and targeted the PLUGIN app (never `/`), no `localhost` in the hop chain; the spike node test asserts the app page holds the token in memory only. **NOT RUN:** the browser-RENDERED clickthrough (a real browser rendering that page, stripping the fragment, keeping it in memory, never mounting the dashboard React UI).
- [ ] 17.2 SM-2 — descriptor seam + current-allowlist trust. Input: the independent server plugin calls `registerBrowserLoginConfig({ pluginId, loginUrl, logoutUrl })`. Trigger: unauth `GET /api/identity/login-config`. Observable: `{ active:true, pluginId, loginUrl, logoutUrl }` relayed + sanitized to same-origin; when the plugin is disabled or not in the CURRENT `identity.trustedResolverPlugins` allowlist ⇒ `{ active:false }` and no details disclosed; core reads no plugin config key. (test-plan SM-2) — **NOT RUN:** the live run never called `GET /api/identity/login-config`; the relayed+sanitized `{active:true,pluginId,loginUrl,logoutUrl}` shape and the current-allowlist trust filter (disabled/untrusted ⇒ `{active:false}`, no details) are unexercised. The spike does invoke `ctx.registerBrowserLoginConfig({pluginId,loginUrl,logoutUrl})` (`server.mjs`), but that proves only that the seam is CALLED, not the response.
- [~] 17.3 SM-3 — bearer API, and the no-bearer control. Input: the smoke page's in-memory token. Trigger: (a) `GET /api/sessions` with `Authorization: Bearer`; (b) the same call with NO bearer. Observable: (a) accepted, owner-scoped payload for that principal; (b) refused (401/403), discloses nothing. (test-plan SM-3) — **PARTIAL, live LAN run:** bearer `GET /api/sessions` ⇒ **200** (0 sessions — empty is valid and proves ACCEPTANCE only); the same call with NO bearer ⇒ **403**. That 403 is the non-loopback network guard's verdict, NOT the identity gate's, and a 0-session payload proves NO owner-scoping. Ownership remains unproven (see SM-5 + the verified gap). A prior Audit "no-bearer 200" claim was DISPROVED by this live 403 — do not carry it.
- [~] 17.4 SM-4 — WS ticket + message. Input: the same bearer. Trigger: mint a browser-scope ws-ticket, then upgrade with `?ticket=` only and exchange a message. Observable: mint 200 + single-use ticket; owner-scoped upgrade accepted; bootstrap/message received; a ticketless upgrade is refused and a wrong-`aud` bearer at mint is refused. (test-plan SM-4) — **PARTIAL, live LAN run:** `POST /api/ws-ticket {scope:"browser"}` ⇒ 200 + ticket; authenticated `?ticket=` upgrade ⇒ `sessions_snapshot` (0 sessions) + a `sessions_page_result` round-trip; ticketless upgrade ⇒ refused (403). **NOT RUN:** wrong-`aud` bearer at mint (expect 401), ticket single-use/replay, and owner-scoped content (0 sessions).
- [ ] 17.5 SM-5 — two-user isolation through the D20 path. Input: real KC anna + béla with distinct `(iss,sub)`. Trigger: each drives list/detail/WS bootstrap with its own bearer. Observable: each sees ONLY its own sessions (list + bootstrap); non-owner detail ⇒ 404 (no owned-vs-not-found oracle); a principal-less caller sees nothing. (test-plan SM-5) — **NOT RUN:** no second user was driven through the D20 path. Also BLOCKED by the verified gap (§8.2): `handleSessionsPage`/`handleListSessions` apply no owner filter, so list/page isolation cannot be asserted until a per-item filter + regression land. Do NOT claim isolation from the SM-3 empty list.
- [ ] 17.6 SM-6 — expiry. Input: a token past `exp`, and a socket that reaches `principalExpiresAt`. Trigger: API call with the stale token, then wait past the socket's expiry. Observable: API refused (401); socket closed (code 4001), subscriptions released, no retained ownership. (test-plan SM-6) — **NOT RUN:** neither the stale-token API refusal (401) nor the socket expiry (close 4001, subscriptions released, ownership not retained) was exercised.
- [~] 17.7 SM-7 — non-goal guard (no handoff dependency). Input: the D20 path run with no core URL-fragment token adoption and no BFF/reverse proxy/global cookie in front of the dashboard. Trigger: full sign-in → API → WS flow. Observable: the flow succeeds with the token never leaving the smoke page's memory; a grep-guard shows no core adoption of a `#access_token=` fragment (the spike's informal label is not wired into core). (test-plan SM-7) — **PARTIAL, live LAN run:** the full login → bearer → WS flow succeeded with the bearer handed only to `/identity-login/app` (plugin page); the dashboard root was never targeted, so this flow needs no core adoption. **BUT the grep-guard FAILS as written:** core DOES ship a `#access_token=` adoption path — `packages/client/src/main.tsx` calls `consumeTokenHandoff()` (`packages/client/src/lib/identity/handoff.ts`) at boot. Not triggered by this flow (the smoke never returns to the dashboard root), but "no core adoption" is untrue.

## 18. Self-lockout guard (D21) — enforcement arms only when fully configured; decided once, latched

- [x] 18.1 `identity/activation.ts`: `isIdentityEnforced` / `identityDisarmedWarning` over `{resolverActive, loginProviderRegistered, authProviderCount, trustedPolicyPlugin, registeredPolicyCount}` (all required); `assertIdentityReadiness` + `IdentityStartupError` removed — no identity misconfiguration aborts boot. Input: the decision-table matrix. Trigger: call. Observable: armed only when fully configured; warning names every reason. Harness: `packages/server/src/identity/__tests__/activation.test.ts` (test-plan LK-1, LK-2)
- [x] 18.2 `identity/identity-registration-tracker.ts`: per-plugin ledger + `releaseFailedIdentityRegistrations`; `server.ts` tracks resolver, descriptor AND policy registrations. Input: a plugin that registered then failed. Trigger: post-load release. Observable: its registrations disposed + logged, loaded plugins untouched, idempotent. Harness: `packages/server/src/identity/__tests__/identity-registration-tracker.test.ts` (test-plan LK-3)
- [x] 18.3 `server.ts` boot latch: one pre-listen block (outside the loader `try`) releases failed registrations, computes `identityArmed`, freezes registrations (`IdentityRegistrationTracker.freeze()`: plugin handles become logged no-ops, late registrations refused via `refuseLateIdentityRegistration`), logs the warning; D8 keys on `legacyConnectorsActive` (legacy `/auth/status` actually mounted), not config keys; every road (`registerResolverHook` `isEnforced` (now required), §9.2 `requireIdentityTicket`, gateway + handler-context `isResolverActive`, `/auth/status`, `login-config` via `publicLoginConfig`) reads `identityEnforced()`. Harness: `packages/server/src/identity/__tests__/resolver-hook.test.ts` (disarmed case), `browser-login-config-registry.test.ts` (`publicLoginConfig`) (test-plan LK-1, LK-9)
- [x] 18.4 `/auth/status` registered whenever the legacy cookie plugin did not register one; enforced ⇒ only a resolved bearer principal is authenticated (loopback / device bearer are not). Harness: `packages/server/src/identity/__tests__/browser-login-auth-status.test.ts` (test-plan LK-6)
- [x] 18.5 Live smoke (disposable instances, 2026-09-23): `:8011` keycloak-resolver only ⇒ inert, warning logged, localhost UI connects (no "Server offline"); `:8012` trusted drop-in registers descriptor then throws ⇒ released, inert, `login-config` `{active:false}`; `:8012` + named-absent `trustedPolicyPlugin` ⇒ boots, warns (no abort); `:8010` fully configured ⇒ no-token `/auth/status` `{authenticated:false}` + `/ws` 403, anna bearer ⇒ `{authenticated:true}` + `/ws` open, UI shows sign-in (not "Server offline"); `:8013` = `:8010` + an unresolvable `auth.providers` entry ⇒ legacy auth not mounted, identity stays ENFORCED (no-token `/ws` refused, anna open).
- [ ] 18.6 Server-level boot test — failed login plugin. Input: `createTestServer` with a trusted drop-in that registers a descriptor then throws, `keycloak-resolver` active. Trigger: boot + requests. Observable: listens; release + NOT-enforced logged; `login-config` `{active:false}`; ticketless genuinely-local `/ws` admitted. Exemplar: `packages/server/src/__tests__/ws-origin-gate.test.ts` (`createTestServer`). (test-plan LK-4)
- [ ] 18.7 Server-level boot test — D8/D9 disarm, never abort. Input: `createTestServer` with (a) named-but-absent `trustedPolicyPlugin`, (b) duplicate policy, (c) resolver active + `auth.providers` connectors that resolve and mount; (d) an `auth.providers` entry that resolves to NO provider. Trigger: boot. Observable: (a)–(c) boot succeeds, reason logged, plane inert; (d) plane stays ENFORCED. Exemplar: `packages/server/src/__tests__/ws-origin-gate.test.ts`. (test-plan LK-5, LK-13)
- [ ] 18.8 Server-level test — `/auth/status` route placement. Input: (a) `auth` block with no resolvable provider + enforced, (b) `auth` block with resolved providers. Trigger: `GET /auth/status`. Observable: (a) identity-aware answer, (b) legacy cookie route + identity inert. Exemplar: `packages/server/src/__tests__/auth.test.ts` ("auth-off tunnel surface" block). (test-plan LK-7)
- [ ] 18.9 Server-level test — latch. Input: armed server; the login plugin's descriptor / resolver / policy unregister handle is called after `listen()`, and a trusted plugin attempts a late registration. Trigger: next request + browser upgrade. Observable: handles are logged no-ops, late registration refused + logged, identity ticket still required, owner gating on, `login-config` still advertises the latched descriptor. Exemplar: `packages/server/src/__tests__/ws-origin-gate.test.ts`. (test-plan LK-8)
- [ ] 18.10 Identity E2E harness arms enforcement. Input: `PI_E2E_IDENTITY=1`. Trigger: `docker/test-entrypoint.sh` seed + `tests/e2e/identity/global-setup.ts`. Observable: a trusted fixture login descriptor is registered alongside `keycloak-resolver` (e.g. extend `packages/fixture-policy-plugin` or add a fixture login-provider, added to `identity.trustedResolverPlugins`); global-setup asserts `GET /api/identity/login-config` `active:true`; the §11.2 spec passes against an ENFORCED plane. Also refresh the stale "ticketless /ws refused by §9.2" skip comment in `test-entrypoint.sh`. Exemplar: `tests/e2e/identity/two-user-isolation.spec.ts`. (test-plan LK-10)
- [x] 18.11 E2E — resolver-only harness stays usable. DONE via the setup matrix (18.16, scenario B: rendered root has a live socket, no "Server offline", ticketless genuinely-local browser socket admitted). Input: harness with `keycloak-resolver` seeded and NO login descriptor. Trigger: open the dashboard. Observable: connects, no "Server offline"; ticketless browser `/ws` admitted. Exemplar: `tests/e2e/identity/two-user-isolation.spec.ts` + `playwright.identity.config.ts`. (test-plan LK-11)
- [ ] 18.12 Operator read-through of the disarm warning + armed-without-token UI state. (test-plan: manual-only) (test-plan LK-12)
- [ ] 18.13 VERIFIED SECURITY GAP (pre-existing, not introduced by D21; blocks any multi-user isolation claim): §9.2 covers the `browser` WS scope only. `/ws/terminal/<id>` (`terminal/terminal-gateway.ts` `handleUpgrade` → `manager.attach`) has no principal check and keeps the legacy loopback / local-token / trusted-network / principal-less-ticket allowances; browser bootstrap sends `terminal_added` for ALL terminals (`pairing/browser-gateway.ts`); `live` likewise ungated. Needs an owner model for terminals + enforced-path gating + regression tests before isolation is asserted.
- [ ] 18.14 Wiring requirement for the (currently unwired) D9 road: when `identity/host-access.ts` `gateHttpNonSession` / `deliverDomainEvent` gain production call sites, they MUST short-circuit to allow/legacy when `identityEnforced()` is false, before consulting `PolicyRegistry.hasPolicy()`; add a regression test (loaded policy + inert plane ⇒ non-session road not 403'd). Exemplar: `packages/server/src/identity/__tests__/host-access.test.ts`.
- [ ] 18.15 DECIDED → D22 (handoff code + in-memory bearer, NO cookies). Original follow-up: (deferred by the user, 2026-09-23): a dashboard-client login seam any user's plugin can plug into, so the dashboard's own UI can offer sign-in/out without core owning a login flow. Constraints already decided: core ships no login UI of its own; enforcement stays disarmed unless a trusted plugin registers a login descriptor (D21); the plugin login seam (`registerBrowserLoginConfig`) is kept as-is. The uncommitted client experiment (banner removal, core auto-redirect on `auth_required`, `#access_token` handoff adoption in `main.tsx` / `lib/identity/handoff.ts`) is NOT adopted and must be re-decided under this item.
- [x] 18.16 L3 setup-matrix E2E `tests/e2e/identity-matrix/` (`npm run test:e2e:identity-matrix`, opt-in, no docker). One dashboard process per setup A–J (`scenarios.ts`: armed / resolver-only / failed login plugin / unresolvable provider / baseline / login-without-resolver / absent policy D9 / mounted legacy provider D8 / IdP down / untrusted login plugin) against an in-process fake OIDC issuer, now with an interactive auth-code + PKCE flow, SSO cookie and end-session (`packages/shared/src/test-support/fake-oidc-issuer.ts`, `__tests__/fake-oidc-issuer-authcode.test.ts`). `setup-matrix.spec.ts`: boot log reason, `login-config` / `/auth/status`, localhost vs non-loopback REST + browser WS with/without bearer. `browser-login.spec.ts`: rendered dashboard root, plugin-frontend sign-in → bearer API + ticketed WS → sign-out ends the IdP session (localhost + LAN for armed rows), IdP-down error page. Evidence: 59 passed, 3 fixme (18.17), 2.0 min; a mutation (B expected armed) turns all 5 B tests red. Login-plane fixture = `spike/identity-login-plane/` (must be committed with the suite). (test-plan LK-4, LK-5, LK-9, LK-11, LK-13)
- [x] 18.17 KNOWN GAP (E2E `test.fixme`, setup-matrix A/D/I): while identity is ENFORCED the REST network guard still admits genuine loopback with no bearer (`GET /api/sessions` 200 on localhost) although the browser socket refuses it. Behind a same-host sidecar / L4 LB that is every remote user. Decide: close it (no loopback bypass on REST when enforced; recovery via local-token CLI / config — makes IdP-down (I) a full browser+REST lockout) or accept + document. Flip the fixme to `test()` when closed. CLOSED by 18.26 (D24 floor). NOTE: until D23 break-glass (18.23) ships, an IdP outage on an enforced dashboard is recoverable only by the local-token CLI or a config edit.

- [x] 18.18 D22 descriptor v2: `tokenUrl` + `postLogoutUrl` in `BrowserLoginConfig` + `sanitizeBrowserLoginConfig` (same-origin, tests first). DONE: `BrowserLoginConfig` + `sanitizeBrowserLoginConfig` (`dashboardUiFields`) + `publicLoginConfig` relay `tokenUrl`/`postLogoutUrl`/`label` (≤40)/`endsProviderSession`; `/auth/status` returns `principal {sub,name?,email?}`; `principal-guard` carries a bounded display `name` (malformed ⇒ dropped); keycloak-resolver maps `name`→`preferred_username`.
- [x] 18.19 D22 `identity.loginProvider` pin; several unpinned descriptors → disarm + warning (activation.ts disarmReasons, tests first). SUPERSEDED by D25 (all providers listed on the login page).
- [x] 18.20 D22 client seam: verifier/challenge, `#pi_handoff` strip + exchange, in-memory bearer on fetch + ws-ticket, re-login on reload/expiry, logout redirect; replaces the uncommitted `#access_token` experiment. UI per `mockups/ui-plan.md` + `mockups/login-logout.html` (v2): empty shell + ONE sign-in dialog whose button navigates straight to the IdP (replaces the amber banner; `loginUrl` must 302 to the IdP), spinner in the same dialog on return, user line with Sign out pinned to the bottom of the session stream (nothing in the top header), same dialog for expiry / signed-out / error. Descriptor adds `label` + `endsProviderSession`; `/auth/status` returns `principal {sub, name?, email?}`. DONE: `lib/identity/{dashboard-login,login-session,signed-in-user}.ts`, `components/identity/{AuthRequired,UserBar}.tsx`, `main.tsx` boot handoff before `<App/>`, dialog rendered ONCE in App `apiProvider` (the banner slot rendered twice on session routes), first-launch modal deferred while `auth_required` (`shouldShowFirstLaunch`). Uncommitted `#access_token` experiment dropped (saved to /tmp).
- [x] 18.21 D22 spike login plane → handoff mode (server-side state, no cookies); setup-matrix browser flow via the dashboard SPA. DONE: spike handoff mode — `/start?returnTo&challenge` 302s straight to the IdP (no cookie), callback → `returnTo#pi_handoff` on the STARTED origin, `POST /token` single-use ≤60 s S256-bound, `/signout` → end_session → `/?pi_signed_out=1`, IdP errors → `#pi_login_error`; plugin-frontend mode (D20) unchanged. Matrix: new D22 dashboard-UI flow + IdP-down dialog rows. Evidence: 62 passed / 3 fixme (18.17); live real-Keycloak run (fixture realm) green; `docker/fixtures/keycloak/realm-identity-plane.json` fixed (Keycloak only honours a trailing `*`; `post.logout.redirect.uris: "+"`).
- [ ] 18.22 `github-login` plugin (resolver + descriptor, `publicBaseUrl`) + matrix scenario K with a fake GitHub.
- [ ] 18.23 D23 break-glass: server endpoint (local-token authenticated) issuing a one-time `pi_local` code (single-use, ≤60 s); client exchanges it like `#pi_handoff`; principal `local-operator` sees everything; warn log on issue + use (tests first).
- [ ] 18.24 D23 CLI `pi-dashboard login --local` prints the link; IdP-error dialog + boot log name the command.
- [ ] 18.25 D23 close 18.17: while enforced, REST loopback without a principal → 401; flip the matrix `test.fixme` rows to `test()`; add a matrix row "IdP down + break-glass gets in".
- [x] 18.26 D24 signed-out floor: server `onRequest` after the resolver hook — enforced + no principal + not local-token + browser namespace (`/api/`,`/editor/`,`/live/`) minus pre-auth set ⇒ 401 `sign_in_required`; client renders no route/overlay while `auth_required`. Closes 18.17 (flip the matrix fixmes). DONE: `identity/identity-floor.ts` (`identityFloorAllows`, parser shared with the network guard) + `onRequest` hook after the resolver hook in server.ts; client pins every route to `/` while `auth_required` (`pinHomeWhileSignedOut`, first target remembered as the sign-in returnTo). Matrix 75/75 (the 18.17 fixmes are real tests now); live Keycloak: no-token `/api/config|providers|sessions` 401, `/api/health` 200, `/settings` signed-out → `/` + dialog → after sign-in back on `/settings/providers`.
- [ ] 18.27 D24 plugin consumer seam `ctx.identity` { isEnforced, principalOf, authorize (namespaced `plugin:<id>:`), userDataDir } + WS principal on plugin WS routes (tests first).
- [ ] 18.28 D24 classify every core non-session road `{action, resource}` through `gateHttpNonSession` (supersedes 18.14); `GET /api/identity/me` → `{principal, can}`.
- [x] 18.29 D25 core login page `/login` (mockup v3): `components/identity/LoginPage.tsx` replaces the D22 dialog; boot phase `checking` + redirect; `loginRedirectFor` on mid-page loss.
- [x] 18.30 D25 several login providers: registry keeps all (`list()`), `login-config.providers`, one button each; handoff/sign-out use the starting provider. Supersedes 18.19.
- [x] 18.31 D25 silent sign-in: descriptor `silentSignIn`, spike `prompt=none` + `login_required` mapping, fake OIDC issuer `prompt=none`; no loop after sign-out / error / miss.
- [x] 18.32 Login plugin only offered when Keycloak is configured (no built-in default); matrix row K.
- [ ] 18.33 E2E: a second login plugin in the matrix (Keycloak + GitHub buttons) once 18.22 (github-login) exists.
- [x] 18.34 Security tests (STRIDE over bearer / handoff code / returnTo+fragments / token storage / pre-auth endpoints): `tests/e2e/identity-matrix/security.spec.ts` (27, live servers A/I/E), `client/src/lib/identity/__tests__/security.test.ts` (42), `LoginPage` security (3), `identity-floor` bypass cases (+23), spike `test/security.test.mjs` (11). Fixes found by them: client `completeHandoff` refuses an off-origin `tokenUrl`; spike `safeReturnPath` rejects control characters. Open finding: the spike derives its callback / post-logout origin from the `Host` header (safe only via the IdP redirect-URI allowlist) — the github-login plugin (18.22) must use a configured public base URL.
- [x] 18.35 Multi-user run on an EMPTY enforced setup (anna+bela, real Keycloak + `tests/e2e/identity-matrix/multi-user.spec.ts`). Fixes found (tests failed first): (1) live `session_added`/`session_updated`/`sessions_reordered` (+ any `sessionId` frame) fanned out to every socket — now owner-scoped in `browser-gateway.ts` `broadcast()`, with a promoted `session_added` (keeping `spawnRequestId`) when the owner is stamped after the first add; (2) owner lost on restart — `sessionToMeta` omitted `principalOwner` (full-overwrite save), the reattach re-register dropped it, the spawn arm skipped the write when the in-memory session lacked `sessionFile`, and a re-register of a session the boot scan could not restore now reads it back from `.meta.json` (never overrides an owner); (3) D23 local-token part of 18.23: a verified local-token request with no principal is the frozen `LOCAL_OPERATOR` on session roads (sees every session; warn log, rate-limited). Remaining for 18.23: the browser `?pi_local=` one-time-code exchange + 18.24 CLI. (4) folder-level `openspec_update`/`git_head_update` (live + on-connect replays) reach only principals owning a session in that folder (cwd, subfolder or worktree), or everyone when the folder is pinned (pins are shared settings, D24).

