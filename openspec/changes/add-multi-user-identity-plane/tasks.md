## 0. Bundled resolver plugin package

- [x] 0.1 Scaffold `packages/keycloak-resolver-plugin` (manifest id `keycloak-resolver`, default-enabled, depends on `pi-dashboard-shared`); verify it type-checks and loads as a plugin.
- [x] 0.2 Add `keycloak-resolver` to `BUNDLED_PLUGINS` (`packages/electron/scripts/bundle-server.mjs`) and update `packages/shared/src/__tests__/bundled-plugins-complete.test.ts`; verify the invariant test passes with the new plugin present. _(derives from bundle-server.mjs — no separate list to edit; invariant test passes.)_
- [x] 0.3 Confirm dashboard core imports NOTHING Keycloak-specific for principal resolution (no jose/JWKS/DPoP in `packages/server/src`); the resolver lives only in the plugin. _(NOTE: pre-existing topology-A cookie connector `packages/server/src/auth/auth.ts` still references Keycloak/OIDC — that is the legacy `auth.providers` login flow, deliberately separate from the resolver; D8 rejects it only when the resolver is active.)_

## 1. Types and shared contract

- [x] 1.1 Add immutable `Principal` (`{ iss; sub; email? }`), `PrincipalResolution` (`{ principal; expiresAt }`), `ResolverReject`, and DPoP-ready `AuthContext` (`{ method; url; authorization?; cookie?; dpop?; isAuthenticated; ip }`) to `packages/shared/src/identity.ts` (shared with `dashboard-plugin-runtime`); both packages type-check.
- [x] 1.2 Add `PrincipalResolverFn`, `HostAction`, `HostResource`, and `HostAccessPolicyFn` plus `isPrincipalResolution`/`isResolverReject`/`principalEquals` helpers; the three resolver outcomes and the policy boolean are type-distinguishable (unit test).
- [x] 1.3 Add `identity` config shape (`trustedResolverPlugins: string[]`; optional `trustedPolicyPlugin?: string`; `resolverTimeoutMs`/`policyTimeoutMs` clamped) to shared config — NO `mode` field; default parse yields an empty trust list and no policy (unit test).

## 2. Activation and startup readiness (no mode flag)

- [x] 2.1 Derive the active/inert state from the resolver: active ⇔ a trusted resolver is registered AND configured (issuer+audience); the plugin owns the issuer+audience decision (only registers when active), so `resolverRegistry.hasResolver()` is the host's active signal. Covered by registry tests (register-when-active) + activation tests.
- [x] 2.2 Add a pre-`listen()` readiness check (`assertIdentityReadiness`, wired in `server.ts` before `fastify.listen()`): a named `trustedPolicyPlugin` must resolve to exactly one registered policy (zero/duplicate fails startup); a missing/empty `identity` block is valid (inert, no policy). Tests: named-but-absent, duplicate, inert happy path. _(NOTE: policy registration lands in §9; until then registeredPolicyCount=0, so a named policy correctly fails as named-but-absent.)_
- [x] 2.3 When the resolver is active, reject a non-empty `auth.providers` (confidential login connectors) as a config error (D8). Tests: active+connectors fails startup; inert dashboard leaves connectors intact.

## 3. Resolver registry with host trust grant

- [x] 3.1 Implement a resolver registry (`packages/server/src/identity/resolver-registry.ts`) keyed by `pluginId`, ordered `(manifest.priority ASC, pluginId ASC)`; duplicate registration from one plugin fails. Unit test of deterministic cross-boot ordering (priority + pluginId tie-break).
- [x] 3.2 Gate `registerPrincipalResolver` on the host trust grant (bundled `keycloak-resolver` or a plugin named in `identity.trustedResolverPlugins`); a self-declared `manifest.priority` grants nothing. Runtime `ServerPluginContext.registerPrincipalResolver` wired in `server.ts`: untrusted plugin gets a no-op registrar (logs + inert unregister). Test: untrusted (incl. low-priority) plugin cannot register.
- [x] 3.3 Validate/copy/freeze resolver output before exposure (`principal-guard.ts`: reject empty `iss`/`sub`, non-future/absent `expiresAt`, over-length; drop malformed display-only `email`; strip extra props; freeze). Test: malformed principal discarded; no prop leakage.
- [x] 3.4 Support override-by-disable: a disabled bundled default never registers, so only the trusted replacement in `trustedResolverPlugins` resolves. Test: disabled default never claims a token (only replacement present).

## 4. Resolution integrated into the auth gate (HTTP plane)

- [x] 4.1 Decorate `request.principal` (null) and `request.principalExpiresAt` (null) once, inside `registerResolverHook` (called at server.ts right after `registerBearerAuth`); `hasRequestDecorator` guard prevents a duplicate-decorator throw. Boot-safe.
- [x] 4.2 Build the curated `AuthContext` in a pure helper (`auth-context.ts`: method, canonical url via `canonicalOrigin` D6a, authorization/cookie/dpop, isAuthenticated, ip). Test: exposes nothing beyond the allowlist; query/fragment stripped; absent headers omitted.
- [x] 4.3 Register the resolver-dispatch `onRequest` hook (`resolver-hook.ts`) AFTER `registerBearerAuth` and BEFORE `registerAuthPlugin`; inert ⇒ returns immediately; claim sets principal + expiry + `isAuthenticated` + `authVia=principal`; reject → 401; `null` continues. Fastify integration test: bearer+no-cookie authenticates (not 401), reject → 401, inert unchanged.
- [x] 4.4 Bound each resolver by the configured timeout (`dispatch.ts` `withTimeout`); throw/reject/timeout per D5 (owned-invalid ⇒ resolver's own reject; unexpected fault ⇒ core coerces to `null`, logged, never 500). Tests: throwing resolver neutralized, slow resolver bounded, malformed claim discarded.
- [x] 4.5 Device-bearer path yields `isAuthenticated` true with `principal === null` (resolver returns null for a device token). Fastify integration test with a simulated device-auth pre-hook.

## 5. Bundled Keycloak resolver (config-seeded, in the plugin)

- [x] 5.1 Config schema + parser (`shared/config.ts`): `issuer`/`audience` required, `authorizedParty`/`jwksUri`/`clockSkewSeconds`/`networkTimeoutMs`/`allowInsecureHttp` optional, read via `getPluginConfig()`; timeouts/skew clamped; provider URLs must be https (or opted-in http). Tests: no issuer/audience ⇒ inert null resolver (no default substituted); http without opt-in ⇒ inert; malformed URL ⇒ inert; clamp bounds.
- [x] 5.2 OIDC discovery + JWKS cache (`jwks.ts`) via jose `createRemoteJWKSet` (cache + coalesced refresh + single-refresh-per-unknown-kid), bounded by `networkTimeoutMs`, `redirect:"error"`, discovery `issuer` echo checked. Tests: concurrent first-fetch coalesced, cache reuse, unknown-kid one coalesced refresh, explicit jwksUri skips discovery, discovery outage ⇒ reject owned tokens (not accept unverified).
- [x] 5.3 Ownership disambiguation (`resolver.ts`): opaque/non-JWT ⇒ null; foreign unverified `iss` ⇒ null; configured-issuer ⇒ owned. Tests for all three.
- [x] 5.4 RFC 9068 validation (RS256 only via header pre-check + jose `algorithms`, exact `iss`, required `aud`, optional `azp`, `exp`, `sub`) ⇒ `{ principal, expiresAt from exp*1000 }`; failure ⇒ reject; `email` only when `email_verified === true`. Tests incl. `alg:none`/UnsecuredJWT rejection, aud/azp/exp/sub rejections, unverified-email omission.
- [x] 5.5 Conditional DPoP (`dpop.ts`): validated only when `cnf.jkt` present — proof JWS under embedded public asymmetric `jwk` (alg pinned, symmetric/private rejected), thumbprint == `cnf.jkt`, `htm`, canonical `htu`, `ath` == b64url SHA-256(token), fresh `iat`, unreused bounded `jti` (single-instance LRU). Missing/mismatch ⇒ reject; unbound token ⇒ plain bearer; malformed `cnf.jkt` ⇒ reject. Tests: bound-without-proof reject, wrong-`ath` reject, unbound plain-bearer accept, valid+replay reject, malformed-cnf reject.
- [x] 5.6 Owned-token crypto/JWKS/DPoP faults caught inside the resolver ⇒ `reject` (never uncaught throw). Test: induced JWKS fault on an owned token ⇒ reject, not fall-through.

## 6. Session ownership (persist + assign)

- [x] 6.1 Added `principalOwner?: { iss; sub }` to `SessionMeta` + `DashboardSession` (+ `PrincipalOwner` type in shared/identity.ts; equality is the existing exact `principalEquals`), restored in `sessionFromMeta`. Persistence round-trip test green.
- [x] 6.2 Owner stamped through the trusted browser `spawn_session` road: `pending-principal-owner-registry.ts` (token-keyed, file-before-await, copy+freeze, 60s TTL, single-use) filed against a PRE-MINTED spawn token before `spawnPiSession`, gated on `isResolverActive() && ws.principal` (identity join key only, email dropped); removed on spawn failure/throw. event-wiring `session_register` resolves by token → stamps `DashboardSession.principalOwner` + persists to `.meta.json` + broadcasts; reconnect/cold-start reads it back via `sessionFromMeta`. cwd never confers ownership; inert-era + principal-less sockets stay ownerless. Wired through `createBrowserGateway` → `BrowserHandlerContext`. Tests: registry 6/6; handler stamps-when-active, inert-ownerless, principal-less-ownerless, removes-on-fail, removes-on-throw. NOTE: the browser `spawn_session` is the ONLY human spawn road that exists in the codebase today — there is no HTTP spawn endpoint (`/api/spawn*` is a failure-log reader) and no owned-spawn plugin API passing a human principal (the plugin-spawn hook files a PLUGIN ownerId via `pending-plugin-ref-registry`, not a human principal). The design's HTTP-spawn / owned-spawn-API roads are seam contracts for future call sites; when added they MUST file the owner through this same pre-mint-token path. Nothing ownerless leaks because those roads do not yet exist.

## 7. Optional host access policy (non-session roads)

- [x] 7.1 `PolicyRegistry` (`identity/policy-registry.ts`): accepts exactly one `authorize()` from the configured `trustedPolicyPlugin`, refuses any other registrant (`PolicyTrustError`) and a second registration (`PolicyDuplicateError`); unnamed ⇒ nothing trusted, `authorize` returns `true` (ungated). Tests: named-accepted, other-refused, duplicate-refused, none-named-ungated, unregister-scoped.
- [x] 7.2 Bounded fail-closed `authorize()` (default 500ms, clamped 50–2000): `false`/throw/timeout/non-boolean/unclassified ⇒ deny + structured `PolicyAuditEvent` (principal `(iss,sub)`, action, resource, reason; no secrets). Tests: allow-true, deny+audit for false/throw/timeout/non-boolean/unclassified, timeout clamp.
- [~] 7.3 Taxonomy built: `identity/host-resources.ts` — `HostActions` constants (workspace/openspec/branch/terminal/system read+write, domain.event) + secret-free `hostResource` builders per family. `PolicyRegistry.authorize` already denies an unclassified road when a policy is registered and returns allow (ungated) when none (§7.1/§7.2 tests cover both). GATE HELPER built: `identity/host-access.ts` `gateHttpNonSession(reply, policy, principal, action, resource)` — the non-session analog of `gateHttpSession`: **pure passthrough when `hasPolicy()` is false** (policy never consulted ⇒ zero production effect), else principal-less ⇒ 403, else policy decides (fail-closed via `authorize`), deny ⇒ 403 (not 404: non-session existence is not owner-secret). 4 unit tests (inert-passthrough / allowed / denied / principal-less-skips-policy). REMAINING: attach `{action,resource}` + the gate call at each non-session route site (mechanical, de-risked by the inert-passthrough property; behavioral gated-path verification is the §11.2 E2E's job with the `fixture-policy` caller).
- [x] 7.4 Inbound message table classified in `identity/ws-message-scope.ts` (session / session-list / non-session) with a coverage test that re-derives ALL 85 `BrowserToServerMessage` union literals from the shared protocol source and fails on any unclassified type (the "new road without owner-equality fails" guarantee). NOTE: bootstrap-FRAME disclosure gating (server→browser non-session state) is the separate §8.4 policy road; the session snapshot frame is already owner-filtered (§8.2).

## 8. Enforce every session read/write road (HTTP + WS) — owner equality, always on when active

<!-- §8 SHARED GUARD built: `identity/session-access.ts` `canAccessSession({active,principal,owner})`
     (inert⇒allow; active+ownerless⇒deny; active+principal-less⇒deny; else exact principalEquals) +
     `filterSnapshotForPrincipal` (per-item sessions + orders + endedTotals). Both unit-tested (10/10). -->
- [x] 8.1 Owner-equality on all 10 session-scoped HTTP roads in `session-routes.ts` via `gateHttpSession(reply, active, principalOf(request), owner)` (deny ⇒ 404, no owned-vs-not-found oracle) + `filterOwned` per-item on `/api/sessions` and `/api/sessions/archived`: events/:sessionId/:seq, tool-result, session-change, session-diff, retained-transcript, session-file, archived/:id (read+DELETE). Archived sessions retain their owner (`principalOwner` added to `ArchivedSessionSummary`, stamped at capture + restored in the cold-boot meta reseed) so an owner keeps access. `isResolverActive` threaded from server.ts. Tests: `gateHttpSession` owner-accept / non-owner-404 / principal-less-404 / ownerless-404 / inert-passthrough. Net-zero new biome complexity (extracted `resolveInCwd`).
- [x] 8.2 WS bootstrap `sessions_snapshot` filtered per-item via `filterSnapshotForPrincipal` in the gateway connect path (sessions by owner-equality, orders to visible ids, endedTotals to groups with a visible session); inert passes through. Tests: two-principal each-sees-only-own, principal-less-sees-nothing, inert-passthrough. TODO within 8.2: `list_sessions` / `sessions_page` REQUEST handlers still need the same per-item filter (currently only the connect snapshot is filtered).
- [x] 8.3 Single owner-equality CHOKE POINT in the browser-gateway dispatch (before the switch): `isResolverActive() && isSessionOwnedMessage(msg.type)` ⇒ look up `sessionManager.get(sessionId)?.principalOwner`, apply `canAccessSession`, silently drop on deny (no oracle) — covers subscribe/unsubscribe/history_backfill + every session command (send_prompt/abort/retry/kill/force_kill/shutdown/stop_after_turn/rename/archive/unarchive/resume/proposals/followup-queue/set_model/set_thinking_level/tags/display-prefs/process-drawer/flow_control/resync/view). Classification `identity/ws-message-scope.ts` (36 session / 2 session-list / 47 non-session; coverage test re-derives all 85 protocol union literals → fails if any is unclassified, satisfying §7.4/D10). Tests: scope 5, integration 4 (owner-forwarded, non-owner-dropped, principal-less-dropped, inert-unchanged) via the real message loop.
<!-- §8.3 CLASSIFICATION ANALYSIS (for the choke-point gate + §7.4 coverage test):
     Approach: single gate in the browser-gateway dispatch switch keyed by a CURATED message table
     (not just "has sessionId"). Data-driven `sessionId`-carrying members (51): abort accept_replace_proposal
     architect_prompt_response archive_session attach_proposal clear_followup_entries close_inline_terminal
     detach_proposal dismiss_replace_proposal edit_followup_entry extension_ui_response fetch_content flow_control
     flow_management force_kill history_backfill kill_process list_files open_inline_terminal promote_followup_entry
     prompt_response prompt_resync_request remove_followup_entry rename_session request_commands request_models
     request_providers request_roles resume_session retry_session role_preset_delete role_preset_load
     role_preset_save role_remove role_set send_prompt session_unview session_view setSessionDisplayPrefs set_model
     set_session_process_drawer set_session_tags set_thinking_level shutdown stop_after_turn subagent_resync_request
     subscribe ui_management unarchive_session unsubscribe watch_files.
     CURATION NEEDED: some carry sessionId only as CONTEXT for a global/workspace op and are NON-session
     (→ §8.4 policy / ungated, NOT owner-equality): request_commands/models/providers/roles, role_* , ui_management,
     flow_management, list_files, watch_files, open_inline_terminal/close_inline_terminal (terminal=workspace road).
     resume_session mutates a session but for a RECOVERY candidate that may pre-date ownership — treat as owner-gated
     when it has an owner, and (open question) how to handle ownerless-recovery under active resolver.
     spawn_session (no sessionId) is gated by principal-presence for stamping, not owner-equality. -->
- [~] 8.4 Gate MECHANISM + CALLER complete + unit-tested, both inert-by-default: `gateHttpNonSession` (§7.3, `identity/host-access.ts`, 4 tests) routes a non-session road through `PolicyRegistry.authorize` only when a policy is registered, else passes through unchanged (today's disclosure); the `fixture-policy` plugin (`packages/fixture-policy-plugin`, 7 tests) is the trusted caller that grants/denies per principal. REMAINING (lands with §11.2, which supplies the running server + policy caller to verify byte-for-byte): attach the gate + `{action,resource}` at the OpenSpec/branch/terminal/system route sites, then the policy-registered-omits-unauthorized vs ungated-discloses-as-today behavioral test against the live harness.

## 9. WebSocket identity binding + lifetime

- [x] 9.1 `WsTicketStore.mint` binds an optional `{ principal, principalExpiresAt }` onto the ticket entry and returns them from `consumeDetailed` (`ws-ticket.ts`); the `/api/ws-ticket` handler passes the resolver-hook `request.principal` for non-bridge scopes (server.ts:~1845); a principal-less caller records none. Tests: mint-binds-and-returns, principal-less-mint-records-none.
- [x] 9.2 New `authorizeWsUpgrade()` (auth-plugin.ts) supersedes the boolean `validateWsUpgrade` on the production upgrade path (server.ts): `requireIdentityTicket = scope==="browser" && resolverRegistry.hasActiveResolver()` ⇒ ONLY a principal-bearing single-use ticket authorizes; cookie/local-IPC/trusted-network/no-ticket/principal-less-ticket all refused. Non-browser scopes + inert era unchanged (unified secret vs no-auth branches via optional `secret`). `validateWsUpgrade` retained for existing callers/tests. Tests: identity-mode refuses local/cookie/no-ticket/principal-less, accepts principal ticket; legacy allowances + no-auth mode intact (68 auth/WS tests green).
- [x] 9.3 Upgrade handler attaches a FROZEN `ws.principal` (copy) + `ws.principalExpiresAt` from the consumed ticket before `emit("connection")`; principal-less (inert) sockets carry neither. Tests: principal surfaced vs absent via `consumeDetailed`.
- [x] 9.4 `installSocketLifetime` (`identity/socket-lifetime.ts`) schedules a one-shot close (code 4001) at `principalExpiresAt` — immediate when already past, none for a principal-less socket — wired into the browser-gateway connection handler with cleanup on close/error (releases subscriptions via the existing close path). Tests: schedules-at-exp, closes-when-past, none-when-principal-less.
- [x] 9.5 Transport-only heartbeat in the same helper (ping each tick, terminate on a missed pong), distinct from bridge ping/pong and untied to any session; a heartbeat NEVER extends identity (independent expiry timer). Tests: ping+terminate-on-missed-pong, pong-keeps-alive, heartbeat-does-not-extend-identity, cleanup-clears-both.

<!-- POLICY WIRING (foundation for §7.3/§8.4/§10) DONE this pass:
     `PolicyRegistry` now INSTANTIATED in server.ts (trustedPolicyPlugin + policyTimeoutMs from config,
     structured audit sink); plugin registration seam `registerHostAccessPolicy` added to the plugin
     server-context (type + impl), trust-gated to the configured plugin (no-op registrar otherwise,
     mirroring registerPrincipalResolver); `assertIdentityReadiness` now reads `policyRegistry.size`
     (no longer hardcoded 0), so a named-but-absent / duplicate policy fails startup for real (D9).
     Green: 86 identity + 409 plugin-runtime. Remaining §10/§7.3/§8.4 = per-road/per-socket policy CALLS. -->
## 10. Permissioned domain-event fan-out

- [x] 10.1 Mechanism built + tested: `identity/domain-fanout.ts` `deliverDomainEvent(targets, action, resource, policy, send)` — no policy ⇒ delivers to every socket (unchanged broadcast); policy ⇒ per-candidate `authorize`, delivers only on true, principal-less socket gets nothing, fail-closed via `PolicyRegistry.authorize`. Tests (4): no-policy-all, policy-per-socket-isolation (Anna sees, Béla doesn't), principal-less-nothing, deny-drops. NOTE: no core path emits a resource-tagged GLOBAL domain event today (`plugin_event` carries a `sessionId` → session road, §10.2); the gateway `broadcastDomainEvent` emit-path wiring lands with the §11.2 fixture policy plugin that exercises it end-to-end.
- [x] 10.2 `flow_control` is classified `session` in `ws-message-scope.ts` and therefore rides the §8.3 owner-equality choke point (dropped for a non-owner, delivered to the owner) — it never enters the domain-event policy road. Verified by the choke-point integration tests (a session command from a non-owner is dropped; from the owner, forwarded) which apply identically to `flow_control`.
- [~] 10.3 INBOUND classification complete + coverage-tested (`ws-message-scope.ts`, §7.4: session-by-`sessionId` vs non-session; undeclared ⇒ non-session/domain). OUTBOUND emit-time classification (session-frame-by-`sessionId` vs domain-by-`resource`) is embodied by the two send paths — owner-gated broadcast for session frames vs `deliverDomainEvent` for domain frames; the per-emit tagging of an outbound domain frame lands with the §10.1/§11.2 emit path.

## 11. Validation and cross-cutting

- [x] 11.1 Inert = today verified at every decision seam (each seam's inert branch is a pure passthrough with an explicit test): `resolver-hook` (no resolver → request unchanged, no principal), `session-access` (inert → allow-all / snapshot passthrough / gate proceeds unconditionally), `domain-fanout` (no policy → unchanged broadcast to all), `activation` (inert happy path, connectors intact). Structural guarantee: every identity gate returns allow/passthrough in its inert branch, so an unconfigured/disabled resolver changes no outcome. Running-server byte-for-byte confirmation is the §11.2 E2E's job.
- [x] 11.2 Docker-harness E2E. DONE + verified: (a) **fixture trusted policy plugin** `packages/fixture-policy-plugin/` (private) — config-seeded allow-list `HostAccessPolicyFn` (matches principal `sub`/`iss`, grants action or `*`, default-deny), registered via `registerHostAccessPolicy` under id `fixture-policy`, inert when disabled / no capability; 7 unit tests (decision matrix incl. Anna-allowed / Béla-denied fan-out, iss pinning, registration+inert), clean tsc+biome. **BRING-YOUR-OWN-KEYCLOAK**: the dashboard never ships/boots Keycloak — every provider field is a `keycloak-resolver` plugin setting (`configSchema.json`), so a developer/user points the plugin at their own IdP. (b) **Importable realm reference** `docker/fixtures/keycloak/realm-identity-plane.json` (realm `pi-identity`, public PKCE S256 client `dashboard-web` + direct-grant, client-level `oidc-audience-mapper` → `pi-dashboard`, users anna/bela) — **LIVE-VALIDATED** against a standalone Keycloak 26: realm imports, anna+bela mint tokens with correct iss/aud/azp + **distinct sub**, and the real `keycloak-resolver` resolves both to distinct principals while rejecting a foreign issuer (`docker/fixtures/keycloak/live-resolver-check.ts`, exit 0). Live testing fixed two fixture bugs the JSON-parse check missed: missing `lastName` (KC26 User Profile → login failed) and an explicit `clientScopes` array that suppressed the built-in `basic` scope (dropped the `sub` claim ownership keys on). **E2E APPROACH = LIGHTWEIGHT FAKE OIDC ISSUER** (best practice for testing token VALIDATION; researched: oauth2-mock-server / mock-oauth2-server): the E2E does NOT boot Keycloak. The `docker/fixtures/keycloak/` realm + `live-resolver-check.ts` demote to a real-Keycloak **production-onboarding reference** (proved the real path once, exit 0). (c-fake) **fake OIDC issuer** `packages/shared/src/test-support/fake-oidc-issuer.ts` — real RS256 keypair, real `/.well-known/openid-configuration` + `/jwks` + `POST /mint`; `issuerUrl` override decouples advertised issuer from bind host (Docker: bind 0.0.0.0, advertise loopback). (c-int) **high-fidelity integration test** `packages/keycloak-resolver-plugin/src/server/__tests__/fake-issuer-integration.test.ts` — the REAL resolver + REAL `JwksSource` (live HTTP discovery + remote JWKS + `jose` RS256 verify) against the fake issuer: **6 tests green** (anna/bela → distinct principals, verified-email carry/drop, foreign-issuer → null, wrong-aud reject, expired reject, HTTP `/mint` cross-boundary path). No Docker, no Keycloak. (c) **opt-in overlay** `docker/compose.test.identity.yml` — boots NO Keycloak; sets `PI_E2E_IDENTITY=1` + `PI_E2E_IDENTITY_PORT`/`_AUDIENCE` and publishes the issuer port on loopback. Merge validated: 3 ports incl. 18090, no keycloak service. (c-run) **in-container runner** `scripts/fake-oidc-run.ts` (under `scripts/` because the Dockerfile bakes it, not `docker/`) — boots the fake issuer on the fixed port; **verified locally** (discovery/jwks/mint all answer, missing-sub → 400). (d) **token helper** `tests/e2e/helpers/identity-tokens.ts` — mints anna/bela (stable subs) via the fake issuer's `/mint` over the mapped port; biome clean. (e) **`PI_E2E_IDENTITY` seed path** in `test-entrypoint.sh` — flag-gated: boots the fake issuer (tsx), waits for discovery, then writes `plugins["keycloak-resolver"]` (enabled/issuer=fake/audience/allowInsecureHttp) + adds it to `identity.trustedResolverPlugins`. Merge-preserving, idempotent; `bash -n` clean. Inert unless `PI_E2E_IDENTITY=1` ⇒ zero change to the default harness. Also fixed a pre-existing regression: `fixture-policy-plugin` needed `pi-dashboard-plugin.fixture:true` (bundled-plugins-complete test); shared suite green (2205). (f) **two-user isolation E2E — LIVE BOOT-TO-GREEN, 8/8.** `tests/e2e/identity/two-user-isolation.spec.ts` drives raw HTTP + `ws` (no browser — an active resolver refuses ticketless browser sockets, §9.2) against an identity-ACTIVE harness (`playwright.identity.config.ts` + `tests/e2e/identity/global-setup.ts`, `npm run test:e2e:identity`): HTTP list owner-scoped both ways, principal-less sees nothing, detail-gate 404-for-non-owner / 200-for-owner (no oracle), ws-ticket rejects wrong-aud (401) + claims valid bearer (200+ticket), §9.2 upgrade refused without an identity ticket, WS bootstrap owner-scoped. Harness pieces: `docker/compose.test.identity.yml` (overlay, publishes issuer port), `test-up.sh` `TEST_EXTRA_COMPOSE` hook, `scripts/fake-oidc-run.ts` (in-container issuer runner), `scripts/seed-identity-sessions.mjs` (Anna+Béla owned ended sessions). Two boot bugs found + fixed live: (i) the resolver + fixture-policy manifests lacked `claims: []` → `discoverPlugins()` dropped them → plane inert (allow-all); guarded by a new `manifest-discoverability.test.ts`. (ii) the entrypoint's ticketless `/ws` smoke check crash-looped PID 1 under active identity (refused by §9.2) — now skipped when `PI_E2E_IDENTITY=1`. Verified end-to-end: cold `--build` image + managed boot + 8 green + clean teardown. The secondary `fixture-policy` domain-fanout assertion stays deferred (its decision matrix is unit-covered: 7 tests).
- [x] 11.3 Issuer pinning verified: `keycloak-resolver` peeks the UNVERIFIED `iss` and returns `null` when it differs from the configured `issuer` (foreign-issuer JWT falls through, never persists an `(iss,sub)`); test `resolver.test.ts` “foreign-issuer JWT yields null” + exact-`iss` match. The host/scheme/port pinning doc sentence folds into §11.6.
- [x] 11.4 Non-human principal policy verified: an in-process/scheduler spawn files no owner token → `resolve` returns `null` → ownerless BY CONSTRUCTION; an ownerless session is denied to every human + principal-less socket when active (inert unchanged). Composition test added to `session-access.test.ts` (“scheduler-spawned session is ownerless and hidden from humans”) tying `pending-principal-owner-registry.resolve` → `canAccessSession`. Explicit sentence lives in design D11 + folds into the §11.6 doc note.
- [x] 11.5 `openspec validate add-multi-user-identity-plane --strict` → “Change 'add-multi-user-identity-plane' is valid”.
- [x] 11.6 Doc note written: `docs/identity-plane.md` (caveman style) — consumer rule (gate on `request.principal`/`ws.principal`, never `isAuthenticated`), activation model (resolver enabled+configured, no mode flag; inert=today, active=owner-equality), bundled `keycloak-resolver` (core imports nothing Keycloak-specific) + issuer pinning, optional non-session-only host policy (`trustedPolicyPlugin`+`policyTimeoutMs`, fail-closed), product-authz-in-product-plugin, non-human ownerless-by-construction, RFC 9068/9700/7636/9449. Indexed in `docs/AGENTS.md` (alphabetical row).

## 12. Browser client plane (topology B)

- [x] 12.1 Full auth-code+PKCE flow (pure fns) + in-memory store. `oidc-flow.ts`: `buildAuthorizeRequest(config,pkce?)` → S256 authorize URL + verifier/state to retain; `exchangeCode(config,{code,verifier},extraHeaders?)` → form-encoded token POST, normalized `{accessToken,expiresIn,tokenType}`, throws non-2xx / missing token (`extraHeaders` carries the §12.5 DPoP proof). Redirect/callback UI glue + server-provided `OidcClientConfig` are the integration seam (§11.2). Test `oidc-flow.test.ts` (5). PLUS PKCE primitives + in-memory token store: `client/src/lib/identity/pkce.ts` (`createCodeVerifier` 43-char base64url, `deriveCodeChallenge` S256, `createPkcePair`, `createRandomState` — WebCrypto via `globalThis.crypto`, S256 only) + `token-store.ts` (module-scoped access token, NEVER localStorage; `setAccessToken(tok, expiresInSec)` / `getAccessToken(now?)` null-past-expiry / `getExpiresAt` / `hasLiveToken` / `clearAccessToken`). Test (9): base64url window, S256 determinism, RFC 7636 Appendix-B known-answer vector, entropy uniqueness, store live/expiry/clear/empty.
- [x] 12.2 `device-auth.ts` gains `getApiBearer()` = identity token (`getAccessToken`) ∥ device bearer (`getDeviceBearer`) fallback; `installDeviceAuthFetch` now attaches `getApiBearer()` to same-origin `/api`/`/v1`, still never overriding an explicit Authorization header. Test `device-auth-bearer.test.ts` (8): precedence (identity > device > null), attach-to-/api, explicit-header-preserved, cross-origin-skip, non-/api-skip, inert-no-attach.
- [x] 12.3 `mintWsTicket` authenticates with `getApiBearer()` (carries the identity bearer); `useWebSocket.connect` gate changed `getDeviceBearer()` → `getApiBearer()`, so an unpaired human with a live identity token mints a `browser`-scope ticket and presents only `?ticket=` (durable/bearer token never rides WS, F6). Test `useWebSocket-ticket.test.ts` (2): plane-active mints + `?ticket=` on socket + token absent from URL + `Bearer <identity>` on mint; no-credential opens ticketless (inert).
- [x] 12.4 Heartbeat = native WS ping/pong (§9.5 uses `ws.ping()`, browser auto-pongs — no app code). Identity expiry: `useWebSocket(url, onIdentityExpired?)` handles server close code `4001` (`IDENTITY_EXPIRED_CLOSE_CODE`, §9.4) by `clearAccessToken()` → awaiting the app re-acquire hook → immediate reconnect (no backoff/offline flicker), via `onIdentityExpiredRef` (no stale closure). Test `useWebSocket-ticket.test.ts` (2 more): 4001 clears+re-acquires+reconnects with fresh token; non-4001 (1006) does NOT re-acquire.
- [x] 12.5 Conditional DPoP (RFC 9449) primitives: `client/src/lib/identity/dpop.ts` — `isDpopBound(resp)` (true only on non-empty `cnf.jkt`; unbound ⇒ no proof path), `generateDpopKey()` (NON-extractable ES256 keypair, exports only public `x`/`y`, never `d`), `createDpopProof(key, htm, htu, nonce?)` (typ `dpop+jwt`/ES256 header + fresh `jti`/`iat` per call, conditional `nonce`). `exchangeCode` already threads a token-request proof via `extraHeaders`. Test `dpop.test.ts` (4): bound-vs-unbound detection, non-extractable key + public-only JWK, proof verifies against exported pubkey + fresh-jti-per-call, nonce omitted when absent. Per-call/per-mint proof ATTACHMENT (holding the bound key + adding the `DPoP:` header in the fetch wrapper + `mintWsTicket`) is the integration seam wired with the §11.2 full flow.
- [x] 12.6 Client inert against an inert dashboard: `getApiBearer()` null ⇒ fetch wrapper attaches no Authorization (test `device-auth-bearer.test.ts` “attaches nothing when no credential (inert)”), `useWebSocket.connect` opens the socket ticketless (test `useWebSocket-ticket.test.ts` “no credential opens ticketless (inert)”, asserts no `/api/ws-ticket` call + bare url), and PKCE/oidc-flow run only when the app explicitly invokes them. Today's cookie/loopback UX unchanged.

_(Scenarios from `scenario-design` fold into §8/§10/§11/§12 as they are drafted.)_
