## 1. Baseline

- [x] 1.1 Run `npm run build` and record the `modulepreload` hrefs and byte sizes from `packages/client/dist/index.html` in `openspec/changes/harden-ios-safari-memory-and-ws-diagnostics/notes.md` (expect `mdi-*.js` ≈ 2,780,948 B plus the total landing JS bytes). Verify: notes.md holds the numbers P1 compares against.

## 2. Lazy icon set — tests first (client-build-config)

- [x] 2.1 Rewrite `packages/client/src/__tests__/mdi-chunk-size.test.ts` (exemplar: its current version plus `packages/client/src/__tests__/lazy-feature-preload.test.ts` for the `index.html` parsing). Input: production `dist/`; trigger: walk `index.html` entry + `modulepreload` + transitive static `import"./x.js"`; observable: no landing-graph chunk contains `mdiZodiacAquarius`, ≥1 emitted chunk outside it does, gz `index` ≤ 900 KB (test-plan #E1). Verify: fails against the 1.1 build.
- [x] 2.2 Add a build-log assertion (exemplar: the no-warnings check behind `client-build-config` "Production build is free of mechanical warnings"). Input: `npm run build` log; trigger: build completes; observable: no line matching `dynamic import will not move module` naming `@mdi/js` (test-plan #E2). Verify: passes before and after (regression guard).
- [x] 2.3 Create `packages/client-utils/src/__tests__/mdi-by-key.test.tsx` (exemplar: `packages/client-utils/src/__tests__/StatusPill.test.tsx`). Input: keys `mdiRefresh`, `mdiTotallyMadeUpName`, `"refresh"`, `""`, `null`; trigger: `loadMdiIconSet()` then `resolveMdiIconSync(key)`; observable: valid key → non-empty path, the other four → `null`, no throw (test-plan #E3). Verify: fails (module missing).
- [x] 2.4 In the same file (exemplar as 2.3). Input: 5 hook consumers resolving different keys mount in one tick; trigger: load resolves; observable: the dynamic import runs exactly once (spy) and all 5 icons render (test-plan #F1). Verify: fails.
- [x] 2.5 In the same file (exemplar as 2.3). Input: mocked dynamic import rejects once, then succeeds; trigger: `useMdiIconByKey("mdiCheck")` mounts, then a second mount after the failure; observable: first mount renders nothing with no unhandled rejection; second mount retries and renders the icon (test-plan #X1). Verify: fails.
- [x] 2.6 Extend `packages/client-utils/src/__tests__/ActionList.test.tsx` (exemplar: the same file). Input: action `icon:"mdiRefresh"`, set not loaded; trigger: first render, then load resolves; observable: first render has no `<svg>` and no placeholder element; after resolve an `<svg>` with the `mdiRefresh` path appears (test-plan #E4). Verify: fails.
- [x] 2.7 Extend `packages/client-utils/src/__tests__/StatusPill.test.tsx` (exemplar: the same file). Input: `icon:"mdiCheck"`; trigger: mount → load resolves; observable: no svg, then an svg with the `mdiCheck` path (test-plan #E5). Verify: fails.
- [x] 2.8 Extend `packages/client/src/__tests__/extension-ui-modal.test.tsx` (exemplar: the same file). Preload the set before its existing sync `resolveMdiIcon` assertions. Input: module `icon:"mdiCheck"`, action `icon:"mdiTotallyMadeUpName"`; trigger: mount → load resolves; observable: module header has the `mdiCheck` svg; action button has no svg and no error (test-plan #E6). Verify: the new case fails.
- [x] 2.9 Extend `packages/client/src/__tests__/extension-ui-decorators.test.tsx` (exemplar: the same file). Input: 3 footer segments (`mdiCheck`, unknown key, no icon); trigger: mount → load resolves; observable: segment 1 has an svg, segments 2–3 have none, all texts render, no hooks-order warning (test-plan #E7). Verify: fails.

## 3. Lazy icon set — implementation

- [x] 3.1 Add `packages/client-utils/src/mdi-by-key.ts` (`loadMdiIconSet` memoized `import("@mdi/js/commonjs/mdi.js")` read as `mod.default ?? mod`; failure resolves to `null` and clears the memo; `resolveMdiIconSync`; `useMdiIconByKey`) and a `./mdi-by-key` subpath in `packages/client-utils/package.json` `exports`. Verify: 2.3–2.5 pass; `npm run lint` passes.
- [x] 3.2 Switch `ActionList.tsx` and `StatusPill.tsx` to `useMdiIconByKey`, rendering nothing while loading or for unknown keys, with props unchanged. Verify: 2.6–2.7 pass.
- [x] 3.3 Make `packages/client/src/lib/preview/mdi-icon-lookup.ts` re-export `resolveMdiIconSync` as `resolveMdiIcon` plus the hook. Move `GenericExtensionDialog.tsx` to the hook, and give `FooterSegmentSlot.tsx` a per-segment `FooterSegment` component that uses the hook. Verify: 2.8–2.9 pass; `rg 'import \* as .* from "@mdi/js"' packages/*/src` returns nothing.
- [x] 3.4 Remove the `mdi` entry and its comment from `manualChunks` in `packages/client/vite.config.ts`, then run `npm run build`. Verify: 2.1 and 2.2 pass; `index.html` has no preload containing the full set.
- [x] 3.5 Dev and embed interop check: `npm run dev`, open a surface with a key-resolved icon, and confirm it renders; build `examples/chat-embed-tester` if it has a build script. If CJS interop fails, switch to the design D1 virtual-module fallback. Verify: icon visible in dev; embed build succeeds.

## 4. Lazy icon set — browser E2E

- [x] 4.1 Author `tests/e2e/lazy-mdi-icon-set.spec.ts` (exemplar: `tests/e2e/lazy-feature-bootstrap.spec.ts` for network recording against the derived harness port). Input: cold load of `/`, with no key-resolved icon on screen; trigger: navigate and wait for `networkidle`; observable: zero requests for the full-icon-set chunk, and total landing JS bytes below the notes.md baseline (test-plan #P1). Verify: passes on the harness built from this branch.
- [x] 4.2 In the same spec (exemplar as 4.1). Input: a harness session showing a key-resolved icon (the extension-ui fixture, or the flows plugin `StatusPill`); trigger: open the session; observable: the icon `<svg>` appears and the lazy icon chunk is requested exactly once (test-plan #F2). Verify: passes on the harness.

## 5. Browser WS diagnostics — tests first (browser-ws-diagnostics)

- [x] 5.1 Add `packages/server/src/__tests__/browser-gateway-close-diagnostics.test.ts` (exemplar: `packages/server/src/__tests__/browser-gateway-handler-errors.test.ts`). Input: client sends 3 frames then closes with `1000`/`"bye"`; trigger: server `close`; observable: exactly one line with the `[browser-gw] browser client disconnected (remaining:` prefix and `code=1000 reason="bye" lifetime=<n>s frames=3 cause=peer` (test-plan #E8). Verify: fails.
- [x] 5.2 In the same file (exemplar as 5.1). Input: close reason `a"b\nc`; trigger: client closes; observable: the line has no raw newline and the reason is JSON-escaped (test-plan #E9). Verify: fails.
- [x] 5.3 In the same file (exemplar as 5.1). Input: the client's underlying TCP socket destroyed; trigger: none; observable: one line `code=1006 reason="" … cause=peer` (test-plan #X3). Verify: fails.
- [x] 5.4 In the same file (exemplar: the stalled fixture in `packages/server/src/__tests__/browser-gateway-dropped-frames.test.ts`). Input: pending bytes over the ceiling; trigger: flush attempt; observable: one disconnect line with `cause=stalled` (test-plan #X4). Verify: fails.
- [x] 5.5 Add `packages/server/src/__tests__/browser-gateway-keepalive.test.ts` (exemplar: `packages/server/src/__tests__/ws-ping-pong.test.ts`). Input: fake timers, `browserPingIntervalMs=1000`, client auto-pongs; trigger: advance 10 ticks; observable: socket open, ≥10 pings seen client-side (test-plan #E11). Verify: fails (no pings).
- [x] 5.6 In the same file (exemplar as 5.5). Input: the client suppresses the pong for tick 1 only; trigger: advance 4 ticks; observable: socket still open, no `cause=keepalive` line (test-plan #E10). Verify: fails.
- [x] 5.7 In the same file (exemplar as 5.5). Input: the client never pongs; trigger: advance 3 ticks; observable: closed by the server on tick 3, not tick 2, with exactly one line `cause=keepalive code=1006` (test-plan #X2). Verify: fails.
- [x] 5.8 In the same file (exemplar as 5.5). Input: gateway with 1 client; trigger: terminate the clients, `wss.close()`, advance 5 ticks; observable: zero pings after close and timer count back to baseline (test-plan #X5). Verify: fails.
- [x] 5.9 Add `packages/server/src/__tests__/ws-upgrade-reject-log.test.ts` (exemplar: `packages/server/src/__tests__/host-gate.test.ts` for the injected clock and log sink). Input: 403, scope `browser`, peer `127.0.0.1`, `x-forwarded-for: 203.0.113.9`, `cookie: sid=SECRET`; trigger: `log(...)`; observable: `status=403 scope=browser peer=127.0.0.1 fwd=x-forwarded-for ticket=absent`, with no `203.0.113.9` and no `SECRET` (test-plan #E12). Verify: fails.
- [x] 5.10 In the same file (exemplar as 5.9). Input: 401 with ticket `tkt-abcdef123`; trigger: `log(...)`; observable: `ticket=present` and no `tkt-abcdef123` (test-plan #E13). Verify: fails.
- [x] 5.11 In the same file (exemplar as 5.9). Input: same key 20× at t=0..59 s, then once at t=60 s; trigger: `log(...)` ×21; observable: exactly 2 lines, the second with `suppressed=19` (test-plan #E14). Verify: fails.
- [x] 5.12 In the same file (exemplar as 5.9). Input: 1000 distinct peers, one rejection each; trigger: `log(...)` ×1000; observable: tracked keys ≤ 256 (test-plan #E15). Verify: fails.
- [x] 5.13 Add `packages/server/src/__tests__/ws-upgrade-reject-wiring.test.ts` (exemplar: `packages/server/src/__tests__/host-gate-upgrade.test.ts`). Input: real server, no auth secret, `/ws` upgrade from loopback with `X-Forwarded-For`; trigger: upgrade; observable: 403 plus exactly one `[ws-upgrade] rejected status=403 scope=browser` line with `fwd=x-forwarded-for` (test-plan #E16). Verify: fails.
- [x] 5.14 In the same file (exemplar as 5.13). Input: server with an auth secret, `/ws` upgrade with no cookie; trigger: upgrade; observable: 401 plus one `[ws-upgrade] rejected status=401` line (test-plan #E17). Verify: fails.
- [x] 5.15 In the same file (exemplar: `packages/server/src/__tests__/bridge-upgrade-auth.test.ts` for tickets). Input: bridge-path upgrade on the dashboard port with a valid single-use ticket; trigger: upgrade; observable: 400, one `status=400` line with `ticket=present`, and the ticket still consumable afterwards (test-plan #E18). Verify: fails.
- [x] 5.16 In the same file (exemplar as 5.13). Input: one upgrade refused by the host gate (bad `Host`) and one refused by the cross-origin `[ws-gate]`; trigger: upgrade; observable: each gets its existing `[host-gate]`/`[ws-gate]` line and zero `[ws-upgrade]` lines (test-plan #E19). Verify: passes before and after (regression guard).

## 6. Browser WS diagnostics — implementation

- [x] 6.1 In `packages/server/src/pairing/browser-gateway.ts`: add per-socket `WeakMap` state (`connectedAt`, `frames`, `missedPongs`, `cause`), count inbound frames, set `cause="stalled"` at the byte-ceiling `terminate()`, and write the new close line (`reason.toString("utf8")` → `JSON.stringify`). Verify: 5.1–5.4 pass.
- [x] 6.2 Add the keepalive: injectable `browserPingIntervalMs` (default 30 000); each tick does `missedPongs >= 2` → `cause="keepalive"` + `terminate()`, else increment + `ping()`; `pong` resets; guarded `unref()`; cleared on `wss.on("close")`. Verify: 5.5–5.8 pass; the existing `browser-gateway-*.test.ts` files still pass.
- [x] 6.3 Export `forwardingHeaderNamesPresent(headers)` from `packages/server/src/auth/localhost-guard.ts` and add `packages/server/src/auth/ws-upgrade-reject-log.ts` (`createWsUpgradeRejectLogger`). Verify: 5.9–5.12 pass; `localhost-guard.test.ts` passes.
- [x] 6.4 In `packages/server/src/server.ts`, create one logger instance and call it only from the silent bridge-400, auth-401 and no-auth-403 branches (`ticketPresent = extractTicket(...) !== null`, not consumed). Verify: 5.13–5.16 pass.

## 7. Mobile tool groups (mobile-resilience)

- [x] 7.1 Extend `packages/client/src/components/__tests__/ToolBurstGroup.test.tsx` (exemplar: the same file; wrap in `MobileProvider` with a `matchMedia` mock returning `matches:true`). Input: running member, `toolGroupDefaultCollapsed:false`, mobile; trigger: render; observable: body not mounted, live header visible (test-plan #E20). Verify: fails.
- [x] 7.2 In the same file (exemplar as 7.1). Input: the 7.1 state; trigger: click the header, then the member completes; observable: body mounted and still mounted after running→done (test-plan #E21). Verify: fails or passes; it pins the override path.
- [x] 7.3 In the same file (exemplar as 7.1 with `matches:false`). Input: as 7.1 but desktop; trigger: render; observable: body auto-mounted while running (test-plan #E22). Verify: passes before and after (regression guard).
- [x] 7.4 In `packages/client/src/components/chat/ToolBurstGroup.tsx`, call `useMobile()` in `ToolBurstGroup`, set `autoOpen = (prefs.toolGroupDefaultCollapsed || isMobile) ? false : isRunning`, and update the doc comment. Verify: 7.1–7.3 pass.

## 8. Closeout

- [x] 8.1 Full test run (`set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log`) plus `npm run quality:changed` and `npm run lint`. Verify: green.
- [x] 8.2 Record the after-numbers next to 1.1 in notes.md. Verify: the full set is gone from the landing preload set.
- [x] 8.3 Update DOX rows with `See change: harden-ios-safari-memory-and-ws-diagnostics`: `packages/client-utils/src/AGENTS.md` (mdi-by-key, ActionList, StatusPill), `packages/client/src/lib/preview/AGENTS.md`, `packages/client/src/components/extension-ui/AGENTS.md`, `packages/client/src/components/chat/AGENTS.md` (ToolBurstGroup), `packages/server/src/pairing/AGENTS.md`, `packages/server/src/auth/AGENTS.md`, and the `server.ts` row. Verify: `rg -l harden-ios-safari-memory-and-ws-diagnostics packages --glob 'AGENTS.md'` lists them.
- [x] 8.4 Add a CHANGELOG `## [Unreleased]` entry (Fixed: iOS cold-load weight / full MDI set no longer eager; Added: browser WS close/keepalive/upgrade-rejection logging). Verify: entry present.
- [ ] 8.5 `review-code` pass on the diff, plus a `security-hardening` pass on 6.3/6.4 (no secret leakage, bounded map). Verify: findings resolved.
- [ ] 8.6 Manual QA on an iPhone (Safari via tunnel, transcript ≥ 2 MB): cold-open the session, stream one turn, background and foreground the tab; expect no spontaneous full-page reload and `cause=` lines in the server log as expected; report on issue #712 (test-plan: manual-only, #F3).
