## 1. Sandbox

- [ ] 1.1 Add `allow-downloads` to the `LiveServerViewer` iframe sandbox; leave `allow-same-origin` absent
- [ ] 1.2 Update the D7 comment above the iframe to state what `allow-downloads` does and does not grant
- [ ] 1.3 Test: the rendered iframe carries `allow-downloads` and does NOT carry `allow-same-origin`
