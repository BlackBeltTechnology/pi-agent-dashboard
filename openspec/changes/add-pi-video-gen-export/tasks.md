## 1. Sidecars (tests first, then `src/sidecars.ts`)

- [ ] 1.1 Extend `src/__tests__/fixture.ts` with sidecar writers (`writeFilm`, `writeShotSidecar`, `writeTimeline`, real PNG/JPEG/WebP byte helpers) and verify existing tests still pass with `npm test -w packages/video-production`
- [ ] 1.2 Test (test-plan E1): no-film.json package, see `src/__tests__/inspect.test.ts` — input: shots/*.md without film.json · trigger: loadSidecars + inspectPackage · observable: enabled false, zero sidecar problems, inspect JSON and human report deep-equal the pre-change snapshot
- [ ] 1.3 Test (test-plan E2): film.json schema, see `src/__tests__/shots.test.ts` — input: valid / empty style / no style / duplicate id `arm` / invalid JSON · trigger: load · observable: one scoped `film` problem per bad variant with the spec message, zero for valid
- [ ] 1.4 Test (test-plan E3): shot sidecar decision table, see `src/__tests__/shots.test.ts` — input: ok, md-without-json, json-without-md, missing prompt.action, unknown visibleCharacters `ghost` · trigger: load · observable: the four named `shot`-scope problems
- [ ] 1.5 Test (test-plan E4): shot durationSec BVA, see `src/__tests__/shots.test.ts` — input: absent, "8", 0.99, 1, 8, 300, 300.01 · trigger: load · observable: 1/8/300 accepted, others a problem naming the shot
- [ ] 1.6 Test (test-plan E5): timeline segment shape, see `src/__tests__/shots.test.ts` — input: shot+image, neither, unknown shot_99, image without durationSec · trigger: load · observable: one `timeline` problem per segment naming its index (and shot_99)
- [ ] 1.7 Test (test-plan E6): timeline enums, see `src/__tests__/shots.test.ts` — input: transition style crossfade, overlay top-right, codec vp9, extra output key · trigger: load · observable: problem per field listing allowed values or naming the key
- [ ] 1.8 Test (test-plan E7): output bounds BVA, see `src/__tests__/shots.test.ts` — input: resolutions 64x64/100x100/1920x1080/1921x1080/4096x4096/4098x4096, fps 0/1/29.97/120/121 · trigger: load · observable: only 100x100, 1920x1080, 4096x4096, fps 1 and 120 accepted
- [ ] 1.9 Test (test-plan E8): segment durations, trim and transitions BVA, see `src/__tests__/shots.test.ts` — input: 8 s shot with durationSec 0.49/0.5/8/8.01, trim 7.99/8, trim 2 + 6 vs 6.01, transition 0/3/3.01/equal-to-segment, transition on last segment · trigger: load · observable: exactly the out-of-range cases produce a problem naming the segment index
- [ ] 1.10 Test (test-plan E9): problem scopes, see `src/__tests__/shots.test.ts` — input: missing voiceover file, unknown timeline shot, missing shot sidecar, bad film · trigger: load · observable: scopes audio, timeline, shot, film
- [ ] 1.11 Test (test-plan X1): sidecar path containment, see `src/__tests__/shots.test.ts` — input: `../../etc/passwd`, `/etc/hosts`, file symlink inside package, dir symlink to outside, missing file · trigger: load · observable: `path escapes package` ×2, `path is a symlink`, `path escapes package`, `file not found`, each naming the field
- [ ] 1.12 Implement `src/sidecars.ts` (`loadSidecars(target)` → `{ enabled, film, shots, timeline, problems: {scope,message}[] }`, reusing `resolvePackage`/`loadShots`) and verify tests 1.2–1.11 pass

## 2. Inspect reporting

- [ ] 2.1 Test (test-plan E18): sidecar reporting shape and placement, see `src/__tests__/inspect.test.ts` — input: sidecar package with shot_02.json missing, clean prompts · trigger: inspectPackage + formatReport · observable: shot_02 `sidecar:"missing"`, others `ok`, `sidecars.enabled` true, `problems` still `[]`, `Sidecars:` block after rows, last line `✓ All shots have a Full Veo prompt block.`
- [ ] 2.2 Implement sidecar fields in `src/inspect.ts` (only when enabled) and verify 2.1 and E1 (1.2) pass

## 3. Export render / timeline (`src/export.ts`)

- [ ] 3.1 Test (test-plan E10): job id derivation and validation, see `src/__tests__/package.test.ts` — input: names booth-2026, `.Trade show v1.2`, café, 80 chars, `___`; --job -abc, `a b`, 64/65 chars, take1 · trigger: build job id · observable: defaults match `^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$` with `-render-<YYYYMMDD-HHMMSS>` suffix, `Trade-show-v1-2-render-` prefix, `package-render-` fallback; only take1 and 64-char --job accepted, rejects happen before mkdir
- [ ] 3.2 Test (test-plan E11): render spec mapping, see `src/__tests__/render.test.ts` — input: 3-shot sidecar package with film style/negative/2 characters · trigger: exportRender · observable: film fields equal film.json, each shot prompt deep-equals its sidecar, no seed/resolution/joined prompt keys
- [ ] 3.3 Test (test-plan E12): seamless continuity, see `src/__tests__/render.test.ts` — input: 02A seamless→02B, last shot seamless, `--no-last-frame` · trigger: exportRender · observable: 02A lastFramePath `video_production/storyboard/shot_02B.png`; last shot none + warning; flag → no lastFramePath + warning listing seamless shots
- [ ] 3.4 Test (test-plan E13): duration rounding and capability flags, see `src/__tests__/render.test.ts` — input: 7.4/7.5/8, `--durations 5-8` with shots rounding to 4 and 9, `--aspect 16:9` vs film 9:16, no flags · trigger: exportRender · observable: 7 and 8 with warnings, 8 without; range failure lists both shots; aspect failure names 9:16; no flags only rounds
- [ ] 3.5 Test (test-plan E14): dropped-field warnings, see `src/__tests__/render.test.ts` — input: seed, reference image, shot_03 negative ≠ film, shot_04 aspect 9:16 vs 16:9, clean package · trigger: exportRender · observable: resolution note always; seed, reference, shot_03, shot_04 warnings; clean → resolution note only
- [ ] 3.6 Test (test-plan E15): unsafe shot names, see `src/__tests__/render.test.ts` — input: shot_01.2.md, `shot_01 final.md` · trigger: exportRender · observable: fails `shot id not accepted by pi-video-gen` naming the shot, no job dir
- [ ] 3.7 Test (test-plan X2): frame preflight, see `src/__tests__/render.test.ts` — input: frame outside cwd, symlink frame, text bytes in .png, 20 MiB+1 PNG, valid .jpeg · trigger: exportRender · observable: failures name shot with the spec reason; .jpeg accepted
- [ ] 3.8 Test (test-plan X4): re-billing guard state sequence, see `src/__tests__/render.test.ts` — input: export, export, export --new-job, delete spec(s), export; prior `--job take1 --out <tmp>/vg` · trigger: exportRender · observable: 2nd fails listing 1st spec + resume hint; --new-job succeeds and registry has 2 entries; stale entries ignored; custom id/out prior spec listed
- [ ] 3.9 Test (test-plan X5): registry fail-closed, see `src/__tests__/render.test.ts` — input: exports.json `{not json`, `{"x":1}`, chmod 000 (skip on win32) · trigger: exportRender with and without --new-job · observable: fails naming the registry file, no job dir
- [ ] 3.10 Test (test-plan X6): package and job-dir preconditions, see `src/__tests__/render.test.ts` — input: no film.json, shot-scope problem, audio-only problem, timeline export without timeline.json, package outside cwd, pre-created job dir · trigger: export render/timeline · observable: spec messages; audio-only succeeds with warning; `job directory exists — exports never reuse a job`
- [ ] 3.11 Test (test-plan E16): timeline spec mapping, see `src/__tests__/render.test.ts` — input: 12 segments incl. repeated shot_01 + image end-card, both clip layouts · trigger: exportTimeline · observable: ids seg-01…seg-12, absolute paths per layout, effective durationSec, sourceAudio.volume, xfade type, no voice/narration/bgm/subtitles keys
- [ ] 3.12 Test (test-plan X3): timeline media preflight, see `src/__tests__/render.test.ts` — input: --clips outside cwd, symlinked clip, missing shot_02 clip · trigger: exportTimeline · observable: `media outside working directory` / `media is a symlink` / missing list names shot_02, nothing written
- [ ] 3.13 Implement `src/export.ts` (`jobIdFor`, `buildRenderSpec`, `buildTimelineSpec` pure; `exportRender`/`exportTimeline` writers; `.pi-veo/exports.json` registry; frame/media preflight) and verify tests 3.1–3.12 pass
- [ ] 3.14 Test (test-plan E27): opt-in external contract, see `/tmp/pvg-sandbox/spike.mjs` pattern and `src/__tests__/render.test.ts` — input: outputs of 3.2/3.3/3.11 · trigger: load pi-video-gen dist from `PI_VIDEO_GEN_DIR`, run `runRender` with throwing rate-limiter gate for seedance-2.0 and kling-3.0, `parseTimelineSpec` on the timeline · observable: render reaches the gate, timeline parses; `it.skipIf(!process.env.PI_VIDEO_GEN_DIR)`; verify it passes locally with `PI_VIDEO_GEN_DIR=/tmp/pvg-sandbox/node_modules/@amaster.ai/pi-video-gen/dist`

## 4. Mux (`src/mux.ts`)

- [ ] 4.1 Test (test-plan E22): mux arg builder decision table, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: picture audio × VO × music × captions {soft,burn,none}, offset 1.5 · trigger: buildMuxArgs · observable: consistent input/-map indices, `adelay=1500`, volumes (music default 0.3), amix normalize=0 + alimiter + apad, `-t <picture dur>`, `-c:v copy` unless burn, `-c:s mov_text` for soft, captions-only has no audio filter and `-map 0:a?`
- [ ] 4.2 Test (test-plan E23): burn path safety, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: captions path `captions/vo,a:b;'c.srt`, --burn · trigger: runMux with injected runner · observable: runner cwd = temp dir holding `captions.srt`, filter arg exactly `subtitles=captions.srt`, all path args absolute, no shell option
- [ ] 4.3 Test (test-plan E24): output location and overwrite, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: no --out; `--out renders/final.mp4 --burn`; existing output with/without --force · trigger: runMux · observable: default `video_production/master/master.mp4`; `renders/final.mp4` relative to invoking cwd; `output exists — pass --force to overwrite`; --force replaces
- [ ] 4.4 Test (test-plan E25): mux input preconditions, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: no timeline.json, no VO/music/captions, missing --picture, only shot-scope problem · trigger: runMux · observable: spec messages; shot-scope → proceeds with warning
- [ ] 4.5 Test (test-plan X7): missing tools, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: runner ENOENT for ffmpeg, then ffprobe · trigger: runMux · observable: error names the tool, no output file
- [ ] 4.6 Test (test-plan X8): VO fit BVA, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: picture 10 s, VO 8 s, offset 1.99/2/2.01 · trigger: runMux with injected ffprobe · observable: 1.99 and 2 proceed; 2.01 fails naming both durations, nothing written
- [ ] 4.7 Test (test-plan X9): burn capability probe, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: `-filters` output without ` subtitles ` · trigger: runMux --burn · observable: fails naming the subtitles filter before any encode call
- [ ] 4.8 Test (test-plan X10): ffmpeg failure, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: runner exits 1 after partial temp write, 25 stderr lines · trigger: runMux · observable: no output file, temp removed, error contains exactly the last 10 stderr lines
- [ ] 4.9 Test (test-plan X11): no shell interpolation, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: in-package paths with `$(touch pwned)`, `;`, spaces · trigger: runMux · observable: literal argv entries, no shell:true, no `pwned` file
- [ ] 4.10 Implement `src/mux.ts` (`buildMuxArgs` pure, `runMux` with injectable runner, ffprobe duration/stream probe, SRT temp copy, temp-output + rename) and verify tests 4.1–4.9 pass
- [ ] 4.11 Test (test-plan E26): opt-in real-ffmpeg end-to-end, see `packages/video-transcription/src/__tests__/audio-decode.test.ts` — input: lavfi 6 s picture with tone, 3 s VO, 10 s music, SRT cue at 00:00:02,500, offset 2 · trigger: `pi-veo mux` · observable: ffprobe duration 6.0±0.05 s, video codec/resolution equal picture, one subtitle stream starting 2.5 s, audio RMS rises after 2 s; `it.skipIf` when ffmpeg/ffprobe absent

## 5. CLI wiring (`src/bin/veo.ts`)

- [ ] 5.1 Test (test-plan E20): dispatch decision table, see `packages/deck3d/src/check/__tests__/check.test.ts` — input: argv `export`, `export foo x`, `export render`, `export timeline <t>`, `bogus` · trigger: spawnSync bin · observable: exit 1 with usage naming both modes / `missing <target>` / `--clips <dir> is required` / usage listing six subcommands
- [ ] 5.2 Test (test-plan E21): flag parsing, see `packages/deck3d/src/check/__tests__/check.test.ts` — input: `--durations 4-15 --aspect 16:9,9:16 --clips renders --job j --picture p.mp4`, `--durations 8-4`, `--durations x` · trigger: parse args · observable: values stored; malformed → exit 1, stderr names `--durations`
- [ ] 5.3 Test (test-plan E19): parse exit code on sidecar problem, see `packages/deck3d/src/check/__tests__/check.test.ts` — input: sidecar package, shot sidecar missing prompt.action · trigger: `parse <target>` · observable: exit 1, problem printed
- [ ] 5.4 Test (test-plan E17): export report, see `packages/deck3d/src/check/__tests__/check.test.ts` — input: render export with one warning; `export timeline --json` · trigger: spawnSync bin · observable: human stdout has spec path, `video_render`, cwd used, outputDir note, warning; JSON has specPath, jobDir, tool `video_compose`, warnings
- [ ] 5.5 Implement `export <render|timeline>` + `mux` dispatch, new value flags, `--out` per-subcommand meaning, and mode-aware `Required target` in `src/bin/veo.ts`; verify 5.1–5.4 pass and existing `parse`/`plan`/`render`/`storyboard` tests stay green

## 6. Package metadata, skills, docs

- [ ] 6.1 Add optional `ffprobe` (`probe: "resolve"`) to `pi.tools` in `packages/video-production/package.json`; verify `npm test -w packages/shared` tool-registry ingestion tests pass
- [ ] 6.2 Update `veo-showreel-production-kit/SKILL.md`: emit `film.json`, `shots/shot_NN.json`, `timeline.json` from the same data as the markdown (schema summary, captions in output time base, VO/music levels); verify `pi-veo parse` on a sample package reports `sidecars.enabled` with zero problems
- [ ] 6.3 Update `veo-generator/SKILL.md`: pi-video-gen path (user-level `pi install npm:@amaster.ai/pi-video-gen`, verified 0.1.18), run exports from the pi session cwd, `video_capabilities` → `--durations/--aspect`, resume = re-call `video_render` never re-export, provider trade-offs (720p OpenRouter Veo, no seed), `export timeline` → `video_compose` → `mux`; verify the skill renders in `pi` skill list
- [ ] 6.4 Update `packages/video-production/README.md` usage block and `AGENTS.md` rows (`src/AGENTS.md`: sidecars.ts, export.ts, mux.ts; package `AGENTS.md`: package.json, skills) — verify rows present and path-alphabetical

## 7. Verify

- [ ] 7.1 Run `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` and verify zero failures; run the opt-in E26/E27 tests locally with ffmpeg and `PI_VIDEO_GEN_DIR` set and verify they pass
- [ ] 7.2 Run `npm run quality:changed` and verify no new Biome findings in touched files
