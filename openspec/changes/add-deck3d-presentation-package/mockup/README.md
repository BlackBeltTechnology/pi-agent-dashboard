# deck3d strategy lab (prototype)

Explore-mode prototype behind this change. Serve dir over HTTP (ESM imports) and open `deck3d-strategy-lab.html`; `C` toggles configurator, export button writes `deck.json` IR. Per-file record (moved from `mockups/AGENTS.md`):

| File | Purpose |
|---|---|
| `deck3d-strategy-lab.html` | Explore-mode strategy lab (v2) for the proposed `packages/deck3d` skill+CLI (markdown slides → self-contained three.js HTML deck). v2 = REAL 3D, not textured cards: titles are extruded PBR letters; diagrams are BUILT animated 3D objects (`DIAGRAMS.brain/loop/swarm` — nano-banana PNG becomes the builder's *design reference*, shown via `Show reference image`); photoreal stack toggles (glass/metal/matte `MeshPhysicalMaterial`, RoomEnvironment reflections, `Reflector` mirror floor + veil, PCF soft shadows, mode-aware bloom, accent rim spot); per-slide animated BACKGROUND scene filling the unused area (`BACKGROUNDS.tokens/rings/swarm/particles`, intensity slider); topology rail/orbit/tunnel with 40-unit spacing + distance culling so neighbour backgrounds don't bleed; transitions dolly/swing/fade/cut; reduced-motion; `dark`/`light` × palettes blackbelt/zenit/dapp/custom. Slides from `~/Documents/Projektek/AI-meetup/workspace/presentation-outline.md`; no API calls. `J` dumps the v2 `deck.json` IR. KEY FINDING: every three.js example `typeface.json` (helvetiker, droid, gentilis) has CORRUPT double-acute glyphs `ő ű Ő Ű` → earcut streaks + following letters vanish. Fix proven here: real TTF (Poppins-Bold) → opentype.js `getPath` → `ShapePath.toShapes` → `ExtrudeGeometry` (`ttfShapes()`); `Font` select keeps the broken path for comparison. Also: bloom threshold/strength must be mode-aware; glass slab transmission lowered in light mode for text contrast. CONFIG MODEL (v2.1): `DECK` defaults + `OV[slideId]` partial overrides → `S = eff(cur)`; panel has Deck/This-slide scope, ● marks overridden controls, deck-only keys (topology/transition/duration/idle/reduced) greyed in slide scope; 💾 Save/Reset persist to `localStorage['deck3d-lab-config']`; overrides surface in `deck.json` `slides[].override` (= the per-slide `3d:` frontmatter the package would write back). Configurator hidden by default — ⚙ button / `C` key. Z-FIGHT FIXES: ONE global floor (`Reflector` + veil) following the camera target instead of 5 coplanar 40×40 floors (also −4 reflector passes/frame); veil alphaMap ramps radially to opaque because Reflector ignores fog (horizon seam); camera near 0.5; shadow bias/normalBias; bullet plane `depthWrite:false` + renderOrder. Light mode: key/fill/exposure/bloom all mode-aware. MERMAID→3D (v2.2, slides `pipeline` flowchart + `seq` sequenceDiagram): no own parser — `harvestMermaid()` takes SEMANTICS from `mermaid.mermaidAPI.getDiagramFromText().db` (`getVertices/getEdges/getSubGraphs/getDirection`; `getActors/getMessages`) and LAYOUT from `mermaid.render()` SVG (`g.node` transform + getBBox, `path.flowchart-link` sampled via getPointAtLength; v11 ids are `<renderId>-flowchart-<node>-<n>` / `<renderId>-L_<from>_<to>_<k>`) → graph IR (`MERMAID_IR`, also emitted in `deck.json` `diagram.ir`). `buildFlowchart`: shape→primitive (stadium→capsule, hexagon/diamond→accent, circle→sphere), rank→depth relief staircase, edges→TubeGeometry along the dagre path + travelling pulse, thick/dotted styles, subgraphs→glass plates. `buildSequence`: actors→header slabs, time runs down+back (staircase), messages light up in order. Package plan: run harvest once at build time in headless chromium, bake IR, output HTML needs no mermaid.js. three.js/opentype/TTF/mermaid via CDN here ONLY; the package inlines + subsets. TEMPORARY: to move into the change's `openspec/changes/<name>/` dir once scaffolded. |
