## 1. Package scaffold

- [x] 1.1 Create `packages/deck3d/` (package.json `@blackbelt-technology/pi-dashboard-deck3d`, `bin: deck3d`, `pi.skills`, tsconfig, vitest.config) modelled on `packages/video-production`; verify `pnpm install` links it and `pnpm -F @blackbelt-technology/pi-dashboard-deck3d test` runs an empty suite green.
- [x] 1.2 Vendor `assets/Poppins-Bold.ttf` (OFL) + LICENSE note; add `three`, `opentype.js`, `mermaid@11` (exact), `ajv`, `esbuild` deps; verify `pnpm install` is clean and `pnpm-lock.yaml` has the exact mermaid pin.
- [x] 1.3 Scaffold `packages/deck3d/AGENTS.md` (per-file rows) and add the package row to `packages/AGENTS.md`; verify `kb dox lint` reports no missing rows for the new directory.

## 2. IR schema + merge (spec: deck3d-ir)

- [x] 2.1 Write `src/ir/schema.json` with `description` on every field (defaults, slides, diagram graph IR, overrides keyed by slide/node/edge id incl. `diagram.scale|offset`, `camera.distance`, `labels.size`, `check.ignore`) and `src/ir/validate.ts` (ajv, unknown-field rejection, dangling-id check); tests: valid fixture passes, `depthRelief:"high"` fails with JSON path, unknown key fails.
- [x] 2.2 Write `src/ir/ids.ts` (slide slug+ordinal, node ids from mermaid, `m<i>` messages) and `src/ir/merge.ts` (derive → deep-merge overrides → warn on orphan override ids); tests: orphan override warns + exit 0, override on existing id applies.
- [x] 2.3 Write `scripts/gen-ir-fields.ts` producing `.pi/skills/deck3d/reference/ir-fields.md` from schema descriptions; test: generated file lists every schema property; add to package `build`.
- [x] 2.4 Implement derived-field-edit detection in `validate` (diff merged IR vs fresh derive from `meta.source` when the source is present); test: edited node `x` outside `overrides` → warning with path.

## 3. Markdown grammar (spec: deck3d-skill)

- [x] 3.1 Write `src/parse/markdown.ts`: front-matter defaults, `# Title` slide split, subtitle paragraph, `-` bullets, ```mermaid block capture, `<!-- deck3d: {...} -->` inline overrides; tests: fixture deck parses to expected slides; invalid inline JSON exits non-zero naming the slide.

## 4. Mermaid harvest (spec: deck3d-mermaid-harvest)

- [x] 4.1 Build `src/parse/harvest/harness.html` + esbuild bundle of mermaid@11; `src/parse/harvest/index.ts` launches Playwright chromium, runs `getDiagramFromText().db` + `render()`, matches node ids `<renderId>-flowchart-<id>-<n>` and edge paths `L_<from>_<to>`, samples paths, normalises coords; prints install hint and exits non-zero when chromium is missing (test).
- [x] 4.2 Flowchart mapping: shapes `rect|stadium|round|hexagon|circle|doublecircle|diamond`, edge kinds `normal|dotted|thick` + labels, subgraph groups, direction; fixture test (`skipIf(!chromium)`) asserts node/edge/group counts, shapes, kinds, Hungarian labels byte-equal.
- [x] 4.3 Sequence mapping: actors in order, messages with from/to/text/kind in source order; fixture test as above.
- [x] 4.4 Unsupported diagram type → warning + `diagram: none` + exit 0 (test with `gantt`).
- [x] 4.5 Determinism: parse the fixture deck twice in one test, assert byte-identical `deck.json`; assert no timestamp/path fields via schema `additionalProperties:false`.

## 5. Runtime port (spec: deck3d-render)

- [ ] 5.1 Port `src/runtime/text/` from the lab: opentype → ShapePath → ExtrudeGeometry for titles; canvas label with `P.bg` outline, `toneMapped:false`, `depthWrite:false`, front-of-surface offset (`h/2` for round shapes); unit test (jsdom/canvas mock) that label canvas width scales with text and outline colour = palette bg.
- [ ] 5.2 Port `materials/`, `scene/` (shared floor, fog, bloom thresholds per mode, shadow bias, lights), `camera/` (rail/dolly, hash deep-link, neighbour culling), `loop.ts` (rAF + `setTimeout` fallback on `document.hidden`); verify by building fixture deck and taking a headless screenshot after transition — camera arrived (distance < 0.5).
- [ ] 5.3 Port `builders/flowchart.ts` + `builders/sequence.ts` (grouped message = tube+head+label, travelling pulse, fixed 1.1 s period) + `builders/{brain,loop,swarm}.ts` + `backgrounds/` scene library; verify snapshot of slides 5 and 6 of the fixture visually matches the lab screenshots (`/tmp/s1.png`, `/tmp/s2.png` archived into `fixtures/reference/`).
- [x] 5.4 Quality tiers: `low|medium|high` toggling bloom, reflector, shadow map size, particle count; test: `low` renders with no bloom pass and reflector absent (runtime unit test on scene graph).

## 6. Renderer (spec: deck3d-render)

- [x] 6.1 `src/render/index.ts`: template + `dist/runtime.js` (esbuild, fixed options, no hashes) + sorted-key IR JSON + subset Poppins (glyph set from merged IR) as base64; test: render twice → byte-identical; output contains no `mermaid` reference and no `http(s)://` resource URLs.
- [x] 6.2 Font subset: compute glyph set from all merged IR strings (titles, bullets, labels incl. overrides); test: override adding `ű` yields a subset containing that glyph.
- [x] 6.3 Offline check test: serve `deck.html` with Playwright `route` blocking all network, assert no failed requests and slide 1 title mesh count > 0.

## 7. CLI + snapshot (spec: deck3d-skill)

- [x] 7.1 `src/cli.ts`: `parse | validate | render | build | check | snapshot`, exit codes, one-line stderr reasons; tests: `build` emits `.json` beside `.html` equal to `parse` output; `--help` exits 0.
- [ ] 7.2 `snapshot <html> [--slide n] [-o png]`: Playwright headless, hash deep-link, wait for camera arrival; test (`skipIf(!chromium)`): PNG written, non-black centre pixel.

## 7c. Check (spec: deck3d-render — measurement hook + fit/legibility check)

- [x] 7c.1 Runtime `window.__deck3d = { gotoSlide, setTime, ready, measure, peaks }`: deterministic clock replaces `performance.now` when set; `measure()` projects each labelled object's `Box3` corners to CSS px, reports label cap height (canvas plane height × projection), kind/id/text, raycast first hit; `peaks()` returns animation peak times per slide (pulse/lift). Test (jsdom-free, headless `skipIf(!chromium)`): two `measure()` calls at same slide/time deep-equal.
- [x] 7c.2 `src/check/rules.ts` pure functions over measurements: fit (safe margin 4 %), legibility (14 px @1080 scaled), overlap (IoU > 0.1), occlusion (hit ≠ self/own node), contrast (≥ 3:1 from sampled pixels); each returns `{severity, slide, id, text, measured, threshold, suggest}`; unit tests per rule with hand-built measurement fixtures, incl. suggestion keys.
- [x] 7c.3 `check <html> [--viewport ...] [--slide n] [--strict] [-o report.json]`: Playwright at dpr 1 per viewport, per slide at `t=0` + each peak, `readPixels`-style sampling via `canvas.toDataURL` crop for contrast; JSON report grouped by viewport; one stderr line per finding; exit codes per spec. Tests (`skipIf(!chromium)`): fixture deck with a deliberately oversized flowchart → fit error + `slides[n].diagram.scale` suggestion, non-zero exit; clean fixture → zero findings, exit 0; `--strict` promotes a legibility warn to failure.
- [x] 7c.4 `build` runs `check` after render, prints findings, exits 0 unless `--strict`; tests: build with findings exits 0 and writes html; `--strict` exits non-zero.
- [x] 7c.5 `snapshot` reuses `__deck3d.ready()` instead of a fixed wait (from 7.2); test unchanged.

## 7b. Props (spec: deck3d-props)

- [x] 7b.1 Extend `schema.json` with `overrides.props[]` (source, id, licence, author, sha256, slide, role, size, restyle, anim); tests: unknown role fails, `node:Foo` without node fails naming prop + id, missing sha256 fails, >5 props warns with totals but exits 0.
- [x] 7b.2 Vendor `assets/props/` CC0 subset (~30 Kenney/Quaternius GLBs ≤300 KB each, tagged `manifest.json`, LICENSES); `src/props/search.ts` vendored source; test: `props search robot` returns manifest hits sorted vendored-first, writes no file.
- [x] 7b.3 Poly Pizza source (`POLY_PIZZA_KEY`, `api.poly.pizza/v1.1/search`): parse to candidate rows; recorded-response fixture test; unreachable/unset key → vendored-only with one-line notice (test).
- [x] 7b.4 `src/props/fetch.ts`: download to `.deck3d/props/<source>-<id>.glb`, glTF magic/MIME check, size cap (default 8 MB), sha256 write-if-absent / fail-if-differs, slug-only cache names; tests with a local HTTP fixture server: mismatch exits non-zero, oversized refused, non-glTF refused.
- [ ] 7b.5 Runtime `props/`: GLTFLoader + meshopt, Box3 normalise to `size`, ground, face camera, anim presets; `restyle: palette` material swap vs `original`; roles `hero|illustration|ambient|node:<id>` (node role keeps label + edge endpoints); test: node role renders model and edges still terminate at node bbox; two `palette` props share material class.
- [ ] 7b.6 Render embeds props base64 and refuses hash mismatch; credits slide generated for non-CC0/non-generated licences; tests: CC-BY prop → final slide titled Credits with author; CC0-only → no credits slide; offline-open test from 6.3 extended with a prop.
- [ ] 7b.7 `src/props/generate.ts`: `props generate --from-image <img> --name <n>` shelling to `python3 -c` `gradio_client` `tencent/Hunyuan3D-2` `/shape_generation` (extract `r[0]['value']`), store as `generated` licence with `restyle: palette`, print override entry; test: missing `gradio_client` → non-zero with install hint (mock the spawn).

## 7d. Effects corpus (spec: deck3d-effects)

- [x] 7d.1 `src/fx/types.ts` interface + `meta.schema.json` (kind, tags, cost, modes, params schema, conflicts, source, licence SPDX enum of permissive ids); `src/fx/index.ts` registry; corpus test: every `*.ts` has a card, every card validates, licence permissive, `source` is a URL.
- [ ] 7d.2 Port the mockup's effects to modules + cards: `tokens rings swarm particles` (bg), `bloom film` (post), `glass metal emissive` (material), `mirror-floor fog soft-shadows room-ibl` (staging/light), `signal-pulse` (edge), `dolly` (transition); test: strategy-lab fixture renders byte-identical before/after the port.
- [ ] 7d.3 Add permissive ports with cards: backgrounds `starfield aurora grid-horizon hex-grid data-columns glyph-rain constellation`; post `vignette chromatic-aberration depth-of-field god-rays n8ao selective-bloom smaa` (via `postprocessing`/`n8ao`, wrapped behind the `fx` interface); materials `holo-fresnel wireframe-overlay iridescent matcap`; lights `lightformers accent-cycle volumetric-spot`; motion `float orbit stagger-reveal trail camera-drift`; edges `dashed-flow glow-tube particle-stream`; transitions `fade iris flythrough`. Test per kind: `fx preview <id>` produces a non-black PNG (`skipIf(!chromium)`).
- [x] 7d.4 `src/fx/catalogue.ts` → `reference/effects.md` (section per effect, params table, thumbnail path) + cards compiled into IR schema `effects[]` param bounds; tests: catalogue hash matches cards (fails when stale); `validate` rejects out-of-range param naming the path.
- [x] 7d.5 `parse` deterministic defaults: static table `src/fx/defaults.ts` (keyword/diagram-kind → content tags → cheapest effect per kind); tests: sequence slide gets `timeline` bg + `sequence` edge identically twice; override replaces list and survives re-parse.
- [x] 7d.6 `render` composition: conflicts → fail naming ids+slide; mode-incompatible → skip + warn; cost sum vs quality budget (6/12/20) → warn; `check` reports skipped + `warn budget`. Unit tests per rule.
- [ ] 7d.7 CLI `fx list [--kind] [--tag] [--json]`, `fx preview <id> [-o png]`; tests: tag+kind filter output; `--json` parses.
- [x] 7d.8 Skill section "Contributing an effect" (search → licence check → port → card → preview → regenerate catalogue → corpus test) with the inspiration-only list (LYGIA, Shadertoy); test: SKILL.md mentions every step and the SPDX allow-list.

## 8. Skill

- [x] 8.1 Write `.pi/skills/deck3d/SKILL.md`: when to use, markdown grammar, the tune loop (parse → validate → render → check → fix suggested keys → snapshot → edit `overrides` only → repeat), styling via `fx list` + `overrides.slides[n].effects`, forbidden edits (HTML, derived fields), pitfalls (chromium prerequisite, typeface.json corruption, bloom vs labels, mermaid pin); the prop loop (keywords from slide content → `props search` → pick by relevance then style, prefer one pack + `palette` → write `overrides.props[]` → `props fetch` → render/snapshot), licence rules; link `reference/ir-fields.md`; verify skill loads in a pi session (`/skills` lists `deck3d`).
- [x] 8.2 Add a worked example under `.pi/skills/deck3d/examples/` — fixture deck, its `deck.json` with three overrides (one slide `mode:light`, one node shape change, one vendored prop with `role: node:LLM`), before/after snapshots; verify `deck3d build` on the example reproduces the committed `deck.json`.

## 9. Parity, retire lab, docs

- [ ] 9.1 Create `fixtures/strategy-lab.md` reproducing the lab's seven slides (incl. Hungarian titles and both mermaid blocks); build it; compare snapshots to the lab side by side and record deviations in `design.md` Open Questions or fix; verify all slides snapshot without console errors.
- [ ] 9.2 Move the lab's learnings (font corruption, mermaid id scheme, hidden-tab loop, label outline) from `mockup/README.md` into `packages/deck3d/AGENTS.md`; the `mockup/` dir archives with the change; verify `kb dox lint` clean.
- [ ] 9.3 DocScribe: `packages/deck3d/README.md` (install, grammar, CLI, tune loop) + `docs/architecture.md` pointer row; verify links resolve (`scripts/check-conventions.mjs`).
- [ ] 9.4 Full test run `set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log` green; `npm run quality:changed` clean; `review-code` pass on the diff.

## 9b. Test infra (from test-plan.md "New infra needed")

- [x] 9b.1 `src/__tests__/helpers/chromium.ts`: `chromiumAvailable()` + shared `launch()` fixture; every browser-driving suite uses `describe.skipIf(!chromiumAvailable())` (exemplar: `packages/document-converter/src/__tests__/integration.test.ts`)
- [ ] 9b.2 CI: `ci.yml` step `npx playwright install chromium --with-deps` scoped to the deck3d test job so the L1‑browser rows run in CI; the no-chromium path stays covered by X1/X2 on the plain job
- [ ] 9b.3 Env test hooks `DECK3D_HARVEST_TIMEOUT_MS`, `DECK3D_CHECK_TIMEOUT_MS`, `DECK3D_HTTP_TIMEOUT_MS`, `DECK3D_HARVEST_STALL` (defaults 60000/120000/10000/unset); documented in `packages/deck3d/AGENTS.md`
- [ ] 9b.4 Runtime read-only debug surface `__deck3d.effects()` (active composer pass ids + skipped list) and `__deck3d.debug.titleGlyphs()` (glyph mesh count) for E20/F4
- [ ] 9b.5 In-test mock HTTP server helper `src/__tests__/helpers/mock-http.ts` (exemplar: `packages/extension/src/__tests__/server-probe.test.ts`) for props search/fetch rows

## 10. Tests folded from test-plan.md (one task per manifest row; ship-it authors them)

Exemplars (copy harness glue from): L1 → `packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts` (CLI spawn), `packages/kb/src/__tests__/kb.test.ts` (fixtures/snapshots), `packages/extension/src/__tests__/server-probe.test.ts` (in-test server); L1‑browser → `packages/document-converter/src/__tests__/integration.test.ts` (`describe.skipIf` opt-in gate) + `tests/e2e/asciidoc-preview.spec.ts` (Playwright page API only — no docker harness).

- [ ] 10.1 E1 chromium-gated vitest `packages/deck3d/src/ir/__tests__/e1-*.test.ts` — ir: Parse is deterministic; input: `fixtures/strategy-lab.md` (7 slides, 2 mermaid) · trigger: `parse` twice into two files in one test · observable: `sha256(a) === sha256(b)`; `deck.json` contains no key matching `/time|date|path|Date/`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E1)
- [ ] 10.2 E2 vitest `packages/deck3d/src/ir/__tests__/e2-*.test.ts` — ir: Parse is deterministic (prior overrides input); input: same md; case A: no `-o` target; case B: target with `overrides.slides["intro"].mode="light"`; case C: B + `--fresh` · trigger: `parse -o out.json` · observable: A and C byte-identical; B differs only inside `overrides` (deep-diff outside `overrides` is empty). Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E2)
- [ ] 10.3 E3 vitest `packages/deck3d/src/ir/__tests__/e3-*.test.ts` — design D1 slide ids; input: md with titles `Ágensrajok`, `Agensrajok`, `Agensrajok`, `` (empty heading), `# Final {#pin}` · trigger: `parse` · observable: ids `agensrajok`, `agensrajok-2`, `agensrajok-3`, `slide`, `pin` in order; every id matches `/^(?!deck3d-)[a-z0-9-]+$/`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E3)
- [ ] 10.4 E4 vitest `packages/deck3d/src/__tests__/e4-*.test.ts` — skill: Markdown grammar (zero headings); input: md body `hello\n- a\n- b` with no `#` · trigger: `parse` · observable: exit 0; `slides.length === 1`; `slides[0].id === "slide"`; bullets `["a","b"]`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E4)
- [ ] 10.5 E5 vitest `packages/deck3d/src/ir/__tests__/e5-*.test.ts` — ir: Overrides survive re-parse; input: target with `overrides.nodes["arch/A"].shape="hexagon"`; md edited so slide `arch` gains one bullet · trigger: `parse -o target` · observable: derived bullet count +1; `overrides.nodes["arch/A"].shape === "hexagon"` unchanged; exit 0. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E5)
- [ ] 10.6 E6 vitest `packages/deck3d/src/ir/__tests__/e6-*.test.ts` — ir: Override target vanished (orphan = warn); input: target with `overrides.nodes["arch/Z"]` where node `Z` removed from mermaid · trigger: `parse` then `validate` · observable: parse exit 0; stderr line `warn orphan override overrides.nodes["arch/Z"]`; `validate` exit 0 with same warning; key still present in file. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E6)
- [ ] 10.7 E7 vitest `packages/deck3d/src/ir/__tests__/e7-*.test.ts` — ir: Title rename without pin; input: target with `overrides.slides["old-title"]`; md title renamed · trigger: `parse` · observable: exit 0; warning names `old-title` and suggests `{#old-title}`; `slides[]` has new slug; override kept. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E7)
- [ ] 10.8 E8 vitest `packages/deck3d/src/ir/__tests__/e8-*.test.ts` — ir: Inline override clobbers deck.json (precedence); input: md slide `<!-- deck3d: {"mode":"light"} -->`; target `overrides.slides[id].mode="dark"`, `.scene="orbits"` · trigger: `parse` · observable: result `mode:"light"`, `scene:"orbits"`; warning names key `overrides.slides["<id>"].mode`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E8)
- [ ] 10.9 E9 vitest `packages/deck3d/src/ir/__tests__/e9-*.test.ts` — ir: Overrides grammar — arrays replace, objects merge; input: derived `slides[id].effects=["tokens","fog"]`; overrides `slides[id].effects=["starfield"]` and `slides[id].camera={distance:9}` · trigger: in-memory `merge(ir)` · observable: merged effects `["starfield"]` exactly; camera `{…derived, distance:9}`; persisted `slides[]` unchanged (derived-only). Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E9)
- [ ] 10.10 E10 vitest `packages/deck3d/src/ir/__tests__/e10-*.test.ts` — ir: Edit outside overrides detected (`derivedHash`); input: valid `deck.json`; case A: edit `slides[0].title`; case B: edit `overrides.slides[id].mode` · trigger: `validate` · observable: A: warning `edited outside overrides` + exit 0; B: no such warning. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E10)
- [ ] 10.11 E11 vitest `packages/deck3d/src/ir/__tests__/e11-*.test.ts` — ir: IR is schema-validated; input: `overrides.slides[id].camera.distance = "far"`; `overrides.nodes[k].colour = "#fff"` (unknown key) · trigger: `render` · observable: exit non-zero; stderr contains JSON path `overrides.slides["<id>"].camera.distance` and `expected number`; second case names `colour` as unknown; no `.html` written. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E11)
- [ ] 10.12 E12 chromium-gated vitest `packages/deck3d/src/ir/__tests__/e12-*.test.ts` — ir: Reworded label; input: `overrides.nodes["arch/A"].label = "Ügyfél"` · trigger: `render` → `measure()` on slide `arch` · observable: measurement row with `id:"A"` has `label:"Ügyfél"`; font subset in html contains glyph `Ü`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E12)
- [ ] 10.13 E13 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/e13-*.test.ts` — harvest: Flowchart shapes + edge kinds; input: fixture flowchart using `[rect] ([stadium]) ((circle)) {diamond} {{hex}} (((double)))` and edges `--> -.-> ==>` with one label · trigger: `parse` · observable: node `shape` fields exactly `rect,stadium,circle,diamond,hexagon,doublecircle`; edge kinds `normal,dotted,thick`; edge label text preserved. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E13)
- [ ] 10.14 E14 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/e14-*.test.ts` — harvest: Node ids stable / edge ids with underscores; input: flowchart nodes `L_A`, `B_C`, edge `L_A --> B_C` · trigger: `parse` twice · observable: edge `{from:"L_A", to:"B_C"}` exactly once; identical across runs. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E14)
- [ ] 10.15 E15 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/e15-*.test.ts` — harvest: Subgraph membership; input: `subgraph Core … end` with 2 nodes · trigger: `parse` · observable: both nodes carry `group:"Core"`; others have no `group`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E15)
- [ ] 10.16 E16 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/e16-*.test.ts` — harvest: Sequence converted; input: sequence with actors `U,S,D` and 5 messages (incl. one `-->>` dotted, one self-message `S->>S`) · trigger: `parse` · observable: actors in declaration order; `messages.length===5`; ids `m0..m4`; kinds preserved; self-message `from===to`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E16)
- [ ] 10.17 E17 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/e17-*.test.ts` — harvest: Hungarian text round-trips; input: node label `Felhasználó őrült űrhajó` · trigger: `parse` · observable: label byte-equal in `deck.json` (NFC). Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E17)
- [ ] 10.18 E18 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/e18-*.test.ts` — harvest: Unsupported type; input: slide with ```mermaid `gantt` · trigger: `parse` · observable: exit 0; `slides[i].diagram === "none"`; stderr `warn unsupported diagram gantt slide <id>`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E18)
- [ ] 10.19 E19 chromium-gated vitest `packages/deck3d/src/render/__tests__/e19-*.test.ts` — render: Deep link (1-based) + credits index; input: 3-slide deck, one CC-BY prop · trigger: open `deck.html#3`, `#4`, `#0`, `#5` · observable: `#3` → slide 3; `#4` → `deck3d-credits`; `#0` and `#5` → slide 1 (clamped), no console error. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E19)
- [ ] 10.20 E20 chromium-gated vitest `packages/deck3d/src/render/__tests__/e20-*.test.ts` — render: Quality tiers vs explicit override; input: rows: quality ∈ {low,medium,high} × overrides.effects ∈ {[], ["bloom"]} · trigger: `render` → inspect runtime `__deck3d.effects()` (composer pass list) · observable: low+[] → no bloom pass; low+["bloom"] → bloom pass present + stderr `warn budget`; medium/high+[] → bloom present. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E20)
- [ ] 10.21 E21 vitest `packages/deck3d/src/fx/__tests__/e21-*.test.ts` — effects: Composition budget; input: `quality:low`, deck effects cost 2 + slide effects cost {3, 4, 7} · trigger: `render` · observable: sums 5, 6 → no warning; 9 → stderr `warn budget slide <id> 9 > 6` and `check` report row `warn budget`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E21)
- [ ] 10.22 E22 vitest `packages/deck3d/src/fx/__tests__/e22-*.test.ts` — effects: Conflict; input: slide effects `["depth-of-field","god-rays"]` (cards conflict) · trigger: `render` · observable: exit non-zero; stderr names both ids and the slide id; no html. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E22)
- [ ] 10.23 E23 vitest `packages/deck3d/src/fx/__tests__/e23-*.test.ts` — effects: Mode-incompatible skipped; input: slide `mode:light`, effect with `modes:["dark"]` · trigger: `render` · observable: exit 0; stderr `warn skipped <id> (dark only) slide <sid>`; effect absent from runtime list. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E23)
- [ ] 10.24 E24 vitest `packages/deck3d/src/fx/__tests__/e24-*.test.ts` — effects: Card drives validation; input: `starfield` params `density` ∈ {min−1, min, max, max+1} · trigger: `validate` · observable: min/max exit 0; min−1 and max+1 exit non-zero naming `overrides.slides["<id>"].effects[0].params.density`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E24)
- [ ] 10.25 E25 vitest `packages/deck3d/src/fx/__tests__/e25-*.test.ts` — effects: Deterministic defaults; input: slides: title only; flowchart; sequence; kicker with word "security" · trigger: `parse` · observable: each slide's `slides[].effects` equals the table row for its kind/keyword; two runs identical. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E25)
- [ ] 10.26 E26 vitest `packages/deck3d/src/fx/__tests__/e26-*.test.ts` — effects: Override replaces defaults; input: slide with derived defaults; `overrides.slides[id].effects=["aurora"]` · trigger: `parse` → merge · observable: merged list `["aurora"]` only; re-parse keeps override. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E26)
- [ ] 10.27 E27 vitest `packages/deck3d/src/fx/__tests__/e27-*.test.ts` — effects: Corpus size and licence audit; input: `src/fx/*.meta.json` · trigger: corpus test · observable: ≥ 15 mockup ids + every listed port id present; every `licence ∈ {MIT, Zlib, CC0-1.0, BSD-2/3, OFL-1.1}`; every `source` is `https://`; `reference/effects.md` regenerated equals committed. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E27)
- [ ] 10.28 E28 vitest `packages/deck3d/src/fx/__tests__/e28-*.test.ts` — effects: Non-permissive licence rejected; input: card with `licence:"CC-BY-NC-SA-4.0"` · trigger: corpus/schema test · observable: schema rejects card naming the licence enum. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E28)
- [ ] 10.29 E29 vitest `packages/deck3d/src/props/__tests__/e29-*.test.ts` — props: Prop selection lives in overrides — dangling node role; input: `overrides.props[0].role="node:Foo"`, no node Foo · trigger: `validate` then `render` · observable: validate exit 0 + warning naming prop + `Foo`; render exit 0; prop absent from embedded list. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E29)
- [ ] 10.30 E30 vitest `packages/deck3d/src/props/__tests__/e30-*.test.ts` — props: Hash mismatch; input: cached GLB byte-flipped after `fetch` · trigger: `props fetch` (verify mode) and `render` · observable: both exit non-zero naming the prop; no html written. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E30)
- [ ] 10.31 E31 vitest `packages/deck3d/src/props/__tests__/e31-*.test.ts` — props: Oversized model (inclusive MiB cap); input: mock server bodies of 8,388,608 and 8,388,609 bytes · trigger: `props fetch` · observable: first accepted (cache file exists); second refused: stderr has actual size, cap, exit non-zero, no cache file. Exemplar: see packages/extension/src/__tests__/server-probe.test.ts (in-test server) + grep-prelude.test.ts (CLI spawn) (test-plan #E31)
- [ ] 10.32 E32 vitest `packages/deck3d/src/props/__tests__/e32-*.test.ts` — props: Prop budget warning; input: 5 props / 10,485,760 B total vs 6 props; vs 5 props / +1 B · trigger: `validate` · observable: 5 props exactly at cap: no warning; 6 props: warning with count+bytes, exit 0; over bytes: warning, exit 0. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E32)
- [ ] 10.33 E33 vitest `packages/deck3d/src/props/__tests__/e33-*.test.ts` — props: `props fetch` writes the entry; input: mock source, empty overrides · trigger: `props fetch mock robot --slide intro --role illustration` · observable: `overrides.props[0]` has `source,id,licence,author,sha256,slide,role`; `validate` exit 0 with no warning. Exemplar: see packages/extension/src/__tests__/server-probe.test.ts (in-test server) + grep-prelude.test.ts (CLI spawn) (test-plan #E33)
- [ ] 10.34 E34 chromium-gated vitest `packages/deck3d/src/props/__tests__/e34-*.test.ts` — props: Licence credits automatic; input: props with licences CC0, CC-BY-4.0, generated · trigger: `render` · observable: CC0-only → no credits slide; CC-BY present → last slide id `deck3d-credits` listing author+licence; not present in `deck.json`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E34)
- [ ] 10.35 E35 chromium-gated vitest `packages/deck3d/src/render/__tests__/e35-*.test.ts` — render: JSON inlined safely; input: node label `</script><b>x` and label with U+2028 · trigger: `render` → open · observable: page has no console error; `measure()` returns label text equal to input. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E35)
- [ ] 10.36 E36 vitest `packages/deck3d/src/render/__tests__/e36-*.test.ts` — render: Font subset from merged text; input: derived text without `ű`; override label adds `ű` · trigger: `render` twice · observable: subset size grows; second html contains glyph `ű`; first does not. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E36)
- [ ] 10.37 E37 vitest `packages/deck3d/src/check/__tests__/e37-*.test.ts` — check: Legibility scaled by viewport; input: measurements with cap heights 14, 13.99 at 1080; 9.34, 9.33 at 720 · trigger: `rules.legibility` · observable: 14 pass / 13.99 warn; 9.34 pass / 9.33 warn (threshold 14·720/1080 = 9.33̅). Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E37)
- [ ] 10.38 E38 vitest `packages/deck3d/src/check/__tests__/e38-*.test.ts` — check: Overlap IoU; input: two rects IoU 0.1 and 0.1001 · trigger: `rules.overlap` · observable: 0.1 → no finding; 0.1001 → `error overlap` naming both ids. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E38)
- [ ] 10.39 E39 vitest `packages/deck3d/src/check/__tests__/e39-*.test.ts` — check: Fit safe margin; input: 1920×1080, margin 4 % → right edge limit 1843.2; rect right 1843, 1844 · trigger: `rules.fit` · observable: 1843 pass; 1844 `error fit … right 1844px > 1843px` with suggestion `overrides.slides["<id>"].diagram.scale`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E39)
- [ ] 10.40 E40 vitest `packages/deck3d/src/check/__tests__/e40-*.test.ts` — check: Suggestions are override keys; input: every finding kind × sample measurement · trigger: `rules.*` · observable: every `suggestion` matches `/^overrides\./` and resolves in `schema.json`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E40)
- [ ] 10.41 E41 chromium-gated vitest `packages/deck3d/src/check/__tests__/e41-*.test.ts` — check: Default viewports + two-viewport grouping; input: fixture html; run without `--viewport`, then `--viewport 1920x1080` · trigger: `check -o r.json` · observable: default report has viewport groups `1920x1080` and `1280x720`; explicit run has one group. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E41)
- [ ] 10.42 E42 chromium-gated vitest `packages/deck3d/src/check/__tests__/e42-*.test.ts` — check: Clean deck; input: `fixtures/strategy-lab.md` built · trigger: `check` · observable: zero findings; exit 0. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E42)
- [ ] 10.43 E43 vitest `packages/deck3d/src/__tests__/e43-*.test.ts` — skill: Grammar error; input: slide with `<!-- deck3d: {mode: light} -->` (invalid JSON) · trigger: `parse` · observable: exit non-zero; stderr names slide id and JSON error position. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E43)
- [ ] 10.44 E44 chromium-gated vitest `packages/deck3d/src/__tests__/e44-*.test.ts` — skill: One-shot build; input: `talk.md` · trigger: `build talk.md -o talk.html` · observable: `talk.json` + `talk.html` exist; `talk.json` byte-equal to `parse talk.md`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #E44)
- [ ] 10.45 E45 vitest `packages/deck3d/src/__tests__/e45-*.test.ts` — skill: Skill and CLI ship together; input: package.json · trigger: test · observable: `pi.skills` lists `.pi/skills/deck3d`; `bin.deck3d` resolves; `SKILL.md` frontmatter `name: deck3d`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E45)
- [ ] 10.46 E46 vitest `packages/deck3d/src/ir/__tests__/e46-*.test.ts` — ir: Field reference documented; input: `schema.json` descriptions · trigger: generated `reference/ir-fields.md` vs committed · observable: equal; every schema leaf appears once. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E46)
- [ ] 10.47 E47 vitest `packages/deck3d/src/fx/__tests__/e47-*.test.ts` — effects: Catalogue in sync; input: cards · trigger: generated `reference/effects.md` vs committed · observable: equal; every id present. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #E47)
- [ ] 10.48 P1 chromium-gated vitest `packages/deck3d/src/render/__tests__/p1-*.test.ts` — render: Build budget; input: `build fixtures/strategy-lab.md` (7 slides, 2 mermaid, no props) · trigger: wall time after chromium launch ≤ 20 s · observable: single run, CI runner. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #P1)
- [ ] 10.49 P2 vitest `packages/deck3d/src/render/__tests__/p2-*.test.ts` — render: Build budget (size); input: same output · trigger: `stat(deck.html).size ≤ 2,621,440` · observable: single run. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #P2)
- [ ] 10.50 P3 vitest `packages/deck3d/src/render/__tests__/p3-*.test.ts` — render: Render is deterministic; input: same `deck.json` · trigger: 3 renders byte-identical and each < 5 s (no browser) · observable: single test. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #P3)
- [ ] 10.51 F1 chromium-gated vitest `packages/deck3d/src/render/__tests__/f1-*.test.ts` — render: Measurement is stable; input: fixture html, slide `arch`, `setTime(1.7)` · trigger: `await ready(); measure()` ×2 · observable: deep-equal results; every rect finite. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F1)
- [ ] 10.52 F2 chromium-gated vitest `packages/deck3d/src/render/__tests__/f2-*.test.ts` — render: Render loop tolerates hidden tabs; input: headless page, `document.hidden === true` · trigger: `setTime(0)` → `setTime(2.2)` → `measure()` · observable: animated message rect differs between t values (loop advanced while hidden). Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F2)
- [ ] 10.53 F3 chromium-gated vitest `packages/deck3d/src/render/__tests__/f3-*.test.ts` — render: Lifted message leaves frame at peak; input: sequence slide with `overrides.slides[id].diagram.scale=2.5` · trigger: `check` · observable: `error fit` row with `t` equal to one of `peaks()` values, not `t=0`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F3)
- [ ] 10.54 F4 chromium-gated vitest `packages/deck3d/src/render/__tests__/f4-*.test.ts` — render: Hungarian titles render intact; input: title `Ágensrajok űrhajó őre` · trigger: render → runtime `__deck3d.debug.titleGlyphs()` · observable: glyph mesh count equals non-space char count (22); no `earcut` console warning. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F4)
- [ ] 10.55 F5 chromium-gated vitest `packages/deck3d/src/render/__tests__/f5-*.test.ts` — render: Label on round node; input: stadium node `Felhasználó` · trigger: `measure()` · observable: label rect fully inside viewport and `occlusion` rule finds no hit other than own node. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F5)
- [ ] 10.56 F6 chromium-gated vitest `packages/deck3d/src/render/__tests__/f6-*.test.ts` — render: Light and dark labels; input: same slide `mode:dark` / `mode:light` · trigger: `measure()` label colour + outline · observable: dark: fill `P.fg`, outline `P.bg`; light: inverted; both `toneMapped:false`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F6)
- [ ] 10.57 F7 chromium-gated vitest `packages/deck3d/src/render/__tests__/f7-*.test.ts` — render: Message order animation; input: sequence with 5 messages, pulse period 1.1 s · trigger: `setTime(k·1.1)` for k=0..4 · observable: lifted group id at step k is `m<k>`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F7)
- [ ] 10.58 F8 chromium-gated vitest `packages/deck3d/src/render/__tests__/f8-*.test.ts` — render: Offline open; input: `deck.html` served with Playwright `route` aborting every non-document request · trigger: open + `ready()` · observable: zero failed requests; slide 1 title glyphs > 0. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F8)
- [ ] 10.59 F9 chromium-gated vitest `packages/deck3d/src/render/__tests__/f9-*.test.ts` — render: Byte-identical re-render (with props cache); input: IR with 1 CC0 prop, cache warm · trigger: `render` ×2 · observable: identical bytes. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #F9)
- [ ] 10.60 Manual: render: aesthetics parity with the strategy lab — fixture deck vs lab screenshots in `mockup/README.md`; human compares 7 slides; [judgment: "looks the same or better" — no automatable observable] (test-plan: manual-only, F10)
- [ ] 10.61 Manual: render: contrast rule on real GPU — fixture on a real GPU browser; human reads `check` contrast warnings vs perceived legibility; [judgment: GPU-dependent, warn-only by design] (test-plan: manual-only, F11)
- [ ] 10.62 Manual: effects: `fx preview` thumbnails — `fx preview <id>` for each v1 id; human inspects PNGs; [judgment: effect recognisable, no black frame] (test-plan: manual-only, F12)
- [ ] 10.63 Manual: props: Two props, same visual language — two props `restyle:palette`; human snapshot; [judgment: material harmony] (test-plan: manual-only, F13)
- [ ] 10.64 X1 vitest `packages/deck3d/src/parse/harvest/__tests__/x1-*.test.ts` — harvest: No browser available; input: `PLAYWRIGHT_BROWSERS_PATH` pointed at empty dir · trigger: `parse` of md with mermaid · observable: exit non-zero; stderr names chromium and contains `npx playwright install chromium`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #X1)
- [ ] 10.65 X2 vitest `packages/deck3d/src/render/__tests__/x2-*.test.ts` — render: Build without chromium; input: same env; md with **no** mermaid · trigger: `build notes.md` · observable: `notes.html` exists; stderr `skip check: chromium missing (npx playwright install chromium)`; exit 0; with `--strict` exit non-zero. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #X2)
- [ ] 10.66 X3 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/x3-*.test.ts` — harvest: Syntax error in a supported type; input: ```mermaid `flowchart TD\nA --> ` · trigger: `parse` · observable: exit non-zero; stderr has slide id and mermaid error text; no `deck.json`. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #X3)
- [ ] 10.67 X4 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/x4-*.test.ts` — harvest: Harvest hangs; input: harness page stubbed to never resolve (test hook `DECK3D_HARVEST_STALL=1`), timeout lowered via `DECK3D_HARVEST_TIMEOUT_MS=500` · trigger: `parse` · observable: exit non-zero within 2 s naming slide + `timeout`; no lingering chromium process (pid gone). Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #X4)
- [ ] 10.68 X5 chromium-gated vitest `packages/deck3d/src/check/__tests__/x5-*.test.ts` — check: timeout per viewport; input: deck whose `ready()` never resolves (test hook), `DECK3D_CHECK_TIMEOUT_MS=500` · trigger: `check` · observable: exit non-zero naming viewport `1920x1080` + `timeout`; browser closed. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #X5)
- [ ] 10.69 X6 vitest `packages/deck3d/src/props/__tests__/x6-*.test.ts` — props: Offline search (timeout); input: mock Poly Pizza endpoint that stalls; `DECK3D_HTTP_TIMEOUT_MS=200` · trigger: `props search robot` · observable: returns vendored rows within 1 s; one stderr line `online source skipped (timeout)`; exit 0. Exemplar: see packages/extension/src/__tests__/server-probe.test.ts (in-test server) + grep-prelude.test.ts (CLI spawn) (test-plan #X6)
- [ ] 10.70 X7 vitest `packages/deck3d/src/props/__tests__/x7-*.test.ts` — props: Offline search (abort / unconfigured); input: endpoint returns ECONNREFUSED; and separately no `POLY_PIZZA_KEY` · trigger: `props search robot` · observable: vendored rows; notice line; exit 0 in both. Exemplar: see packages/extension/src/__tests__/server-probe.test.ts (in-test server) + grep-prelude.test.ts (CLI spawn) (test-plan #X7)
- [ ] 10.71 X8 vitest `packages/deck3d/src/props/__tests__/x8-*.test.ts` — props: Generate fallback unavailable; input: `PATH` without `python3` · trigger: `props generate --from-image x.png --name y` · observable: exit non-zero; stderr contains `pip install gradio_client`; no file in `.deck3d/props/`. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #X8)
- [ ] 10.72 X9 vitest `packages/deck3d/src/props/__tests__/x9-*.test.ts` — props: Non-glTF download; input: mock body `<html>` with `Content-Type: model/gltf-binary` · trigger: `props fetch` · observable: exit non-zero `not a glTF/GLB`; no cache file. Exemplar: see packages/extension/src/__tests__/server-probe.test.ts (in-test server) + grep-prelude.test.ts (CLI spawn) (test-plan #X9)
- [ ] 10.73 X10 vitest `packages/deck3d/src/props/__tests__/x10-*.test.ts` — props: `.gltf` with external URIs; input: `.gltf` referencing `bin/model.bin` · trigger: `props fetch` · observable: exit non-zero naming the external URI (self-containment). Exemplar: see packages/extension/src/__tests__/server-probe.test.ts (in-test server) + grep-prelude.test.ts (CLI spawn) (test-plan #X10)
- [ ] 10.74 X11 vitest `packages/deck3d/src/ir/__tests__/x11-*.test.ts` — ir: Bad edit fails before render; input: `deck.json` with trailing comma (invalid JSON) · trigger: `validate`, `render` · observable: both exit non-zero with JSON position; no html. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #X11)
- [ ] 10.75 X12 chromium-gated vitest `packages/deck3d/src/parse/harvest/__tests__/x12-*.test.ts` — harvest: Engine upgrade breaks ids; input: committed harvest fixture snapshot · trigger: fixture test · observable: current harvest deep-equals snapshot; a mismatch message names `mermaid` pin. Exemplar: see packages/document-converter/src/__tests__/integration.test.ts (describe.skipIf gate) + tests/e2e/asciidoc-preview.spec.ts (Playwright page glue) (test-plan #X12)
- [ ] 10.76 X13 vitest `packages/deck3d/src/render/__tests__/x13-*.test.ts` — render: cache miss on props; input: IR with prop, `.deck3d/props/` deleted · trigger: `render` · observable: exit non-zero naming prop and `deck3d props fetch`; no html. Exemplar: see packages/dashboard-plugin-skill/src/__tests__/grep-prelude.test.ts (CLI spawn) / packages/kb/src/__tests__/kb.test.ts (fixtures) (test-plan #X13)
