## 1. D1: break the sliced-string retention (primary fix)

- [ ] 1.1 Add a retention fixture script plus vitest wrapper `packages/server/src/session/__tests__/session-diff-retention.test.ts` (harness exemplar: `packages/server/src/session/__tests__/session-diff-eventloop.test.ts` for fixture layout; child spawn via `execFileSync(process.execPath, ["--expose-gc","--import","tsx",fixture], { cwd: repoRoot, timeout: 60_000 })`, parsing the last stdout line as JSON). Scenario: 30 MB single-line big.txt chunk + 92-char small.txt tracked chunk · `splitBatchedDiff` → `enrichFilesWithContext`, keep only the small entry, reset RegExp statics, gc ×3 · `retainedMB < 5` (test-plan #E1). Verify it FAILS on current code.
- [ ] 1.2 In the same fixture, add the control arm: same input · hold `map.get("small.txt").trim()` · `retainedMB > 40`, else fail with "control no longer retains — revisit D1" (test-plan #E2). Verify it passes on current code.
- [ ] 1.3 In the same fixture, add the N-generation case: 5 distinct 20 MB batched diffs, each with a small tracked file, run through a `SessionDiffCache` with budget + `sizeOf` · 5 × `cache.run` · `retainedMB < 10` (test-plan #E3). Verify it fails on current code.
- [ ] 1.4 Add a byte-identity test in `packages/server/src/__tests__/session-diff.test.ts` (harness exemplar: its existing `enrichFilesWithContext` cases). Scenario: a batched diff with plain, rename, CRLF, multi-byte UTF-8 and trailing-blank-line chunks · `enrichFilesWithContext` · each `gitDiff === chunk.trim()` of the pre-change split (test-plan #E4).
- [ ] 1.5 Implement `flattenString` (Buffer round-trip, with a do-not-simplify comment) in `packages/server/src/session/session-diff.ts` and store `gitDiff: flattenString(chunk.trim())` at the renderable-chunk site. Verify 1.1–1.4 pass.

## 2. D2 + D4: byte-bounded cache with expiry on access

- [ ] 2.1 Add cache budget-boundary tests to `packages/server/src/session/__tests__/session-diff-cache.test.ts` (harness exemplar: its existing TTL/single-flight cases). Scenario: B=100, `sizeOf: v => v.n`, inserts n=40, 40, 20 · three `run()` · `size === 3`, `totalBytes === 100` (test-plan #E5); then insert n=1 · 4th `run()` · oldest evicted, `size === 3`, `totalBytes === 61` (test-plan #E6).
- [ ] 2.2 Same file, over-budget cases. Scenario: B=100, insert a (n=30) then b (n=150) · `run(b)` twice within TTL · a evicted, b kept alone, compute spy count 1 (test-plan #E7); then insert c (n=10) · `run(c)` · b evicted, `size === 1`, `totalBytes === 10` (test-plan #E8).
- [ ] 2.3 Same file, expiry and accounting cases with fake timers. Scenario: TTL 2000, k1 (n=50), far below `maxEntries` · advance 2001 ms then `run(k2)` · `size === 1`, `totalBytes === n(k2)` (test-plan #E9); k1 (n=50), advance past TTL · `run(k1)` recomputes n=70 · `size === 1`, `totalBytes === 70` (test-plan #E10).
- [ ] 2.4 Same file, default-ctor regression. Scenario: `new SessionDiffCache()` with no options · 101 `run()` inserts of large values · count-capped at 100, no byte eviction, existing cases green (test-plan #E12).
- [ ] 2.5 Implement in `packages/server/src/session/session-diff-cache.ts`: an options type `{ maxBytes, sizeOf }` (sizeOf required with maxBytes); entries `{ value, expires, bytes }`; a single private `remove(key)` owning `totalBytes`; an expired sweep at the start of `run()`; eviction that never removes the just-inserted entry; read-only `size`/`totalBytes` getters. Verify 2.1–2.4 pass.
- [ ] 2.6 Add `sessionDiffResultSize()` (allocation-free iterative walk, 2 bytes per string char + a per-node constant) and a test in `packages/server/src/__tests__/session-diff.test.ts` (harness exemplar: its existing pure-function cases). Scenario: a result with `gitDiff` 1000 chars, `changes[0].content` 500, `edits` old/new 100 each, an `otherChanges` gitDiff 300 · `sessionDiffResultSize(result)` · `>= 4000` (test-plan #E11).
- [ ] 2.7 Wire `new SessionDiffCache<SessionDiffResult>(2000, 100, { maxBytes: 64 MiB, sizeOf: sessionDiffResultSize })` in `packages/server/src/routes/session-routes.ts`. Verify the existing session-routes tests are green.

## 3. D3: bounded batched-diff output

- [ ] 3.1 Add runner limit tests in `packages/shared/src/platform/__tests__/runner-max-buffer.test.ts` (harness exemplar: `packages/shared/src/platform/__tests__/openspec-runner-stripped-path.integration.test.ts` for real-child `runAsync` recipes via `process.execPath -e`). Scenario: child writes exactly 1 MiB, `maxBuffer` 1 MiB · `runAsync` · `ok: true`, `value.length === L` (test-plan #E13); child writes L+1 bytes · `runAsync` · `output-too-large` with `limitBytes === L` and a string `message` (test-plan #E14).
- [ ] 3.2 Same file, byte counting and opt-in. Scenario: `"é".repeat(600_000)` with a 1 MiB limit · `runAsync` · `output-too-large` (test-plan #E15); a recipe without `maxBuffer` writing 5 MB · `runAsync` · `ok: true`, full output (test-plan #E16).
- [ ] 3.3 Same file, fault cases. Scenario: child writes forever with a 1 MiB limit and 15 s timeout · `runAsync` · settles `output-too-large` within 5 s and the child pid is dead within 4 s (test-plan #X1); a child that ignores SIGTERM (`it.skipIf(win32)`) · overflow · SIGKILLed within 4 s (test-plan #X2); overflow with a `parse` spy · child `close` fires · `parse` never called and the promise resolves once (test-plan #X3).
- [ ] 3.4 Implement `Recipe.maxBuffer` (doc: honoured by `runAsync` only) and the `ExecError` variant `{ kind: "output-too-large"; binary; limitBytes; message }` in `packages/shared/src/platform/runner.ts`. Count raw `Buffer` chunk bytes before `toString`. Extract a local `terminate()` shared with the timeout path (SIGTERM → 3 s SIGKILL, recycled-pid guard); `clearTimeout(timer)` on overflow; `close` returns early when `settled`. Verify 3.1–3.3 pass.
- [ ] 3.5 CI compile gate for the new variant (harness exemplar: `.github/workflows/ci.yml` step `pnpm run lint`). Scenario: `ExecError` gains `output-too-large` with `message`, and the `openspec-routes.ts` chain ends in `err.message` · run `pnpm run lint` (`tsc --noEmit`) · exits 0 (test-plan #X9).
- [ ] 3.6 Set `GIT_DIFF_ALL.maxBuffer = 32 MiB` in `packages/shared/src/platform/git.ts`. Switch `buildGitEnrichmentContext` to `git.diffAll`. Add the throttled `[session-diff]` warning (module `Map<cwd, lastWarnMs>`, 10 min window, pruned on warn). Remove the now-orphaned `diffAllOr` and its `git.ts` AGENTS row. Verify `pnpm run lint` exits 0.
- [ ] 3.7 Migrate the `diffAllOr` mocks in `packages/server/src/__tests__/session-diff.test.ts` to `diffAll` resolving `{ ok: true, value }`, and add the migration guard (harness exemplar: the file's existing `vi.mock` factory). Scenario: `diffAll` mock → `{ ok:true, value:<a.ts chunk> }` · `buildSessionDiff` on tracked `a.ts` · `a.ts.gitDiff` defined and equal to the chunk (test-plan #X7).
- [ ] 3.8 Same file, degradation tests. Scenario: `diffAll` → `output-too-large` (32 MiB), numstat counts for `a.ts`, untracked `new.ts` on disk · enrich `[a.ts, new.ts]` · `a.ts` counts without `gitDiff`, `new.ts` keeps its synthetic `gitDiff`, one `console.warn` containing the cwd and `33554432` (test-plan #X4); `diffAll` → `{ kind:"exit", code:128 }` · enrich `[a.ts]` · no `gitDiff` and no `[session-diff]` warning (test-plan #X6).
- [ ] 3.9 Same file, throttle test with fake timers and distinct temp cwds. Scenario: an oversized result for cwd A at t=0 and 1 min, cwd B at 2 min, A at 10 min + 1 ms · each enrich · warn counts 1 → 2 → 3 (test-plan #X5).
- [ ] 3.10 Add a real oversized-repo case to `packages/server/src/session/__tests__/session-diff-eventloop.test.ts` (harness exemplar: its temp-repo `beforeAll`). Scenario: tracked single-line 17 MB JSON committed then modified (≈ 34 MB diff) plus a small tracked `s.txt` edit · `buildSessionDiff([], repo)` · `isGitRepo`, both entries have counts and no `gitDiff`, one warning (test-plan #X8).

## 4. Performance and verification

- [ ] 4.1 Extend `packages/server/src/session/__tests__/session-diff-eventloop.test.ts` (harness exemplar: its existing tick-gap test). Scenario: a tracked ~12 MB single-line file modified (≈ 24 MB diff, under the limit) plus 50 small modified files · `buildSessionDiff` with flatten + size walk · 10 ms interval max gap `< 500 ms` and `ticks > 0` (test-plan #P1).
- [ ] 4.2 Run the full suite (`set -o pipefail; npm test 2>&1 | tee /tmp/pi-test.log`) and `pnpm run lint`; verify both are green.
- [ ] 4.3 Manual soak on the issue §4 repro repo (17 MB single-line tracked JSON, modified) with a live agent making 50 edits to other files. Verify `/api/health` `server.heapUsed` after edit 50 ≤ heap after edit 5 + 100 MB, with no OOM (test-plan: manual-only, #P2).
- [ ] 4.4 Update the `AGENTS.md` rows (or sidecars) for `session-diff.ts`, `session-diff-cache.ts`, `session-routes.ts`, `runner.ts` and `git.ts` with `See change: fix-session-diff-heap-retention`; verify with `kb_search --doc-type agents "fix-session-diff-heap-retention"`.
