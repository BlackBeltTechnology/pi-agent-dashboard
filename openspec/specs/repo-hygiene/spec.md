# repo-hygiene

Repo-level ignore rules that keep local-only working directories, build outputs, and caches out of git.

## Requirements

*No active requirements. The `.shadow/` entry previously mandated for jj workspace clones has been removed. If specific local-only directories need exclusion, add requirements here with concrete scenarios.*
