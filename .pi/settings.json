{
  "packages": [
    {
      "source": "/Users/robson/Project/pi-agent-dashboard",
      "extensions": [
        "+packages/kb-extension/src/index.ts"
      ]
    }
  ],
  "worktreeInit": {
    "gate": "test ! -e node_modules/.modules.yaml || test ! -d .pi/skills/openspec-explore || test ! -f .pi/dashboard/kb/index.db",
    "run": {
      "type": "script",
      "command": "corepack enable && pnpm install && npx @fission-ai/openspec init --tools pi --force && pnpm --filter @blackbelt-technology/pi-dashboard-kb run build && NODE_OPTIONS=--experimental-sqlite npx kb index"
    }
  }
}
