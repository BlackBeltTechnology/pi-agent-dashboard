# session-filter-storage.ts — index

localStorage persistence for session-list filter state. Exports `removeLegacyHiddenSessions`, `getActiveOnly`/`setActiveOnly` (default ON), `getTagAreaOpen`/`setTagAreaOpen` (sidebar tag-area master collapse; absent ⇒ collapsed), and the one-shot collapsed-folders migration helpers: `readLegacyCollapsedGroups`/`writeLegacyCollapsedGroups`/`clearLegacyCollapsedGroups` (legacy `dashboard:collapsedGroups` record; tolerates the pre-change bare array AND the in-progress `{keys,attempts}` shape) + `decideCollapsedFoldersMigration` (pure; send only keys the server is known to lack; monotone completion; 10-load backstop) + `COLLAPSED_MIGRATION_MAX_ATTEMPTS`. Folder-collapse LIVE state moved server-side; localStorage no longer holds it. Keys: `dashboard:activeOnly`, `sidebar.tagArea.open`, legacy `dashboard:collapsedGroups`; removes `dashboard:hiddenSessions`. See changes: sidebar-tag-collapse-and-delete, persist-folder-collapse-server-side.
