# CommandInput.tsx — index

Chat composer textarea + autocomplete. Exports `CommandInput`, `parseViewCommand`, `shouldWalkFileQuery`, `MIN_FILE_QUERY_LEN`, `DASHBOARD_LOCAL_COMMANDS`. Merges `BUILTIN_COMMANDS` + `DASHBOARD_LOCAL_COMMANDS` + server commands. `/`-command (grouped by source, badges, arg hints) and `@`-file/URL autocomplete with viewport-flip dropdown. Intercepts `/view` → `onViewLocal` (never reaches bridge). History recall (ArrowUp/Down when empty). Controlled `draft`/`images` modes lifted to App.

Unified card composer (change: redesign-prompt-input): one bordered card = attachments row → textarea → inner toolbar (`＋` attach menu · `ModelSelector` chip · `TranslateButton` · `ThinkingLevelSelector` chip · `Steer|Queue` delivery control · terminal · morphing action button). Model/thinking/context props relocated from StatusBar. Single morphing action button: `send-button` → `stop-button` → `force-stop-button` → `killing-button`; `resetStopSignal` reverts failed force-kill to Force Stop; `forceStopEmphasized` after 5s aborting. `stop-after-turn` = slim secondary affordance. `retrying` treated as working. Delivery `Steer|Queue` maps Enter/Alt+Enter contract. Focus-revealed footer + context-left indicator. `＋` attach menu. Container-based toolbar folding (`@[44rem]:`). See change: fix-split-composer-overflow.
