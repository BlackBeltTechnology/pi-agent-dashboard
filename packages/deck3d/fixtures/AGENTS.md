# DOX — packages/deck3d/fixtures

Fixture decks used by the harvest/parse/render test suites.

| File | Purpose |
|------|---------|
| `AGENTS.md` | This file. |
| `harvest.md` | Two-slide mermaid deck (Hungarian `flowchart LR` + `sequenceDiagram`) exercising shapes, edge kinds, actors/messages and accented labels. Parse determinism + unsupported-type CLI tests. |
| `strategy-lab.md` | The strategy-lab mockup reproduced as Markdown: seven slides (Hungarian titles, `flowchart LR` + `sequenceDiagram`). Inline `<!-- deck3d: … -->` overrides tune `camera.distance`/`labels.size` so `check` is clean, with `check.ignore:["contrast"]` (GPU-dependent). Parity fixture for the render/check suites. |
| `strategy-lab.json` | Parsed IR for `strategy-lab.md` (written by `build`). Committed expected IR (derived fields only; tuning lives under `overrides`). |
| `business-2031/` | Corpus showroom: 40 slides (23 lifted from `presentations/business-next-5-years`, 17 authored) presenting EVERY corpus card exactly once plus one local module, all nine built kinds and both mermaid kinds. `deck.md`/`overrides.json` are GENERATED by its `build-*.mjs`. Builds `check: clean` + `style: 40/40`. Drives #E42/#P1 budget tests, #P3 and the #E56 coverage gate. See change: deck3d-cinematic-worlds. |
