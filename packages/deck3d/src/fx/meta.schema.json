{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "deck3d effect card",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "kind", "tags", "cost", "modes", "params", "conflicts", "source", "licence"],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "kind": { "enum": ["background", "post", "material", "light", "motion", "edge", "transition"] },
    "tags": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mood", "content"],
      "properties": {
        "mood": { "type": "array", "items": { "type": "string" } },
        "content": { "type": "array", "items": { "type": "string" } },
        "topic": {
          "type": "array",
          "description": "Closed vocabulary parse routes content slides by. Free-form belongs in `content`.",
          "items": {
            "enum": ["ai", "agents", "geo", "trust", "security", "compute", "data", "money", "work", "timeline", "sales", "process"]
          }
        }
      }
    },
    "cost": { "type": "integer", "minimum": 1, "maximum": 5 },
    "modes": { "enum": ["dark", "light", "both"] },
    "params": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["type"],
        "properties": {
          "type": { "enum": ["number", "string", "boolean"] },
          "default": {},
          "minimum": { "type": "number" },
          "maximum": { "type": "number" },
          "enum": { "type": "array" }
        }
      }
    },
    "conflicts": { "type": "array", "items": { "type": "string" } },
    "source": {
      "description": "Upstream URL for a corpus card, or the literal `local` for a per-deck module in `fx/`.",
      "anyOf": [{ "type": "string", "pattern": "^https://" }, { "const": "local" }]
    },
    "licence": { "enum": ["MIT", "Zlib", "BSD-2-Clause", "BSD-3-Clause", "CC0-1.0", "Apache-2.0", "OFL-1.1"] }
  }
}
