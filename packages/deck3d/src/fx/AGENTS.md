# DOX — packages/deck3d/src/fx

Curated, licence-clean effect corpus + deterministic defaults + composition (design D9). See change: add-deck3d-presentation-package.

| File | Purpose |
|------|---------|
| `AGENTS.md` | This file. |
| `types.ts` | `FxKind`/`FxMode`/`FxCard`/`FxContext`/`FxHandle`/`FxFactory`/`FxEntry` + `PERMISSIVE_LICENCES` + `FX_TOPICS`/`FxTopic` (closed 12-value topic vocabulary). Every effect module implements `create(ctx, params) → { object?, pass?, material?, tick?, dispose() }`. `FxContext` carries `rng` (seeded per slide) + read-only `slide`. See change: deck3d-cinematic-worlds. |
| `meta.schema.json` | Card JSON Schema (draft-07): id, kind, tags (`mood`/`content` free-form + optional closed `topic[]`), cost 1–5, modes, params schema, conflicts, `https://` source OR the literal `local`, SPDX licence enum. See change: deck3d-cinematic-worlds. |
| `index.ts` | GENERATED registry: pairs each `<id>.meta.json` card with its `<id>.ts` factory; `FX_IDS`, `cardFor`. |
| `<id>.ts` + `<id>.meta.json` | One module + card per effect (55 ids, see `reference/effects.md`). Topic-world backgrounds `globe-arcs`/`city-grid`/`neural-mesh`/`vault-glyphs`/`server-racks`/`market-tape`/`orbit-agents`/`paper-stack` use `ctx.rng`, expose `object.userData.count`, scale counts by `quality`, and put their own depth offset on an INNER group (the runtime pins the returned object's z). See change: deck3d-cinematic-worlds. `tokens`/`rings`/`swarm`/`particles` delegate to `runtime/backgrounds.ts`; the rest are permissive three.js-example ports behind the interface. |
| `compose.ts` | `composeEffects(effects, mode, quality, slideId, localCards?)` — a `local:<name>` id resolves through the embedded cards so per-deck effects enter the same gating. Also `LocalCards`. See change: deck3d-cinematic-worlds. Original: mode gating (skip + warn), conflicts, quality budget (low 6 / medium 12 / high 20). `validateEffectParams(ir)`: card-bound param checks with `overrides.slides["<id>"].effects[i].params.<p>` paths (E24). |
| `defaults.ts` | `defaultEffectsFor(slide, autoStyle?)` / `defaultSceneFor`: v1 precedence kept as a strict PREFIX (keyword → title-ish → `BY_DIAGRAM` for `flowchart`/`sequence` only), then a topic-keyword step inserted exactly where v1 fell to `particles`. `backgroundForTopic` picks the cheapest `background` card carrying the topic, ties by id. `autoStyle:false` restores the v1 fallback. See change: deck3d-cinematic-worlds. |
| `catalogue.ts` | Rows carry optional `topic[]` (drives `fx list --topic`). See change: deck3d-cinematic-worlds. `catalogue()` sorted rows, `renderCatalogue()` → `reference/effects.md`, `catalogueHash()` (drift detector). |
| `local.ts` | Per-deck effect resolution (design D1): `LOCAL_NAME_RE` `^[a-z][a-z0-9-]{0,63}$`, 64 KiB cap, card load + schema check, `kind ∈ {background,motion}`, sha256 pin, `lintModule` (leading `export default function`, single export, no `import`/`require`/`eval`/`Function` outside strings/comments), `toFactorySource`, `localRefs`. Used by `validate` (when the deck dir is known) and by `render` before embedding. See change: deck3d-cinematic-worlds. |
| `scaffold.ts` | `scaffoldLocalEffect` (writes a working stub + card, never overwrites, prints the pinned override entry), `hashLocalEffect`, `promoteLocalEffect` (both `--source`/`--licence` required, permissive-licence gate, corpus verify with rollback). See change: deck3d-cinematic-worlds. |
