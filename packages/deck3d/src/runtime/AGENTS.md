# DOX — packages/deck3d/src/runtime

Browser runtime, bundled by esbuild into `dist/runtime.js` and inlined into `deck.html`. See change: add-deck3d-presentation-package.

| File | Purpose |
|------|---------|
| `AGENTS.md` | This file. |
| `index.ts` | Bundle entry + engine. `poseAt(t)` poses+draws, `applyTime(t)` = poseAt + PIN the clock; boot calls `poseAt(0)` — calling the pinning variant at boot shipped every deck frozen at t=0 (camera transitions still ran, nothing animated). `debug.motion()` fingerprints animated transforms; `debug.backdropLeaks()` lists fx subtrees that escaped onto the content layer (`check` rule `fx-content-layer`). Reads `window.__DECK` (merged IR) + `window.__DECK_FONT` (base64 TTF) + `window.__DECK_PROPS` (prop GLBs) + `window.__DECK_LOCAL_FX` (per-deck effect `{card,src}`); builds one `THREE.Group` per slide (title/slab/diagram/background/props), rail camera + dolly/swing/fade transitions, hash deep-link (1-based; `#0`/out-of-range → slide 1), live navigation via `navTo` (keydown `ArrowRight`/`ArrowDown`/`PageDown`/`Space` forward, `ArrowLeft`/`ArrowUp`/`PageUp` back, `Home`/`End`; left-click `pointerup` advances; `hashchange` follows; clamped at both ends, mirrors index into `location.hash`), rAF loop with `setTimeout` fallback on `document.hidden`. Exposes `window.__deck3d` (`gotoSlide`, `setTime`, `ready`, `measure`, `peaks`, `effects` incl. `budget`, `debug.titleGlyphs`/`liftedMessage`, `current`). `effects()` also returns `errors[]` (`{slide,effectId,phase}`). Builds the configurator via `createHud` and suppresses navigation + the `C` toggle while a panel input has focus or a click lands inside the panel. `fxSeedFor(slideId)` seeds each effect's `ctx.rng`. Deterministic clock when `setTime` is used. See change: deck3d-cinematic-worlds. `applySlide` (configurator) REBUILDS the current slide via `buildSlideGroup(..., patch)` — colours/labels/anchor/background FX are baked at build time, so re-running `applyLook` alone reached only the lights; ~10-21 ms measured on the heaviest real slide. `disposeBuild` frees the discarded build but SKIPS the props subtree (prop clones share geometry/material by reference with the shared template). `debug.look()` reports live bg/fog/rim/title/camZ for configurator tests. `billboardLabels()` points every diagram label at the camera each frame: `globe`/`loop`/`swarm`/`orbit-cluster` spin a group CONTAINING their captions, so a live clock turned them edge-on then backwards. `debug.labelFacing()` measures it — `measure()` structurally cannot (its projection is rotation-invariant for rails). `debug.look()` also returns `cam`/`camTarget`/`anim`. See change: deck3d-cinematic-worlds. `disposeLocalFx` DETACHES handle nodes (dispose alone leaks them; revival then doubled the backdrop); `reviveLocalFx(i)` re-instantiates a revisited slide's `local:` fx, skipping ones that threw on create. `fxParamEdits`/`paramsFor` overlay live configurator param edits on authored `ref.params`. `applyEffectParams(id, patch)` rebuilds ONE effect in place. Probes: `debug.localFx()` (live handles, scoped), `debug.sceneNodes()` (orphan detector), `debug.motion()` now includes instance matrices. See change: deck3d-cinematic-worlds. Camera smoothing (0.2 lerp) applies ONLY when `anim === null`; during a fly the camera tracks `camState` exactly, or a finished transition leaves the view parked over the previous slide. See change: deck3d-cinematic-worlds. |
| `types.ts` | `RuntimeDeck`/`SlideConfig`/`Deck3dApi`/`Measurement` + `window.__DECK`/`__DECK_FONT`/`__DECK_PROPS`/`__deck3d` global augmentation. `effects()` carries the per-slide `budget` (`{sum,limit,warning?}`) and `errors[]` for local-effect failures. See change: deck3d-cinematic-worlds. `Deck3dApi.debug.look()` returns `{bg,fog,rim,title,camZ}`. |
| `palette.ts` | `PALETTES` (blackbelt/zenit/dapp/midnight/ember/arctic/forest/mono/neon) + `resolvePalette` (mode-aware; `custom` derives bg/text from the card colour). Every named palette meets WCAG AA for text-on-bg AND text-on-card in both modes, asserted by the corpus test. See change: deck3d-cinematic-worlds. `mixPalette(a,b,k)` blends two palettes channel-wise for the transition morph. See change: deck3d-cinematic-worlds. |
| `quality.ts` | `qualityProfile(low|medium|high)` → `{ bloom, mirror, shadowMapSize, particles }` (D6). |
| `materials.ts` | `diagramMaterial` (glass/metal/matte; accent vs second) + `titleMaterial`. |
| `text.ts` | `loadFont` (opentype parse from base64), `ttfShapes`, `wrap`, `buildTitle` (extruded, reports missing glyphs), `buildLabel` (canvas plane, `P.bg` outline, `toneMapped:false`), `bulletTexture`. Real TTF only — no corrupt `typeface.json` fallback. Diagram captions draw with `depthTest: false` + `renderOrder: 12` — a caption is an annotation, so plinth/globe geometry must not swallow it. Every `buildLabel` caller is a diagram label (builders.ts only). See change: deck3d-cinematic-worlds. |
| `measure.ts` | `projectRect(object, camera, w, h)` → screen rect, used by `measure()` and therefore every `check` fit/overlap/occlusion rule. Takes the bounds in the OBJECT'S OWN frame then applies `matrixWorld`; a world-axis-aligned `Box3` inflated up to 1.5× on a slide turned by `orbit`/`helix` and raised false `fit` findings. See change: deck3d-cinematic-worlds. |
| `node-geometry.ts` | `nodeGeometry(shape, w, h)` → three.js primitive (rect/stadium/round/circle/doublecircle/diamond/hexagon/cylinder). |
| `props.ts` | glTF props in the runtime: `loadPropModels` (GLTFLoader + meshopt from `__DECK_PROPS`), `createPropMaterials` (one shared metal/matte family per deck), `applyProps` by role (`hero`/`illustration`/`ambient`/`node:<id>`), `Box3` normalise + ground + face camera, `restyle:palette`, anim presets as `t` functions. |
| `builders.ts` | `buildDiagram` dispatch. `buildFlowchart` (layout → staircase, tubes, arrow-heads, group plates, node overrides), `buildSequence` (actor slabs, time staircase, `1.1 s` message pulse), and NINE built topologies driven by `diagram.data`: `brain`/`loop`/`swarm` (captioned members pinned to an even `ringPoint` so captions never overlap) plus `bars` (shallow columns, height ∝ value, zero → stub, captions in front of the plinth), `funnel`, `timeline-rail` (captions alternate above/below the rail), `globe`, `orbit-cluster`, `stack`. Captions clip at `CAPTION_MAX` 22 chars. All return `{ g, tick, nodes, labels }` so `check` measures them. See change: deck3d-cinematic-worlds. Captions come from `addPart` ONLY (measurable + billboarded + honours `labels.size`); builders must NOT also extrude a `buildTitle` per node. See change: deck3d-cinematic-worlds. |
| `backgrounds.ts` | Scene library `tokens`/`rings`/`swarm`/`particles` (+ `backgroundFor`); deterministic seeded RNG. |
| `camera.ts` | `anchorFor(i, distance?, spacing?, rail?, count?)` rail anchor + `cullRadius(spacing, rail)` + `DEFAULT_SPACING` (40) + `CULL_RADIUS` (52 = 40×1.3, back-compat). `rail`: `line` (x = i·spacing), `orbit` (ring, radius = spacing·count/2π, `rotY` = θ), `tunnel` (-Z, cull 0.6×spacing so the slide behind cannot float in frame), `helix` (orbit + 0.18·spacing rise), `grid` (cols = ⌈√count⌉, rows 0.8×spacing). Camera always sits at slide-local `(0.6, 0.6, distance)` turned by `rotY`, so framing is rail-invariant. See change: deck3d-cinematic-worlds. |
| `scene.ts` | `createSceneRig`: renderer (ACES tone map), scene, camera, `EffectComposer` (bloom pass only when the quality profile enables it), RoomEnvironment PMREM, key/fill/rim lights, ONE global floor (Reflector mirror + radial-alpha veil), `applyLook` (mode-aware bloom/fog/lights/shadows), `render`/`resize`/`updateFloor`. `applySurface` repaints background/veil on EVERY call (a first-run-only guard froze the deck's dominant colour after boot). `rimColor()` exposes the live rim light. `BACKDROP_LAYER` (1) + `markBackdrop(object)` + `BackdropRenderPass` + `renderLayered`: backdrop and content render as TWO passes with the depth buffer cleared between, so a background can never occlude slide text however deep its geometry (fixed `geo-fragments` plates slicing the card). Lights `layers.enable(BACKDROP_LAYER)` or standard materials go black there; the content pass suppresses `autoClear*` because a Color background force-clears on every render. Pass still reports as `RenderPass` in `effects().active`. See change: deck3d-cinematic-worlds. Floor/veil/mirror are marked backdrop too — on the content layer the floor painted over every backdrop pixel below its horizon (depth is cleared between passes), clipping backgrounds along a hard line. The content pass suppresses the clear via `renderer.autoClear = false` + `scene.background = null`, NOT the `autoClearColor/Depth/Stencil` sub-flags: `Reflector` clears its own target only when `autoClear === false`, so the sub-flag spelling left the mirror uncleared and reflections accumulated every past frame. See change: deck3d-cinematic-worlds. |
| `rng.ts` | Deterministic LCG so no scene layout uses `Math.random`. |
| `hud.ts` | Configurator (design D4): `createHud(host)` builds the hidden `#deck3d-hud` panel — slide counter, Deck/This-slide scopes, seven collapsible `<details>` blocks (Look, Lighting & FX, Camera & labels, Layout, Motion, Quality, Effects) driven by the `BLOCKS` control-plane table; only the first opens by default and per-block open state persists in `HudState.open`. A block with no control for the active scope is omitted — `rail`/`spacing` are deck-only, `diagram.*`/`cardOffset.*` slide-only. Effects checklist, bounded autoplay (`parseAutoplay`: 0 = off, else integer 1–600), `●` markers from the embedded `overriddenKeys`, and Export → `overrides.json` in D1 grammar. State lives in memory + `localStorage["deck3d:"+derivedHash]`; `window.__DECK` is never mutated. See change: deck3d-cinematic-worlds. `paramRows` generates per-effect controls from the card's declared params (slider on min/max, checkbox on boolean, select on enum); `commitParam` applies live + stages for export as `effects[].params`. See change: deck3d-cinematic-worlds. |
| `local-fx.ts` | Local-effect instantiation (design D1): `SHADOWED` global list, `compile` via `new Function` with those names bound to `undefined` and a frozen `Math` whose `random` is `ctx.rng`. NO `"use strict"` — `eval`/`Function` are illegal strict-mode parameter names, so strict mode made every local module fail to compile. `createLocalEffect` wraps create/tick/dispose; a throw disables that effect only and records `{slide,effectId,phase}`. `localFxRegistry`/`localCards` read `window.__DECK_LOCAL_FX`. See change: deck3d-cinematic-worlds. |
