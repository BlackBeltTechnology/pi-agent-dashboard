# invoicebot-plugin/src/server

Server entry, routes, session-linkage seam. Engine bindings under `engine/`.

| File | Purpose |
|------|---------|
| `__tests__/ib-app-level-rebroadcast.test.ts` | Full-server integration (regression): `plugin_pi_message` in over pi WS → unchanged `ib_domain_event` frame to an unsubscribed browser; malformed skip; reconnect delta-only. Ported from retired core `event-wiring-ib-app-level.test.ts`. |
| `index.ts` | `registerPlugin(ctx)`: selects engine; builds session link from trusted host hooks. Adapts `engine.query(view:"runs")` to newest-first recorded session IDs. Registers `registerPiHandler("ib_domain_event")` → app-level `{type:"ib_domain_event",sessionId,event}` browser broadcast (malformed skipped, rate-limited warn; see change: relocate-ib-domain-events-to-plugin). Mounts routes with flow dispatch + scoped-session bootstrap. Logs binding. See change: attach-live-scoped-invoice-session. |
| `routes.ts` | `mountInvoiceBotRoutes(fastify, {engine, dispatchFlow, ensureScopedSession})`. 4 selector POST routes + `POST /scoped-session` + `GET /blob` + `POST`/`GET /automation`. `/scoped-session` validates cwd/invoice_id, ensures intake, returns real scoped sessionId or 503; chat continues over existing `/ws`. See change: attach-live-scoped-invoice-session. `badCwd()` (existing dir, no NUL → 400). `normalize()` → `{ok,text,data,sessionId?,consequential?}`. `isConsequential()` per api-contract §10. Flow-triggering results (`.flow`) → `dispatchFlow` → attach `sessionId`. `GET /blob` streams a retained original via `resolveBlobPath`: `Content-Type` by ext, `inline`, `Accept-Ranges`, `nosniff`; `parseRange()` → 206/416; 400/403/404 on reject. See change: serve-invoice-original-blob. `Content-Disposition` comes from `contentDispositionFor` (never raw interpolation). See change: fix-blob-content-disposition-encoding. `POST /upload` (write-side twin of `/blob`) registers `@fastify/multipart` (`throwFileSizeLimit:false`, `limits {fileSize:20 MB, files:20}`); streams each file part to a Buffer (truncated → per-file reject(too large), never forwards partial bytes), reads the `cwd` field, calls `engine.ingest(cwd, files)`, merges boundary rejects, returns `{results, landed, skipped, rejected}` verbatim; dispatches no flow; 400 only on bad `cwd` / no file parts (fully-rejected batch still 200). See change: add-upload-intake. `POST /automation {cwd,name,enabled}` flips ONLY `disabled` in place via `flipAutomationDisabled` (badCwd + badAutomationName → 400; absent → 404; `{ok,name,enabled}`). `GET /automation?cwd` → `{automations:[{name,enabled}]}` via `listInvoicebotAutomations`. LIMITATION: engine `intake_paused` gate can still swallow processing after enable — two switches contradict; reconcile deferred. See change: surface-automation-enable. On every workspace-touching handler (query/review/setup/rules/POST+GET automation/upload; NOT `/blob`) calls `engine.ensureAutomation(cwd)` via shared `ensureIntake()` after `badCwd` passes — idempotent, non-fatal, ensures the disabled `invoicebot-intake` drain scaffold on first touch. See change: ensure-intake-automation. |
| `automation-toggle.ts` | `flipAutomationDisabled(cwd,name,enabled)`: read `<cwd>/.pi/automation/<name>/automation.yaml`, set ONLY `disabled=!enabled` via `yaml` Document API (comments + other fields preserved), re-validate, atomic tmp+rename. Throws `AutomationNotFoundError` when absent (never creates). `badAutomationName()` rejects empty/`..`/`.`/`runs`/separators/NUL. `listInvoicebotAutomations(cwd)` scans `.pi/automation/` for `invoicebot`-prefixed dirs → `{name,enabled}` sorted. First invoicebot FS-write surface (no engine port — no `ib_*` touches `disabled`). Repeated flips re-arm live via automation-plugin watcher (eventual-consistent; atomic rename needs its periodic re-attach). See change: surface-automation-enable. |
| `blob.ts` | `resolveBlobPath(cwd, handle)` → `{ok,abs}` \| typed reject (`invalid-input`\|`traversal`\|`not-found`). Two-stage containment under `<cwd>/.pi/flows/invoicebot-state/blobs/`: lexical (defeats `..`/absolute) + realpath (defeats symlink) + isFile. `contentTypeFor(ext)`: pdf/png/jpe?g, else octet-stream. See change: serve-invoice-original-blob. `contentDispositionFor(filename)` builds a header-safe `Content-Disposition`: allow-list ASCII fallback (printable US-ASCII minus `"` `\` `/`; everything else → `_`; blank → `document`) plus RFC 5987 `filename*=UTF-8''<percent-encoded>` (attr-char tightened over encodeURIComponent) ONLY when sanitisation loses information — header-safe ASCII keeps the plain unchanged form. Prevents `ERR_INVALID_CHAR` 500s on non-Latin-1 (e.g. Hungarian `ő`) filenames and neutralises header injection. See change: fix-blob-content-disposition-encoding. |
| `session-link.ts` | `createSessionLink` exposes flow dispatch + `ensureScopedSession`. Scoped bootstrap reuses exact live links/restored encoded metadata, checks engine-recorded IDs newest-first (live or ended+existing file), else guarded flow-less spawn with scoped env; correlates by runId; never returns spawnToken as sessionId. Existing flow dispatch behavior preserved. `recordedSessionIdsFromDetails` parses `view:"runs"`. See changes: scope-session-toolset-by-profile, attach-live-scoped-invoice-session. |
| `__tests__/engine.test.ts` | Fake per-selector `details` shapes + flow-vs-pure classification (§3.5). |
| `__tests__/routes.test.ts` | Forwarding, validation, cwd isolation, flow dispatch, consequential flags. Scoped-session success/400/503 + workspace initialization. Fastify inject + recording engine. See change: attach-live-scoped-invoice-session. |
| `__tests__/automation-toggle.test.ts` | Flip helper (enable↔disable toggles ONLY `disabled`, byte-identical elsewhere + inline comment preserved; never-create throw) + `/automation` routes (flip 200/`{ok,name,enabled}`; missing/traversal/slash name → 400 no write; absent → 404; bad/absent/NUL cwd → 400; non-boolean enabled → 400; GET lists two + one). Faux, zero-network. See change: surface-automation-enable. |
| `__tests__/automation-rearm.integration.test.ts` | Integration: real automation-plugin watcher + scheduler. Flip enable → scheduler arms `folder:invoicebot-intake`; re-attach (models production `attachWatchers()`), flip disable → disarms. No reload. Faux, zero-network. See change: surface-automation-enable. |
| `__tests__/blob.test.ts` | Unit: `resolveBlobPath` happy path, `..`/absolute/symlink → traversal, missing file → not-found, blank cwd/handle + NUL → invalid-input; `contentTypeFor` mapping. See change: serve-invoice-original-blob. |
| `__tests__/blob-route.test.ts` | Route: PDF/PNG/octet-stream inline + nosniff + Accept-Ranges (3.1); `Range: bytes=0-99` → 206 + Content-Range, unsatisfiable → 416 (3.2); traversal/absolute/symlink → 403, absent → 404, missing params → 400 (3.3). See change: serve-invoice-original-blob. |
| `__tests__/session-link.test.ts` | Existing flow reuse/spawn correlation + scope env. Scoped bootstrap exact-live reuse, recorded live/resumable selection, flow-less spawn metadata/no-flow correlation, failure/timeout, and newest-first runs parsing. See change: attach-live-scoped-invoice-session. |
| guard | `session-link` marks its spawns `guard: true` (change: constrain-agent-tool-surface) → host disables built-in tools for the spawn AND registers the workspace cwd as guarded, so the client-spawned "Ask"/Kérdezz session there is guarded too (origin ∪ cwd). |
