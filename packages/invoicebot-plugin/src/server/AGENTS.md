# invoicebot-plugin/src/server

Server entry, routes, session-linkage seam. Engine bindings under `engine/`.

| File | Purpose |
|------|---------|
| `index.ts` | `registerPlugin(ctx)`: `selectEngine()` (await; loader awaits before listen), build `createSessionLink` from ctx (spawnSession/emitEventToSession/getSession/listAll/onEvent — trust-gated first-party), `mountInvoiceBotRoutes`. Logs engine binding. |
| `routes.ts` | `mountInvoiceBotRoutes(fastify, {engine, dispatchFlow})`. 4 POST routes + `GET /blob` + `POST`/`GET /automation`. `badCwd()` (existing dir, no NUL → 400). `normalize()` → `{ok,text,data,sessionId?,consequential?}`. `isConsequential()` per api-contract §10. Flow-triggering results (`.flow`) → `dispatchFlow` → attach `sessionId`. `GET /blob` streams a retained original via `resolveBlobPath`: `Content-Type` by ext, `inline`, `Accept-Ranges`, `nosniff`; `parseRange()` → 206/416; 400/403/404 on reject. See change: serve-invoice-original-blob. `POST /upload` (write-side twin of `/blob`) registers `@fastify/multipart` (`throwFileSizeLimit:false`, `limits {fileSize:20 MB, files:20}`); streams each file part to a Buffer (truncated → per-file reject(too large), never forwards partial bytes), reads the `cwd` field, calls `engine.ingest(cwd, files)`, merges boundary rejects, returns `{results, landed, skipped, rejected}` verbatim; dispatches no flow; 400 only on bad `cwd` / no file parts (fully-rejected batch still 200). See change: add-upload-intake. `POST /automation {cwd,name,enabled}` flips ONLY `disabled` in place via `flipAutomationDisabled` (badCwd + badAutomationName → 400; absent → 404; `{ok,name,enabled}`). `GET /automation?cwd` → `{automations:[{name,enabled}]}` via `listInvoicebotAutomations`. LIMITATION: engine `intake_paused` gate can still swallow processing after enable — two switches contradict; reconcile deferred. See change: surface-automation-enable. |
| `automation-toggle.ts` | `flipAutomationDisabled(cwd,name,enabled)`: read `<cwd>/.pi/automation/<name>/automation.yaml`, set ONLY `disabled=!enabled` via `yaml` Document API (comments + other fields preserved), re-validate, atomic tmp+rename. Throws `AutomationNotFoundError` when absent (never creates). `badAutomationName()` rejects empty/`..`/`.`/`runs`/separators/NUL. `listInvoicebotAutomations(cwd)` scans `.pi/automation/` for `invoicebot`-prefixed dirs → `{name,enabled}` sorted. First invoicebot FS-write surface (no engine port — no `ib_*` touches `disabled`). Repeated flips re-arm live via automation-plugin watcher (eventual-consistent; atomic rename needs its periodic re-attach). See change: surface-automation-enable. |
| `blob.ts` | `resolveBlobPath(cwd, handle)` → `{ok,abs}` \| typed reject (`invalid-input`\|`traversal`\|`not-found`). Two-stage containment under `<cwd>/.pi/flows/invoicebot-state/blobs/`: lexical (defeats `..`/absolute) + realpath (defeats symlink) + isFile. `contentTypeFor(ext)`: pdf/png/jpe?g, else octet-stream. See change: serve-invoice-original-blob. |
| `session-link.ts` | `createSessionLink(deps) → {dispatchFlow, resolveSessionId, links, dispose}`. Reuse a live cwd-matched invoicebot session via `emitEventToSession`, else spawn + correlate by stamped `automationRun.runId` (NEVER cwd — footgun); deliver-on-register. `isInvoicebotSession` gate (cwd match + `automationRun.name` starts `invoicebot`). `invoice_id ↔ sessionId` map. `resolveSessionId` → link, else `listAll` scan, else null. `SessionLinkDeps.spawnSession` opts gain optional `env`; `spawnAndBind` sets per-invoice scope env `{IB_TOOLSET:"scoped-invoice", IB_INVOICE_ID:<id>}` ONLY when an `invoiceId` is bound — an unbound spawn ("Ask"/Kérdezz) carries no scope env, keeps the full surface. See change: scope-session-toolset-by-profile. |
| `__tests__/engine.test.ts` | Fake per-selector `details` shapes + flow-vs-pure classification (§3.5). |
| `__tests__/routes.test.ts` | Forwarding, 400s (missing cwd/selector/bad dir), cwd isolation (concurrent A/B), flow dispatch + sessionId, consequential flags (§4.5, §5.5). Fastify `inject` + recording stub engine. |
| `__tests__/automation-toggle.test.ts` | Flip helper (enable↔disable toggles ONLY `disabled`, byte-identical elsewhere + inline comment preserved; never-create throw) + `/automation` routes (flip 200/`{ok,name,enabled}`; missing/traversal/slash name → 400 no write; absent → 404; bad/absent/NUL cwd → 400; non-boolean enabled → 400; GET lists two + one). Faux, zero-network. See change: surface-automation-enable. |
| `__tests__/automation-rearm.integration.test.ts` | Integration: real automation-plugin watcher + scheduler. Flip enable → scheduler arms `folder:invoicebot-intake`; re-attach (models production `attachWatchers()`), flip disable → disarms. No reload. Faux, zero-network. See change: surface-automation-enable. |
| `__tests__/blob.test.ts` | Unit: `resolveBlobPath` happy path, `..`/absolute/symlink → traversal, missing file → not-found, blank cwd/handle + NUL → invalid-input; `contentTypeFor` mapping. See change: serve-invoice-original-blob. |
| `__tests__/blob-route.test.ts` | Route: PDF/PNG/octet-stream inline + nosniff + Accept-Ranges (3.1); `Range: bytes=0-99` → 206 + Content-Range, unsatisfiable → 416 (3.2); traversal/absolute/symlink → 403, absent → 404, missing params → 400 (3.3). See change: serve-invoice-original-blob. |
| `__tests__/session-link.test.ts` | Reuse; spawn+runId correlation (same-cwd decoy not mis-bound); stale/unrelated sessionId → spawn, never injected; bind-timeout → spawnToken; resolveSessionId link/scan/null (§5.5, §6.1). |
| guard | `session-link` marks its spawns `guard: true` (change: constrain-agent-tool-surface) → host disables built-in tools for the spawn AND registers the workspace cwd as guarded, so the client-spawned "Ask"/Kérdezz session there is guarded too (origin ∪ cwd). |
