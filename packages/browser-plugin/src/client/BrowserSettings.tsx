/**
 * Browser settings section — PLACEHOLDER (workstream 2a scaffold).
 *
 * The manifest claims this component for the `settings-section` slot so the
 * claim resolves from day one; the real profile list / token paste / kill
 * switch UI is workstream 4's task (see change: add-browser-relay task 4.1).
 * No user-facing strings yet (i18n stub stays empty).
 */
export function BrowserSettings(): null {
  return null;
}
