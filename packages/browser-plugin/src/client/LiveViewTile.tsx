/**
 * Live-view tile — PLACEHOLDER (workstream 2a scaffold).
 *
 * Claimed for the `content-view` slot so the manifest resolves; the real
 * screencast tile (browser_relay_frame rendering, no-frames overlay,
 * DevTools-conflict notice) is workstream 4's task (see change:
 * add-browser-relay task 4.3).
 */
export function LiveViewTile(): null {
  return null;
}
