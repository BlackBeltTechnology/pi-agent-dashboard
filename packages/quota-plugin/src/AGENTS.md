# DOX — packages/quota-plugin/src

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `client.tsx` | Client entry. `QuotaWidget` (content-inline-footer): ONE COMPACT LINE per provider — label + slim 56px bar, NO percentage number — from `GET /api/quota` (polled 60s), fill = pace severity of worst window, `now` tick, `title` tooltip; renders `null` when empty (honest degradation). Stays in `content-inline-footer` on purpose: the composer's context slider is draft-conditional (vanishes on empty input), so aligning with it would hide quota too. `QuotaDialog`: shared `ui:dialog` primitive via `useUiPrimitive(UI_PRIMITIVE_KEYS.dialog)`, `All · per-provider` selector, per-window pace bars + projected %, plus a `quota-now-legend` naming the vertical tick as `now`. `QuotaSettings` (settings-section): printed ToS WARNING (not a gate) + master `enabled` + per-provider toggles GATED on installed peers (`useQuotaSources()` reads `sources` from `/api/quota` once per mount; unservable rows render disabled at 0.55 opacity, forced-unchecked, with a `quota-provider-<id>-needs` hint naming the package that would unlock them — absent/failed `sources` ⇒ treat as nothing installed, i.e. disable rather than silently enable), committed through the host Settings panel's GLOBAL Save via `useSettingsDraftSource({id:"plugin:quota"})` — no local Save button. Anthropic INCLUDED. Re-exports `catalog`. Imports only client-safe modules (never the server entry / pi-quotas). See change: fix-quota-plugin-defects. |
| `pace.ts` | Pure pace/burn-rate math. `computePace({usedPercent,resetsAt,windowSeconds}, now)` → `{state, elapsedPercent, projected, overage, warn, severity}`. Guards BEFORE dividing: `windowSeconds<=0`/non-finite→unavailable; non-finite `resetsAt`→unavailable; `secondsToReset<=0`→stale; `elapsedRaw<=PACE_EPS(0.01)`→unavailable. Severity green/orange/red (`projected>=150` or `used>=90`→red). `paceLabel()` → `over by X%`/`on pace`/`pace unavailable`/`reset pending`. Never `Infinity`/`NaN`. |
| `pace.test.ts` | Unit tests for `computePace`/`paceLabel`: on/ahead/critical pace, just-reset→unavailable, stale, `windowSeconds<=0`/NaN `resetsAt`→unavailable, ms↔s consistency. |
| `sources.ts` | SINGLE SOURCE OF TRUTH for the two-peer capability table, shared by server + client. `QuotaSourceId` = `pi-quotas`\|`usage-bars`; `SOURCE_PACKAGES` maps each to its npm package; `PROVIDER_SOURCES` maps provider→sources in PREFERENCE order (first installed wins). `anthropic` lists ONLY `usage-bars` because pi-quotas guards on `token.startsWith("sk-ant-")` and so classifies an OAuth `sk-ant-oat01-` subscription token as a direct API key → `not_applicable` unconditionally (structural, not configurable). `github-copilot`/`synthetic`/`opencode-go` = pi-quotas only; `deepseek`/`minimax` = usage-bars only; `openai-codex`/`openrouter`/`zai`/`kimi-coding` = either. `resolveSource()` picks the serving source, `isProviderServable()` is the settings-checkbox predicate, `requiredPackagesFor()` names the package a disabled row needs, `peerProviderId()` aliases canonical→peer ids (anthropic→claude, openai-codex→codex, kimi-coding→kimi). `TRACKED_PROVIDERS` replaces the old client-side provider list. See change: add-provider-quota-plugin. |
| `types.ts` | Wire contract: `QuotaWindowDto` (label, usedPercent, resetsAt ISO, windowSeconds, optional currency), `ProviderQuota`, `ApiQuotaResponse`, `QuotaPluginConfig` (`enabled` + `providers`; no `acknowledgedToS` — ToS is a printed warning, not a gate). No token fields. |
| `i18n.ts` | i18n `catalog` (zh-CN + hu) merged under `plugin.quota.*`; English fallbacks passed inline at call sites via `useT`. |
| `__tests__/widget.test.tsx` | `QuotaWidget` render tests: renders from `/api/quota` with `now` tick; NO percentage in the compact row; bar-fill severity colour; Anthropic rendered (no longer excluded); renders nothing when empty or on fetch failure. |
| `__tests__/settings-gating.test.tsx` | `QuotaSettings` gating tests (mock runtime hooks + `/api/quota` `sources`): only pi-quotas → anthropic checkbox DISABLED w/ `needs @hk_net/pi-usage-bars` hint, copilot enabled; only usage-bars → anthropic enabled, copilot/opencode-go disabled, dual-source `zai` still enabled; both → every provider tickable, no hints; NO peer → all disabled (never silently enabled); Anthropic ToS warning stays on its own row. |
| `__tests__/sources.test.ts` | Capability-table semantics: pi-quotas never serves anthropic, usage-bars does; copilot/synthetic/opencode-go pinned to pi-quotas; dual-source providers prefer pi-quotas and fall back to usage-bars; nothing servable with no peer; every provider servable by ≥1 source; `requiredPackagesFor`/`peerProviderId` mappings. |
| `__tests__/dialog.test.tsx` | `QuotaDialog` tests against the REAL client-utils `Dialog` primitive: `role=dialog`+`aria-modal`, pre-selection, selector switch (All / per-provider), Esc closes. |

See change: add-provider-quota-plugin.
