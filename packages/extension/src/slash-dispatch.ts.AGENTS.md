# slash-dispatch.ts — index

Extension slash-command dispatch (routing-step 9). ONE in-process call: gate pi >= 0.84.2 → `pi.sendUserMessage(text, {expandPromptTemplates: true, deliverAs})`, in every session kind (headless RPC, tmux, terminal, user-launched) — no session-kind probe. Exports `tryDispatchExtensionCommand`, `FeedbackSink`, `_resetDispatchWarnings` (test seam). `MIN_DISPATCH_PI_VERSION` `[0,84,2]` + local `compareTriplet` yield three distinct gate outcomes: parseable-below → `started` + `error` "…require pi 0.84.2+"; unparseable-non-empty or reader-throws or `undefined` → treated as NEW + `console.warn` once per process per reason (module-level `warnedReasons` Set); >= 0.84.2 → dispatch. Reader is injectable (6th param, defaults `readRunningPiVersion` — argv-anchored, never a by-name hoisted copy). Detection (`pi.getCommands()` + `isExtensionSlashCommand`) sits INSIDE the helper's try so a stale-ctx throw returns `false` (caller falls through); callers never evaluate detection themselves. `started` precedes the version read so the old-pi branch still reports a terminal; read + call both sit in one try so no throw escapes between them. Guarantees EXACTLY ONE `started` + EXACTLY ONE terminal (`completed` xor `error`). Retired by change `retire-slash-dispatch-via-expand-prompt-templates`: Path B (`pi.dispatchCommand`, never shipped), Path C (`dispatch_extension_command` → server → keeper UDS → pi stdin), Path D (tmux/WT error feedback), the `connection` param and the `DispatchConnection` type. `deliverAs` is inert for an extension command (pi consults `streamingBehavior` only AFTER `_tryExecuteExtensionCommand`) — forwarded for uniformity. Header pins the verified pi 0.86.1 line refs.
