/**
 * mcp-client-plugin · CORE entry — pure, host-free logic.
 *
 * No host or React imports here: this entry is imported by the hostless
 * `apple-tools` installer CLI as well as by the dashboard server. The full
 * implementation lands in change extract-mcp-client-plugin sections 2-3.
 */
export {};
