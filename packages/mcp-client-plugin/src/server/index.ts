/**
 * mcp-client-plugin · SERVER entry — dashboard plugin registration.
 * Full implementation lands in change extract-mcp-client-plugin section 4.
 */
import type { ServerPluginContext } from "@blackbelt-technology/dashboard-plugin-runtime/server";

export function registerPlugin(_ctx: ServerPluginContext): void {
  // wired in section 4
}
