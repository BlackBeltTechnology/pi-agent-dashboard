# DOX — packages/mcp-client-plugin/src/client

Files in this directory. One row per source file. See change: extract-mcp-client-plugin.

| File | Purpose |
|------|---------|
| `McpSettings.tsx` | `settings-section` root. `McpSettingsClaim` → `McpSettings`. ONE adapter status (`useAdapterStatus`) drives pill + banner + page-wide `readOnly`. Error states: 504 `adapter-timeout` (retry + `#mcp-adapter-timeout` link), 403 `not-allowed`, generic. `EditorForEditing` mounts `ServerEditor` keyed on `editing` (`""` = add) so a reopen REMOUNTS the editor (reveal state resets). See change: extract-mcp-client-plugin (tasks 7.2-7.4). |
| `ServerEditor.tsx` | Schema-driven server editor (tasks 7.4 + 7.5). Modal `role="dialog"` (`mcp-editor-dialog`), Escape closes, focus in on open / back to trigger on close. Fetches the schema itself (`fetchSchema`). Transport tabs (`TRANSPORT_FIELDS` grouping; switching CLEARS the other transports' keys from the draft so `transport-conflict` cannot occur; initial tab = `transportOf(entry)` else `command`). Advanced `<details>` (`COMMON_FIELDS` split, collapsed default). ONE primary Save → `patchServer(name, {...scopeToWire({kind:"global"}), set, unset})` via `computePatch` (changed keys only; unknown fields preserved; sentinels never sent); `ApiError.fields` mapped inline + message in summary. Non-editable server → View variant (fields as text, footer `Override at Pi global` → override draft); page-wide `readOnly` → view render + Save disabled. Atomic-override per-field action (`mcp-override-<name>`) starts draft `{}` + "N inherited keys incl. K secrets" note (`atomicCounts`). Secrets: password inputs + per-field reveal (`mcp-reveal-<name>`); redacted sentinels render placeholder, NO reveal, excluded from patch. |
| `ServerList.tsx` | Global server list (tasks 7.2 + 7.3). Exports `ServerRow`, `ServerList`, `transportOf`, `provenanceBadges`, `isEditable`, type `Badge`; re-exports `Transport` from `schema.js`. Rows sorted by name; badges Pi global / Shared 🔒 / Other; parse-error rows; empty + skeleton states; enable/disable switch with pending + revert-on-failure (`setServerDisabled` global scope). |
| `api.ts` | REST surface over `/api/mcp-client/*`. `API_BASE`, `EffectiveResponse`, `ScopeWire`, `scopeToWire`, `ApiError` (`.isAdapterTimeout` 504, `.isNotAllowed` 403, `.fields`), `fetchEffective`, `fetchSchema`, `fetchAdapterVerdict`, `patchServer` (set/unset), `removeServer`, `setServerDisabled`, `patchSettings`. No caching, no React; types TYPE-ONLY from core (browser bundle must not pull `node:*`). See change: extract-mcp-client-plugin (task 7.1). |
| `hooks.ts` | `useEffectiveConfig`/`loadEffective`/`invalidateEffective` — module-level store, ONE in-flight request per key, cached success. `useAdapterStatus`/`deriveAdapterStatus`/`AdapterStatus` — the single status object (pill/banner/readOnly agree). See change: extract-mcp-client-plugin (task 7.1). |
| `index.tsx` | Client barrel for the plugin manifest. Exports `McpSettingsClaim`. |
| `schema.ts` | Schema model over `schema/mcp-config.schema.json`. `fieldsOf` (dotted `FieldSchema` incl. nested-group children), `widgetFor` (NEVER null — unknown shapes → `json` fallback; toggle-list = boolean+string[] oneOf; nested-group = x-atomic + object branch), `atomicFieldsOf` (derived from `x-atomic`: env, headers, oauth, requestHeadersCommand, searchKeywords). `TRANSPORT_FIELDS` + `visibleUnderTransport` (x-transport markers + spec tab groups). Masking: `SECRET_KEY_PATTERN` = `/authorization\|token\|key\|secret/i`, `isSecretKeyName`. Redaction: `isRedacted`, `redactedKeys`, `atomicCounts`, `stripRedacted` (recursive — sentinel never sent). Draft model: `clone`, `deepEqual`, `computePatch` (set = changed-after-strip, unset = removed), `getPath`/`setPath`, `validateDraft` (transport primary required, numeric/enum/list/record/JSON checks). See change: extract-mcp-client-plugin (tasks 7.4, 7.5). |
