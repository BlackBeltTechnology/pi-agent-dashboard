# DOX — packages/automation-plugin

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `README.md` | Package overview. Schedule-triggered background agent runs. Reads automation defs: per-folder `<repo>/.pi/automation/<name>/automation.yaml`, global `~/.pi/automation/<name>/automation.yaml`. Server-owned scheduler arms triggers (phase 1 cron `schedule`). Fire → spawns pi session `kind="automation"` (model, action `prompt`\|`skill`, mode, sandbox). Runs watchable in Automation view. Results → `<scope>/.pi/automation/runs/<date>-<name>/result.md`. Keeps last 100 per automation. |
| `src/__tests__/async-work-source-fanout.test.ts` | Fan-out over an ASYNC work source (`schedule.batch`): one child per vended item, distinct item per child, per-child action `env` forwarded to the spawn (C-ENV), stable idempotency key + automation's own run name, bound caps + excess DEFERS, and the three async lease invariants (empty vend → `done` + zero children, rejected vend → `error` + nothing leased, failed spawn → its handle nacked). Replaces the retired per-invoice-fanout + run-now-fanout tests. See change: relocate-fanout-to-work-source. |
| `src/__tests__/run-work-item.test.ts` | `engine.runWorkItem(automation, key)`: one child for the named item (no fan-out), `in_flight` refusal on a live lease, different key unblocked, guard shared with batch fan-out in BOTH directions, dead run releases its item, `unsupported` without `take`/without a source. Replaces the retired run-invoice + single-flight-per-invoice tests. See change: relocate-fanout-to-work-source. |
| `src/__tests__/work-source-registration-seam.test.ts` | The cross-plugin registration seam (D-WSREG): contribution collection (single + array, malformed/duplicate dropped with a warning), lazy provider resolution (published AFTER the provider is still found), local id wins a collision, a throwing provider is isolated. See change: relocate-fanout-to-work-source. |
| `vitest.config.ts` | Vitest config for automation-plugin package. jsdom env, `pool: "forks"`, `maxWorkers: "50%"`, includes `src/**/__tests__/**/*.test.{ts,tsx}`. globalSetup imports `@blackbelt-technology/pi-dashboard-shared/test-support/setup-home.ts`. |
