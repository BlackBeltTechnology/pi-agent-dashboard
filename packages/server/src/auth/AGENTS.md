# DOX — packages/server/src/auth

Files in this directory. One row per source file. See change: fold-oversized-agents-directories.

| File | Purpose |
|------|---------|
| `auth-plugin.ts` | Fastify plugin registers OAuth routes + `onRequest` JWT gate. → see `auth-plugin.ts.AGENTS.md` |
| `auth.ts` | OAuth2 core: providers (GitHub, Google, Keycloak, generic OIDC via `.well-known` discovery), JWT sign/verify,… → see `auth.ts.AGENTS.md` |
| `bearer-auth.ts` | Bearer device-auth branch (D5/D7). `registerBearerAuth(fastify,{registry})` adds an `onRequest` hook… → see `bearer-auth.ts.AGENTS.md` |
| `bind-reachability-service.ts` | Server inputs to the shared reachability predicate. `initBindReachability({resolvedBindHost, hostFlag})` freezes the boot value; `computeBindReachability(loadConfig)` re-resolves `pendingBindHost` against the CURRENT config; `getLastBindReachability()` backs the WS connect-replay; `formatBindReachabilityWarning` emits the `[bind-reachability]` startup line. Deliberately NOT on `/api/health` (no preHandler; this is network topology). See change: warn-unreachable-trusted-networks. |
| `bridge-ticket-eligibility.ts` | WHO may mint a `bridge`-scoped WS ticket. `decideBridgeTicketMint({authorization,ip,headers,verifyDeviceBearer})` — allows a paired-device durable bearer or a genuinely-local caller ONLY; a cookie session or trusted-network host is refused, because the bridge surface (session registration + event attribution) is more privileged than `/ws`. Enforced in `server.ts::/api/ws-ticket`. See change: add-pi-gateway-transport-identity (task 6.2, @review Audit). |
| `bypass-urls.ts` | Import-free leaf: `isBypassed(url, bypassUrls)` — `auth.bypassUrls` prefix match (`startsWith`). Extracted from `auth-plugin.ts` so the OAuth plugin AND the universal network guard share ONE predicate without a cycle (the plugin already imports the guard). Keep import-free. The two callers MUST agree — a divergence opens or closes one of the two paths. Re-exported from `auth-plugin.ts` so its import surface is unchanged. See change: add-universal-network-guard. |
| `cors-origin.ts` | Pure CORS origin allow-decision extracted from the `@fastify/cors` callback in `server.ts` so it is… Adds `getLiveTunnelOrigins` — every CONNECTED tunnel origin, compared origin-to-origin (a provider URL carrying a path would never match a raw string equality). Deliberately a SEPARATE input from `getTunnelUrl`: widening who may READ must not widen which origin mints OAuth URIs. The pre-existing `*.share(s).zrok.io` wildcard is UNCHANGED for CORS. Now ALSO the admission decision — `isOriginAdmitted` / `isWsOriginTrusted` / `isMutationOriginTrusted` + `sanitizeHeaderForLog`, sharing one path with CORS so read-allowance and act-allowance cannot drift. See change: fix-ws-origin-cswsh. Carries the host-admission options (`allowedHosts`/`publicBaseUrls`/`bindHost`/`hostGateMode`) on the one shared options object and tightens `isSameOriginByHost` to require an admissible Host ONLY in `enforce` mode. See change: add-host-allowlist-admission. → see `cors-origin.ts.AGENTS.md` |
| `csp.ts` | Baseline Content-Security-Policy (defense in depth). `buildCsp()` (default-src/object-src… → see `csp.ts.AGENTS.md` |
| `host-admission.ts` | Pure Host-admission decision + admitted-source classifier (issue #637, D1). `isHostAdmitted(hostHeader, opts)`, `classifyAdmittedHostname`, `safeLiveTunnelOrigins` (throwing source fails empty, X4), `HostAdmissionOptions` (allowedHosts / publicBaseUrls / configuredOrigins / getLiveTunnelOrigins / bindHost). Hostname-only: port stripped, IPv6 brackets stripped, trailing dot stripped, case-folded. First-match order loopback → `net.isIP` literal → bindHost name → `<label>.local` → publicBaseUrls → cors origins → live tunnels → allowedHosts; missing/malformed fails closed; unparseable URL entries skipped. See change: add-host-allowlist-admission. |
| `host-gate.ts` | The Host-admission gate + operator state (issue #637, D1/D4/D5/D8). `resolveHostGateMode(env, configMode)` (recognised env wins, else live config, else `report`), `hostGateEnvWarning` (one-shot boot line for a typo'd env value), `HostGateState` (refusal ring: 50 LRU-by-lastSeen, every refusal counted, `(malformed)` key; rate-limited log: 1/host/min, 60/min global, one `suppressed <n>` summary, 256-host map), `createHostGate` (onRequest factory, registered BEFORE cors), `evaluateHostGate` (shared by REST hook + WS upgrade), `admittedHostRows`, `buildHostGateResponse`, `wantsHtml`, `renderHostGateHtml` (escaped Host, no script/assets, no admitted-host enumeration), `hostGateRefusalBody`. See change: add-host-allowlist-admission. |
| `identity.ts` | Persistent Ed25519 server identity (D2, TOFU pinning). `ensureServerIdentity(path?)` generates/loads keypair… → see `identity.ts.AGENTS.md` |
| `local-token.ts` | Local-IPC allowlist token (D10, narrowed). `ensureLocalToken(dir?)` writes high-entropy secret to… → see `local-token.ts.AGENTS.md` |
| `localhost-guard.ts` | Network access guard: `createNetworkGuard(trustedNetworks, {localToken?})`… → see `localhost-guard.ts.AGENTS.md` `isLoopback`/`LOOPBACK_ADDRESSES` now live in `auth/loopback.ts` (imported, NOT re-exported — a re-export keeps the cycle edge). See change: cleanup-import-cycles. Guard semantics UNCHANGED by the reachability advisory — it never consults the predicate. See change: warn-unreachable-trusted-networks. Now ALSO the single universal guard: `createNetworkGuardHook` (jurisdiction `/api/`,`/v1/`,`/editor/`,`/live/`; deny-by-default within; pass conditions identical to `createNetworkGuard`) + `guardPathname`/`isGuardJurisdiction`/`GUARD_DENY_REASON` + the shared `sendNetworkDenied`. See change: add-universal-network-guard. Exports `forwardingHeaderNamesPresent(headers)` — NAMES only (extended plugin list, superset of core), never values; diagnostics use. See change: harden-ios-safari-memory-and-ws-diagnostics. |
| `loopback.ts` | Import-free leaf: `LOOPBACK_ADDRESSES` set + `isLoopback(ip)`. Extracted from `localhost-guard.ts` so it and `tunnel/tunnel-block-events.ts` depend on this leaf instead of each other (broke the 2-edge cycle). Keep import-free — an import back into either former side re-forms the cycle. See change: cleanup-import-cycles. |
| `mutation-origin-gate.ts` | Cross-site MUTATION gate (issue #625). `createMutationOriginGate(getOpts)` → Fastify `onRequest` hook: gates `/api/*` + `POST /auth/logout`, skips GET/HEAD/OPTIONS, refuses `403 {error:"untrusted origin"}` when `isMutationOriginTrusted` denies, logs one sanitized `[csrf-gate]` line. Matches on `req.routeOptions.url` (post-routing route PATTERN) — NEVER raw `req.url`, which path tricks defeat. Registered in `server.ts` after the CORS plugin. A factory, not an inline hook, so the rule is `fastify.inject`-testable. See change: fix-ws-origin-cswsh (D4). |
| `node-guard.ts` | Re-exports `isAffectedNode`/`isOutOfEnginesRange` from shared `node-version.ts` (public API unchanged). → see `node-guard.ts.AGENTS.md` |
| `pi-oauth-types.ts` | Local structural types for the slice of pi-ai's OAuth surface the server drives: `LoginPrompt` (text/secret/select/manual_code), `LoginEvent` (info/auth_url/device_code/progress), `LoginInteraction`, `OAuthLoginFlow`, `OAuthRegistryEntry`. Local because server must never import `@earendil-works/pi-ai` (hoisted 0.75.5 predates bundled OAuth providers); pi-coding-agent re-exports `ModelRuntime` but not these types. Drift surfaces as `error: "unsupported prompt: <kind>"` on one flow, never a crash. See change: delegate-provider-oauth-to-pi-ai (D1, D3). |
| `provider-auth-adapter.ts` | `AuthInteraction` adapter + in-memory flow store. `notify` sets sticky `authUrl` / `device_code` pending / `message`; `prompt` becomes a tagged `pending` resolved by `/flow/:flowId/input` (`secret` rejected). Flow-controller abort REJECTS the pending prompt so pi-ai's `finally` closes the callback listener. Exports `startFlow`, `getFlow`, `deleteFlow`, `flowStoreSize`, `cancelFlow`, `pruneFlows`, `abortAllFlows`, `pendingFlowsFor`, `toFlowStatus`, `startFlowPruneTimer`, `FLOW_START_TIMEOUT_MS`, `SUPERSEDE_SETTLE_TIMEOUT_MS`. TTL 10 min; device-code extends to `expiresInSeconds + 60 s`; `expired` derived from deadline, never message text; ids `crypto.randomUUID()`. See change: delegate-provider-oauth-to-pi-ai (D1, D2, D6, D7). |
| `provider-auth-handlers.ts` | Barrel re-export: `OAuthRegistryEntry` type + `getOAuthRegistry` from `provider-auth-registry.ts`. Keeps the storage/routes import surface stable. → see `provider-auth-handlers.ts.AGENTS.md` |
| `provider-auth-registry.ts` | Builds the OAuth provider registry once, lazily, from `ModelRuntime.create({modelsPath:null, credentials:EMPTY_READONLY_STORE})`; filters `auth?.oauth`, excludes `radius`. `mapProviders()` pure; `oauthRegistryReady()`, `getOAuthRegistry()`, `getRegistryError()`, `resolveVersionFallback()`, `FLOW_TYPE_HINT` (UI hint only). Any failure → empty snapshot + error naming resolved pi-coding-agent version; never rejects. See change: delegate-provider-oauth-to-pi-ai (D1, D3, D6). |
| `provider-auth-storage.ts` | Reads/writes `~/.pi/agent/auth.json` for pi provider credentials via `proper-lockfile` + atomic write. Async writers, bounded `ELOCKED`-only retry, `realpath:false` preserved. OAuth rows = registry ∪ stored `{type:"oauth"}` ids. → see `provider-auth-storage.ts.AGENTS.md` |
| `spawn-token.ts` | Spawn correlation token. Exports `mintSpawnToken()` (UUIDv4), `SPAWN_TOKEN_ENV_VAR =… → see `spawn-token.ts.AGENTS.md` |
| `test-env-guard.ts` | Exports `isUnsafeTestHomeScan()` — defense-in-depth against destructive PID-registry sweeps during vitest… → see `test-env-guard.ts.AGENTS.md` |
| `ws-ticket.ts` | Single-use WS upgrade tickets (D11/F4/F6). `WsTicketStore(now?)`: `mint(scope)` high-entropy in-memory ticket… → see `ws-ticket.ts.AGENTS.md` |
| `route-tier-gate.ts` | `createRouteTierGate`, `tierRefusalFor`, `sendTierRefusal`. Global `onRequest` hook after both admission hooks; refuses an off-host device bearer below its route's `ROUTE_TIERS` tier with 403 + `WWW-Authenticate: Bearer error="insufficient_scope", scope="<tier>"`; genuinely-local/trusted exempt; refuses only, never admits. See change: expand-mcp-tiered-surface. |
| `ws-upgrade-reject-log.ts` | `createWsUpgradeRejectLogger({windowMs=60_000,maxKeys=256,now,log})` → `{log(rejection), trackedKeys()}`. Line `[ws-upgrade] rejected status= scope= peer= fwd=<names\|none> ticket=<present\|absent>[ suppressed=N]`. Never reads header values, cookies, ticket strings. One line per `status\|scope\|peer` per window; first line after the window carries `suppressed=`; state capped (prune expired, then evict oldest). One instance per server; called only from the silent bridge-400 / auth-401 / no-auth-403 upgrade branches in `server.ts`. See change: harden-ios-safari-memory-and-ws-diagnostics. |
