# DOX — packages/server/src/access

Files in this directory. One row per source file.

| File | Purpose |
|------|---------|
| `access-denials.ts` | Path-denial registry (design D20): `recordPathDenial`, `getPathDenial`, `__resetPathDenials`. A containment refusal records `{denialId, subject, site, session, ancestors, at}` so the remedy a grant binds to is one the OPERATOR saw, never a free-form path (design D12/D15). `denialId` is a `randomUUID`, returned only in the denial body and never by any read route. Bounded TTL + CAP (oldest evicted on record); an evicted or expired id resolves to `undefined`, so a grant against it is refused (409/410 class). Written ONLY by the denial path — no route creates an entry (task 9a.31/9c.6). See change: add-access-grants-and-review. |
| `access-grants.ts` | The path-grant store (design D2/D10/D11/D16/D17). `recordGrant` / `listGrants` / `revokeGrant` / `grantedSubjects` / `normalizeGrantSubject` / `__resetAccessGrants`. Subject = **realpath at grant time** (a retargeted symlink does not move the grant); non-directory subjects normalize onto their containing directory. Per-scope cap 200 with oldest-by-`grantedAt` eviction, never crossing scope. Atomic write (tmp+rename) → an interrupted write leaves old-or-new, never truncated. Load-once cache; `revokeGrant` invalidates it so revocation takes effect without a restart. A write failure returns `{ok:false}` and leaves the cache untouched, so the admitted set never widens on a failure (D11). **Forbidden backstop:** `isUngrantableSubject` is re-applied to the NORMALIZED subject, because normalizing a FILE subject onto its parent could otherwise store `$HOME` for a denial that named `$HOME/<file>` (design D15). Store path overridable via `PI_ACCESS_GRANTS_STORE`. See change: add-access-grants-and-review. `AccessGrant.via?: "prompt"` (+ `RecordGrantInput.via`) marks grants written by a prompt verdict; optional, additive, no migration (task 8.2). |
| `access-health.ts` | `/api/health` counters (design D11, task 9.3): `snapshotAccessGrantHealth(deps)` + `AccessGrantHealth` = `prompting` {enabled, killSwitch, hostGateMode, operatorChannels}, `registry` (every transition counter, degrade and flood reasons counted SEPARATELY so per-channel starvation is told apart from the global cap, plus `pending`), `yolo` {active, source, unscoped, roots, expiresAt, cumulative autoAllowed / refusedByPriorRefusal}, `refusals`. Lets an operator tell "prompting off" / "report mode" / "rate-limited" / "YOLO answering" apart without reading logs. See change: add-access-grant-dialog. |
| `access-plane.ts` | The plane registration seam (design D5/D2a/D3, tasks 5.1/5.5/2b.2/8b.1). `AccessPlane` = `HeldAccessPlane | DeferredAccessPlane`, `AccessPlaneRegistry`, `promptPrecondition`, `holdsRequest`, `persistVerdict`. A plane owns subject normalisation (`subjectOf`/`keyOf`), prompt copy (`describe`) and its OWN store write (`grant`). `yoloEligible` is typed `?: false` on a deferred plane (unrepresentable) and `register` REJECTS a cast `true` at runtime (#X9); duplicate id and unknown mode also throw. `mode` is a ceiling: `promptPrecondition` applies the ladder to every plane alike, in order: disabled/kill switch, then report-mode (every plane, D2), then the per-mode proof (D2a): HELD requires the REQUEST to hold a capability, DEFERRED requires a live operator channel and asks nothing of the request. `holdsRequest` is true only for a held plane whose precondition passed, so a deferred denial never suspends. `persistVerdict` writes ONLY on `allow-always`; `allow-once`/`deny` persist nothing. See change: add-access-grant-dialog. `isGrantPromptKilled(env)`: `PI_DASHBOARD_DISABLE_GRANT_PROMPT` engages ONLY on the exact value `1` and maps to the `disabled` rung, so it suppresses prompts while grants stay in force and denials are still recorded (task 9.1). |
| `ancestor-ladder.ts` | `offeredAncestorLadder(subject)` — the nearest-first rungs a denial remedy may also offer (task 7b.1a). Stops BELOW `$HOME` when there is no repository, and AT the checkout root inside one; never offers a symlink, never the filesystem root, and never a rung that SUBSUMES a forbidden subject (`/private` would admit `/private/etc` on macOS) or sits inside a secret store. Degrades to the no-repo boundary when `git` is unavailable. A ladder failure must never turn a denial into a 500 — `containment-gate` catches and falls back to no offer. See change: add-access-grants-and-review.  Unresolvable subject (dangling symlink, missing dir) -> `[]`, never lexical tail (#X8). Checkout-root probe cached per real dir: `CHECKOUT_ROOT_CACHE_TTL_MS`=5000, `CHECKOUT_ROOT_CACHE_MAX`=256 (oldest evicted), throws not cached, `__resetCheckoutRootCache`; forbidden sets derived once per ladder. p95 ~1.3 ms 12-deep (#P3, was ~26 ms git spawn per denial). See change: add-access-grant-dialog. |
| `capability-issuance.ts` | Who is issued a prompt capability (design D1a, tasks 2b.1/3.5). `shouldIssuePromptCapability(headers, corsOpts)` + `UpgradeHeaders`. `isOriginAdmitted` returns true for an ABSENT Origin (`cors-origin.ts:244`) so non-browser local clients keep working, which is exactly why admission alone cannot gate issuance. Requires ALL of: a non-absent, non-empty Origin that `isOriginAdmitted` admits (same-origin-by-Host, or configured with the zrok wildcard DISABLED); `Sec-Fetch-Site` of `same-origin` or `cross-site` (`cross-site` exists for the `pi-dashboard.dev` shell and qualifies only because the admission rule already admitted its Origin; `same-site`/`none`/absent refuse). The UI credential tier is enforced by the upgrade gate BEFORE the socket exists (`server.ts:2938-2950`), so it is not re-derived. Repeated (array) headers refuse. Fail-closed on anything missing. These are PROVENANCE SIGNALS, NOT AN AUTHENTICATION BOUNDARY: a same-machine process can forge them (design D1b R-A/R-C). Honest caveat, tested: a DNS-rebound page is same-origin-by-Host, so in `report` mode it IS issued a capability; what stops it is `maySuspend`'s `enforce` requirement, not issuance. Under `enforce` it is not issued one. See change: add-access-grant-dialog. |
| `canonical-subject.ts` | Volume-appropriate canonical form for access-subject comparison (task 2b.3a). `volumeCaseInsensitive` / `canonicalSubject` / `isSameSubject` / `isSubjectWithin`. Probes case sensitivity FROM THE VOLUME (flip an existing component's case, ask whether the flipped sibling exists; memoised per device) instead of assuming it from `process.platform` as the codebase-wide `samePath` does — which is wrong for a case-SENSITIVE volume on a case-insensitive host (APFS case-sensitive, a container bind mount, a network share), where two subjects differing only in case are different objects and folding them together silently widens a grant. NFC-normalises before folding. Containment is component-wise (`path.relative`), never a string prefix, so `/repo-secrets` is not inside `/repo`. An unresolvable path returns `null` and never matches. Deliberately does NOT modify `samePath`: that primitive's semantics are depended on across the server, so changing them here would alter shipped containment everywhere at once; the boundary is that a caller comparing an ACCESS SUBJECT uses this module. See change: add-access-grant-dialog (task 2b.3a). `isSubjectWithin` treats a child named `..foo` as inside (only `..` / `../x` escape). See change: add-access-grant-dialog (2b.7 review). |
| `containment-gate.ts` | The ONE gate every body-emitting file-read site routes through; before it each of the ten sites inlined `isAllowed` and hand-rolled its own 403, which is why remedy fields could not be added consistently. `evaluateContainment(resolved, anchors, {site, session, allowGrant?, subjectKind?})` order is load-bearing (design D1/D9): ①/② `isAllowed` (untouched, still authoritative) → ③ the grant predicate ONLY on a miss → record the denial and return the ADDITIVE remedy (`reason`/`hint`/`subject`/`denialId`/`ancestors`), leaving every pre-existing status code and `error` string byte-identical (design D7). The remedy's subject comes from `remedySubject(resolved, subjectKind)`: `"file"` (default) = `grantableSubjectOf` (lexical dirname) because granting the containing directory is what unblocks a refused file; `"directory"` = the refused directory ITSELF, since naming its parent would offer every SIBLING tree in one click; `"auto"` = whichever the target actually is, for polymorphic sites (`/api/file`, `/api/file/exists`). `allowGrant:false` returns BEFORE any denial or remedy is created, so a grant-ineligible site (the two OS spawn routes) can never originate a grant it could not honour (task 4.5). A body-less site (`grep-routes`, `resolve-file-mention`) consumes grants but can never originate one. See change: add-access-grants-and-review. Takes an optional `hold: HoldTarget` (the live request/reply) so a site can SUSPEND a miss while the operator is asked; every miss is reported through `holdDenial` either way (no `hold` = recorded, answered at once). An allow verdict authorises a RE-EVALUATION, never a skip (`reEvaluate`): layers run again against the filesystem as it is NOW, so a subject swapped for a symlink after the verdict, a revoked grant, or a sibling outside the answered subject is still denied; `allow-always` must be admitted by the persisted grant, `allow-once` applies the answered subject as a transient real-path grant for this request only and never for a forbidden subject. The deny path is untouched, so every site's body is byte-identical. See change: add-access-grant-dialog (tasks 6.2/6.4/2b.5). |
| `denial-hold.ts` | The one call a denial site makes (task 6.1-6.3). `installGrantCoordinator` / `grantCoordinator` / `holdDenial(facts, target?)` + `HoldTarget`. Reads the prompt capability FROM THE LIVE REQUEST at the site (D6), keys the requester by its capability socket or `source:<ip>`, asks the installed coordinator, and for a held denial keeps the connection alive via `awaitHold`. No coordinator installed = immediate deny, so tests and embedders see exactly today's denial. See change: add-access-grant-dialog. Capability-less channel key = `sourceChannel(ip)` (allocation, not address). |
| `forbidden-subjects.ts` | Subjects that may never be granted, and why (design D15). `forbiddenGrantSubjects` / `isForbiddenGrantSubject` / `subsumesForbiddenGrantSubject` / `isUngrantableSubject`. Matching is deliberately SPLIT: `whole` (`/`, `$HOME`, platform system roots) matches EXACTLY, while `sensitive` (`~/.ssh`, `~/.pi`) matches exact-OR-descendant. A blanket descendant rule would forbid every project directory (they all live under `$HOME`) and macOS temp dirs under `/private/var`, making the feature unable to grant anything. `subsumes` rejects an ancestor RUNG that would admit a forbidden subject. Comparison is against realpaths, so `/etc` reached through the `/private/etc` alias is caught. Callers MUST pass the subject that will be PERSISTED, not a pre-normalization input. See change: add-access-grants-and-review.  `subsumesForbiddenGrantSubject(candidate, env, sets?)` accepts precomputed `forbiddenGrantSubjects(env)` for multi-rung callers (#P3). See change: add-access-grant-dialog. |
| `grant-coordinator.ts` | Joins a denial to the registry, the planes and the operator's browsers (design D3/D6/D7/D8, tasks 6.1-6.3). `GrantCoordinator` (`onDenial(ctx, holdable)` -> `Hold`, `onResponse(msg)`, `registry`) + `DenialContext`, `Resolution`, `Hold`. Records via `PendingGrantRegistry`, broadcasts `grant_request` when prompted, arms ONE unref'd expiry timer per entry, and suspends only a held plane whose precondition passed AND whose entry is actually prompted (never on a flooded/degraded entry). First well-formed `grant_response` settles; `persistVerdict` writes only on allow-always; an allow-always whose store write FAILS resolves as a deny (D11: the admitted set never widens on failure). Coalesced held requests share one verdict; the last waiter aborting releases the entry. Waiters are resolved BEFORE the `grant_dismiss` is sent, and every broadcast goes through `send()`, which reports failure instead of throwing: an undeliverable prompt resolves `broadcast-failed` and is not held (#X5), so a gateway failure can neither turn a 403 into a 500 nor hang a held request. Remembers each entry's denying session so allow-always grants are attributed to it. See change: add-access-grant-dialog. Consults `yolo.decide` at the prompt point, BEFORE the registry: an auto-allow returns an `allow-once` resolution with no dialog and no entry (still re-evaluated by the caller), and prompt suppression (disabled / kill switch) does not stop it, since YOLO is itself the explicit opt-in (8b.6); a prior refusal resolves `refused-by-prior-refusal` without prompting. An operator's explicit `deny` on a held, YOLO-eligible plane calls `recordRefusal` (8b.4a). Review fixes (2b.7): `onResponse` (WS) settles only PROMPTED entries under live `enforce`, else ignored; `settle(msg)` = any-surface settle (Access page), never throws, deny `settle-failed` on store/ledger throw, always `finish`. Allow-once releases ONLY first waiter (raiser); coalesced waiters get deny `allow-once-not-shared`. `GRANT_MAX_WAITERS_PER_ENTRY`=8, overflow -> immediate `waiters-full`. Persisted allow-always logs `[access-grant] persisted plane= subject= store= origin=`. `settle`/`onResponse` return `SettleResult` (`{ok,record}` | reason incl. `ignored`). `recentVerdicts()` = bounded ring (`GRANT_VERDICT_HISTORY`=200) of `GrantVerdictRecord {promptId,plane,subject,outcome allow-once|allow-always|deny|expired|aborted|failed,store?,widenedFrom?,at}`; store only on a persisted allow-always (8.1/8.2). |
| `hold-request.ts` | `awaitHold(request, reply, hold)` (design D7, task 6.3): the `git-routes` worktree-init transport in shape. Captures `socket.timeout`, `setTimeout(0)` for the hold, restores it on `reply.raw` `finish` behind `!socket.destroyed`. Client abort is detected on the RESPONSE (`reply.raw` `close` while `!writableFinished`), NOT `request.raw` `close` as D7 first named: measured on Node v24, `request.raw` emits `close` as soon as Fastify has consumed the body, BEFORE the handler returns, so the literal listener would abort every held request. Unheld holds pass straight through without touching the socket. See change: add-access-grant-dialog. |
| `named-grant.ts` | `grantNamedPathSubject` (task 5.2): the ONE path-grant creation sequence, extracted from `POST /api/access/grants` so the route and the filesystem plane cannot drift. Canonical compare of the chosen subject against the denied subject + offered rungs (`unnamed` otherwise); forbidden filter on the NORMALIZED subject (`forbidden`; a lexical-dirname FILE subject normalizing to `$HOME` was a real one-click escalation); `recordGrant` with `widenedFrom`; write failure returned as `write-failed`, never swallowed. See changes: add-access-grants-and-review (origin), add-access-grant-dialog (extraction). Passes optional `via` through to `recordGrant` (8.2). |
| `pending-grant-registry.ts` | The pending access-request registry (design D4/D8/D9/D11, tasks 4.1-4.4 + 2b.6). `PendingGrantRegistry` with `record(input, precondition, now)` / `settle(response, now)` / `forget(promptId)` / `expire(now)` / `list` / `get` / `snapshotStats`, plus `formatTransition` and the `GRANT_*` constants (TTL 120 s, capacity 64, backoff 120 s, 5 prompts/plane/min, 2 concurrent dialogs, 12 entries + 1 dialog per channel, 1 prompt/plane/min per DEFERRED channel). Keyed `(plane, subject)`; subject arrives plane-normalised. Pure: no timers, `now` injected. Departs from `ResyncRequesterRegistry` twice ON PURPOSE: overflow REFUSES the new denial and never evicts a live entry (it may hold a suspended request), and expiry is reported through `onExpire`, never silent. Caller supplies the ladder `precondition` (ineligible / report-mode / disabled / no-audience) from the live request (D6); the registry owns only capacity, channel share, backoff, rates and dialog caps. Refusals (`capacity`, `channel-share`) create NO entry; floods (`backoff`, `plane-rate`, `channel-rate`, `concurrent-cap`, `channel-concurrent`) record an unprompted entry that stays answerable on the Access surface. Per-channel checks run before global ones so starvation is attributed to the requester (E23). A denial joining an unprompted entry re-evaluates it, so a key does not stay unprompted past its backoff. `settle` validates untrusted input: first well-formed answer wins; duplicate (remembered settled id) vs malformed vs unknown are counted separately; wrong plane and `allow-once` on a deferred plane are malformed; a subject that is neither the denied one nor an OFFERED rung is refused and the entry stays pending; `widenedFrom` is set when an offered rung is chosen. Log lines JSON-quote the subject (attacker-shaped CORS origins cannot forge a line). See change: add-access-grant-dialog. `GRANT_DEFERRED_MAX_ENTRIES`=16: deferred entries together capped, refusal `deferred-share` (counted in `flooded`), held keeps >=48 (D9 resolved, rotating sources). |
| `planes.ts` | The four registered planes (tasks 5.2-5.4): `createFilesystemPlane` (held, YOLO-eligible, `normalizeGrantSubject` so verdict and grant subjects are byte-identical and `/a/b` == `/a/b/`, grant via `grantNamedPathSubject` at project scope, ladder copy only when the denial offered rungs), `createCwdPlane` (held, pins EXACTLY the named directory, forbidden subjects refused), `createNetworkPlane` (deferred, IP literal only, IPv4-mapped collapsed, appended verbatim to `trustedNetworks`, never duplicated), `createCorsPlane` (deferred, bare http(s) origin only: no wildcard, path, query, credentials or `null`; appended verbatim to the RAW `cors.allowedOrigins`, preserving every other `cors` key). Store writers injected via `PlaneDeps`, the same `writeConfigPartial` the Access revoke routes use. Deferred planes offer only `allow-always`/`deny`. See change: add-access-grant-dialog. cwd `subjectOf` returns null for `isUngrantableSubject` dirs (`/`, `$HOME`): never prompted (2b.7 review). Filesystem plane grants with `via: "prompt"` (8.2). |
| `prompt-channel.ts` | Per-connection prompt capabilities (design D1/D1a/D2, tasks 3.1/3.3/3.4). `issuePromptChannel` / `resolvePromptChannel` / `releasePromptChannel` / `promptChannelCount` / `isPromptEligible` / `maySuspend` + `GRANT_CHANNEL_HEADER` (`x-pi-grant-channel`). The browser gateway issues each socket a 256-bit value, the browser echoes it, and a denial is prompt-eligible by resolving back to the issuing socket. Memory only, NEVER persisted (a restart issues fresh values); `releasePromptChannel` on socket close makes a capability die with its connection; comparison is `timingSafeEqual` over the whole scan with NO early `break`, so lookup cost does not depend on where the match is; absent, empty, wrong, and non-string all resolve to the SAME `null`, so a caller cannot distinguish "sent nothing" from "guessed wrong". Eligibility consults the capability and NOTHING ELSE — not auth, not the CORS decision, not `Sec-Fetch-*`, not an `Origin`/`Host` compare (the four recorded defeats). `maySuspend(request, mode)` additionally requires `enforce` and takes the mode as an ARGUMENT so it cannot mutate it; the caller resolves the live mode via `resolveHostGateMode` at the denial site. See change: add-access-grant-dialog. |
| `refusal-ledger.ts` | The remembered-refusal ledger (tasks 2b.9, 8b.4a): the ONE piece of persisted state this change owns. `recordRefusal` / `isRefused` / `listRefusals` / `clearRefusal` / `refusalLedgerPath` / `__resetRefusalLedger`. An operator's EXPLICIT deny on a YOLO-eligible plane is recorded so a later YOLO session refuses that subject without prompting (refused-by-prior-refusal) instead of auto-allowing it. DURABLE (C4): survives restart, never self-expires, cleared only by the operator on the Access surface. An expiry is not a refusal. Grants nothing, so it is fail-safe across rollback. Store conventions mirror `access-grants.json`: config-dir default `access-refusals.json`, `PI_ACCESS_REFUSALS_STORE` override, atomic temp+rename, missing/malformed = empty, failed write leaves the cache unchanged. See change: add-access-grant-dialog. Keyed on canonical real path via `canonicalSubject` (symlink/case alias of refused dir refused; unresolvable keys raw; legacy raw entries matched by re-canonicalising). Failed disk write still caches refusal in-process (fail closed). See change: add-access-grant-dialog (2b.7 review). Stores real path in its own spelling (listable); matching + `clearRefusal` go through `refusalKey` on both sides, so an alias clears it. |
| `source-channel.ts` | `sourceChannel(ip)` -> remote requester key by allocation: IPv4 `/24`, IPv6 `/64` (hextets expanded, zone stripped), `::ffff:` mapped as IPv4, else `source:<raw>`/`source:unknown`. Used by `server.ts` network observer + `denial-hold.ts`; subject stays exact address. See change: add-access-grant-dialog (D9 resolved, rotating sources). |
| `verified-read.ts` | Handle-verified byte serving for GRANT-admitted reads (design D14, task 2.8). `openVerifiedRegularFile` → `readFileVerified` / `readFileVerifiedUtf8` / `assertRegularFile`, plus `VerifiedReadRefused`. Closes the check→open window: `lstat` FIRST and require a regular file (`open(2)` on a FIFO BLOCKS a libuv worker, so refusing before any open is a liveness guard, not just correctness) → `open` with `O_NOFOLLOW`/`O_NONBLOCK` → `fstat` the HANDLE and require the same dev+ino the `lstat` described → the caller serves FROM that handle. Refusals are translated by callers into the site's EXISTING containment shape, so no new user-facing error string enters the surface. Layers ①/② keep their pre-existing (identical, untouched) window. Residuals are documented in the header: `O_NOFOLLOW` covers only the final component, and hardlinks defeat a realpath-based predicate. See change: add-access-grants-and-review. |

| `yolo-env.ts` | `parseYoloEnv` + `YOLO_ENV` (`PI_DASHBOARD_GRANT_YOLO`) + `YOLO_UNSCOPED` (task 2b.8, syntax decided with the operator): one plain absolute path, a JSON array of absolute paths for several (a path may contain `:` or `;`, so no delimiter is safe), or the literal `unscoped`. Anything else (`1`, `true`, relative, `[]`, malformed JSON, `UNSCOPED`) is `invalid` and leaves YOLO inactive. Parse only; root resolution and the forbidden rule live in `yolo-session.ts`. See change: add-access-grant-dialog. |
| `yolo-session.ts` | `YoloController` (design D13, tasks 8b.2-8b.6a/8b.8): `decide` (at the prompt point: `auto-allow` / `refused-by-prior-refusal` / `null`), `activate`, `addRoot`, `end`, `status`, `offerRoots`, `activateFromEnv`, `history` + `YOLO_DURATIONS_MS` (15/30/60 min only). `decide` requires exactly the proof a dialog would: held + `yoloEligible` plane, `enforce`, a capability-holding request; then the subject's REAL path inside a root (or unscoped), never a forbidden subject or descendant, never a subject in the refusal ledger. Persists nothing. Sessions: expiry fixed at activation, never renewed by activity, `end` effective on the next denial; scope defaults to the session cwd subject to the forbidden rule, falling back to the narrowest legal rung (`no-legal-root` when none, e.g. a `$HOME` cwd); roots only from `offerRoots` (base + ladder, forbidden filtered: no free text); activating while live ADDS a root (timer unchanged), never a second session, never widens to unscoped. Environment session: process lifetime (`expiresAt: null`), refused outright on report mode, an unparseable value, or ANY unresolvable/forbidden root (no subset, no unscoped fallback). `report` mode: nothing activates, nothing is auto-answered (#X10). Auto-answers are logged `(no human answered)` and kept in a bounded in-memory `history` that outlives the session. See change: add-access-grant-dialog. `counters()` is CUMULATIVE (the `history` ring is bounded at 200, so counting from it would silently cap). |
