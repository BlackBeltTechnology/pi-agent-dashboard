# architecture.md — index

Full architecture reference. 3 components: bridge extension, Node server, React client. WS ports 9999 (bridge) + 8000 (client). In-memory + JSON persistence. `/site` marketing app product-adjacent, separate build. "Server-Keypair Device Pairing" section: Topology 3 static PWA shell, Ed25519 TOFU identity, QR pairing, compare-code approval, bearer + WS-ticket auth, genuine-local trust. Global Escape dismissal: `escape-stack.ts` LIFO stack arbitrates portaled dismissible surfaces. `useEscapeDismiss` hook replaces self-managed `keydown` listeners; stacked overlays peel one layer per Escape. Adopted by Dialog, ImageLightbox, FilePreviewOverlay. Overlay layering: `--z-*` token scale in `packages/client/src/index.css`; shared `Dialog` at `z-dialog` (50), toast 60 above; nested dialogs tie at 50, later portal/DOM mount wins; `z-layer-lint` `SCAN_DIR` skips `packages/client-utils` (raw-z gate blind spot). Cold-start recovery section: exit-intent boot record (`~/.pi/dashboard/boot-state.json`), recovery gate rule, recording points (restart/shutdown/user-quit/idle/signal), deferred offer broadcast, keeper reattach probe. "Plugin settings pages" section: settings contribution = `settings-section` claim OR intent; `contributesSettings()` predicate shared by nav rail, cog, route guard; contribution renders on `/settings/plugins/<id>` under host-owned chrome; `claim.tab` inert; enable-state desired-state (overlay) vs `loaded` runtime truth. "Project-scope disable of global resources" section: origin classification by longest-prefix path match (never `metadata.scope`/`source`/`baseDir`). 4 write forms: `.pi` loose `-rel`; `.agents` base dir; package delta `{source, autoload:false}` mandatory at project scope; global-loose = own-file `~` entry + anchored glob. Normalised package identity. Ownership `~/.pi/dashboard/resource-entry-ownership.json`. `POST /api/resources/trust` + `defaultProjectTrust` gate. Settings write NOT JSONC-preserving. Session-start resolve. User image attachments: two-phase display fit. Bounded `attachmentState:"pending"` placeholder row first. Fitted 768px derivative = own `attachment_fitted` event, sha256-addressed. Originals via `GET /api/sessions/:sessionId/attachments/:attachmentId`, transcript-backed, not load-bearing. OAuth `auth.redirectBaseUrl` subsection: base precedence (`auth.redirectBaseUrl` → `getTunnelUrl()` → `http://localhost:<port>`); 3 auth-plugin call sites; hot-reload chain; boot-time empty-registry trap (`_reloadAuth` never installed); `warnOnInvalidRedirectBase` warn-not-discard; scope = OAuth redirect URIs only; Settings ▸ Security input `redirect-base-url-input`. Gateway URL Management section: `publicBaseUrls` top-level promotion (legacy `pairing.publicBaseUrls` fallback, not OAuth tier D7); gateway action one statement → one `PUT /api/config` + `gateways[]` provenance (D12/D13); mtime-gated config snapshot `config-snapshot.ts` (D15); `trustProxy` stays off (D14, `forwarded-ip-trust.test.ts`). See changes: add-server-keypair-pairing, fix-stacked-escape-closes-layers, add-embed-session-lifecycle, fix-recovery-exit-intent, plugin-settings-pages, fit-attachments-for-display, project-scope-disable-global-resources, config-override-oauth-redirect-base, add-folder-actions-menu. Plugin requirement probes: `paths` 4th category + `${configKey}` interpolation, `shouldReconcilePath` reconciliation, client warning pill. Health `/api/health` observability: `storeTrim` (trimmedEvents/evictedSessions/collapsedUpdates) from `eventStore.getTrimStats()`; collapse keeps ≤2 `tool_execution_update` per `toolCallId`; superseded ticks dropped at retention only, never suppresses live broadcast. See changes: add-apple-tools-imcp-plugin, collapse-superseded-tool-execution-updates. "Knowledge Base (KB)" section: single FTS5 table `chunks` over `node:sqlite`; zero network/LLM, mechanical ranking. Backend `SqliteFtsStore` (`packages/kb/src/sqlite-store.ts`); config layered project → global → defaults (`packages/kb/src/config.ts`). "KB retrieval pipeline" `store.search()`: one BM25 pass + staged pipeline. Stage order: body-hash collapse (byte-exact dedup, `akaPaths`, runs first) → source dedup (one hit per `(root,path)`, best BM25 score, `suppressedSections`) → MMR (`diversity`, config-gated default on) → coverage rerank (default off) → lane interleave → limit slice → parent expand (`expandParent` default on, attaches parent `headingPath`). `limit` bounds distinct sources not chunks (BREAKING); fetch depth `limit × 6` source-dedup / `limit × 4` body-hash-only, cap 4000. Lane quota `ranking.laneQuota` (default 0.5): `agents` 3.2% of index ~3× longer, BM25 length normalisation buries ~30:1; second FTS pass `doc_type='agents'` shallow pool interleaved; explicit `docType` bypasses quota. Coverage rerank + RM3-style PRF (`queryExpansion.mode: "prf"`) default off; measured regression (markdown-intent R@10 0.630→0.491, latency ~4×). Condensed render emits leaf heading not breadcrumb + `(+N more sections)`; full `headingPath` retained. `kb_get` path-only fetch returns first chunk + `suppressedSections`, never silent 1-of-N. Measured 31,121-chunk index K=10: combined R@10 0.363→0.566, duplicate-slot share 0.48→0.00, distinct sources/page 5.2→10.0, render tokens −32.8%. Latency budget met at median (lane-quota 53.2 ms), p95 fails (84.8 ms; ~22k chunks ≈60 ms). Eval + sweep table: `packages/kb/eval/` (`run-fixtures.ts`, `measure-render.ts`), `measurements.md`. See change: fix-kb-search-retrieval-quality. Bridge Connection Contention section: one-live-bridge-per-session-id invariant enforced at gateway; claim point = first-message identity block (`pi-gateway.ts`) not `session_register` dispatch; two-factor liveness rule (pong → keep, TCP writable → busy → keep, neither → displace) in `bridge-contention.ts`; probe window 5 s; terminal `register_rejected` before close, watchdog stays armed; `stop()` terminates `wss.clients`; close identity-scoped on `connections.get(id)===ws`; resume session-file guard `findLiveSessionBySessionFile` at both sites (D1 liveness, fork exempt); health `bridgeContentionCount` + `contendedSessionIds` + `piGatewayPort`; refusal rate-limited 1/id/5 s; prompt annotates + `delivered:true`. "Bind-vs-trust reachability" section: `bindHost` vs `trustedNetworks`/`auth.bypassHosts` coupling; predicate home `packages/shared/src/bind-reachability.ts` (one impl, both packages import); 6-step evaluation order; `resolvedBindHost` vs `pendingBindHost` (`--host` → `PI_DASHBOARD_HOST` → `config.bindHost` → `127.0.0.1`, draft outranks); `reachability` on the GUARDED `/api/config`, never `/api/health`, stripped on write; `reachability_updated` broadcast + connect replay; advisory independent of `BlockEventTrustBanner`; `/api/network-interfaces` `label`/`pointToPoint`/`suggestions`, one entry per address, dropdown dedupes on suggestion `value`; interface offer table. See changes: fix-duplicate-bridge-registration, warn-unreachable-trusted-networks. Pi Gateway Transport & Identity section (shipped, add-pi-gateway-transport-identity): transport default = unix socket `<dashboardConfigDir>/gateway-<piPort>.sock` (`~/.pi/dashboard/gateway-<piPort>.sock`), mode 0600, dir 0700; TCP opt-in only, `PI_GATEWAY_TCP` truthy (`packages/server/src/pi/gateway-transport-policy.ts`); Windows always loopback + `X-Pi-Local-Token`; stale-socket fail-closed via `gateway-<piPort>.sock.lock` probe/unlink/bind. Endpoint resolution D3 precedence ladder (`packages/extension/src/endpoint-resolution.ts`): explicit PINNED 1-3 > HOME rendezvous record > paired remote > mDNS suggest-only, never overrides; stickiness `pinned` + `decideRetarget`. Per-transport auth (`packages/server/src/pi/bridge-upgrade-auth.ts`): none unix / `X-Pi-Local-Token` loopback / single-use bridge-scoped ws ticket remote; server identity = Ed25519 fingerprint; instance id names, never entitles (D14). Move (`/dashboard-connect`, `/dashboard-list`, `/dashboard-where`): provisional registration → verify instance id → `session_move_commit`; origin serves till commit; `session_moved` → `movedTo` + `status:"ended"`. Remote transcripts read-only boundary (D12/D13): `decideTranscriptRequest` refuses path-bearing fields; remote-origin refuses local file reads + resume; retention `~/.pi/dashboard/remote-transcripts/<sessionId>.jsonl`. See change: add-pi-gateway-transport-identity. "OpenSpec main-spec integrity" section: main specs `openspec/specs/<cap>/spec.md`; h2 `## Purpose` + `## Requirements` parse contract (`MarkdownParser.parseSpec` throws otherwise); `findSection` exact title match, case-insensitive (`## ADDED Requirements` != `## Requirements`); delta headers (`## ADDED|MODIFIED|REMOVED|RENAMED Requirements`) changes-tree only; archive once copied delta verbatim — 80/546 unparseable, 384 blocks invisible to validate/list/show/archive; fix `node scripts/repair-main-specs.mjs` (`--dry-run`, `--specs-dir <path>`), idempotent, refuses `## REMOVED Requirements`; gate `npm run spec:validate` = `openspec validate --specs --no-interactive`, step "Validate OpenSpec main specs" in `ci` job; corrupt archive blocks `develop`; retired zero-requirement cap: tombstone if authored `**DEPRECATED**` pointer, else delete. See change: repair-corrupted-main-specs. Model selector pairing rule section: RUN-CONFIGURING selectors MUST pair thinking-level control; REFERENCE-LISTING MUST NOT. `:<level>` suffix on model ref, parsed by `splitThinkingSuffix` (`packages/extension/src/provider-register.ts`). Levels `off` `minimal` `low` `medium` `high` `xhigh` `max`. `off` writes bare ref. No server change. See change: add-default-thinking-level. Plugin Architecture runtime bullets: `manifest-validator.ts` rejects unknown `shell-overlay-route` `presentation` FATAL; `slot-consumers.tsx` `ShellOverlayRouteSlot` renders claim body only (flex height wrapper); `presentation` consumed by host via `useShellOverlayRoutePresentation` (default `"dialog"`, `"page"` opts out) — host lifts dialog claim into `RouteBackedOverlay` (underlay must cover viewport, slot cannot); `dialogContainer` prop + `OverlayContainerProps`/`OverlayContainerComponent` removed; hook returns string, no `client-utils` → `dashboard-plugin-runtime` cycle; D2a SUPERSEDED; bundled gate `__tests__/bundled-overlay-claims.test.ts`. See change: add-route-backed-overlay-dialogs. Pending-Prompt Recovery section: critical-frame exemption (4-frame per-delivery cap + `MAX_WS_BUFFER`+1 MB ceiling, blocking-drop counter), `prompt_resync_request` round trip (non-consuming requester `peek`, requester-scoped critical reply), FIVE client reset-site carry + multi-batch re-tail, 5 s desync affordance. See change: fix-pending-prompt-lost-on-replay. `### Cross-Site Request Gate` section: issue #625 CSWSH/CSRF admission gate; shared `isOriginAdmitted` in `auth/cors-origin.ts` (Host-match delta, `allowZrokWildcard:false`); WS `upgrade` gate `isWsOriginTrusted` 403+`socket.destroy()` before ticket spend, `live` scope admits `Origin: null`; REST `createMutationOriginGate` onRequest hook on `/api/*`+`/auth/logout`, skips GET/HEAD/OPTIONS; pi-gateway `bridge-upgrade-auth.ts` refuses any `headers.origin` as `browser-origin`; one `corsOpts()` closure feeds all three. See change: fix-ws-origin-cswsh. MCP endpoint dual-era contract: legacy Streamable-HTTP (2025-03-26/2025-06-18/2025-11-25, initialize handshake, unrecorded Mcp-Session-Id, notifications 202, ping {}, streaming refused) + modern 2026-07-28 (stateless, subscriptions/listen streaming); single resolution site routes.ts; negotiate-down rules; mcp.json provisioning pinned 2026-07-28; direct device-token mint POST /api/paired-devices (operator-gated, host-admitted, manual source, plaintext-once). See change: mcp-legacy-clients-and-token-issuance.

Gains `### Subagent Fan-out Admission (bound-subagent-fanout-under-host-pressure)`: parent event loop stall mitigation, in-flight concurrency bound per session, `bridge.ts` registration order after pass-through forwarder, `DEFAULT_MAX_CONCURRENT_SUBAGENTS = 2`, `0` disables, fail-open on malformed, private 5 s sampler `subagent-saturation.ts` (process vs machine domains, 80 % hysteresis exit), release on `tool_execution_end`, durable `subagent-admission-refused` records, ungated grandchildren/flows scope boundary. See change: bound-subagent-fanout-under-host-pressure.

Gains `#### Plugin Staleness Detection and Served-Build Coherence (add-served-build-coherence-and-hash-parity)`: single client-registry selector `selectClientRegistryPlugins` (clientEntryPath, isProd fixture policy, bundleRoots), build declaration `pi-dashboard-build.json` ({schemaVersion, pluginRegistryHash, fixturePolicy}), static client resolver `resolveStaticClientDir` (installed package over workspace), startup coherence snapshot `/api/health.clientBuild`, rebuild sync gate `scripts/sync-served-client.mjs` (mirrors workspace to served directory, verifies declarations match before restart). See change: add-served-build-coherence-and-hash-parity.

Gains `### Access Grants and Denial Remedies (add-access-grants-and-review)`: filesystem-plane grant layer, companion to `### Network Access Control`. Single containment gate `access/containment-gate.ts` `evaluateContainment(resolved, anchors, {site, session})` (layers ①/② `isAllowed` → grant layer `isGrantAdmitted` → record path denial). Grant is a dedicated subtree predicate, NOT an extra `isAllowed` anchor: `isAllowed` runs `checkoutAnchors(anchor)` over every anchor and admits all bound checkout roots, so an anchor-shaped grant for `…/repo/sub` would admit all of `…/repo` (D1). Subject realpath'd at grant time and persisted verbatim (`normalizeGrantSubject`); check is `within(realpath(resolved), storedSubject)` — realpath on the REQUEST side only, stored subject never re-resolved (D2); double-realpath rejected. Store `access/access-grants.ts` → `~/.pi/dashboard/access-grants.json` (`{version:1, grants}`), fields subject/scope/grantedAt/origin/widenedFrom, cap 200 per scope oldest-evicted, atomic temp+rename, malformed/absent→empty, load-once + invalidate-on-write, empty short-circuit zero syscalls (D10/D11/D16); `session` scope = server process, not pi session (D17). Denial registry `access/access-denials.ts`: subject-keyed `denialId`, site, session, timestamp, ancestors, TTL 10 min, cap 200, written only by containment denial path (D20). Offered-ancestor ladder `access/ancestor-ladder.ts`: real-path, checkout-root-truncated or below-$HOME, forbidden-filtered. Forbidden subjects `access/forbidden-subjects.ts` compared as real paths (`realpath("/etc")` → `/private/etc`), `whole` exact + `sensitive` (`~/.ssh`, `~/.pi`) descendant, `isUngrantableSubject` applied post-normalization (D15). Grant endpoint `routes/access-routes.ts` `POST /api/access/grants`: denialId-bound, canonical-subject membership, auth + cross-origin `createMutationOriginGate`, local-process limit stated. Handle-verified grant reads `access/verified-read.ts` D14 (`lstat` regular-file → `O_NOFOLLOW`/`O_NONBLOCK` open → `fstat` dev+ino compare → serve from fd; `assertRegularFile` for out-of-process renderers; range-parse-before-open fd-leak/EMFILE fix). Settings→Access tab `AccessSection.tsx` reviews + revokes only, never creates (D12); reads eight stores in place, revokes each via its own write path; revoke invalidates in-memory set → effective next request, no restart. See change: add-access-grants-and-review.

Full architecture reference. 3 components: bridge extension, Node server, React client. KB retrieval pipeline (FTS5 BM25 staged rerank). Pi Gateway Transport & Identity section (shipped): unix-socket default, D3 endpoint precedence ladder, per-transport auth, session move, remote transcripts read-only. Auto Session Naming: `@naming`→`@fast`, adaptive cap 1024→2048, starved/waiting verdicts, 3-attempt budget, `autoNamerState` persistence, `GET /api/auto-name-outcomes`. See change: fix-auto-naming-reasoning-model. → see `architecture.md.AGENTS.md` Gains `### OpenSpec Readiness (add-openspec-init-affordances)`: state table, config keys, signature provider, init endpoint contract. See change: add-openspec-init-affordances. Gains `### Node runtime family selection`: node/npm/npx family enum + atomic select, coherence, /api/node/installs, win32 npm peer anchor. See change: add-node-runtime-family-selection. Gains `### Git checkout-root resolution`: shared resolver, measured 9-state git table, persisted phantom `mainPath` repair. See change: add-git-checkout-root-resolver. Gains `#### DOX doctrine injection (doctrine config)`: `doctrine {inject,write}` layered project→global→defaults, `doctrineSource`, per-turn `before_agent_start` injection before the cwd anchor, first-contact + migration nudges, legacy seed guard, fault warn-once. See change: inject-dox-doctrine-and-describe. Gains `### Pending-Prompt Recovery (fix-pending-prompt-lost-on-replay)`: critical-frame exemption (4-frame per-delivery cap + `MAX_WS_BUFFER`+1 MB ceiling, blocking-drop counter), `prompt_resync_request` round trip (non-consuming requester `peek`, requester-scoped critical reply), FIVE client reset-site carry + multi-batch re-tail, 5 s desync affordance. See change: fix-pending-prompt-lost-on-replay. Gains `### MCP Endpoint dual-era contract`: legacy 2025 Streamable-HTTP (`initialize`, opaque session id, notifications 202, ping, negotiate-down to 2025-11-25) + modern 2026-07-28 (stateless, subscriptions/listen), single resolution site routes.ts, operator-gated direct device-token mint `POST /api/paired-devices` (`source: "manual"`). See change: mcp-legacy-clients-and-token-issuance. Gains `### Subagent Fan-out Admission`: concurrent `Agent` in-flight bound per session, bridge `tool_call` gate registered after the pass-through forwarder, default cap 2, saturation cap narrowing to 1 via private 5 s sampler, release on `tool_execution_end`, durable `subagent-admission-refused` entries. See change: bound-subagent-fanout-under-host-pressure. Gains `#### Plugin Staleness Detection and Served-Build Coherence`: single client-registry selector, build declaration (`pi-dashboard-build.json`), static client resolver, `/api/health.clientBuild` snapshot, rebuild sync gate (`sync-served-client.mjs`). See change: add-served-build-coherence-and-hash-parity. Gains `### Bridge Streaming Coalescing (change: coalesce-bridge-message-update-snapshots)`: fixed 50 ms window, text-only single slot, non-text immediate flush, generation identity barrier, fail-open reload drop rule, entry-point flush choke points, transport vs session lifecycle split, bridge wire/serialization cost cut. See change: coalesce-bridge-message-update-snapshots. Gains `### Network Access Control` rewrite: universal `onRequest` guard `createNetworkGuardHook` (server.ts, LAST, unconditional) replaces per-route-only coverage; jurisdiction `/api/` `/v1/` `/editor/` `/live/` trailing-slash anchored, outside no-op for SPA shell/static/`/auth`/`/sw.js`; in-namespace exceptions `GET`+`HEAD` `/api/health` + pairing + `bypassUrls`; `/v1` via `isAuthenticated` from proxy gate; `/mcp` enumerated independent namespace; second port loopback-only; namespace-coverage test. See change: add-universal-network-guard. Gains `### Access Grants and Denial Remedies`: filesystem-plane grant layer. Grant = subtree predicate `isGrantAdmitted` after `isAllowed` miss, NOT an extra anchor (widens to checkout roots). Subject realpath'd at grant time, stored verbatim; check realpaths request side only. Store `access-grants.json`, cap 200/scope, load-once. Denial registry `denialId`, TTL 10 min. Forbidden subjects compared as real paths. Handle-verified reads D14. Access tab reviews+revokes only. See change: add-access-grants-and-review.

Gains `#### Provider OAuth sign-in (delegated to pi-ai)`: dashboard supplies an `AuthInteraction` to pi-ai's own `login()`, never a flow. Registry built once lazily from `ModelRuntime.create({modelsPath:null, credentials:EMPTY_READONLY_STORE})`; `mapProviders()` filters `auth?.oauth`, excludes `radius`; pi-coding-agent 0.86.1 set = 7 ids (anthropic, openai-codex, github-copilot, openrouter, kimi-coding, meta, xai). `FLOW_TYPE_HINT` UI-only, never a gate. Server imports only `@earendil-works/pi-coding-agent` (public `ModelRuntime`), never pi-ai, never either `dist/`; six governed pins move together (server dep `^0.86.1`, `piCompatibility.{minimum,recommended}`, pnpm-workspace override, docker/Dockerfile, verify-release-deps.mjs minVersion). Routes: `POST /api/provider-auth/start {provider, enterpriseDomain?}`, `GET|DELETE /api/provider-auth/flow/:flowId`, `POST /api/provider-auth/flow/:flowId/input {value}`; removed `/authorize`, `/device-code`, `/device-status/:flowId`. Flow record = sticky `authUrl` + tagged `pending` union (`device_code` render-only / `manual_code` / `text` / `select`; `secret` rejected); abort REJECTS the pending prompt so pi-ai's `finally` closes the listener; `preAnswers` answers first free-text prompt; 15 s start handshake (`FLOW_START_TIMEOUT_MS`); TTL 10 min, device-code extends to `expiresInSeconds + 60 s`; prune per request + 60 s timer; `expired` derived from deadline, never message text. `/input` value is a secret — never logged, never echoed; ids `crypto.randomUUID()`. Degradation: registry failure → `/handlers` `{ids: []}`, `/api/health` `providerAuth.error` naming resolved pi-coding-agent version, every other route serves, stored OAuth credential stays visible + removable (`oauthIdSet` union). See change: delegate-provider-oauth-to-pi-ai.
