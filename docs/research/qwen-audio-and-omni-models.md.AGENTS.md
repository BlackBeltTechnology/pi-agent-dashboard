# research/qwen-audio-and-omni-models.md — index

Qwen cloud audio/omni landscape + measured transcription test. Qwen3.8-Omni-Flash EXISTS (2026-09-18, Qwen3.8-Flash-Next; earlier "stops at 3.5" assumption WRONG), ids `qwen3.8-omni-flash` + `-realtime`, text/image/audio/video in → text out, 991K in / 131K out / 1M ctx / 262K reasoning / 2M TPM / 30K RPM; `qwen3.8-livetranslate-flash-realtime` audio+image in → audio+text out, 49K in / 4K out / 53K ctx / 100K TPM / 10 RPM, 60 langs in / 29 spoken. Two disjoint Alibaba products: Token Plan (`sk-sp-`, token-plan.*.maas.aliyuncs.com, 14 ids, NO omni/asr) vs Model Studio PAYG (`sk-ws-`, dashscope*.aliyuncs.com, 170 ids, omni+asr YES); keys not interchangeable — `sk-sp-` at dashscope-intl → `invalid_api_key` (live). PAYG 3.8 family: omni-flash, livetranslate, max(+0902), flash, 27b, 2.4t-a95b; `qwen3.8-flash` probe OK (reviewer "invented id" refuted); OpenRouter carries ZERO omni ids (refuted). pi CANNOT carry audio — input union `("text"|"image")[]` closed (custom-provider.md ~726, models.md ~207); no built-in `dashscope` provider (`model-resolver.js` only qwen-token-plan*); custom provider fix in `~/.pi/agent/models.json`, `api: "openai-completions"`, chmod 0600; do NOT set `thinkingTokenBudgetField` on DashScope Qwen (conflicts `reasoning_effort`). MEASURED: 300 s of 50-min HU meeting (`~/Movies/2026-08-07 10-01-27.mp3`, 1325 Soniox cues) → ffmpeg mono 16k 32k slice /tmp/omnitest/slice.mp3 = 1,200,608 B / 1,600,812 b64 → one `input_audio` + text msg, non-streaming. 31.9 s ≈ 9.4x realtime; prompt 2204 (audio 2102 = ~7 tok/s, text 102), completion 2624 (reasoning 623, text 2001), total 4828. DEFECT: silent truncation 0–210 s of 300 s, mid-sentence, normal `finish_reason`, no error; 583 HU vs 687 Soniox words (−15%). Timestamps unusable: 8 marks / 5 min, gaps 10–57 s, drifting. Quality where present BETTER than Soniox: clean sentences, correct jargon (MQTT/TinyPC/Samba/blob/Docker), fixed `szervizek`→`services-ek`. Cost ≈ $0.019/hr ($0.0038 audio + $0.0148 out) vs Soniox $0.10/hr STT / $0.18/hr translation. Soniox same window: 60 cues, ~5.2 s median gap, exhaustive, but 80% cues mid-word; WER-vs-cpWER NOT apples-to-apples (different corpora/metrics). Verdict: ASR = exhaustive timing+diarization spine, translate TEXT only; audio prompt needs ≤2 min chunks + coverage assertion. Drove openspec `add-srt-translation-pass` → `pi-translate-srt`, pkg `packages/video-transcription/`. Open: 3.8 omni Hungarian? subtitle-grade timestamps? livetranslate OFFLINE vs Soniox/Omni? audio into pi loop? Keys by prefix+path only, no values. Comprehensive prose doc per user request.

Research dossier. Qwen cloud audio/omni landscape + measured transcription test. Qwen3.8-Omni-Flash EXISTS (2026-09-18; "stops at 3.5" WRONG). Token Plan (`sk-sp-`) vs Model Studio PAYG (`sk-ws-`) = disjoint products, non-interchangeable keys, 14 vs 170 ids, omni only PAYG. pi input union `("text" or "image")[]` closed → no audio. MEASURED: `qwen3.8-omni-flash` 9.4x realtime, ~$0.019/hr, better prose than Soniox BUT silent truncation 210s/300s, normal `finish_reason`, −15% words. Verdict: ASR = timing spine, translate text only → drove `add-srt-translation-pass`, `pi-translate-srt`. 4 open questions. → see `research/qwen-audio-and-omni-models.md.AGENTS.md`
