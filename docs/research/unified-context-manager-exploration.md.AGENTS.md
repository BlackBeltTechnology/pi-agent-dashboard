# research/unified-context-manager-exploration.md — index

Research artifact. Explore-mode, no change / no impl. Status: exploration in progress; umbrella change drafted = `openspec/changes/unify-context-manager` (proposal + design, skip_specs; phases 1–6 = kernel, forks, lessons-and-cues, exec-and-web, lesson-miner, plugin-cutover); `consolidate-retrieval-planes` marked SUPERSEDED. Goal: ONE unified context-manager pi extension + ONE dashboard plugin replacing pi-hermes-memory, pi-blackhole, context-mode, kb-extension (+ plugins `hermes-memory-plugin`, `blackhole-plugin`, `kb-plugin`). Primary target = MODEL CONFUSION (3 session-search tools, 3 prompt injections); RAM/disk wins = bonus.

DECISIONS (user answers): migration = big-bang fork of hermes + blackhole (both MIT) into one package; context-mode (Elastic-2.0) = clean-room re-implement sandbox exec ONLY (`ctx_execute`/`_file`/`batch`) — drop rest, do NOT port code; drop `ctx_fetch_and_index` — pi-web-access stays sole ingestion, manager taps `tool_result` of `fetch_content`/`web_search`/`source_check` → persistent `web` scope; old tool names aliased one release, deactivated from model view via `pi.setActiveTools`; keep hermes + blackhole distillation pipelines separate inside package (for now); triggers explicitly agent-authored when writing a lesson; supersede `consolidate-retrieval-planes` with `unify-context-manager` (proposal rejected fork (Alt 4) + declared shim-over-third-party-store top risk (D3) — fork removes shim); storage model RESOLVED = ONE-FILE-PER-LESSON (md + YAML frontmatter) canonical, kb-indexed, stats in disposable `node:sqlite` (detail §14).

INVENTORY 2026-09-24: pi-hermes-memory 0.9.9 MIT ~17.0k TS LOC, better-sqlite3, `~/.pi/agent/pi-hermes-memory` 186 MB + `projects-memory` 14 MB; pi-blackhole 0.5.6 MIT ~32.7k TS LOC, no engine, session JSONL `custom` entries, `~/.pi/agent/pi-blackhole` 2.7 MB; context-mode 1.0.169 Elastic-2.0 compiled only, better-sqlite3 + MCP SDK, `~/.pi/context-mode` + stray `~/.claude/context-mode` 383 MB; kb + kb-extension ours MIT ~8.1k LOC, node:sqlite. 33 pi processes avg RSS ~148 MB; context-mode MCP bridge = 16 child procs 616 MB RSS. Hook overlap: `before_agent_start` (hermes/ctx/kb), `session_before_compact` (hermes/blackhole/ctx), `context` (blackhole/ctx), `tool_result` capture (hermes/ctx/kb), `tool_call` guard (ctx/kb). Session-search tools: `session_search`, `recall`, `ctx_search`. hermes+ctx+kb all REPLACE `systemPrompt` in `before_agent_start` → prompt-cache churn; pi docs recommend `systemPromptOptions` sections/deltas.

CONTEXT-MODE ANATOMY (`build/adapters/pi/extension.js`): usage from `docs/context-mode-roi-report.md` (623 transcripts) = sandbox ~4,427 calls 97%, retrieval ~108 (2%), `ctx_fetch_and_index` 12. Pieces: sandbox KEEP clean-room · fetch_and_index DROP · content index `ctx_index`/`ctx_search` → kb store scope · `tool_call` Bash guard (block curl/fetch/requests) fold into kb guard · `tool_result`+`before_provider_response`+`turn_end` capture DROP (3rd copy) · `before_agent_start` routing line + `<active_memory>` ~500 tok DROP · `context` hook synthetic user msg DROP · `session_before_compact` resume snapshot DROP · MCP bridge child DROP → native tools · admin stats/doctor/upgrade/purge/insight drop. Pieces 5–8 = full parallel session-memory system duplicating blackhole.

BLACKHOLE ANATOMY: merge of pi-vcc + pi-observational-memory. `core/` 5.6k + `extract/` 2.3k deterministic compaction KEEP; `om/` 7.0k + `agents/` 1.5k + `ledger/` 1.3k = Observer/Reflector/Dropper in-process LLM workers, ledger = session JSONL custom entries (`om.observations.recorded` etc.); `project-recall/` 2.8k rescans session JSONL dirs; `tools/recall.ts` 0.5k; `pi-base/settings` + config ~8.2k TUI settings framework DROP → dashboard plugin; changelog/commands 1.8k mostly drop. `record_observations`/`record_reflections`/`drop_observations` = worker-agent-only tools, not main model surface. `om/inline-compaction.ts` ~1.0k monkeypatches `AgentSession.compact` prototype (recognizes pi 0.81/0.84 shapes, peer floor 0.85.1, fails closed).

HERMES ANATOMY: `store/` 7.7k = `MEMORY.md`/`USER.md`/`failures.md` + SQLite mirror, session-indexer (JSONL → `sessions.db` 186 MB), skill store, standing instructions, atomic-lock-coordinator; `handlers/` background-review + correction-detector + session-flush + auto-consolidate ALL spawn child `pi` processes (`execChildPrompt`); TUI commands ~2.5k DROP; migrations ~0.7k DROP; tools 1.4k. Injects ~3.3 KB memory-policy every turn. Config `~/.pi/agent/hermes-memory-config.json` `llmModelOverride` `opencode-go/deepseek-v4.1-flash`.

DUPLICATION: session JSONL re-read 4× (hermes session-indexer, blackhole project-recall, ctx hook capture, session-distiller); background LLM distillation 4 pipelines (hermes 4 handlers, blackhole 3 workers, session-distiller, ctx none); durable store formats 4 (markdown+sqlite mirror, JSONL custom entries, per-session DBs, markdown+node:sqlite). ~12k LOC droppable at fork time (TUI settings, changelog, migrations, TUI commands) → fork ≈ 30k LOC, not 50k.

MEMORY FILES WRITE-ONLY TODAY: `projects-memory/pi-agent-dashboard/MEMORY.md` 272 KB 263 entries not injected; `pi-hermes-memory/MEMORY.md` 25 KB 34 entries not injected; `USER.md` 27 KB 49 entries not injected; `failures.md` 408 KB 466 entries — only ≤5 failures ≤7d injected. hermes `memoryMode: "policy-only"`; files 10–100× over char ceilings. Census (`consolidate-retrieval-planes` proposal): 461 memory writes vs 66 `memory_search` reads / 504 conversations (~7:1). Global `failures.md` holds mixed-scope content (contacts, Apple Mail, doc-engine) = general lesson log, not failures. Tags: 353/812 untagged, tool-quirk 316, insight 87, correction 31, failure 12, convention 12, preference 1; 68/812 look like status/episodic entries (`COMPLETE`, commit hashes).

EXTERNAL RESEARCH: "Delivery, Not Storage" (arXiv 2607.20972) — voluntary memory use ~0 (0 memory ops / 114 turns, pre-seeded store); deterministic harness injection delivered every seeded run, zero false alarms; 39% intra-session re-reads re-buy pre-compaction content; facts in conversation vanish at first compaction, absent 106/108 compactions; harness-store facts survive 138 compact-resumes; quote "Delivery, not storage, is the product." Proactive Memory Agent (arXiv 2607.08716) — selective intervention beats passive bank exposure, always-on injection, general retrieval; +8.3 pp Terminal-Bench 2.0, +6.8 pp τ²-Bench. Retrieval vs Utilization (arXiv 2603.02473, ICLR'26 MemAgents WS) — retrieval method spread 20 pts (57.1%→77.2%, hybrid rerank best) vs 3–8 pts across write strategies; raw chunks (zero LLM calls) match/beat Mem0-extraction + MemGPT-summarization. Letta "Is a Filesystem All You Need?" (letta.com/blog/benchmarking-ai-agent-memory) — filesystem tools + iterative search 74.0% LoCoMo (GPT-4o mini) vs Mem0 reported 68.5%. Harness the Memory (arXiv 2608.15008) — no substrate dominates; excessive retrieval harms sequential decision-making. Verdict: drop curated files as model-facing memory ✔; "search tools suffice" ✘ (need harness-owned delivery); kb read-guard = existing cue-anchored example.

PI-WEB-ACCESS vs CTX_FETCH_AND_INDEX: pi-web-access 0.30.0 MIT ~30k LOC — extraction (readability, PDF+OCR, GitHub clone/issues/PRs, YouTube, video frames, RSC, cookie `authFetch`), SSRF protection, 30+ search providers, `source_check` citations, curator; store `~/.pi/agent/web-search-cache` TTL 1 h (`CACHE_TTL_MS`), 128 entries / 128 MB; retrieval responseId + findText (exact/case-insensitive/fuzzy, 400-ch windows), offset/limit, answer mode; no cross-page ranked search. ctx: turndown HTML→md, 24 h cache / 14-day retention, BM25+trigram RRF cross-source. Decision: web-access ingests; manager taps `tool_result` and indexes into persistent `web` scope.

DELIVERY CHANNELS (pi 0.87.1 `dist/core/extensions/types.d.ts`): A `tool_result` → content append (`ToolResultEventResult.content`) = default cue delivery, adjacent, tail = cache-safe; B `tool_call` → block+reason (`ToolCallEventResult`) = hard guards only; C `before_agent_start` → `message` (`BeforeAgentStartEventResult.message`) = prompt-matched cues; C′ `before_agent_start` → `systemPrompt` = pinned tier only (cache-busting); D `turn_end`/`agent_before_settle` → entries + continue (`BoundaryResult`) = pre-settle checklist, mid-run compaction; E `session_before_compact` → compaction (`SessionBeforeCompactResult`) = carry pinned/cued facts through compaction.

TRIGGER VOCAB (draft): path(glob) · command(pattern) · error(pattern) · tool(name, argPattern?) · symbol(name) · prompt(terms, BM25≥θ) · event(session_start|post_compact|model|before_settle) · behaviour(grep-without-kb|repeat-failure|reread-after-compact). Lesson schema: `{ id, kind: preference|convention|gotcha|failure-fix|guard, scope: global|{project}, card: ≤200 ch, body, triggers: Trigger[] (OR-ed; empty ⇒ pull-only), severity: hint|warn|block, provenance: {sessionId, created}, stats: {fired, lastFired?, followed?} }`. Firing policy: silent default; once per lesson per session per compaction epoch; ≤2 cards / ~600 ch per turn; block only command/tool; precompiled matchers at session_start; track `fired` + `followed`. Observed FP: kb guard fired ~6× reading third-party `~/.pi/agent/npm/node_modules/` (not kb-indexed) → detector must require target path inside indexed root. Observed tool cues: `mcpScript` → `tools.call('get_search_content')` fails "native Pi tool"; `ctx_execute` python sandbox HOME/cwd differs → `os.walk(~/.pi/...)` returned 0.

SPIKE 1 — COMPACTION PREREQUISITE ALREADY MET: pi 0.87.0 (2026-09-21) CHANGELOG adds actionable `turn_end`/`agent_before_settle`, return `{ entries: [...event.entries, draft], continue: true }`; `CompactionEntryDraft { type:"compaction", summary, firstKeptEntryId: string|null, details?, usage? }` at `types.d.ts:593`; `SessionBoundaryDraft = Custom | CustomMessage | ContextEdit | Compaction`; public exports `findCutPoint`, `shouldCompact`, `estimateTokens`, `calculateContextTokens`; `prepareCompaction` NOT in root barrel. blackhole inline-compaction purpose (`om/inline-compaction.ts:794`) = "Run Pi's native compaction pipeline at an awaited turn_end boundary without aborting the active agent run" → now official API; monkeypatch retirable; blackhole summary deterministic → computable inside boundary handler, no LLM.

SPIKE 2 — TRIGGER DERIVATION DRY-RUN + REPLAY (scripts `/tmp/cue-spike/`, NOT committed, deleted after recording): auto-derived triggers from 812 entries (path/command/error/tool regexes), replayed over repo pi session JSONL (`~/.pi/agent/sessions/--Users-robson-Project-pi-agent-dashboard--/`, 1109 files). NAIVE (80 recent sessions, 10,598 tool events): unique lessons fired/session median 117, p90 180, max 328; error triggers on real errors 59/1401 (4%); 22 triggers fire in >25% sessions (`ask_user`, `kb_search`, `git status`, `spec.md`, …); precision ~5–15%. STRICT (df ≤3% calibrated on 240 older sessions, eval 80 recent; errors gated on `isError`; tool-name-only dropped): median 2, p90 20, max 41; 29/80 sessions zero fires; 514 fires (path 197, command 271, error 46); hand-judged 30 sample → 7 TP, 4 plausible, 4 weak, 15 FP ⇒ precision ~25–35%. TP examples: `openspec validate --change` → "validate does NOT accept --change"; `npm run quality:changed` → "--changed resolves vs LOCAL default branch"; `npx playwright install` → `score_mockup` Executable missing; path `packages/server/src/auth/provider-auth-storage.ts` → `_buildAuthStatus` fact. FP modes: incidental token mentions; status/episodic entries fired as lessons; generic path suffix (`src/config.ts`); semantic duplicates (`biome --changed` ×3, `score_mockup` ×2; lexical 3-gram Jaccard≥0.25 finds only 5 pairs). Conclusions: prose-derived triggers below bar → explicit agent-authored triggers; rarity (df) filter = strongest lever (~60× volume cut) → reuse replay as write-time validator + periodic self-audit; import needs triage (archive status entries, semantic dedupe, re-kind, triggers only where precise cue exists); prompt-triggers untested.

STORAGE MODEL — RESOLVED: 4 data classes, only lessons ours to author. repo docs/code canonical = git working tree (we index via kb); session history incl. blackhole observations canonical = pi session JSONL (we index; hermes `sessions.db` = copy, 1,960 sessions 45,635 messages — user chose KEEP copy for speed → copy becomes THE ONE unified `node:sqlite` FTS index, NOT separate better-sqlite3 DB); web content canonical = pi-web-access cache 1 h TTL (index row = copy); lessons/pinned/skills canonical = ONE FILE PER LESSON md + YAML frontmatter (we author). Lesson storage L2 = `<id>.md` + frontmatter (kind/scope/triggers/severity/card); kb indexes via existing `kb-frontmatter-structural-indexing` typed filters/facets; stats `fired`/`followed` in disposable `node:sqlite` (`~/.pi/agent/context/stats.db`). Locations project → `<repo>/.pi/lessons/<id>.md` (team-shared in-repo DEFAULT, git/PR review); global → `~/.pi/agent/lessons/<id>.md` (personal). Project identity = git common dir (worktrees share parent lessons); fallback `realpath(cwd)`. Evidence vs hermes md-canonical+SQLite-mirror: `/memory-sync-markdown` reconciler; `atomic-lock-coordinator.ts` 375 LOC + `markdown-mutation-lock.ts`; 80 `.MEMORY.md.recovery-*`; `projects-memory/` 40 dirs (25 worktree-named, 13 empty, basename identity); single 272 KB file → whole-file rewrites; one-file-per-lesson removes shared-file lock + reconcile (~1k LOC hermes). Rejected L1 SQLite-canonical (not diffable/shareable), L3 JSONL event log (append atomicity, stats noise). Team-shared default ⇒ PII/secret scrub mandatory before write (global `failures.md` held colleague emails); hermes `store/content-scanner.ts` start point.

RETROSPECTIVE LESSON MINER (design): user tool = OLD session JSONL → lessons retrospectively. Command/skill e.g. `/lessons mine [--project|--all] [--since] [--n 2]` + `/lessons import-hermes`; dry-run default; dashboard background job + progress + review screen. Stage 1 deterministic = existing `packages/session-distiller` (ours ~1.2k LOC, no LLM); dry-run `--n 1` over this repo: 1.5 s, 451 sessions, 5,258 candidates — fault 309 (72 recur ≥2), user_correction 214 (17), ask_user_decision 669 (30), procedure 1,547 (noise), documentation 2,519 (noise); artifact body = stub → needs LLM stage. Evidence-derived triggers: fault pairs carry literal failing command + error text → trigger from observed event, not prose (fixes spike-2 precision). Pipeline: distiller → compact window (≤ ~1k tok) → TRIAGE (System-1 if configured, else LLM) → MAP card writing (LLM) → trigger replay gate (df ≤3%) → REDUCE semantic dedupe + PII scrub → VERIFY still-true (tool-using subagents, shortlist) → staged files. Executor (user choice) = parallel subagents for ALL LLM stages, bounded by EXISTING `maxConcurrentSubagents` (`packages/shared/src/config.ts`, default 2, 0 = disabled, malformed = uncapped fail-open) + admission gate `packages/extension/src/subagent-fanout-admission.ts` (+ optional `subagentSaturation` eventLoopDelayMs/cpuPercent/loadAvg1m) + heap coupling `packages/shared/src/heap-limits.ts` (`perChildMb = maxOldSpaceMb/(cap+1)`); no new setting; batch windows per subagent ~20 — cap 2 makes batching matter. Review gate (user choice) = auto-accept above confidence threshold, review rest. Accepted/rejected triage decisions saved as labelled dataset (user: yes) → future System-1 fine-tuning. Volume est. (this repo): ~1.2k windows × 4–8k tok ≈ 5–10M input tok; `--n 2` gate cuts ~10×; $ not priced.

SYSTEM-1 TIER (typed decision models): user idea = selectable “System 1” triage models; FALLBACK RULE = no System-1 endpoint configured → pure LLM triage (config-level switch, not per-item). All three speak `POST /v1/systemone` (TypeSafe spec) → one adapter. Jev (TypeSafe, proprietary cloud-only early access, API, jabr v2 96.6%) · Von (`wfzyx/von`, Apache-2.0, Python server `/v1/systemone`+`/v1/models` or in-process, `von-sdk` TS drop-in for `@typesafe-ai/sdk`, CUDA/ROCm/MPS/OpenVINO/CPU, jabr v2 72.0% / JevBench hard 38.7%) · Laya (`NandhaKishorM/laya`, Apache-2.0, `laya[serve]` `/v1/systemone`, `laya-ts` ONNX in-process Node no Python, CUDA/CPU + Node CPU/CUDA + browser WebGPU/WASM, typed decisions 0.362 base → 0.766 fine-tuned). Modes (user: all three) = remote endpoint URL+key; dashboard-managed local server (von / `laya[serve]` via uv or Docker); in-process `laya-ts`. Runtime use (user: yes) = cue relevance gate at `tool_call`/`tool_result` when configured. Pitfall: `von-sdk` examples default port 8000 = dashboard port → pick another. Sources: `typesafe.ai/blog/introducing-system-one-models-and-jev`, `github.com/wfzyx/von`, `github.com/NandhaKishorM/laya`.

SPIKE 3 — TRIAGE BAKE-OFF (scripts `/tmp/von-spike/`, NOT committed, deleted after recording; identical harness = same 50 windows, same reference labels, `/tmp/von-spike/analyze.py`): host Apple M5 Pro 48 GB MPS; Von 1.2.2 (torch 2.14, weights von-1.2.0), Laya 0.3.20 (english checkpoint), isolated uv venv py3.12. 50 windows from this repo session JSONL (20 fault, 12 correction, 8 ask_user decision, 10 routine; avg 816 chars); reference labels = `anthropic/claude-opus-5` subagent (proxy, not human) = 12/50 lessons (fault 7, correction 3, decision 1, routine 1); ref kinds routine 20, status 18, convention 6, gotcha 5, preference 1. Questions: `is_lesson` noul, `kind` choice 6, `scope` choice 2, `cue` choice over deterministic candidates, `sensitive` noul; reframed variant = concrete 4-way choice, score `1−P(routine)`. Metrics — is_lesson AUC Von 0.425 / Laya 0.616 / LLM opencode-go/deepseek-v4.1-flash 0.951; reframed AUC Von 0.679 / Laya 0.500; range min/med/max Von 0.21/0.36/0.52, Laya 0.10/0.63/0.90, LLM 0.05/0.30/0.85; best precision @ recall≥0.9 Von 0.24 (50/50) / Laya 0.24 (45/50) / LLM 0.69 (th 0.50, keeps 16); kind agree all/lessons Von 0.14/0.42, Laya 0.20/0.25, LLM 0.54/0.50; scope 0.64/0.58, 0.70/0.92, 0.76/0.58; cue 0.58/0.50, 0.58/0.58, 0.76/0.92; sensitive AUC (1 regex positive, not meaningful) 0.17 / 0.18 / 0.46; top kind convention ×33 / failure_fix ×26 / gotcha ×19. Runtime (MPS, in-process, both auto-select `mps`) — first run 39.3 s / 12.8 s; warm load 3.2 / 3.0 s; 5-question median/p90 270/440 ms / 117/155 ms; 1-question 52/79 / 59/79 ms; max RSS/peak Von 0.8/4.0 GB, Laya 3.0/4.5 GB. Verdict: neither System-1 model can pre-filter (`is_lesson` recall ≥0.9 keeps 45–50/50); Laya ~2.3× faster than Von on 5 questions; Laya strong on scope (0.92 lessons); 1-question ~55 ms → acceptable runtime relevance gate (accuracy untested); Jev untested (no TypeSafe key on host); System-1 kept pluggable opt-in, revisit via fine-tuning on miner labels. Side finding: 5/12 reference lessons = context-mode Bash guard blocking inline curl/HTTP (w06, w11, w12, w16 + health-check retry) → block `reason` does not persist across sessions → agent re-hits same wall; cue on `command: curl` at `tool_call` + dedupe → one lesson = live “delivery, not storage” evidence.

QUESTION-METHOD STUDY (decomposition / structure / aggregation; same 50 windows + reference as spike 3, scripts `run_methods.py`/`analyze_methods.py`): guidance from `systemonemodels.org/guides/how-to-build-with-system-one-models` (narrow atomic questions, nested JSON state, composite scoring = weights in code, parallel fan-out ~free), `…/choice-score-noul` (include none-of-above; threshold noul by distance from 0.5), `huggingface.co/convaiinnovations/laya-typed-decisions` (0.766 vs base 0.362, noul acc 0.857, still over-confident ECE 0.213), `minimallysufficient.com/posts/llm-classification-is-feature-extraction` (outputs as features → LR calibration/threshold/added features, irony F1 0.747 → 0.779); Von + Laya both accept noul `criteria` map, Laya accepts dict state. Methods — M0 abstract noul blob; M1 structured JSON state (task/failed_call/error_output/agent_note/fixing_call/agent_did/user_replied/agent_asked/user_answered) + yes/no desc; M2 11 atomic observable nouls one call (policy_block +, tool_quirk +, simple_mistake −, transient_state −, approach_changed +, user_rule +, user_redirect +, general_policy_answer +, routine_progress −, surprising_cause +, repo_specific 0) hand-signed sum, zero-shot; M3 LOOCV L2 LR on M2 answers ± deterministic features (stratum one-hot, log length, block-regex). AUC (Von / Laya base / Laya-td) — M0 0.425/0.616/—, M1 0.435/0.673/0.708, M2 0.706/0.658/0.689, M3 0.577/0.511/0.327, M3+det 0.489/0.458/0.336, det-only 0.463/—/—, LLM 0.951. Precision @ recall≥0.9 — Von M2 0.29 (41/50), Laya M1 0.27 (41/50), Laya-td M1 0.30 (37/50), Laya-td M2 0.28 (39/50), LLM 0.69 (16/50). Latency MPS — 1 noul ~47–52 ms all backends; 11 atomic median/p90 Von 457/800, Laya 190/326, Laya-td 194/345. Per-question AUC (Von/Laya/Laya-td) surprising_cause 0.68/0.63/0.70, policy_block 0.72/0.51/0.62, user_redirect 0.67/0.66/0.59, tool_quirk 0.62/0.52/0.65, routine_progress 0.32/0.43/0.38 (correct neg direction), transient_state 0.64/0.48/0.52 (wrong dir Von). Verdict: decomposition lifts Von 0.43→0.71 zero-shot, structure+domain checkpoint lifts Laya 0.62→0.71; learned aggregation overfits n=50/12 positives → needs miner labels (hundreds); no viable pre-filter (best 37–41/50 at recall 0.9 vs LLM 16/50); design refinement (unify-context-manager D11) = decomposed atomic nouls over structured state, never one abstract judgement, LR aggregation re-eval once labelled dataset exists; scratch deleted after recording.

SCORE-PRIMITIVE STUDY (§17, `score` vs `noul` as `is_lesson` prefilter; harness rebuilt — scratch had been deleted, `windows.py` recovered from session log; window set 50 → 78 = fault 32 + correction 17 (pool exhausted) + decision 14 + routine 16 from 220 most recent session JSONLs, avg 800 chars; NEW set → numbers NOT comparable with spike-3/question-method tables, all methods re-run; reference `anthropic/claude-opus-5` subagent identical rubric/prompt = 25/78 lessons 32% (fault 15, correction 7, decision 2, routine 1); LLM baseline `opencode-go/deepseek-v4.1-flash` + extra anchored 1–5 `level`; score variants = generic "how reusable" vs 5 anchored observable levels (1 routine step; 2 simple mistake/temporary state; 3 fix/decision for this task only; 4 tool/CLI/env quirk, policy/guard block, or project rule; 5 standing user rule/preference), blob vs structured JSON, rankers = expected value / P(level≥4) / P(level≥3); metrics = AUC 1000× bootstrap 95% CI + precision @ recall ≥0.9 + kept/78). AUC Von 1.2.2 / Laya base / Laya-td — M0 abstract noul blob 0.448 [0.30–0.59] / 0.600 / 0.785 [0.67–0.88]; M1 abstract noul structured 0.489 / 0.620 / 0.755; M2 11 atomic nouls signed sum 0.701 [0.58–0.82] / 0.655 / 0.722; S generic 1–5 E 0.421 / 0.410 / 0.435; S anchored blob E 0.629 / 0.490 / 0.638; S anchored structured E 0.605 / 0.547 / 0.638; S anchored structured P(≥4) 0.632 / 0.528 / 0.568; S anchored structured P(≥3) 0.707 [0.59–0.82] / 0.509 / 0.611; LOOCV LR atomic 0.601 / 0.518 / 0.652; LOOCV LR atomic + score distribution 0.661 / 0.579 / 0.688; M2 sum + anchored E (z, equal weight) 0.700 / 0.620 / 0.712; LLM probability 0.945 [0.86–1.00], anchored `level` 0.921 [0.85–0.98], `level≥4` hard cut 0.882. Precision @ recall ≥0.9 / kept of 78 — Laya-td M0 0.41/56; Laya-td M1 0.42/55; Von M2 0.36/66; Von S anchored P(≥3) 0.40/57; LLM probability 0.68/34; LLM anchored level 0.60/40; LLM level≥4 hard cut 0.32/78. Latency MPS median — 1 score question 44–58 ms ≈ 1 noul; 11 atomic nouls Von 468, Laya 191, Laya-td 192. Verdict: generic unanchored score worse than random on every backend (0.41–0.44) → never use; anchored levels rescue score (Von 0.42 → 0.71 via P(≥3)) but never beat best noul method per backend; Von one anchored score ≈50 ms ties 11 atomic nouls 0.707 vs 0.701 at ≈470 ms = ~9× cheaper equal ranking; Laya-td best System-1 overall = abstract noul 0.785 (CI 0.67–0.88), score weaker 0.57–0.64 consistent with model card (score 0.723 < noul 0.857); LLM probability ≥ anchored level (0.945 vs 0.921), hard level cut loses ranking; Von decomposition lift replicates on new set (0.701 vs 0.706 before); learned aggregation still no win n=78 / 25 positives; prefilter verdict unchanged (best System-1 keeps 55–57/78 at recall 0.9, precision 0.41–0.42 vs base 0.32; LLM keeps 34/78 = 0.68; CIs wide, System-1 upper bounds ~0.88 stay below LLM point estimate); design (unify-context-manager D11) = `score` selectable question type per step, anchored observable levels mandatory, `noul` stays default, score recommended only where it measures equal at lower cost (Von); scratch `/tmp/von-spike` deleted again after recording.

LABELING APPROACHES BEYOND WINDOW JUDGEMENT (§18): 8 sources beyond one-window model judgement. Hindsight utility labels (Hindsight Memory-PRM arXiv 2608.29605; retrieval hits + citations = audit trail; one deletion-and-reanswer probe; label by future = situation recurs + costs). Contrastive labels (AutoGuide NeurIPS 2024 divergence-point→state-aware "when S do X" guideline, WebArena 43.7% vs ExpeL 21.8% vs ReAct 8.0%; CONTRAMEM arXiv 2608.22533 same-task outcome variation 26.2%→55.3%; Experience Memory Graph arXiv 2607.13884 deterministic failed→successful graph edit path, no LLM reflection; Trajectory-Informed Memory arXiv 2603.10600 strategy/recovery/optimization tips, +14.3 pp AppWorld; us = fault→fix micro, same-change clean-vs-costly-session macro). Weak supervision (Snorkel "Language Models in the Loop" ACM 10.1145/3617130 multiple prompts→votes/abstains→label model; Alfred 2305.18623; ScriptoriumWS 2502.12366; DataSculpt EDBT 2025; noisy abstaining LFs, label model learns LF accuracy, no gold). Query-side/click-through labels (doc2query 1904.08375; Doc2Query-- ECIR 2023 hallucinated queries filtered; us = kb_search→open relevant, grep fallback = gap). Knowledge-type labels (ISPY ASE 2021 issue–solution pairs; F2Chat 9-category dev-thread taxonomy; DRMiner 2405.19623 design rationale from issue logs; types lesson/rationale/procedure/fact/episode, each own destination). Procedure labels by sequence mining (Agent Workflow Memory 2409.07429; Voyager 2305.16291; SkillWeaver 2504.07079; recurring successful tool-call subsequences, values→slots = skill candidates). Proposition + graph units (Dense X Retrieval EMNLP 2024 retrieval-unit choice changes retrieval+downstream, propositions; A-Mem, G-Memory graph memories; deterministic entities from tool calls — files/commands/packages/env vars/ports — + relations fixes/requires/replaced_by/fails_with → kb_neighbors). Implicit feedback from git/CI (revert/checkout of agent edit = negative; agent commit→CI fail→fix commit = gotcha; merged PR = positive).

SPIKE 4 CLICK-THROUGH LABELS (kb_search behaviour): 501 sessions this repo, 123 kb_search calls in 51 sessions (2 empty); next ≤6 calls grep_fallback 54%, requery 16%, click 15%, none 8%, open_other 7%; click rank 1 = 39% of clicks, ≤3 = 67%; doc_type=agents (n=23) click 9% grep 83% vs unset (n=100) click 16% grep 47%; 68 fallbacks, 39 end opening a file, 34/39 opened file NOT in kb hits (4 rank 1, 1 rank 5), missed types .md 16/.ts 10/.tsx 4/.html 3; yield ~60 labelled pairs (18 clicks + 34 misses + 9 open_other, some noisy e.g. unrelated SKILL.md); use = kb eval fixtures packages/kb/eval + doc2query-style expansion of missed docs + gap report, 54% grep-fallback = baseline metric.

SPIKE 1 HINDSIGHT UTILITY LABELS (/tmp/label-spike/hs.py): sessions by mtime, oldest 60% past (300) vs newest 40% future (201), 140 windows (fault 90 distinct signatures, correction 20, decision 15, routine 15); error signature = `tool|first error line` normalised (paths <p>, hex <h>, strings <s>, digits <n>), fix head = tool or bash first 2 tokens; 90 faults recur ≥1 future 23, ≥2 16, same sig + same fix 11, changed approach (fix head ≠ fail head) recurring 13; most recurrent = generic agent-behaviour errors ((no output) grep 134 past sessions, edit oldText mismatch, ENOENT, timeouts), real lessons = openspec unknown command + aborted curl (context-mode guard); ref claude-opus-5 same rubric vs hindsight P(ref lesson|recurs) 0.17 vs P(ref lesson|not) 0.16 kappa 0.01, specificity cap past-df ≤5% 0.20 vs 0.16 kappa 0.05, changed-approach variants kappa ≈0; predicting recur-&-specific LLM AUC 0.472, opus 0.521, past recurrence count 0.748, specific-only 0.791; verdict lesson-ness + recurrence = independent axes, judges cannot estimate utility, past recurrence can, recurring failures mostly agent-behaviour noise not knowledge, hindsight replay = right offline utility harness but judgement still needed for content; caveats crude signatures, only fault windows measurable, preferences/rationale not recurrence-testable, n=90/15 lessons.

SPIKE 3 WEAK SUPERVISION LABEL MODEL: 28 LFs vote +1/−1/abstain = 20 System-1 atomic nouls (Von 1.2.2, Laya typed-decisions; vote p≥0.7 intended direction) + 8 heuristics (block regex, quirk regex, generic error, past recurrence + changed approach, same-tool retry, routine stratum, decision stratum, rule words); Dawid–Skene EM with abstain, no gold; System-1 LFs almost never vote at 0.7 (Laya-td ≈0 coverage, Von tool_quirk 35% coverage 0.20 accuracy), heuristics accurate only as negatives (routine 1.00, generic_error 0.86, decision 0.80, same_tool_retry 0.78), no positive LF with useful coverage; results vs reference (140 windows, 26 lessons; AUC [95% CI] / precision @ recall≥0.9 / kept) LLM deepseek-v4.1-flash 0.935 [0.89–0.97]/0.57/42, Von atomic sum 0.550, Laya-td atomic sum 0.603, majority vote all LFs 0.588, label model all LFs 0.576, label model heuristics-only 0.461, label model System-1-only 0.424, label model all LFs + LLM as one voter 0.676 (dilutes LLM); vs hindsight faults label model 0.709 (recurrence-heuristic driven, partly circular), LLM 0.472, majority 0.466; negative-prefilter check drop generic errors −10% loses 2/26, drop same-tool retries −19% loses 6/26 (gotchas often same tool different args), both −24% loses 8/26 → not safe; verdict weak supervision fails, no positive LFs, System-1 still not viable even as voters.

KNOWLEDGE-TYPE CENSUS (reference, 140 windows): episode 57 (41%), none 45 (32%), lesson 23 (16%), fact 11 (8%), rationale 4 (3%), procedure 0; by stratum fault {episode 34, none 33, lesson 14, fact 7, rationale 2}, correction {episode 12, lesson 8}, decision {episode 11, rationale 2, lesson 1, fact 1}, routine {none 12, fact 3}; LLM-vs-reference type agreement 0.53; windows cannot surface procedures → separate sequence-mining stage.

DESIGN IMPACT (unify-context-manager): D10 knowledge_type decides destination (lesson→cue-fired file; fact→lesson file kind:fact delivery:pull; rationale→link OpenSpec change else staged; procedure→skill candidate via sequence mining; episode/none→session index only); D10 utility = second axis, review ranking = judged lesson × specific past recurrence, time-split replay = miner offline eval harness, online fired/followed closes loop, no deterministic negative prefilter; D6 click/miss labels from context_search behaviour → kb eval fixtures + gap report, baseline 15% click / 54% grep fallback; NOT adopted weak-supervision label model + System-1 as LF voters; NOT yet spiked #2 contrastive macro pairs, #6 sequence mining, #7 propositions/graph, #8 git/CI feedback; scratch /tmp/label-spike deleted after recording.

OPEN QUESTIONS: Jev evaluation (needs TypeSafe key); fine-tune Laya/Von on miner labels — label count needed; runtime relevance-gate latency budget; MAP card-writing quality spike pending; prompt-trigger spike (BM25 cards vs real prompts) for `USER.md`-style preferences; distiller placement (in-process vs child `pi` — current choice keep both pipelines); `followed` metric definition for path hints; contrastive macro-pair mining (same change, clean vs costly session) untested; sequence mining for procedures untested; git/CI implicit feedback untested; better error-signature normalisation for hindsight labels; does a delivered card actually prevent recurrence (needs online fired/followed data).
