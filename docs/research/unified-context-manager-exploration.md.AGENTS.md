# research/unified-context-manager-exploration.md — index

STATUS (§1): Research artifact. Explore-mode; no change, no impl. Status: exploration in progress. Umbrella change drafted = `openspec/changes/unify-context-manager` (proposal + design, skip_specs; phases 1–6 = kernel, forks, lessons-and-cues, exec-and-web, lesson-miner, plugin-cutover). `consolidate-retrieval-planes` SUPERSEDED. Goal: ONE unified context-manager pi extension + ONE dashboard plugin replacing pi-hermes-memory, pi-blackhole, context-mode, kb-extension. Primary target = MODEL CONFUSION (3 session-search tools, 3 prompt injections); RAM/disk = bonus.

DECISIONS (§1): big-bang fork hermes + blackhole (MIT) into one package; context-mode (Elastic-2.0) clean-room sandbox exec ONLY (`ctx_execute`/`_file`/`batch`), no code port; DROP `ctx_fetch_and_index` → pi-web-access sole ingestion, manager taps `tool_result` → persistent `web` scope; old tool names aliased one release, then `pi.setActiveTools` deactivation; keep hermes + blackhole distillation pipelines separate (for now); triggers agent-authored; storage RESOLVED = ONE-FILE-PER-LESSON (md + YAML frontmatter) canonical, kb-indexed, stats disposable `node:sqlite` (§14). `consolidate-retrieval-planes` rejected fork (Alt 4) + declared shim-over-third-party-store top risk (D3); fork removes shim.

INVENTORY (§2, measured 2026-09-24): hermes 0.9.9 MIT ~17.0k TS LOC, better-sqlite3, `~/.pi/agent/pi-hermes-memory` 186 MB + `projects-memory` 14 MB; blackhole 0.5.6 MIT ~32.7k LOC, session JSONL `custom` entries, 2.7 MB; context-mode 1.0.169 Elastic-2.0 compiled, 16 child procs 616 MB RSS; kb + kb-extension ours MIT ~8.1k LOC, node:sqlite. Hook overlap: `before_agent_start` (hermes/ctx/kb), `session_before_compact` (hermes/blackhole/ctx), `context` (blackhole/ctx), `tool_result` (hermes/ctx/kb), `tool_call` guard (ctx/kb). 3 session-search tools: `session_search`, `recall`, `ctx_search`. hermes+ctx+kb all REPLACE `systemPrompt` in `before_agent_start` → prompt-cache churn.

CONTEXT-MODE ANATOMY (§3): usage (`docs/context-mode-roi-report.md`, 623 transcripts) = sandbox ~4,427 calls 97%, retrieval ~108 2%. KEEP sandbox (clean-room). DROP fetch_and_index; DROP `tool_result`+`before_provider_response`+`turn_end` capture (3rd copy); DROP `before_agent_start` routing + `<active_memory>` + `context` synthetic msg + `session_before_compact` resume snapshot; DROP MCP bridge child → native tools. `ctx_index`/`ctx_search` → kb store scope; `tool_call` Bash guard → kb guard. Pieces 5–8 = parallel session-memory system duplicating blackhole.

BLACKHOLE ANATOMY (§4): merge of pi-vcc + pi-observational-memory. KEEP `core/` 5.6k + `extract/` 2.3k deterministic compaction. Observer/Reflector/Dropper = in-process LLM workers (`om/` 7.0k + `agents/` 1.5k + `ledger/` 1.3k). DROP `pi-base/settings` + config ~8.2k TUI framework → dashboard plugin; `project-recall/` 2.8k rescans session JSONL dirs; changelog/commands mostly drop. `record_observations`/`record_reflections`/`drop_observations` = worker-only tools. `om/inline-compaction.ts` monkeypatches `AgentSession.compact` (fails closed).

HERMES ANATOMY (§5): `store/` 7.7k = `MEMORY.md`/`USER.md`/`failures.md` + SQLite mirror, session-indexer (JSONL → `sessions.db` 186 MB), skill store, standing instructions, atomic-lock-coordinator. `handlers/` ALL spawn child `pi` (`execChildPrompt`). DROP TUI commands ~2.5k, migrations ~0.7k. Injects ~3.3 KB memory-policy every turn. Config `~/.pi/agent/hermes-memory-config.json` `llmModelOverride` `opencode-go/deepseek-v4.1-flash`.

DUPLICATION (§6): session JSONL re-read 4×; LLM distillation 4 pipelines (hermes 4 handlers, blackhole 3 workers, session-distiller, ctx none); 4 store formats. ~12k LOC droppable → fork ≈30k LOC, not 50k.

MEMORY FILES WRITE-ONLY (§7): `projects-memory/.../MEMORY.md` 272 KB 263 entries not injected; `failures.md` 408 KB 466 entries — only ≤5 failures ≤7d injected. hermes `memoryMode: "policy-only"`; files 10–100× over char ceilings. Census (`consolidate-retrieval-planes`): 461 memory writes vs 66 `memory_search` reads / 504 conversations (~7:1). Global `failures.md` mixed-scope = general lesson log. Tags: 353/812 untagged; 68/812 status/episodic (`COMPLETE`, commit hashes).

EXTERNAL RESEARCH (§8): "Delivery, Not Storage" (arXiv 2607.20972) — voluntary memory use ~0; harness-store facts survive 138 compact-resumes. Proactive Memory Agent (2607.08716) — selective intervention beats passive exposure; +8.3 pp Terminal-Bench 2.0. Retrieval vs Utilization (2603.02473) — retrieval method spread 20 pts vs 3–8 pts write strategies; raw chunks match/beat extraction+summarization. Letta filesystem 74.0% LoCoMo vs Mem0 68.5%. Harness the Memory (2608.15008) — no substrate dominates. Verdict: drop curated files as model-facing memory ✔; "search tools suffice" ✘ (need harness-owned delivery).

PI-WEB-ACCESS vs CTX_FETCH_AND_INDEX (§9): pi-web-access 0.30.0 MIT ~30k LOC — extraction (readability, PDF+OCR, GitHub, YouTube, RSC, `authFetch`), SSRF protection, 30+ search providers, `source_check`; store `~/.pi/agent/web-search-cache` TTL 1 h, 128/128 MB; no cross-page ranked search. ctx: turndown HTML→md, 24 h cache, BM25+trigram RRF cross-source. Decision: web-access ingests; manager taps `tool_result` → persistent `web` scope.

DELIVERY CHANNELS (§10, pi 0.87.1 `types.d.ts`): A `tool_result` → content append = default cue delivery, adjacent, tail = cache-safe; B `tool_call` → block+reason = hard guards only; C `before_agent_start` → `message` = prompt-matched cues; C′ → `systemPrompt` = pinned tier only (cache-busting); D `turn_end`/`agent_before_settle` → entries + continue (`BoundaryResult`) = pre-settle checklist, mid-run compaction; E `session_before_compact` → carry pinned/cued facts through compaction.

TRIGGER VOCAB (§11, draft): path(glob) · command(pattern) · error(pattern) · tool(name, argPattern?) · symbol(name) · prompt(terms, BM25≥θ) · event(session_start|post_compact|model|before_settle) · behaviour(grep-without-kb|repeat-failure|reread-after-compact). Lesson schema `{ id, kind, scope, card ≤200 ch, body, triggers: Trigger[] (OR-ed; empty ⇒ pull-only), severity: hint|warn|block, provenance, stats: {fired, lastFired?, followed?} }`. Firing: silent default; once/lesson/session/compaction-epoch; ≤2 cards / ~600 ch per turn; block only command/tool; track `fired`+`followed`. Observed FP: kb guard fired ~6× on third-party `~/.pi/agent/npm/node_modules/` → target must be inside indexed root; `ctx_execute` python sandbox HOME/cwd differs.

SPIKE 1 — COMPACTION PREREQUISITE MET (§12): pi 0.87.0 (2026-09-21) adds actionable `turn_end`/`agent_before_settle`, return `{ entries: [...event.entries, draft], continue: true }`; `CompactionEntryDraft { type:"compaction", summary, firstKeptEntryId, details?, usage? }` at `types.d.ts:593`; public exports `findCutPoint`, `shouldCompact`, `estimateTokens`, `calculateContextTokens`; `prepareCompaction` NOT in root barrel. blackhole `om/inline-compaction.ts:794` monkeypatch now official API → retirable; blackhole summary deterministic → computable in boundary handler, no LLM.

SPIKE 2 — TRIGGER DERIVATION + REPLAY (§13, `/tmp/cue-spike/`, deleted): auto-derived triggers from 812 entries replayed over repo session JSONL (1109 files). NAIVE (80 sessions): lessons fired/session median 117, p90 180, max 328; precision ~5–15%. STRICT (df ≤3% on 240 older sessions, errors gated on `isError`): median 2, p90 20, max 41; 29/80 zero fires; hand-judged 30 → 7 TP, 4 plausible, 4 weak, 15 FP ⇒ precision ~25–35%. FP modes: incidental token mentions; status/episodic entries as lessons; generic path suffix; semantic duplicates. Verdict: prose-derived triggers below bar → explicit agent-authored triggers; df filter = strongest lever (~60× volume cut) → reuse replay as write-time validator + periodic self-audit; prompt-triggers untested.

STORAGE MODEL (§14, RESOLVED): 4 data classes; only lessons ours to author. repo docs/code canonical = git working tree; session history = pi session JSONL (hermes `sessions.db` = copy, 1,960 sessions/45,635 messages → THE ONE unified `node:sqlite` FTS index, NOT separate better-sqlite3); web content = pi-web-access cache; lessons/pinned/skills canonical = ONE FILE PER LESSON md + YAML frontmatter (`<id>.md`, kind/scope/triggers/severity/card); kb indexes via `kb-frontmatter-structural-indexing`; stats `fired`/`followed` in disposable `node:sqlite` (`~/.pi/agent/context/stats.db`). Locations: project `<repo>/.pi/lessons/<id>.md` (team-shared DEFAULT, git/PR review); global `~/.pi/agent/lessons/<id>.md`. Project identity = git common dir; fallback `realpath(cwd)`. Rejected L1 SQLite-canonical, L3 JSONL event log; one-file-per-lesson removes hermes md+SQLite lock + reconcile (~1k LOC).

RETROSPECTIVE LESSON MINER (§15): user tool = OLD session JSONL → lessons. Command e.g. `/lessons mine [--project|--all] [--since] [--n 2]` + `/lessons import-hermes`; dry-run default; dashboard background job + review screen. Stage 1 deterministic = existing `packages/session-distiller` (~1.2k LOC, no LLM); dry-run `--n 1`: 451 sessions, 5,258 candidates — fault 309 (72 recur ≥2), user_correction 214 (17), rest noise → needs LLM stage. Evidence-derived triggers: fault pairs carry literal failing command + error → trigger from observed event, not prose. Pipeline: distiller → compact window (≤1k tok) → TRIAGE (System-1 if configured else LLM) → MAP card (LLM) → trigger replay gate (df ≤3%) → REDUCE dedupe + PII scrub → VERIFY still-true → staged files. Executor = parallel subagents bounded by existing `maxConcurrentSubagents` (default 2); batch ~20 windows/subagent. Review gate = auto-accept above confidence, else review.

SYSTEM-1 TIER (§16): selectable "System 1" triage models; FALLBACK = no endpoint → pure LLM triage (config). All speak `POST /v1/systemone` → one adapter. Jev (TypeSafe, cloud, jabr v2 96.6%) · Von (`wfzyx/von`, Apache-2.0, `von-sdk` TS drop-in, jabr v2 72.0%) · Laya (`NandhaKishorM/laya`, Apache-2.0, `laya-ts` ONNX in-process Node, typed decisions 0.362 → 0.766). Modes: remote URL+key; dashboard-managed local server; in-process `laya-ts`. Runtime = cue relevance gate at `tool_call`/`tool_result`. Pitfall: `von-sdk` default port 8000 = dashboard port → pick another.

SPIKE 3 — TRIAGE BAKE-OFF (§17, `/tmp/von-spike/`, deleted; identical harness 50 windows): host Apple M5 Pro 48 GB MPS; Von 1.2.2, Laya 0.3.20. 50 windows (20 fault, 12 correction, 8 decision, 10 routine); reference `claude-opus-5` = 12/50 lessons. is_lesson AUC Von 0.425 / Laya 0.616 / LLM 0.951; best precision @ recall≥0.9 Von 0.24 (50/50) / Laya 0.24 (45/50) / LLM 0.69 (keeps 16); 1-question ~55 ms. Verdict: neither can pre-filter (recall ≥0.9 keeps 45–50/50); Laya ~2.3× faster; acceptable runtime gate (accuracy untested); Jev untested; System-1 pluggable opt-in, revisit via fine-tuning on miner labels. Side finding: 5/12 reference lessons = context-mode Bash guard blocking curl; `reason` not persisted → cue on `command: curl` + dedupe.

QUESTION-METHOD STUDY (§17): same 50 windows. M0 blob; M1 structured JSON state; M2 11 atomic nouls one call; M3 LOOCV L2 LR. AUC Von/Laya base/Laya-td — M0 0.425/0.616/—, M1 0.435/0.673/0.708, M2 0.706/0.658/0.689, LLM 0.951. Precision @ recall≥0.9 — best Von 0.29 (41/50), Laya-td 0.30, LLM 0.69 (16/50); 1 noul ~47–52 ms. Verdict: decomposition lifts Von 0.43→0.71 zero-shot, structure+domain checkpoint lifts Laya 0.62→0.71; learned aggregation overfits n=50/12 positives; no viable pre-filter; D11 = decomposed atomic nouls over structured state, never one abstract judgement, LR re-eval once labelled dataset exists.

SCORE-PRIMITIVE STUDY (§17): window set 50→78 (fault 32, correction 17, decision 14, routine 16); reference `claude-opus-5` = 25/78 lessons 32%. Score = generic "how reusable" vs 5 anchored observable levels (1 routine step; 2 simple mistake/temporary state; 3 fix/decision for this task only; 4 quirk/policy block/project rule; 5 standing user rule). AUC Von/Laya base/Laya-td — M0 0.448/0.600/0.785; M1 0.489/0.620/0.755; M2 0.701/0.655/0.722; S generic 0.421/0.410/0.435; S anchored P(≥3) 0.707/0.509/0.611; LLM probability 0.945 ≥ anchored `level` 0.921 > hard cut 0.882. Verdict: generic unanchored score worse than random → never use; anchored levels rescue score (Von 0.42→0.71) but never beat best noul per backend; Von one anchored score ≈50 ms ties 11 atomic nouls = ~9× cheaper; Laya-td best System-1 = abstract noul 0.785 (CI 0.67–0.88); prefilter unchanged (best System-1 keeps 55–57/78 vs LLM 34/78); D11 = `score` selectable per step, anchored observable levels mandatory, `noul` default.

LABELING APPROACHES (§18): 8 sources beyond one-window judgement. Hindsight utility (label by future = recurs + costs). Contrastive (AutoGuide; CONTRAMEM 2608.22533). Weak supervision (Snorkel; noisy abstaining LFs, label model learns LF accuracy, no gold). Query/click-through (doc2query 1904.08375). Knowledge-type (ISPY ASE 2021; F2Chat 9-category). Procedure labels by sequence mining (Agent Workflow Memory 2409.07429; SkillWeaver 2504.07079). Proposition + graph units (Dense X Retrieval 2024; deterministic entities + relations → kb_neighbors). Implicit git/CI feedback (revert = negative; agent commit→CI fail→fix = gotcha; merged PR = positive).

SPIKE 4 CLICK-THROUGH LABELS (§18): 501 sessions, 123 kb_search calls in 51 sessions; next ≤6 calls grep_fallback 54%, click 15%; click rank 1 = 39%, ≤3 = 67%; doc_type=agents (n=23) click 9% grep 83% vs unset (n=100) click 16% grep 47%; 34/39 fallback-opened files NOT in kb hits; yield ~60 labelled pairs. Use = kb eval fixtures `packages/kb/eval` + doc2query expansion + gap report; 54% grep-fallback = baseline; prior art `packages/kb/eval/mine-golden-sets.mjs`. D6.

SPIKE 1 HINDSIGHT UTILITY LABELS (§18): oldest 60% past (300) vs newest 40% future (201); 140 windows (fault 90, correction 20, decision 15, routine 15); 90 faults recur ≥1 future 23, ≥2 16, changed approach 13. Ref `claude-opus-5` vs hindsight kappa 0.01; past recurrence count AUC 0.748 vs LLM 0.472. Verdict: lesson-ness + recurrence = independent axes; judges cannot estimate utility; past recurrence can; recurring failures mostly noise; hindsight replay = right offline utility harness but judgement still needed.

SPIKE 3 WEAK SUPERVISION (§18): 28 LFs = 20 System-1 atomic nouls + 8 heuristics; Dawid–Skene EM with abstain, no gold; System-1 LFs almost never vote at 0.7; heuristics accurate only as negatives; no positive LF with useful coverage. AUC vs reference (140 windows, 26 lessons): LLM 0.935, Laya-td atomic 0.603, label model all LFs 0.576, Von atomic sum 0.550, heuristics-only 0.461. Negative-prefilter drops lose 2/26, 6/26, 8/26 → not safe. Verdict: weak supervision FAILS; System-1 not viable even as voters.

KNOWLEDGE-TYPE CENSUS (§18): reference 140 windows — episode 57 (41%), none 45 (32%), lesson 23 (16%), fact 11 (8%), rationale 4 (3%), procedure 0. LLM-vs-reference agreement 0.53; windows cannot surface procedures → separate sequence-mining stage. D10.

DESIGN IMPACT (§18, `unify-context-manager`): D10 knowledge_type decides destination (lesson→cue-fired file; fact→file kind:fact delivery:pull; rationale→link OpenSpec change else staged; procedure→skill candidate via sequence mining; episode/none→session index only); D10 utility = second axis; review ranking = judged lesson × specific past recurrence; time-split replay = miner offline harness; online fired/followed closes loop; no deterministic negative prefilter; D6 click/miss → kb eval fixtures + gap report (baseline 15% click / 54% grep fallback); NOT adopted weak-supervision label model; NOT yet spiked #2 contrastive macro pairs, #6 sequence mining, #7 propositions/graph, #8 git/CI.

MEMORY / KB IMPROVEMENTS (§19): 9 areas. (1) Memory helps? "Evaluating AGENTS.md" 2602.11988 no task-success gain, cost +20%+; SkillsBench 2602.12670 SWE only +4.5 pp 16/84 negative; VibeMemBench 2609.23570 +1.1–4.5 pp, 11/12 pairings fail memory-off → review mandatory, pinned = non-standard rules only, every tier A/B-gated. (2) Code-anchored staleness — Temporal Validity 2608.20685 superseded value served 36–38%; limpet flips stale on anchored-code change → lesson `anchors[]` + kb FRESH/STALE. (3) Update semantics — Zep/Graphiti 2501.13956 bi-temporal invalidate-not-delete; Mem0 → supersede-on-write. (4) Evolving playbooks — ACE 2510.04618 +10.6%, delta-only updates → one file per lesson; hermes auto-consolidate NOT ported. (5) Retrieval — Anthropic Contextual Retrieval −49%/−67% with rerank. (6) Repo map — Aider → pull-only symbol map. (7) Sleep-time — Letta 2504.13171 idle jobs. (8) Poisoning — MINJA, AgentPoison, OWASP ASI06 → team-shared `.pi/lessons/` vector, `trust` field, no auto-accept block/pinned/web. (9) Abstention — LongMemEval 2410.10813 → score floor returns nothing.

SPIKE A (§19): deterministic contextual BM25. Fresh repo index 38,851 chunks / 3,474 files via `packages/kb`; scored `golden.markdown-intent` n=73 + `golden.source-intent` n=104. P@1 markdown-intent baseline 0.151 / C1 0.164 / C2 0.137; source-intent 0.048 / 0.038 / 0.067. Verdict: mixed within noise, ≠ Anthropic LLM-context method, NOT adopted.

SPIKE B (§19): memory eval design, 501 sessions; cross-session refs 6%; memory/recall calls 4%; recurring faults 11–16 of 90. Tier R offline = deliver gold evidence within injection budget; Tier O outcome before cutover = replay later task in docker harness memory off / old stack / unified. TIER R v0 MEASURED: cases = recurring_fault 50 / user_flagged 23 / explicit_ref 2 / negative 30; valid 33/50, 21/23, 2/2, 11/30 → ~34% noise. Helpful: session_search newest-first 19/56 34%, bm25 17/56 30%, kb_search 15/56 27%, memory_search 4/56 7%; any shipped 30/56 54%. Verdict: ceiling ~54% vs delivery ≈ small fraction (recall tools 4% sessions); distilled memory weakest 7% (raw chunks ≥ extraction); newest-first vs relevance → hybrid; D9 = index tool results + BM25+recency+score floor + auto delivery + abstain, Tier R baseline numbers.

DESIGN IMPACT (§19): D4 lifecycle frontmatter `anchors[]`/`supersedes`/`valid_from`/`valid_to`/`trust`, anchor staleness via kb verdicts (STALE stops firing → verify queue), supersede-on-write, delta-only consolidation, idle-time jobs, poisoning controls; D5 abstention score floor, "injection must be earned", pinned = non-standard rules only, every tier A/B-gated; tasks.md §2 Evaluation gates block 1.6 cutover = 2.1 Tier R + Tier O, 2.2 A/B each injection tier via `scripts/ab-context` (non-inferior on success AND fewer steps/tokens else ships disabled); NOT adopted deterministic contextual BM25.

ONTOLOGY FRONTMATTER (§19): entities/references/relations in distilled-md frontmatter? prior art `add-kb-semantic-annotation-plane` (0/28 tasks) LLM annotator writes OKF `kb:` block, design §9.4 `type` + `kb.relations[]` `{predicate, object, status, source}`, indexer builds typed edges never calls LLM, deterministic Tier-1a edges first (`add-kb-deterministic-provenance-edges`). Against LLM open-ontology: distilled memory_search weakest 7% vs raw session_search 34%; prose-derived triggers 25–35% precise vs observed-event exact. Ablation: 856 hermes memories → 760 89% ≥1 entity; Tier R 56 valid positives; hit@5 V0 body BM25 19/56 34% full / 2/56 4% as-of, V1/V2/V3 all 17/56 → entities do NOT help ranking or firing; keep deterministic entities for anchors/staleness, observed-event triggers, dedupe/supersession keys, graph joins; closed relation set `kb:about`/`kb:fixes`/`kb:supersedes`/`kb:decidedIn`/`kb:replacedBy`, lesson files only; LLM open-ontology not now (docs via `add-kb-semantic-annotation-plane`); D4 reuses kb §9.4 shape; `kb:fixes` untested.

CARD-WRITING SPIKE (§19, umbrella task 0.2): can miner MAP stage turn triage-accepted windows into good D4 cards + D5 triggers, cheap model enough? 64 windows; triage `deepseek-v4.1-flash` is_lesson ≥0.5 → 20 accepted; writers cheap `deepseek-v4.1-flash` vs strong `claude-opus-5`; replay over 202 future sessions; blinded judge `kimi-k3`. Replay: cards 20/20 vs 19/20; per-card df ≤3% 12/19 vs 10/19; per-TRIGGER gate drops 7/34 & 9/34, keeps 17/20 & 17/19, recall unchanged. Blinded judge: cheap faithful 4.65 vs 4.63, general 3.90 vs 3.89, accept 90% vs 95%; ~1 invented card in 10. Verdict: cheap model suffices (~30× cheaper); per-trigger replay gate mandatory (per trigger NOT per card); human review stays; D5 guard = per-trigger gate + evidence, D10 default = cheap model + gate + review. Hand spot-check: 15/64 triage-accepted, 10-card sample → human accept 4 / edit 4 / reject 2 → usable 8/10 40% vs judge 90–95%; gate and review catch different faults; auto-accept OFF by default until human-vs-judge agreement measured; tasks.md 0.2 done.

OPEN QUESTIONS (§20): Jev eval (needs TypeSafe key); fine-tune Laya/Von on miner labels (label count?); runtime relevance-gate latency budget; prompt-trigger spike (BM25 cards vs real prompts); distiller placement (keep both pipelines); `followed` metric for path hints; contrastive macro-pair mining untested; sequence mining untested; git/CI feedback untested; does a card prevent recurrence; LLM contextual chunks (~39k calls) untested; repo-map untested; Tier O + unified-stack Tier R pending; `kb:fixes` untested; human-vs-judge agreement; openai/google transports empty.

Full detail → unified-context-manager-exploration.md (§ numbers above).

See change: unify-context-manager, consolidate-retrieval-planes, add-kb-semantic-annotation-plane, add-kb-deterministic-provenance-edges, add-kb-frontmatter-structural-indexing.
