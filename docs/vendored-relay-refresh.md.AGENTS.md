# vendored-relay-refresh.md — index

Operator page for the vendored Playwright relay: per-file provenance kinds, the non-reorderable fetch → verify-in-memory → copy → patch → rehash order, upstream-never-from-disk, Apache-2.0 §4(b)/§4(d), the three guards, never hand-edit `relay/vendor/`. See change: fix-browser-plugin-vendor-specifier-resolution.
